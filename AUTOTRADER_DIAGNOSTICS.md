# Диагностика Автотрейдера - Почему не срабатывает стартовая закупка?

## Дата: 17 ноября 2025

## Проблема
Пользователь включил валюту и автоторговлю, но стартовая закупка не происходит.

## Улучшения в коде

### 1. Подробное логирование в `_try_start_cycle`
Теперь при попытке стартовой закупки выводится:
- ✅ Текущая цена
- ✅ Объём покупки в USDT
- ✅ Keep резерв
- ✅ Минимальные квоты пары (min_quote_amount, min_base_amount)
- ✅ Итоговая покупка (количество базовой валюты)
- ✅ Результат отправки ордера

### 2. Диагностика в основном цикле
- Показывает список включённых/выключенных валют при старте
- Логирует обработку каждой валюты (раз в 10 циклов)
- Показывает, получена ли цена для валюты

### 3. Диагностика получения цены
- Логирует ошибки REST API при получении цены (один раз на пару)

## Как проверить

### Шаг 1: Проверьте логи при запуске автотрейдера
```
[AutoTrader] Запущен breakeven алгоритм
[AutoTrader] Валюты с разрешением торговли: ['BTC', 'WLDU', ...]
[AutoTrader] Валюты БЕЗ разрешения торговли: ['ETH', ...]
```

### Шаг 2: Проверьте, что валюта обрабатывается
```
[AutoTrader] 🔄 Цикл запущен с разрешениями:
  • Включено: ['WLDU']
  • Выключено: []
[AutoTrader][WLDU] Обработка: получение цены WLDU_USDT...
[AutoTrader][WLDU] Цена получена: 0.12345678 USDT
```

### Шаг 3: Смотрите детали попытки закупки
```
[AutoTrader][WLDU] 📊 Попытка стартовой закупки:
  • Текущая цена: 0.12345678 USDT
  • Объём покупки: 3.0000 USDT
  • Keep резерв: 0.0000 USDT
  • Min quote: 1.0000, Min base: 0.00000001
  • Итоговая покупка: 24.32187654 WLDU за 3.0000 USDT
[AutoTrader][WLDU] 🔄 Отправка ордера на покупку...
```

## Возможные причины проблем

### 1. ❌ Валюта не включена в разрешения
**Симптом**: Валюта не появляется в списке "Включено"
**Решение**: 
- Проверьте `app_state.json` - должно быть `"trading_permissions": {"WLDU": true}`
- Через UI: Переключите кнопку разрешения торговли для валюты

### 2. ❌ Не получена цена
**Симптом**: `⚠️ Цена не получена, пропуск`
**Возможные причины**:
- Неправильный символ валюты (проверьте в `currencies.json`)
- Пара не торгуется на Gate.io
- Проблемы с WebSocket или REST API

**Решение**:
```bash
# Проверьте, что пара существует на Gate.io
curl "https://api.gateio.ws/api/v4/spot/tickers?currency_pair=WLDU_USDT"
```

### 3. ❌ Таблица не рассчитана
**Симптом**: `❌ Стартовая закупка невозможна: таблица не рассчитана`
**Решение**: Проверьте параметры breakeven для валюты в `app_state.json`

### 4. ❌ Недостаточно USDT
**Симптом**: `❌ Недостаточно USDT: нужно X, доступно Y`
**Решение**: 
- Пополните баланс USDT
- Уменьшите `start_volume` в параметрах валюты

### 5. ❌ Объём меньше минимального
**Симптом**: `⚠️ Объём покупки < min_quote, увеличиваем`
**Решение**: Параметры автоматически корректируются, но проверьте минимальные квоты пары

### 6. ❌ Ордер не исполнен
**Симптом**: `❌ Старт цикла НЕ выполнен: partial/none fill`
**Возможные причины**:
- Недостаточная ликвидность
- Цена изменилась слишком быстро
- Проблемы с API ключами

**Решение**:
- Проверьте баланс на Gate.io
- Проверьте, что API ключи правильные и имеют права на торговлю
- Проверьте режим сети (work/test)

## Проверка конфигурации

### 1. Проверьте `app_state.json`
```json
{
  "auto_trade_enabled": true,
  "trading_permissions": {
    "WLDU": true
  },
  "breakeven_params": {
    "WLDU": {
      "steps": 16,
      "start_volume": 3.0,
      "start_price": 0.0,
      ...
    }
  }
}
```

### 2. Проверьте `currencies.json`
```json
[
  {
    "code": "WLDU",
    "name": "World Liberty Financial",
    "symbol": "🌐",
    "enabled": true
  }
]
```

### 3. Проверьте режим сети
```python
# В логах при запуске:
[INFO] Режим сети: work  # или test
```

## Типичный рабочий лог

```
[AutoTrader] Запущен breakeven алгоритм
[AutoTrader] Валюты с разрешением торговли: ['WLDU']
[AutoTrader] 🔄 Цикл запущен с разрешениями:
  • Включено: ['WLDU']
[AutoTrader][WLDU] Обработка: получение цены WLDU_USDT...
[AutoTrader][WLDU] Цена получена: 0.12345678 USDT
[AutoTrader][WLDU] 📊 Попытка стартовой закупки:
  • Текущая цена: 0.12345678 USDT
  • Объём покупки: 3.0000 USDT
  • Keep резерв: 0.0000 USDT
  • Min quote: 1.0000, Min base: 0.00000001
  • Итоговая покупка: 24.32187654 WLDU за 3.0000 USDT
[AutoTrader][WLDU] 🔄 Отправка ордера на покупку...
[AutoTrader][WLDU] ✅ Старт цикла row=0 price=0.12345678, filled=24.32187654
```

## Команды для диагностики

### Проверить статус автотрейдера
```bash
curl http://localhost:5000/api/autotrade/status
```

### Проверить разрешения торговли
```bash
curl http://localhost:5000/api/trade/permissions
```

### Проверить параметры валюты
```bash
curl "http://localhost:5000/api/trade/params?base_currency=WLDU"
```

### Проверить цену пары
```bash
curl "http://localhost:5000/api/pair/data?base_currency=WLDU&quote_currency=USDT"
```

## Решение проблем

### Если валюта называется WLDU, а должна быть WLD
1. Отредактируйте `currencies.json`
2. Измените `"code": "WLDU"` на `"code": "WLD"`
3. Перезапустите сервер
4. Проверьте, что пара `WLD_USDT` существует на Gate.io

### Если символ неправильный
1. Синхронизируйте валюты с Gate.io через UI (кнопка Sync)
2. Или вручную обновите символ в `currencies.json`

### Если автоторговля не запускается
1. Проверьте, что `auto_trade_enabled: true` в `app_state.json`
2. Убедитесь, что есть хотя бы одна валюта с `enabled: true` в разрешениях
3. Проверьте логи на наличие ошибок при старте

## Дополнительно

После внесения изменений в код автотрейдера теперь выводятся подробные диагностические сообщения, которые помогут быстро найти причину, почему не срабатывает стартовая закупка.

Следите за эмодзи в логах:
- 🔄 - процесс работает
- ✅ - успешная операция
- ❌ - ошибка
- ⚠️ - предупреждение
- 📊 - статистика/данные
