# Раздельные логи - Краткая инструкция ✅

## 🎯 Что изменилось?

**Было:** Все логи в одном файле `trade_logs.jsonl`  
**Стало:** Каждая валюта имеет свой файл в папке `trade_logs/`

## 📁 Где искать логи?

```
trade_logs/
  ├── BTC_logs.jsonl   ← Логи Bitcoin
  ├── DOGE_logs.jsonl  ← Логи Dogecoin
  ├── WLD_logs.jsonl   ← Логи Worldcoin
  └── ETH_logs.jsonl   ← Логи Ethereum
```

## ✅ Преимущества

- ✅ **Быстрее:** Читаем только нужную валюту
- ✅ **Безопаснее:** Повреждение одного файла не влияет на другие
- ✅ **Удобнее:** Легко найти/удалить логи конкретной валюты
- ✅ **Масштабируемо:** Неограниченное количество валют

## 🔧 Как это работает?

### Автоматически
- Покупка/продажа → запись в файл валюты
- Нет файла → создаётся автоматически
- Директория создаётся при первом запуске

### API не изменился
```python
# Логи для DOGE (только)
GET /api/trade/logs?currency=DOGE

# Логи для всех валют (объединённые)
GET /api/trade/logs

# Очистить логи DOGE
POST /api/trade/logs/clear
{"currency": "DOGE"}

# Очистить все логи
POST /api/trade/logs/clear
```

## 💡 Примеры

### Посмотреть логи DOGE
1. Откройте `trade_logs/DOGE_logs.jsonl`
2. Каждая строка = одна сделка

### Удалить логи DOGE
1. Удалите файл `trade_logs/DOGE_logs.jsonl`
2. Или через API: `POST /api/trade/logs/clear` с `{"currency": "DOGE"}`

### Бэкап логов BTC
1. Скопируйте `trade_logs/BTC_logs.jsonl` в безопасное место

## 🔍 Проверка

### Windows PowerShell:
```powershell
# Список файлов логов
dir trade_logs

# Содержимое логов DOGE
type trade_logs\DOGE_logs.jsonl
```

### Linux/Mac:
```bash
# Список файлов логов
ls trade_logs/

# Содержимое логов DOGE
cat trade_logs/DOGE_logs.jsonl
```

## ⚙️ Настройки

### Ограничения
- **10,000 записей** на валюту (в файле и памяти)
- Автоматическая ротация (старые записи удаляются)

### Директория
- По умолчанию: `trade_logs/`
- Создаётся автоматически
- Должна быть доступна для записи

## ❓ FAQ

**Q: Что будет со старым файлом `trade_logs.jsonl`?**  
A: Он не используется. Можете удалить или сделать бэкап.

**Q: Как объединить логи всех валют?**  
A: Используйте API: `GET /api/trade/logs` (без параметра `currency`)

**Q: Можно ли вернуться к одному файлу?**  
A: Нет, новая архитектура лучше во всех отношениях.

**Q: Логи синхронизируются между устройствами?**  
A: Нет, логи хранятся локально на сервере.

**Q: Что если файл повреждён?**  
A: Другие валюты не пострадают. Удалите повреждённый файл.

**Q: Как очистить все логи сразу?**  
A: `POST /api/trade/logs/clear` без параметров или удалите всю папку `trade_logs/`

## 🎉 Итог

Теперь логи каждой валюты независимы и изолированы!

- Покупка BTC → запись в `BTC_logs.jsonl`
- Покупка DOGE → запись в `DOGE_logs.jsonl`
- Быстро, безопасно, удобно!

---

**Подробнее:** `PER_CURRENCY_LOGS.md`
