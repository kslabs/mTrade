# 🔍 ИНСТРУКЦИЯ ПО ПРОВЕРКЕ

## ✅ Сервер работает правильно!

Логи показывают:
```
[NETWORK] Текущий режим сети загружен из state_manager: test
[INIT] AutoTrade загружен из state_manager: OFF
```

Это означает, что **серверная часть сохраняет и загружает состояние корректно!**

---

## 🌐 Проверка в браузере

### ШАГ 1: Очистите кеш браузера

**ОБЯЗАТЕЛЬНО!** Браузер может использовать старую версию `app.js`

**Способ 1 - Жёсткая перезагрузка:**
- Windows/Linux: `Ctrl + Shift + R` или `Ctrl + F5`
- Mac: `Cmd + Shift + R`

**Способ 2 - Очистка кеша:**
1. Нажмите `Ctrl + Shift + Delete`
2. Выберите **"Изображения и файлы, сохранённые в кеше"**
3. Период: **"За всё время"**
4. Нажмите **"Удалить данные"**

### ШАГ 2: Откройте консоль разработчика

1. Нажмите **F12**
2. Перейдите на вкладку **Console**
3. Перезагрузите страницу (**F5** или жёсткая перезагрузка)

### ШАГ 3: Проверьте логи

**ВЫ ДОЛЖНЫ УВИДЕТЬ:**

```
[INIT] Начало инициализации приложения
[INIT] UI State загружен
UI State: режим сети восстановлен - test
UI State: автотрейдинг восстановлен - OFF
[NETWORK] Текущий режим: test
```

**ЕСЛИ НЕ ВИДИТЕ** этих логов:
- ❌ Браузер использует старый `app.js` из кеша
- 🔄 Повторите очистку кеша (Шаг 1)

---

## 🧪 Тест после очистки кеша

### Тест 1: Проверка AutoTrade

1. Откройте сайт: http://localhost:5000
2. Посмотрите на переключатель **AutoTrade**
3. ✅ **Должно быть:** AutoTrade показывает **OFF** (потому что в логах `[INIT] AutoTrade загружен из state_manager: OFF`)

4. Переключите на **ON**
5. Нажмите **F5**
6. ✅ **Должно быть:** AutoTrade остался **ON**

### Тест 2: Проверка режима сети

1. Посмотрите на переключатель **WORK/TEST**
2. ✅ **Должно быть:** активен **TEST** (потому что в логах `режим сети загружен: test`)

3. Посмотрите на **баланс** (справа от кнопок 💰 💸)
4. ✅ **Должно быть:** тестовый баланс (например, 10000.00)

5. Переключите на **WORK**
6. Подождите 2-3 секунды
7. ✅ Баланс должен измениться

---

## 🐛 Если всё равно не работает

### Проверка 1: Версия файла app.js

**В консоли браузера выполните:**

```javascript
console.log('autoTradeEnabled defined:', typeof autoTradeEnabled !== 'undefined');
```

**Если выведет `false`:**
- ❌ Загружен старый `app.js`
- 🔄 Удалите папку кеша вручную

**Где находится кеш:**
- Chrome: `C:\Users\Администратор\AppData\Local\Google\Chrome\User Data\Default\Cache`
- Firefox: `C:\Users\Администратор\AppData\Local\Mozilla\Firefox\Profiles\XXXX\cache2`

### Проверка 2: Timestamp файла

**В консоли браузера выполните:**

```javascript
fetch('/static/app.js').then(r => r.text()).then(t => console.log(t.substring(0, 200)));
```

**Вы должны увидеть в начале файла:**

```javascript
window.__diagLogs=[];
function logDbg(m){
...
let autoTradeEnabled = true; // По умолчанию включено (ON), будет загружено из state
```

**Если там:**
```javascript
let autoTradeActive=false;
let tradingPermissions = {};
```
**БЕЗ строки `let autoTradeEnabled = true;`**
- ❌ Загружен старый файл
- 🔄 Очистите кеш снова

---

## 🔄 Радикальное решение

Если ничего не помогло:

### 1. Откройте URL напрямую:

```
http://localhost:5000/static/app.js
```

- Нажмите `Ctrl + F` и найдите текст: `autoTradeEnabled`
- Вы должны увидеть строку: `let autoTradeEnabled = true;` в **начале файла** (строка ~15)

### 2. Если не видите эту строку:

**Сервер отдаёт старый файл из кеша!**

**Решение:**
1. Остановите сервер (`Ctrl + C` в терминале)
2. Найдите процесс Python и завершите его:
   ```powershell
   Get-Process python | Stop-Process -Force
   ```
3. Запустите сервер заново:
   ```powershell
   python mTrade.py
   ```

### 3. Откройте в режиме инкогнито:

**Chrome:**
- `Ctrl + Shift + N`

**Firefox:**
- `Ctrl + Shift + P`

Откройте: `http://localhost:5000`

В режиме инкогнито кеш не используется вообще!

---

## 📞 Для отчёта об ошибке

Если всё равно не работает, пришлите:

1. **Скриншот консоли браузера (F12 → Console)** после перезагрузки страницы
2. **Результат этой команды в консоли браузера:**
   ```javascript
   console.log('autoTradeEnabled:', typeof autoTradeEnabled, autoTradeEnabled);
   console.log('currentNetworkMode:', currentNetworkMode);
   ```
3. **Первые 20 строк из** `http://localhost:5000/static/app.js`

---

**Дата:** 12 ноября 2025 г.
