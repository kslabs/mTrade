# Добавление валюты BTC в список

## Дата: 2025-01-XX

## Что было сделано:

### 1. Обновлён файл currencies.json
- Добавлена валюта BTC (₿) в начало списка валют
- BTC теперь отображается первой в списке доступных валют

### 2. Проверена интеграция
- Валюта BTC уже была в дефолтном списке в `mTrade.py` (строка 152)
- API эндпоинты `/api/currencies` (GET/POST) корректно работают
- Фронтенд (static/app.js) корректно загружает и отображает валюты
- Функция `renderCurrencyTabs()` отображает все валюты, включая BTC

### 3. Порядок валют в системе:
1. **BTC** (₿) - Bitcoin
2. WLD (🌐) - Worldcoin  
3. ETH (Ξ) - Ethereum
4. SOL (◎) - Solana
5. BNB (🔶) - Binance Coin
6. XRP (✕) - Ripple
7. ADA (₳) - Cardano
8. AVAX (🔺) - Avalanche
9. DOT (⬤) - Polkadot
10. DOGE (💠) - Dogecoin
11. GT - GateToken

## Как это работает:

### Для каждой валюты независимо:
✅ Параметры безубыточности сохраняются отдельно (в `app_state.json`)
✅ Таблица безубыточности рассчитывается индивидуально
✅ Торговые параметры хранятся независимо
✅ Разрешения на торговлю устанавливаются для каждой валюты

### API эндпоинты:
- `GET /api/currencies` - получить список валют
- `POST /api/currencies` - сохранить список валют
- `GET /api/trade/params?currency=BTC` - получить параметры для BTC
- `POST /api/trade/params` - сохранить параметры для валюты
- `GET /api/breakeven/table?currency=BTC&...` - построить таблицу для BTC

### UI управление:
- Вкладки валют отображаются в верхней части интерфейса
- Клик по вкладке переключает активную валюту
- Все параметры и данные автоматически загружаются для выбранной валюты
- Есть менеджер валют для добавления/удаления валют

## Тестирование:

Чтобы проверить работу с BTC:

1. Перезапустите сервер (если он запущен)
2. Откройте веб-интерфейс
3. Убедитесь, что вкладка BTC отображается первой
4. Кликните по вкладке BTC
5. Измените параметры безубыточности
6. Проверьте, что таблица обновляется автоматически
7. Переключитесь на другую валюту (например, ETH)
8. Вернитесь на BTC - параметры должны сохраниться

## Примечания:

- Валюта BTC работает аналогично остальным валютам
- Все изменения сохраняются в `app_state.json` с префиксом валюты
- Защита секретных файлов работает независимо от списка валют
- Git hooks продолжают блокировать коммиты с секретными данными

## Связанные файлы:

- `currencies.json` - список валют
- `mTrade.py` - сервер и API
- `static/app.js` - фронтенд логика
- `state_manager.py` - управление состоянием
- `breakeven_calculator.py` - расчёт безубыточности
