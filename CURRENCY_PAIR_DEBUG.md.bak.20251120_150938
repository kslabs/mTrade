# 🔍 Диагностика проблемы с валютной парой

## Проблема
Баланс USDT уменьшается, но баланс DOGE не увеличивается.

## Причина
**Система покупает не ту валюту!**

Из логов видно:
```
[QUICK_ORDER] Создание ордера: BTC_USDT, buy, amount=0.0001, price=50000.0
```

Но на странице отображается **DOGE_USDT**!

## Диагностика

### 1. Проверка текущей валюты в браузере

Откройте **DevTools** (F12) → вкладка **Console** и выполните:

```javascript
console.log('currentBaseCurrency:', currentBaseCurrency);
console.log('currentQuoteCurrency:', currentQuoteCurrency);
```

**Ожидаемый результат:**
```
currentBaseCurrency: DOGE
currentQuoteCurrency: USDT
```

Если показывает `BTC` вместо `DOGE` — это и есть проблема!

### 2. Проверка селектора валют

Посмотрите на **верхнюю панель** интерфейса:
- Какая валюта выбрана в выпадающем списке?
- Это должна быть **DOGE**, а не **BTC**

### 3. Обновление страницы

**Попробуйте:**
1. Обновить страницу (F5 или Ctrl+R)
2. Выбрать валюту **DOGE** в селекторе
3. Дождаться загрузки данных (баланс, цены, таблица)
4. Только потом нажимать кнопку "🛒"

### 4. Проверка в подтверждении

После нажатия кнопки "🛒" появится диалог подтверждения.

**Теперь в нём будет строка:**
```
Валютная пара: DOGE_USDT
```

**Проверьте эту строку перед подтверждением!**

Если показывает `BTC_USDT` — **НЕ ПОДТВЕРЖДАЙТЕ**, сначала исправьте проблему.

### 5. Логи в консоли

При нажатии на кнопку "🛒" в консоли должны появиться логи:

```
[QUICK_TRADE] 🛒 Покупка минимального ордера...
[QUICK_TRADE] currentBaseCurrency: DOGE
[QUICK_TRADE] currentQuoteCurrency: USDT
[QUICK_TRADE] Сумма покупки из таблицы: 3
[QUICK_TRADE] Текущая цена покупки (ask): 0.177
[QUICK_TRADE] Рассчитанное количество: 16.949152542
```

**Проверьте значение `currentBaseCurrency`!**

---

## Решение

### Вариант 1: Простое решение

1. **Обновите страницу** (Ctrl+R)
2. **Выберите DOGE** в селекторе валют
3. **Дождитесь загрузки** всех данных (5-10 секунд)
4. **Проверьте** валютную пару в диалоге подтверждения
5. **Нажмите "🛒"**

### Вариант 2: Принудительная установка валюты

Откройте **DevTools** → **Console** и выполните:

```javascript
currentBaseCurrency = 'DOGE';
currentQuoteCurrency = 'USDT';
console.log('Установлено:', currentBaseCurrency, '/', currentQuoteCurrency);
```

Затем нажмите "🛒".

### Вариант 3: Загрузка через API

В консоли выполните:

```javascript
// Загрузить данные для DOGE
loadPairData('DOGE', 'USDT');
```

Дождитесь завершения и нажмите "🛒".

---

## Проверка успешной покупки

### 1. Лог сервера

После покупки в логах должно быть:

```
[QUICK_ORDER] Создание ордера: DOGE_USDT, buy, amount=16.949152, price=0.177
[QUICK_ORDER] ✅ Ордер создан: 207123456
```

**Проверьте валютную пару!** Должно быть `DOGE_USDT`, а не `BTC_USDT`.

### 2. Алерт в браузере

Должен показать:

```
✅ Ордер на покупку создан!

ID: 207123456
```

### 3. Баланс

- **USDT** уменьшился на сумму покупки (например, на 3 USDT)
- **DOGE** увеличился на количество (например, на 16.95 DOGE)

Если оба баланса изменились — всё работает правильно!

---

## Сохранение параметров таблицы безубыточности

### Проблема
После перезагрузки страницы параметры возвращаются к значениям по умолчанию.

### Проверка

1. Измените параметры (например, `steps` на 20)
2. Нажмите **"Сохранить параметры"**
3. **Проверьте:** должна появиться надпись **"✓ Сохранено"** (зелёный цвет) на 3 секунды
4. Обновите страницу (F5)
5. Выберите ту же валюту
6. **Проверьте:** параметр `steps` должен быть 20

### Лог сохранения

В логах сервера должно быть:

```
[PARAMS] BTC -> {'steps': 20, 'start_volume': 5.0, ...}
```

### API проверка

В консоли выполните:

```javascript
fetch('/api/trade/params?base_currency=BTC')
  .then(r => r.json())
  .then(d => console.log('Параметры BTC:', d.params));
```

Должно вернуть сохранённые параметры.

---

## Важные команды для диагностики

### Проверка текущего состояния

```javascript
// Текущая валюта
console.log({
  base: currentBaseCurrency,
  quote: currentQuoteCurrency,
  mode: currentNetworkMode,
  autoTrade: autoTradeActive
});

// Таблица безубыточности
console.log('breakEvenTableData:', breakEvenTableData);

// Первая строка таблицы
if (breakEvenTableData && breakEvenTableData[0]) {
  console.log('Первая строка:', breakEvenTableData[0]);
}
```

### Принудительная загрузка данных

```javascript
// Загрузить все данные для DOGE
async function loadAllDOGE() {
  currentBaseCurrency = 'DOGE';
  currentQuoteCurrency = 'USDT';
  
  await loadPairData('DOGE', 'USDT');
  await loadPairBalances();
  await loadBreakEvenTable();
  
  console.log('✅ Все данные для DOGE загружены');
}

loadAllDOGE();
```

---

## Контрольный список перед покупкой

- [ ] Страница обновлена (F5)
- [ ] Выбрана нужная валюта в селекторе (DOGE)
- [ ] Загружены балансы (видны значения DOGE и USDT)
- [ ] Загружена таблица безубыточности (видны строки)
- [ ] Открыта консоль DevTools (F12)
- [ ] Проверена текущая валюта: `console.log(currentBaseCurrency)` → `DOGE`
- [ ] В диалоге подтверждения валютная пара: `DOGE_USDT`
- [ ] Нажата кнопка "🛒"

---

## Файлы с исправлениями

- ✅ `trading_engine.py` - добавлено извлечение `order_id`
- ✅ `state_manager.py` - добавлено сохранение параметров
- ✅ `static/app.js` - добавлены логи и проверки
- ✅ `api_routes.py` - корректная обработка запросов

---

**Автор:** GitHub Copilot  
**Дата:** 11 ноября 2025 г., 22:50  
**Версия:** Debug Guide v1.0
