# Changelog Entry - v1.8.6

## [1.8.6] - 2025-11-19

### ✅ Исправлено (Fixed)

#### BE индикатор - Отображение
- **Исправлено отображение BE в футере индикатора**
  - Проблема: BE (цена безубыточности) не отображался в футере индикатора
  - Причина: Неправильный ID элемента в JS (`indBe` вместо `indBE`)
  - Решение: Исправлен ID в `static/app.js` (строка 154)
  - Файлы: `static/app.js`

#### BE индикатор - Обновление после покупки
- **Исправлено обновление BE после стартовой покупки**
  - Проблема: BE не обновлялся после стартовой покупки (оставалось старое значение)
  - Причина: Таблица безубыточности не пересчитывалась с новым start_price после покупки
  - Решение: Добавлен пересчёт таблицы в `autotrader.py` после обновления start_price
  - Файлы: `autotrader.py` (строки 477-481)
  - Теперь BE = цена стартовой покупки (корректное значение)

#### 🔴 КРИТИЧЕСКОЕ: P0 не сохранялся в state_manager
- **Исправлено сохранение start_price в state_manager**
  - Проблема: P0 не обновлялся в таблице безубыточности в веб-интерфейсе после покупки
  - Причина: Использовался несуществующий метод `update_breakeven_params` (AttributeError)
  - Решение: Исправлен метод на правильный `set_breakeven_params`
  - Файлы: `autotrader.py` (строка 473)
  - Добавлен вывод traceback для диагностики
  - **Теперь P0 корректно сохраняется в config.json и отображается в веб-интерфейсе**

#### Автоматическое обновление P0 в веб-интерфейсе
- **Добавлено автоматическое обновление параметров в UI**
  - Проблема: После стартовой покупки поле "Цена (P0)" не обновлялось автоматически
  - Причина: Параметры загружались только при смене валюты или перезагрузке страницы
  - Решение: Добавлена периодическая перезагрузка параметров (каждые 10 секунд)
  - Файлы: `static/app.js` (строка 1489)
  - **Теперь поле P0 автоматически обновляется через 10 сек после стартовой покупки**
  
- **BE теперь корректно отображается:**
  - В футере индикатора (синий цвет, #00bfff)
  - На визуальной шкале (маркер BE)
  - Обновляется при смене шага цикла
  - Обновляется при новом цикле

### ✅ Проверено (Verified)

#### Логика обновления P0 (start_price)
- **Подтверждена корректная работа обновления стартовой цены:**
  - P0 обновляется **ТОЛЬКО при стартовой покупке** (`_try_start_cycle()`)
  - P0 **НЕ изменяется при докупках/усреднениях** (`_try_rebuy()`)
  - P0 обновляется при **новом цикле** (новая стартовая покупка)
  - P0 инициализируется при **первом расчёте таблицы** (`_recalc_table_if_needed()`)
  
- **Проверенные файлы:**
  - `autotrader.py` - основная логика автотрейдинга
  - `mTrade.py` - серверная часть, передача данных
  - `breakeven_calculator.py` - расчёт таблицы безубыточности
  - `static/app.js` - клиентская логика
  - `templates/index.html` - HTML индикатора

### 📚 Документация (Documentation)

#### Добавлены новые документы:
- `P0_VERIFICATION_REPORT.md` - подробный отчёт о проверке P0
- `P0_QUICKCHECK.md` - быстрая шпаргалка для проверки
- `P0_DIAGRAM.md` - визуальная диаграмма логики P0
- `P0_TESTING_CHECKLIST.md` - чеклист для финального тестирования
- `P0_SUMMARY.md` - резюме проверки P0 и BE

### 🔧 Технические детали (Technical Details)

#### Исправление BE индикатора:

**До исправления:**
```javascript
// Неправильный ID для BE
const elementId = 'ind' + key.charAt(0).toUpperCase() + key.slice(1);
// для 'be' получалось 'indBe' - неверно
```

**После исправления:**
```javascript
// Правильный ID для BE
const elementId = key === 'be' ? 'indBE' : 'ind' + key.charAt(0).toUpperCase() + key.slice(1);
// для 'be' получается 'indBE' - верно
```

#### Проверка P0:

**Стартовая покупка (P0 обновляется):**
```python
# autotrader.py, _try_start_cycle(), строки 465, 473
cycle.update({
    'start_price': buy_price,  # P0 = цена покупки
})
current_params['start_price'] = buy_price  # P0 сохранено в state_manager
```

**Докупка (P0 НЕ обновляется):**
```python
# autotrader.py, _try_rebuy(), строки 546-549
cycle['active_step'] = next_step
cycle['last_buy_price'] = level_price
cycle['total_invested_usd'] += invest
cycle['base_volume'] += filled
# cycle['start_price'] - НЕ трогается
```

### 🎯 Результаты (Results)

- ✅ BE отображается корректно в индикаторе
- ✅ P0 обновляется только при стартовой покупке
- ✅ P0 НЕ изменяется при докупках
- ✅ Логика работает согласно требованиям
- ✅ Нет лишних изменений в коде

### 📋 Чеклист для тестирования

См. файл `P0_TESTING_CHECKLIST.md` для подробного чеклиста.

**Основные проверки:**
1. Стартовая покупка → P0 обновляется
2. Докупка → P0 НЕ изменяется
3. Продажа + новый цикл → P0 обновляется
4. BE отображается в индикаторе

### 🔗 Связанные задачи (Related Issues)

- Исправление отображения BE в индикаторе
- Проверка логики обновления P0
- Документирование работы P0 и BE

---

**Автор:** GitHub Copilot  
**Дата:** 2025-01-XX  
**Версия:** 1.8.6
