# ✅ ОТЛАДКА: Проблема с обновлением таблицы безубыточности

## Дата: 2025-12-13
## Статус: 🔍 ДОБАВЛЕНО ОТЛАДОЧНОЕ ЛОГИРОВАНИЕ

---

## 📋 Проблема

При изменении **геометрического множителя** (geom_multiplier) в веб-интерфейсе таблица безубыточности **не обновляется**.

---

## 🔍 Возможные причины

1. **Фронтенд не передаёт параметр в query string**
   - При запросе таблицы `/api/breakeven/table` не указывается `?geom_multiplier=...`

2. **Параметры не сохраняются перед запросом таблицы**
   - Фронтенд не вызывает `/api/trade/params` (POST) перед запросом таблицы

3. **Кэширование на стороне фронтенда**
   - Браузер кэширует ответ с таблицей

---

## ✅ Добавлено отладочное логирование

### Файл: `trade_params_routes.py`
### Метод: `get_breakeven_table()`

Теперь при каждом запросе таблицы выводится:

```
[BREAKEVEN_TABLE] 🔍 Входящие query params: {'base_currency': 'BTC', 'geom_multiplier': '2.5'}
[BREAKEVEN_TABLE] 📋 Загруженные params: {'steps': 16, 'start_volume': 10.0, 'geom_multiplier': 2.0, ...}
[BREAKEVEN_TABLE] 💱 Currency: BTC
[BREAKEVEN_TABLE] ✅ geom_multiplier переопределён: 2.5
[BREAKEVEN_TABLE] 🎯 Финальные params перед расчётом: {'steps': 16, 'start_volume': 10.0, 'geom_multiplier': 2.5, ...}
```

---

## 🧪 Как проверить

### Шаг 1: Перезапустите сервер
```powershell
# Остановите текущий сервер (Ctrl+C)
# Затем запустите снова:
python mTrade.py
```

### Шаг 2: Откройте веб-интерфейс
- Перейдите на страницу с таблицей безубыточности
- Откройте консоль разработчика (F12) → вкладка "Network"

### Шаг 3: Измените geom_multiplier
- Измените значение множителя геометрии (например, с 2.0 на 2.5)
- Нажмите кнопку обновления таблицы

### Шаг 4: Проверьте логи сервера
Посмотрите, что выводится в консоли:

**Если видите:**
```
[BREAKEVEN_TABLE] 🔍 Входящие query params: {'base_currency': 'BTC'}
```
**Проблема:** Фронтенд НЕ передаёт `geom_multiplier` в query string.

**Если видите:**
```
[BREAKEVEN_TABLE] 🔍 Входящие query params: {'base_currency': 'BTC', 'geom_multiplier': '2.5'}
[BREAKEVEN_TABLE] ✅ geom_multiplier переопределён: 2.5
```
**Проблема:** Где-то в другом месте (кэширование, ошибка рендеринга).

### Шаг 5: Проверьте Network в браузере
- Найдите запрос к `/api/breakeven/table`
- Проверьте Query String Parameters
- Убедитесь, что там есть `geom_multiplier=...`

---

## 🛠️ Решения

### Если параметр не передаётся (фронтенд):
1. Проверьте JavaScript-код, который формирует URL
2. Убедитесь, что все изменённые параметры добавляются в query string
3. Пример правильного URL:
   ```
   /api/breakeven/table?base_currency=BTC&geom_multiplier=2.5&steps=16&...
   ```

### Если параметры не сохраняются:
1. Добавьте вызов `/api/trade/params` (POST) перед запросом таблицы
2. Сохраните параметры через API:
   ```javascript
   await fetch('/api/trade/params', {
       method: 'POST',
       headers: {'Content-Type': 'application/json'},
       body: JSON.stringify({
           base_currency: 'BTC',
           geom_multiplier: 2.5,
           ...
       })
   });
   ```

### Если проблема в кэше:
1. Добавьте `cache-control` в заголовки ответа
2. Или добавьте timestamp в query string:
   ```
   /api/breakeven/table?base_currency=BTC&_t=1702418400000
   ```

---

## 📝 Следующий шаг

**Запустите сервер, измените параметр и проверьте логи!**  
По логам будет сразу видно, в чём проблема.
