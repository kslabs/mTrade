# 🔍 Диагностика: Почему P0 не обновляется в таблице

## Что проверить после стартовой покупки:

### 1. Логи консоли сервера (должны быть):
```
[AutoTrader][XXX] 📊 Обновлён start_price in state_manager: 0.XXXXX
[AutoTrader][XXX] 📊 Таблица пересчитана с новым P0: 0.XXXXX
```

### 2. Если этих логов НЕТ:
- Значит сработал except и была ошибка
- Должен быть лог с traceback

### 3. Проверить config.json:
Открыть файл `c:\Users\Администратор\Documents\bGate.mTrade\app_state.json`

Найти секцию для вашей валюты (например DOGE):
```json
"breakeven_params": {
  "DOGE": {
    "start_price": 0.XXXXX  ← должно быть = цена покупки
  }
}
```

### 4. Проверить веб-интерфейс:
- Открыть таблицу безубыточности
- Посмотреть поле "Цена (P0):"
- Должно быть = цена стартовой покупки

---

## Пожалуйста, пришлите:

1. **Логи консоли** после стартовой покупки (последние 20 строк)
2. **Цена покупки** из лога (например: `Buy{Курс:0.1585}`)
3. **Значение P0 в таблице** (что показывает веб-интерфейс)
4. **Содержимое app_state.json** (секция breakeven_params для вашей валюты)

Это поможет понять, на каком этапе проблема.
