# Система логирования торговых операций

**Дата создания:** 13 ноября 2025  
**Версия:** 1.0

---

## 📋 Описание

Система логирования торговых операций предназначена для записи и отображения всех торговых действий (покупок и продаж) в режиме реального времени.

---

## 🎯 Основные возможности

### 1. **Запись логов**
- ✅ Автоматическая запись всех операций покупки и продажи
- ✅ Формат логов: `[HH:MM:SS] Type{Параметры}`
- ✅ Ограничение: максимум 10,000 записей
- ✅ Автоматическое удаление старых записей при превышении лимита

### 2. **Отображение**
- ✅ Переключатель "Таблица безубыточности / Логи"
- ✅ Цветовое выделение: покупки (красный), продажи (зелёный)
- ✅ Авто-обновление каждые 5 секунд
- ✅ Прокрутка к последним записям

### 3. **Управление**
- ✅ Кнопка обновления (🔄)
- ✅ Кнопка очистки (🗑️)
- ✅ Фильтрация по валюте
- ✅ Статистика: количество операций, общий PnL

---

## 📝 Формат логов

### Покупка (Buy)
```
[17:19:39] Buy{Объем:44.7746; Курс:0.7745; ↓Δ%:0.96; ↓%:3.6; Инвест:85.9807}
```

**Параметры:**
- `Объем` - объём покупки в базовой валюте
- `Курс` - цена покупки
- `↓Δ%` - изменение цены с предыдущей покупки (%)
- `↓%` - общее падение от начальной цены (%)
- `Инвест` - общая сумма инвестиций

### Продажа (Sell)
```
[16:15:08] Sell{Объем:1.0155; Курс:0.8021; ↑Δ%:0.64; PnL:0.016}
```

**Параметры:**
- `Объем` - объём продажи
- `Курс` - цена продажи
- `↑Δ%` - изменение цены (%)
- `PnL` - прибыль/убыток (Profit and Loss)

---

## 🔧 API Endpoints

### 1. Получить логи
```http
GET /api/trade/logs?limit=100&currency=BTC&formatted=1
```

**Параметры:**
- `limit` - максимальное количество записей (по умолчанию: 100)
- `currency` - фильтр по валюте (опционально)
- `formatted` - возвращать отформатированные строки (0/1)

**Ответ:**
```json
{
  "success": true,
  "logs": [
    "[17:19:39] Buy{Объем:44.7746; Курс:0.7745; ↓Δ%:0.96; ↓%:3.6; Инвест:85.9807}",
    "..."
  ],
  "count": 50
}
```

### 2. Получить статистику
```http
GET /api/trade/logs/stats?currency=BTC
```

**Ответ:**
```json
{
  "success": true,
  "stats": {
    "total_entries": 150,
    "total_buys": 80,
    "total_sells": 70,
    "total_investment": 1234.5678,
    "total_pnl": 45.6789,
    "currency": "BTC"
  }
}
```

### 3. Очистить логи
```http
POST /api/trade/logs/clear
Content-Type: application/json

{
  "currency": "BTC"  // опционально, если не указано - очистить все
}
```

**Ответ:**
```json
{
  "success": true,
  "message": "Логи для BTC очищены"
}
```

---

## 💻 Использование в коде

### Python (backend)

```python
from trade_logger import get_trade_logger

# Получить экземпляр логгера
logger = get_trade_logger()

# Логировать покупку
logger.log_buy(
    currency='BTC',
    volume=1.5,
    price=45000.0,
    delta_percent=-0.5,
    total_drop_percent=2.3,
    investment=67500.0
)

# Логировать продажу
logger.log_sell(
    currency='BTC',
    volume=1.5,
    price=46000.0,
    delta_percent=2.2,
    pnl=1500.0
)

# Получить логи
logs = logger.get_logs(limit=50, currency='BTC')

# Получить статистику
stats = logger.get_stats(currency='BTC')

# Очистить логи
logger.clear_logs(currency='BTC')  # для конкретной валюты
logger.clear_logs()                # все логи
```

### JavaScript (frontend)

```javascript
// Экземпляр менеджера логов доступен глобально
const logsManager = window.tradeLogsManager;

// Переключить на отображение логов
logsManager.switchView('logs');

// Переключить на таблицу безубыточности
logsManager.switchView('breakeven');

// Обновить логи
await logsManager.refreshLogs();

// Очистить логи
await logsManager.clearLogs();

// Загрузить логи для конкретной валюты
await logsManager.loadLogs('BTC');
```

---

## 📂 Файлы системы

### Backend
- **`trade_logger.py`** - модуль логирования торговли
- **`trade_logs.jsonl`** - файл хранения логов (JSON Lines формат)

### Frontend
- **`static/trade-logs-manager.js`** - менеджер отображения логов
- **`templates/index.html`** - подключение скриптов

### API
- **`mTrade.py`** - API endpoints для работы с логами:
  - `/api/trade/logs` (GET)
  - `/api/trade/logs/stats` (GET)
  - `/api/trade/logs/clear` (POST)

---

## 🎨 UI Элементы

### Переключатель видов
```
+------------------------------------------+
| [Таблица безубыточности] / [Логи]       |
+------------------------------------------+
```

- Активная кнопка выделена синим цветом
- При клике происходит мгновенное переключение
- Только один вид отображается одновременно

### Панель логов
```
+------------------------------------------+
| Записей: 150 | Покупок: 80 | Продаж: 70 |
| PnL: 45.6789        [🔄] [🗑️]           |
+------------------------------------------+
| [17:19:39] Buy{...}                      |
| [16:46:24] Buy{...}                      |
| [16:30:35] Buy{...}                      |
| ...                                      |
+------------------------------------------+
```

---

## ⚙️ Настройки

### Лимиты
- **MAX_LOG_ENTRIES**: 10,000 записей
- **AUTO_REFRESH_INTERVAL**: 5 секунд

### Хранение
- Формат: JSON Lines (`.jsonl`)
- Кодировка: UTF-8
- Автосохранение после каждой операции
- Автоматическая обрезка файла при превышении лимита

---

## 🔄 Интеграция с автотрейдером

Автотрейдер автоматически записывает логи при выполнении операций:

```python
# В autotrader.py
from trade_logger import get_trade_logger

class AutoTrader:
    def __init__(self):
        self.logger = get_trade_logger()
    
    def execute_buy(self, params):
        # Выполнить покупку...
        
        # Записать в лог
        self.logger.log_buy(
            currency=params['currency'],
            volume=params['volume'],
            price=params['price'],
            delta_percent=params['delta'],
            total_drop_percent=params['drop'],
            investment=params['invest']
        )
    
    def execute_sell(self, params):
        # Выполнить продажу...
        
        # Записать в лог
        self.logger.log_sell(
            currency=params['currency'],
            volume=params['volume'],
            price=params['price'],
            delta_percent=params['delta'],
            pnl=params['pnl']
        )
```

---

## 🐛 Troubleshooting

### Логи не отображаются
1. Проверьте консоль браузера на наличие ошибок
2. Убедитесь, что файл `trade-logs-manager.js` подключен
3. Проверьте, что API endpoint `/api/trade/logs` доступен

### Логи не сохраняются
1. Проверьте права доступа к файлу `trade_logs.jsonl`
2. Убедитесь, что импорт `from trade_logger import get_trade_logger` работает
3. Проверьте логи сервера на наличие ошибок

### Переключатель не работает
1. Проверьте, что элемент `.breakeven-table h2` существует на странице
2. Откройте DevTools и проверьте, что обработчики событий установлены
3. Убедитесь, что нет конфликтов с другими скриптами

---

## ✅ Checklist готовности

- [x] Backend модуль `trade_logger.py` создан
- [x] API endpoints добавлены в `mTrade.py`
- [x] Frontend менеджер `trade-logs-manager.js` создан
- [x] Скрипт подключен в `index.html`
- [x] Стили для отображения добавлены
- [x] Файл логов добавлен в `.gitignore`
- [x] Документация создана

---

## 🚀 Следующие шаги

1. ✅ Протестировать отображение логов
2. ⏳ Интегрировать с автотрейдером
3. ⏳ Добавить экспорт логов в CSV
4. ⏳ Добавить графики на основе логов
5. ⏳ Добавить уведомления о критических событиях

---

**Статус:** ✅ Готово к использованию  
**Версия:** 1.0  
**Дата:** 13 ноября 2025
