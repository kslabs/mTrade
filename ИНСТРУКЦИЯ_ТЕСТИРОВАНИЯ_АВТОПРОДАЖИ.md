# ✅ ТЕСТИРОВАНИЕ АВТОПРОДАЖИ ОСТАТКОВ

## Что исправлено?
Бот теперь **автоматически продаёт остатки монет** в IDLE состоянии, даже если цикл не активен.

## Как протестировать?

### Вариант 1: Естественное тестирование
1. Запустите бота: `python autotrader_v2.py`
2. Дождитесь, пока бот обнаружит остатки DOGE (или другой монеты)
3. Наблюдайте за логами - должна появиться попытка автопродажи

**Ожидаемый результат:**
```
[DOGE] 🔴 ОБНАРУЖЕНЫ ОСТАТКИ: 12.56 DOGE
[DOGE] 🔄 Обнаружены остатки монет в IDLE состоянии → попытка автопродажи
[DOGE] 🔄 === АВТОПРОДАЖА ОСТАТКОВ В IDLE СОСТОЯНИИ ===
[DOGE] 💰 Доступный баланс для продажи: 12.56000000
[DOGE] 🚀 Создаем ордер на продажу остатков...
[DOGE] ✅ Ордер создан успешно
[DOGE] 🔄 Сбрасываем цикл после продажи остатков
```

### Вариант 2: Принудительное тестирование
1. Убедитесь, что на балансе есть остатки DOGE (или другой монеты)
2. Убедитесь, что в `state.json` для этой монеты `state = "idle"`
3. Запустите бота
4. Бот должен немедленно попытаться продать остатки

### Вариант 3: Симуляция через веб-интерфейс
1. Вручную купите небольшое количество DOGE через веб-интерфейс биржи
2. Запустите бота (цикл для DOGE должен быть в IDLE)
3. Бот обнаружит остатки и попытается их продать

## Что проверить в логах?

### ✅ Успешная автопродажа:
- `[DOGE] 🔄 === АВТОПРОДАЖА ОСТАТКОВ В IDLE СОСТОЯНИИ ===`
- `[DOGE] ✅ Ордер создан успешно: {order_id}`
- `[DOGE] 🔄 Сбрасываем цикл после продажи остатков`

### ⚠️ Остатки слишком малы:
- `[DOGE] ⚠️ Объем {amount} меньше минимального {min_amount}`
- `[DOGE] 🗑️ Сбрасываем цикл, остатки слишком малы для продажи`

### ❌ Ошибка API:
- `[DOGE] ❌ Ошибка при автопродаже остатков: {error}`
- `[DOGE] 🔄 Сбрасываем цикл из-за ошибки`

## После тестирования

### Если автопродажа сработала:
1. ✅ Проверьте баланс DOGE через веб-интерфейс - должен быть 0 (или близко к 0)
2. ✅ Проверьте `state.json` - цикл DOGE должен быть в IDLE с пустой таблицей
3. ✅ Проверьте, что бот готов запустить новый цикл для DOGE

### Если автопродажа НЕ сработала:
1. ❌ Проверьте логи на ошибки
2. ❌ Проверьте минимальные объёмы для пары DOGE_USDT
3. ❌ Проверьте, что баланс DOGE доступен (не заблокирован в ордере)
4. ❌ Отправьте логи разработчику для диагностики

## Важно!

- Автопродажа происходит **только в IDLE состоянии**
- Автопродажа **не происходит во время активного цикла**
- После автопродажи цикл **автоматически сбрасывается**
- Бот **никогда не зависнет** с остатками монет

---

**Дата:** $(Get-Date)  
**Версия:** autotrader_v2.py (с автопродажей остатков)  
**Статус:** ✅ Готово к тестированию
