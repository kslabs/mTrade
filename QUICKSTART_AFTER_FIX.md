# 🔧 Быстрый старт после исправления

## ✅ Что исправлено
- **Ошибка 500** при покупке минимального ордера
- Форматирование количества в научной нотации (`6e-05` → `0.000060`)
- Добавлено детальное логирование для диагностики
- Добавлен метод `get_ticker()` для получения текущих цен

## 📋 Инструкции по запуску

### 1. Перезапустите Flask сервер

**Остановите старый процесс** (если запущен):
- Нажмите `Ctrl+C` в терминале, где запущен сервер
- Или закройте окно терминала

**Запустите новый процесс**:
```powershell
python mTrade.py
```

### 2. Откройте веб-интерфейс
Перейдите в браузере: **http://localhost:5000**

### 3. Проверьте функционал

#### Быстрая покупка:
1. Выберите валютную пару (например, BTC/USDT)
2. Убедитесь, что в режиме **TESTNET** (переключатель слева)
3. Нажмите кнопку **"Купить мин. ордер"**
4. Подтвердите действие
5. Проверьте:
   - ✅ Появилось сообщение об успехе/ошибке
   - ✅ Запись добавилась в "Логи торговли"
   - ✅ Балансы обновились

#### Быстрая продажа:
1. Выберите валюту с ненулевым балансом
2. Нажмите кнопку **"Продать все"**
3. Подтвердите действие
4. Проверьте логи и балансы

### 4. Проверьте логи в консоли

При нажатии "Купить мин. ордер" в консоли сервера должны появиться:

```
[INFO] quick_buy_min: создание ордера BTC_USDT, amount=0.000060, type=market
[INFO] quick_buy_min: ответ API: {'id': '...', 'status': 'closed', ...}
```

**Если видите ошибку:**
```
[ERROR] quick_buy_min: ошибка API - INSUFFICIENT_BALANCE
```
→ Пополните тестовый баланс в testnet

```
[ERROR] quick_buy_min: ошибка API - INVALID_PARAM_VALUE
```
→ Проверьте параметры пары, возможно пара неактивна

### 5. Тестовые скрипты (опционально)

**Безопасный тест (без реальных ордеров):**
```powershell
python test_buy_min.py
```

**Реальный тест в testnet:**
```powershell
python test_buy_min_real.py
```
Введите `yes` для подтверждения.

## 🐛 Если что-то не работает

### Проблема: Кнопки не реагируют
**Решение**: Очистите кэш браузера (`Ctrl+Shift+R` или `Ctrl+F5`)

### Проблема: Ошибка 500 всё ещё появляется
**Решение**: 
1. Убедитесь, что сервер перезапущен с новым кодом
2. Проверьте консоль сервера на наличие Python-ошибок
3. Запустите тестовый скрипт для диагностики:
   ```powershell
   python test_buy_min.py
   ```

### Проблема: Нет логов в консоли
**Решение**: 
- Убедитесь, что сервер запущен в терминале (не в фоне)
- Проверьте, что используете правильный файл `mTrade.py`

### Проблема: Баланс нулевой в TESTNET
**Решение**: 
1. Перейдите на https://www.gate.io/testnet
2. Войдите в аккаунт
3. Получите тестовые средства (faucet)

## 📝 Дополнительная информация

Подробности об исправлениях: `QUICK_TRADE_FIX.md`

---
**Дата исправления**: 14 ноября 2024
**Изменённые файлы**: `mTrade.py`, `gate_api_client.py`
