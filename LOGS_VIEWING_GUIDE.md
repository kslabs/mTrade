# 📋 Руководство по просмотру логов торговли

## ✅ Исправлено

Добавлена инициализация менеджера логов в `static/app.js`. Теперь кнопка переключения на логи появляется автоматически при загрузке страницы.

## 🔍 Где находится кнопка переключения на логи?

### В веб-интерфейсе:

1. **Откройте браузер** и перейдите на http://localhost:5000
2. **Найдите карточку** "📊 Таблица безубыточности"
3. **В заголовке карточки** вы увидите два переключателя:
   ```
   [Таблица безубыточности] / [Логи]
   ```

### Расположение кнопки:

```
┌─────────────────────────────────────────────────────────────┐
│ [Таблица безубыточности] / [Логи]      💾 Сохранить        │  ← ЗДЕСЬ!
├─────────────────────────────────────────────────────────────┤
│ Параметры торговли (Шагов, Старт и т.д.)                   │
│                                                             │
│ Таблица безубыточности...                                   │
└─────────────────────────────────────────────────────────────┘
```

## 🎯 Как использовать

### 1. Переключение на логи:
- Кликните на кнопку **"Логи"** в заголовке таблицы безубыточности
- Таблица скроется, а вместо неё появится панель с логами

### 2. Возврат к таблице:
- Кликните на кнопку **"Таблица безубыточности"**
- Логи скроются, а таблица безубыточности снова отобразится

### 3. Управление логами:

В режиме просмотра логов доступны кнопки:

- 🔄 **Обновить** — принудительное обновление логов
- 🗑️ **Очистить** — удалить все логи (с подтверждением)

## 📊 Формат отображения логов

Логи отображаются в следующем формате:

### Покупка (Buy):
```
[15:30:45] Buy{Объем:0.01; Курс:50000; ↓Δ%:-2.5; ↓%:-5.0; Инвест:500$}
```

### Продажа (Sell):
```
[15:35:20] Sell{Объем:0.01; Курс:51000; Профит:2%; Прибыль:10$}
```

### Цветовое выделение:
- 🟢 **Зелёный** — операции покупки (Buy)
- 🔴 **Красный** — операции продажи (Sell)

## 🔄 Автообновление

Логи автоматически обновляются каждые **5 секунд**, когда панель логов открыта.

Статус обновления отображается в заголовке панели логов.

## 🧪 Тестирование

Если вы хотите протестировать отображение логов без совершения реальных сделок:

```bash
# Запустите тестовый скрипт
python test_trade_logger.py
```

После этого логи появятся в интерфейсе.

## 📝 Проверка через API

Вы также можете проверить логи через API:

### PowerShell:
```powershell
# Получить последние 10 логов (форматированные)
Invoke-WebRequest -Uri "http://localhost:5000/api/trade/logs?limit=10&formatted=1" -UseBasicParsing | Select-Object -ExpandProperty Content

# Получить статистику
Invoke-WebRequest -Uri "http://localhost:5000/api/trade/logs/stats" -UseBasicParsing | Select-Object -ExpandProperty Content

# Очистить логи
Invoke-WebRequest -Uri "http://localhost:5000/api/trade/logs/clear" -Method POST -Headers @{"Content-Type"="application/json"} -Body '{}' -UseBasicParsing
```

## 🔧 Техническая информация

### Файлы логов:
- Расположение: `trade_logs.jsonl`
- Формат: JSON Lines (по одной записи на строку)
- Максимум записей: 10,000 (старые автоматически удаляются)

### Структура записи:
```json
{
  "timestamp": "2025-11-14T15:30:45.123456",
  "type": "buy",
  "currency": "BTC",
  "volume": 0.01,
  "price": 50000,
  "delta_percent": -2.5,
  "drop_percent": -5.0,
  "investment": 500
}
```

## 🚀 Быстрый старт

1. **Откройте браузер** → http://localhost:5000
2. **Найдите заголовок** "📊 Таблица безубыточности"
3. **Кликните** на кнопку **"Логи"**
4. **Готово!** Вы увидите все торговые операции

## ⚠️ Если кнопка не появляется

Выполните следующие действия:

1. **Очистите кэш браузера**:
   - Нажмите `Ctrl+Shift+Delete`
   - Выберите "Кэшированные изображения и файлы"
   - Нажмите "Очистить данные"

2. **Жёсткая перезагрузка страницы**:
   - Нажмите `Ctrl+F5` или `Ctrl+Shift+R`

3. **Проверьте консоль браузера**:
   - Нажмите `F12`
   - Перейдите на вкладку "Console"
   - Проверьте наличие ошибок

4. **Перезапустите сервер**:
   - Остановите сервер (`Ctrl+C`)
   - Запустите снова: `python mTrade.py`

## 📚 Дополнительная документация

- **Полное руководство**: `TRADE_LOGS_SYSTEM.md`
- **Готовность системы**: `TRADE_LOGS_READY.md`
- **Как просматривать логи**: `HOW_TO_VIEW_LOGS.md`

---

**Дата обновления**: 14 ноября 2025  
**Статус**: ✅ Готово к использованию
