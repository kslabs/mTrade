# 🔍 МОНИТОРИНГ ЛОГОВ — Что смотреть

**Цель:** Понять, вызывается ли метод `_try_sell` и какие значения он видит

## Что искать в консоли сервера

### 1. Проверка вызова метода (новый код)
Ищите строку:
```
[XRP] 🔴 _try_sell вызван | КОД ВЕРСИЯ: 2025-12-08_07:30 | Цена: X.XXXX
```

**Если этой строки НЕТ** → метод `_try_sell` НЕ вызывается вообще!

### 2. Проверка активного цикла
Ищите строку:
```
[XRP] 🟢 Цикл АКТИВЕН (step=0) → вызываем _try_sell
```

**Если этой строки НЕТ** → цикл НЕ считается активным!

### 3. Проверка условий продажи
Ищите строку:
```
[XRP] Проверка продажи:
  Текущая цена: X.XXXX
  Расчетный курс шага (rate): X.XXXX
  Цена безубыточности: X.XXXX
  Целевая дельта (от rate): +0.15%
  Целевая цена продажи: X.XXXXXXXX
```

**Важно:** Целевая цена продажи должна быть **ВЫШЕ** текущей цены!

### 4. Пропуск продажи
Ищите строку:
```
[XRP] [SKIP] Цена еще не достигла цели (X.XXXX < X.XXXX)
```

Это **нормально** — значит код работает и **НЕ продаёт** ниже порога!

## Как проверить

### Вариант 1: Поиск в консоли сервера
Если консоль открыта, смотрите прямо в окно.

### Вариант 2: Поиск в файле логов
```powershell
Get-Content "server_restart_debug2.log" -Tail 100 | Select-String -Pattern "XRP.*🔴|XRP.*🟢|XRP.*Проверка продажи"
```

### Вариант 3: Живой мониторинг
```powershell
Get-Content "server_restart_debug2.log" -Wait -Tail 50
```

## Что делать, если...

### Если НЕТ строки `🔴 _try_sell вызван`
**Причина:** Метод `_try_sell` не вызывается.

**Возможные варианты:**
1. Цикл НЕ активен (проверить состояние в `autotrader_cycles_state.json`)
2. Автотрейдер НЕ запущен (проверить `AUTO_TRADER.running`)
3. Авто-торговля отключена в настройках

### Если НЕТ строки `🟢 Цикл АКТИВЕН`
**Причина:** Цикл в состоянии IDLE (не активен).

**Проверить:**
```powershell
Get-Content "autotrader_cycles_state.json" | ConvertFrom-Json | Select-Object -ExpandProperty XRP
```

Должно быть: `"active": true`

### Если ЕСТЬ все логи, но продажа всё равно происходит ниже порога
**Причина:** Продажа происходит **НЕ через autotrader_v2.py**!

**Возможные источники:**
1. Ручная продажа через веб-интерфейс
2. Старый `autotrader.py` (если он где-то запущен)
3. Внешний скрипт/бот

## Текущие ожидания

После перезапуска (08:35) при следующей покупке XRP должны появиться логи:
```
[XRP] 🟢 Цикл АКТИВЕН (step=0) → вызываем _try_sell
[XRP] 🔴 _try_sell вызван | КОД ВЕРСИЯ: 2025-12-08_07:30 | Цена: 2.0890
[XRP] Проверка продажи:
  Текущая цена: 2.0890
  ...
  Целевая цена продажи: 2.09203500
[XRP] [SKIP] Цена еще не достигла цели (2.0890 < 2.0920)
```

**Если продажа происходит БЕЗ этих логов** → ищем другой источник продаж!
