# ✅ ИСПРАВЛЕНО: Проблема с отображением кнопки "Сохранить"

**Дата:** 16 ноября 2024  
**Проблема:** Кнопка "💾 Сохранить" мелькала и пропадала при загрузке страницы  
**Причина:** CSS переопределял inline-стили

---

## 🔍 НАЙДЕННАЯ ПРОБЛЕМА

В файле `static/style.css` строка 43:

```css
.card h3 {
    color:#4CAF50;
    margin-bottom:12px;
    font-size:14px;
    border-bottom:1px solid #3a3a3a;
    padding-bottom:8px;
    font-weight:600
}
```

Это правило **НЕ содержит `display:flex`**, поэтому переопределяло inline-стиль из HTML:

```html
<h3 style="display:flex;...">
```

CSS имеет **более высокий приоритет**, чем inline-стили без `!important`, поэтому `h3` становился блочным элементом, и правый `div` с кнопкой переносился на новую строку и **выходил за пределы видимой области**.

---

## ✅ РЕШЕНИЕ

Добавлен `!important` к `display:flex` в inline-стилях обоих заголовков:

### 1. Заголовок "Рынок и стакан" (строка 90)

```html
<h3 style="display:flex !important;justify-content:space-between;align-items:center;gap:10px;margin-bottom:12px;border-bottom:1px solid #3a3a3a;padding-bottom:8px;">
    <span>📈 Рынок и стакан</span>
    <div style="display:flex;align-items:center;gap:8px;">
        <!-- кнопки справа -->
    </div>
</h3>
```

### 2. Заголовок "Таблица безубыточности" (строка 133)

```html
<h3 style="display:flex !important;justify-content:space-between;align-items:center;gap:10px;margin-bottom:12px;border-bottom:1px solid #3a3a3a;padding-bottom:8px;">
    <span>📊 Таблица безубыточности</span>
    <div style="display:flex;align-items:center;gap:8px;">
        <div id="paramsSaveStatus" class="params-save-status"></div>
        <button id="saveParamsBtn" class="save-params-btn-compact">💾 Сохранить</button>
    </div>
</h3>
```

---

## 📋 ИЗМЕНЕНИЯ

### `templates/index.html`

1. Добавлен `!important` к `display:flex` в обоих h3
2. Добавлены недостающие стили из CSS:
   - `margin-bottom:12px`
   - `border-bottom:1px solid #3a3a3a`
   - `padding-bottom:8px`

Это обеспечивает:
- ✅ Flexbox работает корректно
- ✅ Кнопки отображаются справа
- ✅ Стиль соответствует дизайну

---

## 🎯 РЕЗУЛЬТАТ

- ✅ Кнопка "💾 Сохранить" теперь **всегда видна** справа от заголовка
- ✅ Стиль заголовков **идентичен** во всём интерфейсе
- ✅ Inline-стили имеют приоритет над CSS
- ✅ Не требуется очистка кэша браузера

---

## 📝 ТЕХНИЧЕСКАЯ СПРАВКА

### Приоритет CSS:

1. **!important** в inline-стилях (высший приоритет)
2. Inline-стили без !important
3. ID селекторы (#id)
4. Классы, атрибуты, псевдоклассы (.class)
5. Элементы (h3, div)

### Почему это работает:

```css
/* CSS файл */
.card h3 { display: block; } /* Специфичность: 0,0,1,1 */
```

```html
<!-- HTML без !important -->
<h3 style="display:flex"> <!-- Специфичность: 1,0,0,0 -->
<!-- НО .card h3 может переопределить, если загружается после -->
```

```html
<!-- HTML с !important -->
<h3 style="display:flex !important"> <!-- Высший приоритет -->
<!-- Всегда работает, независимо от CSS -->
```

---

## ✅ ПРОВЕРКА

Откройте в браузере: `http://localhost:5000`

Должны увидеть:
1. ✅ Кнопка "💾 Сохранить" справа от "📊 Таблица безубыточности"
2. ✅ Кнопки управления справа от "📈 Рынок и стакан"
3. ✅ Оба заголовка одинакового стиля

---

**Проблема решена!** Кнопка теперь всегда отображается.
