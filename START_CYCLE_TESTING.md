# Тестирование стартовой покупки и создания цикла

> **Версия:** 1.0  
> **Дата:** 6 декабря 2025  
> **Статус:** Готов к тестированию  
> **Автотрейдер:** AutoTraderV2

---

## ✅ Что реализовано

### **1. Метод `_try_start_cycle()`**
- ✅ Проверка состояния цикла (IDLE)
- ✅ Проверка активных BUY ордеров
- ✅ Проверка баланса базовой валюты (если есть → режим продажи)
- ✅ Проверка баланса USDT
- ✅ Создание MARKET BUY ордера
- ✅ Проверка исполнения ордера
- ✅ Активация цикла
- ✅ Логирование всех шагов

### **2. Защита от дублей**
- ✅ Lock на валюту (все операции под Lock)
- ✅ Проверка активных ордеров
- ✅ Флаг `_buying_in_progress`
- ✅ Проверка баланса базовой валюты

### **3. Режим продажи без покупки**
- ✅ Если монеты уже есть на балансе → активируем цикл в режиме продажи
- ✅ Не создаём стартовую покупку
- ✅ Рассчитываем эквивалент в USD

---

## 🧪 Сценарии тестирования

### **Сценарий 1: Нормальная стартовая покупка**

**Предусловия:**
- Автотрейдер выключен
- Баланс USDT >= start_volume (например, 10 USDT)
- Баланс базовой валюты = 0 (нет монет)
- Нет активных BUY ордеров

**Шаги:**
1. Включить автотрейдер через веб-интерфейс
2. Наблюдать логи в консоли

**Ожидаемый результат:**
```
[BTC] 🔥 Создание MARKET BUY ордера...
[BTC]   Сумма: 10.0 USDT
[BTC]   Цена: ~94500.0
[BTC] ✅ MARKET ордер создан: 123456789
[BTC] ✅ Ордер исполнен!
[BTC]   Цена: 94520.5
[BTC]   Объём: 0.000105 BTC
[BTC]   Стоимость: 9.92 USDT
[BTC] ✅ ЦИКЛ ЗАПУЩЕН!
[BTC]   Стартовая цена: 94520.5
[BTC]   Объём: 0.000105 BTC
[BTC]   Инвестировано: 9.92 USDT
[BTC]   Таблица: 16 шагов
```

**Проверки:**
- ✅ Создан ровно ОДИН MARKET ордер
- ✅ Ордер исполнен (status = 'closed')
- ✅ Цикл активирован (state = 'ACTIVE')
- ✅ В веб-интерфейсе отображается активный цикл

---

### **Сценарий 2: Монеты уже есть (режим продажи)**

**Предусловия:**
- Автотрейдер выключен
- Баланс базовой валюты > 0 (например, 0.001 BTC)
- Баланс USDT >= start_volume

**Шаги:**
1. Вручную купить монеты (или они остались с прошлого цикла)
2. Включить автотрейдер

**Ожидаемый результат:**
```
[BTC] 💰 Монеты уже есть на балансе: 0.001 >= 0.0001
[BTC] 🔄 Активация цикла в режиме ПРОДАЖИ (без стартовой покупки)
[BTC] ✅ Цикл активирован (режим продажи)
[BTC]   Баланс: 0.001 BTC
[BTC]   Стартовая цена: 94500.0
[BTC]   Эквивалент USD: 94.50
```

**Проверки:**
- ✅ НЕ создаётся MARKET ордер
- ✅ Цикл активирован (state = 'ACTIVE')
- ✅ Баланс соответствует фактическому

---

### **Сценарий 3: Недостаточно USDT**

**Предусловия:**
- Баланс USDT < start_volume (например, 2 USDT при start_volume=10)
- Баланс базовой валюты = 0

**Шаги:**
1. Включить автотрейдер

**Ожидаемый результат:**
```
[BTC] ⚠️ Недостаточно USDT: 2.00 < 10.00
```

**Проверки:**
- ✅ Цикл НЕ активирован (state = 'IDLE')
- ✅ НЕ создаётся ордер
- ✅ Автотрейдер продолжает работать для других валют

---

### **Сценарий 4: Есть активный BUY ордер**

**Предусловия:**
- Есть незакрытый BUY ордер (например, limit ордер с ценой ниже рынка)

**Шаги:**
1. Вручную создать BUY limit ордер
2. Включить автотрейдер

**Ожидаемый результат:**
```
[BTC] ⏸️ Пропуск: есть активный BUY ордер
```

**Проверки:**
- ✅ Цикл НЕ активирован
- ✅ НЕ создаётся новый ордер
- ✅ Автотрейдер ждёт завершения активного ордера

---

### **Сценарий 5: Повторный запуск автотрейдера**

**Предусловия:**
- Цикл уже активирован (есть активный цикл)
- Автотрейдер работает

**Шаги:**
1. Остановить автотрейдер
2. Запустить снова

**Ожидаемый результат:**
```
[BTC] Пропуск: цикл уже активен (state=ACTIVE)
```

**Проверки:**
- ✅ НЕ создаётся дублирующий ордер
- ✅ Цикл остаётся активным
- ✅ Данные цикла сохранены в памяти

---

### **Сценарий 6: Несколько валют одновременно**

**Предусловия:**
- Включены 3 валюты: BTC, ETH, WLD
- Баланс USDT >= 30 (по 10 на каждую)
- Баланс базовых валют = 0

**Шаги:**
1. Включить автотрейдер

**Ожидаемый результат:**
```
[BTC] 🔥 Создание MARKET BUY ордера...
[BTC] ✅ ЦИКЛ ЗАПУЩЕН!
[ETH] 🔥 Создание MARKET BUY ордера...
[ETH] ✅ ЦИКЛ ЗАПУЩЕН!
[WLD] 🔥 Создание MARKET BUY ордера...
[WLD] ✅ ЦИКЛ ЗАПУЩЕН!
```

**Проверки:**
- ✅ Для каждой валюты создаётся ровно ОДИН ордер
- ✅ Все циклы активированы
- ✅ Нет race condition между валютами

---

## 🔍 Проверка через веб-интерфейс

### **1. Статус автотрейдера**
```
GET /api/autotrade/status
```

**Ожидаемый ответ:**
```json
{
  "success": true,
  "enabled": true,
  "running": true,
  "stats": {
    "total_cycles": 3,
    "active_cycles": 3,
    "total_buy_orders": 3,
    "total_sell_orders": 0,
    "last_update": 1733500000.0
  }
}
```

### **2. Статистика по валюте**
```
GET /api/autotrader/stats?base_currency=BTC
```

**Ожидаемый ответ:**
```json
{
  "success": true,
  "stats": {
    "base_currency": "BTC",
    "enabled": true,
    "running": true,
    "version": "v2",
    "state": "active",
    "active": true,
    "active_step": 0,
    "start_price": 94520.5,
    "last_buy_price": 94520.5,
    "base_volume": 0.000105,
    "total_invested_usd": 9.92,
    "last_action_at": 1733500000.0
  }
}
```

### **3. Сброс цикла**
```
POST /api/autotrader/reset_cycle
{
  "base_currency": "BTC"
}
```

**Ожидаемый ответ:**
```json
{
  "success": true,
  "message": "Цикл BTC успешно сброшен...",
  "old_state": "active",
  "new_state": "IDLE"
}
```

---

## 🐛 Отладка

### **Логи в консоли**
Все операции логируются с префиксом `[ВАЛЮТА]`:
- `🔥` - Создание ордера
- `✅` - Успешная операция
- `⚠️` - Предупреждение (недостаточно средств и т.д.)
- `❌` - Ошибка
- `⏸️` - Пропуск операции
- `💰` - Информация о балансе
- `🔄` - Активация цикла

### **Проверка баланса**
```python
# В консоли Python
from gate_api_client import GateAPIClient
client = GateAPIClient(api_key, api_secret, 'test')
balance = client.get_account_balance()
print(balance)
```

### **Проверка ордеров**
```python
# Открытые ордера
orders = client.get_open_orders('BTC_USDT')
print(orders)

# Конкретный ордер
order = client.get_order(order_id, 'BTC_USDT')
print(order)
```

---

## ✅ Чеклист тестирования

### **Перед запуском:**
- [ ] Сервер запущен (`python mTrade.py`)
- [ ] Веб-интерфейс открыт (http://localhost:5000)
- [ ] Тестовая сеть активна (network_mode = 'test')
- [ ] API ключи тестовой сети настроены
- [ ] Баланс USDT достаточен для тестов

### **Тесты:**
- [ ] **Сценарий 1:** Нормальная стартовая покупка
- [ ] **Сценарий 2:** Монеты уже есть (режим продажи)
- [ ] **Сценарий 3:** Недостаточно USDT
- [ ] **Сценарий 4:** Есть активный BUY ордер
- [ ] **Сценарий 5:** Повторный запуск автотрейдера
- [ ] **Сценарий 6:** Несколько валют одновременно

### **Проверки:**
- [ ] Отсутствие дублирующих ордеров
- [ ] Корректная активация цикла
- [ ] Логи понятны и информативны
- [ ] Веб-интерфейс отображает актуальные данные
- [ ] Сброс цикла работает корректно
- [ ] Нет утечек памяти (проверить через `top` / Task Manager)

### **После тестирования:**
- [ ] Вручную продать монеты (для очистки баланса)
- [ ] Сбросить циклы через API
- [ ] Остановить автотрейдер
- [ ] Сохранить логи для анализа

---

## 📝 Примечания

1. **Тестовая сеть (test):**
   - Используйте тестовую сеть для безопасного тестирования
   - Тестовые USDT можно получить через faucet Gate.io
   - Цены в тестовой сети могут отличаться от реальных

2. **Ручная продажа:**
   - Для тестирования можно вручную продавать монеты через веб-интерфейс
   - После продажи сбросьте цикл: `POST /api/autotrader/reset_cycle`

3. **Логи:**
   - Все логи выводятся в консоль сервера
   - Для детальной отладки можно временно добавить `print()` в код

4. **Баланс:**
   - Проверяйте баланс перед каждым тестом
   - Убедитесь, что USDT достаточно для всех активных валют

---

## 🎉 Критерии успеха

✅ **Тест пройден, если:**
1. Для каждой валюты создаётся **РОВНО ОДИН** стартовый ордер
2. Нет дублей даже при быстром перезапуске автотрейдера
3. Режим продажи активируется, если монеты уже есть
4. Все проверки (баланс, ордера) работают корректно
5. Логи понятны и помогают в отладке
6. Веб-интерфейс отображает актуальное состояние

---

**Готов к тестированию!** 🚀

После успешного тестирования переходим к реализации:
- Докупки (rebuy)
- Продажи (sell)
