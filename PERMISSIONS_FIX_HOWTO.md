# ИНСТРУКЦИЯ ПО ПРОВЕРКЕ ИСПРАВЛЕНИЙ

## ✅ Что было исправлено

1. **Кнопка включения/выключения валюты**
   - Теперь отображается на каждой вкладке валюты (в правом верхнем углу)
   - Зелёная 🟢 = торговля разрешена
   - Красная 🔴 = торговля запрещена
   - Клик переключает состояние

2. **Номер торгового шага**
   - Отображается в правом нижнем углу вкладки
   - Синий индикатор с номером активного шага (например, "3")
   - Появляется только если есть активный цикл торговли

---

## 🚀 Как проверить

### 1. Запустите сервер
```powershell
python mTrade.py
```

### 2. Откройте браузер и перезагрузите страницу
Перейдите по адресу: `http://localhost:5050`

**ВАЖНО:** Нажмите `Ctrl + F5` для полной перезагрузки (очистка кеша)

### 3. Проверьте кнопки включения/выключения валюты

**Что должно быть видно:**
- На каждой вкладке валюты (BTC, ETH, SOL и т.д.) в **правом верхнем углу** должен быть кружок
- Кружок зелёный 🟢 или красный 🔴

**Что проверить:**
1. Наведите курсор на кружок - должна появиться подсказка:
   - "Торговля разрешена (клик для запрета)" - если зелёный
   - "Торговля запрещена (клик для разрешения)" - если красный

2. Кликните на кружок:
   - Цвет должен измениться (зелёный → красный или наоборот)
   - Вкладка НЕ должна переключиться (только цвет кнопки)

3. Откройте консоль браузера (F12):
   - Должны появиться сообщения типа:
     ```
     [DEBUG] toggleTradingPermission: BTC false -> true
     [SUCCESS] Торговля для BTC: разрешена
     ```

### 4. Проверьте отображение номера шага

**Если есть активный цикл торговли:**
- В **правом нижнем углу** вкладки (чуть левее от края) должен появиться синий индикатор
- В нём должен быть номер активного шага (например, "3")
- При наведении должна появиться подсказка "Активный шаг: 3"
- Индикатор шага должен быть **ПОВЕРХ** кнопки разрешения (если они визуально пересекаются)

**Если нет активного цикла:**
- Индикатор шага не отображается (это нормально)

**Визуальная схема вкладки:**
```
┌──────────────────────────┐
│ 🟢                  BTC  │  ← Кружок вверху справа
│                       3  │  ← Номер шага внизу справа
└──────────────────────────┘
```

---

## 🧪 Тест API (опционально)

Если сервер запущен, можно протестировать API:

```powershell
python test_permissions_api.py
```

**Ожидаемый результат:**
```
============================================================
ТЕСТ API РАЗРЕШЕНИЙ ТОРГОВЛИ
============================================================

1. Получаем текущие разрешения:
   Статус: 200
   Успех: True
   Разрешения: {
     "BTC": true,
     "ETH": false,
     ...
   }

2. Включаем торговлю для BTC:
   Статус: 200
   Успех: True
   Сообщение: Торговля BTC: разрешена

3. Проверяем обновленные разрешения:
   BTC: True
   ✓ Разрешение для BTC установлено корректно

4. Отключаем торговлю для BTC:
   Статус: 200
   Успех: True
   Сообщение: Торговля BTC: запрещена

5. Проверяем финальное состояние:
   BTC: False
   ✓ Разрешение для BTC отключено корректно

============================================================
ТЕСТ ЗАВЕРШЕН
============================================================
```

---

## 📝 Изменённые файлы

1. `static/app.js` - добавлены функции для управления разрешениями
2. `static/style.css` - добавлены стили для индикатора шага
3. `test_permissions_api.py` - тест API (новый файл)
4. `PERMISSIONS_AND_STEPS_FIX.md` - подробная документация

---

## ❓ Что делать если что-то не работает

### Кнопка не отображается:
1. Перезагрузите страницу с очисткой кеша (Ctrl + F5)
2. Очистите кеш браузера полностью
3. Проверьте консоль на ошибки (F12)

### Номер шага не отображается:
1. Убедитесь, что есть активный цикл торговли
2. Перезагрузите страницу с очисткой кеша (Ctrl + F5)
3. Проверьте, что данные загружаются:
   - Откройте консоль (F12)
   - Найдите сообщения `[INDICATORS] Загружены данные для...`
   - Проверьте, что `step=` не равен `null` или `undefined`
4. Проверьте z-index в инспекторе:
   - F12 → Elements → выберите `.step-indicator`
   - Вкладка Styles → проверьте `z-index: 3`

### Номер шага перекрыт кнопкой:
**Это исправлено!** Если проблема осталась:
1. Очистите кеш браузера (Ctrl + Shift + Delete)
2. Проверьте в DevTools (F12) что загружен новый CSS:
   - Network → перезагрузите → найдите `style.css`
   - Проверьте, что статус 200 (не 304)
3. В консоли выполните:
   ```javascript
   document.querySelectorAll('.step-indicator').forEach(el => {
     console.log('z-index:', window.getComputedStyle(el).zIndex);
     console.log('right:', window.getComputedStyle(el).right);
   });
   ```
   Должно быть: `z-index: 3` и `right: 24px`

### API не работает:
1. Убедитесь, что сервер запущен (`python mTrade.py`)
2. Проверьте, что порт 5050 свободен
3. Проверьте логи сервера на ошибки

---

## 📚 Дополнительная информация

См. файл `PERMISSIONS_AND_STEPS_FIX.md` для подробной технической документации.
