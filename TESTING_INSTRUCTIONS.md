# ИНСТРУКЦИИ ПО ТЕСТИРОВАНИЮ ИСПРАВЛЕНИЯ

## Дата: 2024-12-07

## ЧТО БЫЛО ИСПРАВЛЕНО

Исправлены **ДВЕ критические проблемы**:
1. ✅ Ручные продажи не логировались в `trade_logs/*.jsonl`
2. ✅ Цикл автотрейдера не сбрасывался после ручной продажи

## ШАГ 1: ПЕРЕЗАПУСТИТЕ СЕРВЕР

**Обязательно перезапустите сервер**, чтобы изменения вступили в силу:

```powershell
# Остановите текущий сервер (Ctrl+C)
# Затем запустите заново:
python mTrade.py
```

## ШАГ 2: ПОДГОТОВЬТЕ МОНИТОРИНГ

### Откройте 3 окна PowerShell:

#### Окно 1: Мониторинг консоли сервера
```powershell
# Просто наблюдайте за выводом сервера
# Должны увидеть:
# [TRADE_LOG] Продажа залогирована: ...
# [MANUAL_SELL][XXX] Цикл сброшен ...
```

#### Окно 2: Мониторинг логов сделок
```powershell
cd "c:\Users\Администратор\Documents\bGate.mTrade"
Get-Content "trade_logs\*_logs.jsonl" -Wait | Select-String '"type":"sell"'
```

#### Окно 3: Мониторинг всех событий
```powershell
cd "c:\Users\Администратор\Documents\bGate.mTrade"
Get-Content "trade_logs\BTC_logs.jsonl" -Tail 20 -Wait
```

## ШАГ 3: ВЫПОЛНИТЕ ТЕСТОВУЮ ПРОДАЖУ

### Вариант A: BTC (если есть баланс)

1. Откройте веб-интерфейс: http://localhost:5000
2. Найдите карточку **BTC**
3. Нажмите кнопку **"Продать всё"**
4. Подтвердите продажу

### Вариант B: Любая другая валюта с балансом

1. Откройте веб-интерфейс: http://localhost:5000
2. Найдите любую валюту с **зелёным балансом > 0**
3. Нажмите кнопку **"Продать всё"**
4. Подтвердите продажу

## ШАГ 4: ПРОВЕРЬТЕ РЕЗУЛЬТАТЫ

### ✅ Проверка 1: Консоль сервера

В консоли должны появиться **ОБА** сообщения:

```
[TRADE_LOG] Продажа залогирована: BTC, volume=0.001, price=90000
[MANUAL_SELL][BTC] Цикл сброшен после ручной продажи: active=False, last_sell_time=1234567890.123
[MANUAL_SELL][BTC] Состояние автотрейдера сохранено
```

**Если НЕТ** - исправление не работает! ❌

### ✅ Проверка 2: Окно мониторинга логов (Окно 2)

Должна появиться новая строка с продажей:

```json
{"timestamp":"2025-12-07T23:30:00.123456","type":"sell","currency":"BTC","volume":0.001,"price":90000,...}
```

**Если НЕТ** - логирование не работает! ❌

### ✅ Проверка 3: Файл логов напрямую

```powershell
Get-Content "trade_logs\BTC_logs.jsonl" -Tail 5
```

Последняя строка должна быть `"type":"sell"`:

```json
{"timestamp":"...","type":"sell","currency":"BTC",...}
```

**Если последняя строка - BUY** - логирование не сработало! ❌

### ✅ Проверка 4: Отсутствие повторных покупок

**Подождите 2-3 минуты** и убедитесь, что:

❌ В консоли **НЕТ** сообщений о новых покупках BTC
❌ В логах **НЕТ** новых записей с `"type":"buy"` для BTC

Проверьте:
```powershell
Get-Content "trade_logs\BTC_logs.jsonl" -Tail 10
```

Если после продажи появились новые покупки - **сброс цикла не работает**! ❌

### ✅ Проверка 5: Файл состояния автотрейдера

```powershell
Get-Content "autotrader_cycles_state.json" | ConvertFrom-Json | Select-Object -ExpandProperty BTC
```

Должно быть:
- `"active": false`
- `"active_step": -1`
- `"last_sell_time": <timestamp>` (НЕ 0)

**Если active=true или last_sell_time=0** - сброс не сработал! ❌

## ШАГ 5: МНОЖЕСТВЕННОЕ ТЕСТИРОВАНИЕ

Повторите Шаги 3-4 для **нескольких валют** (минимум 3):

- ETH
- SOL
- DOGE
- LTC
- XRP

Для каждой валюты должны сработать **все 5 проверок**.

## ИЗВЕСТНЫЕ ПРОБЛЕМЫ И РЕШЕНИЯ

### Проблема: "Недостаточно баланса для продажи"

**Решение**: Сначала выполните покупку:
1. Нажмите "Купить минимум" в карточке валюты
2. Дождитесь подтверждения покупки
3. Затем нажмите "Продать всё"

### Проблема: В консоли есть [TRADE_LOG], но нет [MANUAL_SELL]

**Причина**: Автотрейдер не запущен или сброс цикла не сработал

**Решение**: 
1. Проверьте, запущен ли автотрейдер (должен быть AUTO_TRADER в mTrade.py)
2. Перезапустите сервер
3. Повторите тест

### Проблема: Есть [MANUAL_SELL], но нет [TRADE_LOG]

**Причина**: Логирование продажи не сработало (filled_base=0 и amount=0?)

**Решение**:
1. Проверьте файл `handlers/quick_trades.py` (строки ~485-497)
2. Убедитесь, что там исправленный код (см. FIX_MISSING_TRADE_LOGS.md)
3. Перезапустите сервер

### Проблема: После продажи сразу начинается новая покупка

**Причина**: Сброс цикла не сработал или last_sell_time не установлен

**Решение**:
1. Проверьте `autotrader_cycles_state.json` - должен быть `last_sell_time`
2. Проверьте, что в `handlers/quick_trades.py` есть вызов сброса цикла
3. Убедитесь, что `active=false` в файле состояния

## КРИТЕРИИ УСПЕХА

Тест считается **УСПЕШНЫМ**, если после продажи:

✅ 1. В консоли есть `[TRADE_LOG] Продажа залогирована`
✅ 2. В консоли есть `[MANUAL_SELL][XXX] Цикл сброшен`
✅ 3. В `trade_logs/XXX_logs.jsonl` появилась запись `"type":"sell"`
✅ 4. В течение 5 минут НЕТ новых покупок
✅ 5. В `autotrader_cycles_state.json` установлены `active=false` и `last_sell_time`

## ЧТО ДЕЛАТЬ ЕСЛИ ТЕСТ НЕ ПРОШЁЛ

### Если НЕ сработало логирование (проверка 1-3):

```powershell
# 1. Проверьте изменения в файле:
Get-Content "handlers\quick_trades.py" -Encoding UTF8 | Select-String -Pattern "TRADE_LOG" -Context 5

# 2. Должно быть что-то вроде:
# print(f"[TRADE_LOG] Продажа залогирована: {base_currency}, volume={volume_to_log}, price={best_bid}")

# 3. Если не нашли - файл не изменён, повторите исправление
```

### Если НЕ сработал сброс цикла (проверка 4-5):

```powershell
# 1. Проверьте изменения в файле:
Get-Content "handlers\quick_trades.py" -Encoding UTF8 | Select-String -Pattern "MANUAL_SELL" -Context 10

# 2. Должен быть блок кода с:
# at.cycles[b].update({
#     'active': False,
#     'last_sell_time': current_time,
#     ...
# })

# 3. Если не нашли - файл не изменён, повторите исправление
```

## ДОПОЛНИТЕЛЬНОЕ ТЕСТИРОВАНИЕ

### Стресс-тест: Множественные продажи подряд

1. Выполните покупку для 5 валют
2. Продайте все 5 валют подряд (в течение 1 минуты)
3. Убедитесь, что для **каждой** валюты:
   - ✅ Есть запись в логах
   - ✅ Цикл сброшен
   - ❌ Нет повторных покупок

### Долгосрочный тест: Наблюдение в течение часа

После продажи наблюдайте за системой **1 час** и убедитесь, что:
- ❌ НЕТ автоматических покупок для проданных валют
- ✅ Автотрейдер работает корректно для других валют
- ✅ Нет ошибок в консоли

## ОТЧЁТ О ТЕСТИРОВАНИИ

После завершения тестирования создайте файл `TEST_RESULTS.md`:

```markdown
# РЕЗУЛЬТАТЫ ТЕСТИРОВАНИЯ ИСПРАВЛЕНИЯ

Дата: 2024-12-07
Тестировщик: [Ваше имя]

## Тесты пройдены:
- [ ] BTC - Продажа залогирована + Цикл сброшен
- [ ] ETH - Продажа залогирована + Цикл сброшен
- [ ] SOL - Продажа залогирована + Цикл сброшен
- [ ] DOGE - Продажа залогирована + Цикл сброшен
- [ ] Множественные продажи (5 валют подряд)
- [ ] Долгосрочное наблюдение (1 час)

## Обнаруженные проблемы:
[Если есть, опишите здесь]

## Итоговая оценка:
[ ] ✅ Все тесты пройдены - исправление работает
[ ] ❌ Есть проблемы - требуется дополнительная доработка
```

## КОНТАКТЫ

При возникновении проблем предоставьте:
1. Скриншот консоли с ошибкой
2. Последние 20 строк из `trade_logs/XXX_logs.jsonl`
3. Содержимое `autotrader_cycles_state.json` (для проблемной валюты)
4. Описание шагов, которые привели к проблеме

---

**Удачи с тестированием! 🚀**
