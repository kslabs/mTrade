# КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ - 08.12.2025 11:00

## ПРОБЛЕМА
При пересчёте таблицы безубыточности после стартовой покупки функция `calculate_breakeven_table()` игнорировала параметр `current_price` и использовала старое значение `start_price` из параметров.

### Пример проблемы (XRP):
- Реальная цена покупки: **2.117**
- В таблице rate (шаг 0): **2.116** (старое значение)
- Результат: таблица рассчитана с неправильной ценой!

## КОРНЕВАЯ ПРИЧИНА
В `breakeven_calculator.py` логика была:
```python
start_price = params.get('start_price', 0.0)
# Если start_price = 0, используем current_price
if start_price == 0 or start_price is None:
    start_price = current_price if current_price > 0 else 1.0
```

Это значит: **если в параметрах уже есть `start_price` (не 0), то `current_price` игнорируется!**

## ИСПРАВЛЕНИЕ
Изменена логика в `breakeven_calculator.py` (строки 83-89):
```python
# 🔴 ИСПРАВЛЕНИЕ: Если передан current_price > 0, ВСЕГДА используем его!
# Это важно для пересчёта таблицы с реальной ценой покупки
if current_price > 0:
    start_price = current_price
elif start_price == 0 or start_price is None:
    start_price = 1.0
```

Теперь приоритет:
1. **Если передан `current_price > 0` → используем его** (для пересчёта после покупки)
2. Иначе используем `params['start_price']`
3. Иначе используем значение по умолчанию 1.0

## РЕЗУЛЬТАТ
После исправления:
- ✅ Таблица всегда пересчитывается с РЕАЛЬНОЙ ценой покупки
- ✅ Целевая цена продажи всегда выше цены покупки
- ✅ Для всех валют (ETH, SOL, XRP и др.) продажи будут происходить только при достижении целевой цены

## ТЕСТИРОВАНИЕ
Создан тест `test_current_price_fix.py` - подтверждает правильность работы.

## ДЕЙСТВИЯ
**Необходимо перезапустить автотрейдер** для применения исправления:
```
SAFE_RESTART.bat
```

После перезапуска все новые циклы будут использовать правильную логику.
