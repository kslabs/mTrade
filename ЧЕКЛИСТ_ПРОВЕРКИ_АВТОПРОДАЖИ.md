# ✅ ЧЕКЛИСТ: Проверка автопродажи DOGE

## Перед запуском

- [ ] Убедитесь, что файл `autotrader_v2.py` обновлён
- [ ] Убедитесь, что на балансе есть остатки DOGE (для теста)
- [ ] Убедитесь, что в `state.json` цикл DOGE в состоянии `idle`

## Во время работы бота

### ✅ Проверьте логи на наличие:
- [ ] `[DOGE] 🔴 ОБНАРУЖЕНЫ ОСТАТКИ: X DOGE`
- [ ] `[DOGE] 🔄 Обнаружены остатки монет в IDLE состоянии → попытка автопродажи`
- [ ] `[DOGE] 🔄 === АВТОПРОДАЖА ОСТАТКОВ В IDLE СОСТОЯНИИ ===`
- [ ] `[DOGE] 💰 Доступный баланс для продажи: X`
- [ ] `[DOGE] 🚀 Создаем ордер на продажу остатков:`
- [ ] `[DOGE] ✅ Ордер создан успешно: {order_id}`
- [ ] `[DOGE] 🔄 Сбрасываем цикл после продажи остатков`

### ⚠️ Или (если остатки слишком малы):
- [ ] `[DOGE] ⚠️ Объем X меньше минимального Y`
- [ ] `[DOGE] 🗑️ Сбрасываем цикл, остатки слишком малы для продажи`

### ❌ Или (если ошибка):
- [ ] `[DOGE] ❌ Ошибка при автопродаже остатков: {error}`
- [ ] `[DOGE] 🔄 Сбрасываем цикл из-за ошибки`

## После работы бота

### ✅ Проверьте результаты:
- [ ] **Баланс DOGE** через веб-интерфейс биржи → должен быть 0 (или минимальные остатки)
- [ ] **state.json** → цикл DOGE в `idle` с пустой таблицей `"table": []`
- [ ] **История ордеров** на бирже → появился ордер на продажу DOGE
- [ ] **Логи бота** → нет ошибок, цикл сброшен корректно

## Если что-то пошло не так

### Проблема: Автопродажа не запустилась
**Возможные причины:**
1. Цикл DOGE не в состоянии `idle`
2. Остатков DOGE нет на балансе
3. Функция `_check_and_reset_if_empty` не обнаружила остатки

**Решение:**
- Проверьте `state.json` → должно быть `"state": "idle"`
- Проверьте баланс DOGE через API или веб-интерфейс
- Проверьте логи на строку `[DOGE] 🔴 ОБНАРУЖЕНЫ ОСТАТКИ`

### Проблема: Ордер не создан
**Возможные причины:**
1. Остатки DOGE меньше минимального объёма (`min_base_amount`)
2. Стоимость остатков меньше минимальной (`min_quote_amount`)
3. API биржи недоступен

**Решение:**
- Проверьте логи на строки с `min_base=` и `min_quote=`
- Убедитесь, что остатки DOGE >= `min_base_amount`
- Проверьте соединение с API биржи

### Проблема: Ордер создан, но не исполнен
**Возможные причины:**
1. Недостаточная ликвидность в стакане на выбранном уровне
2. Цена из стакана устарела

**Решение:**
- Проверьте стакан DOGE_USDT вручную через веб-интерфейс
- Убедитесь, что есть достаточный объём на продажу

## Успешное завершение

### ✅ Всё работает, если:
1. В логах появилась попытка автопродажи
2. Ордер создан успешно
3. Баланс DOGE = 0 (или близко к 0)
4. Цикл DOGE сброшен в `idle` с пустой таблицей
5. Бот готов запустить новый цикл для DOGE

### 🎉 Поздравляем!
Проблема с остатками DOGE решена! Бот теперь автоматически продаёт остатки и никогда не зависает с монетами на балансе.

---

**Дата:** $(Get-Date)  
**Версия:** autotrader_v2.py (финальная)  
**Статус:** ✅ Готово к продакшену
