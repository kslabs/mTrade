# ✅ КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ — P0 не обновлялся после покупки

**Дата:** 8 декабря 2025, 08:45  
**Проблема:** Продажи с убытком из-за устаревшей таблицы

## КОРНЕВАЯ ПРИЧИНА НАЙДЕНА! 🎯

### Что происходило:

```
1. Таблица рассчитывается ДО покупки: P0 = 2.028
2. Покупка происходит:                P_real = 2.092 (цена выросла!)
3. Таблица НЕ пересчитывается!
4. rate в таблице = 2.028
5. start_price = 2.092

target_sell_price = rate * (1 + 0.15/100) = 2.028 * 1.0015 = 2.031

Результат: продажа по 2.031, хотя купили по 2.092 → УБЫТОК!
```

### Пример из логов:

```json
{
  "start_price": 2.092,  ← Реальная цена покупки
  "table": [
    {
      "rate": 2.028,     ← Устаревший курс!
      "breakeven_price": 2.092,
      "target_delta_pct": 0.45
    }
  ]
}
```

**Расчёт целевой цены:**
```
target_sell_price = 2.028 * (1 + 0.45/100) = 2.037
```

**Но купили по 2.092!** Поэтому продажа по 2.037 = убыток -2.6%!

## РЕШЕНИЕ

Добавлен **пересчёт таблицы ПОСЛЕ покупки** с реальной ценой:

```python
# В методе _try_start_cycle, после исполнения ордера:

executed_price = float(order_status.get('avg_deal_price', price))

# КРИТИЧЕСКИ ВАЖНО: Пересчитываем таблицу с РЕАЛЬНОЙ ценой покупки!
print(f"[{base}] [DEBUG] Пересчитываем таблицу с реальной ценой покупки {executed_price}...")
table_with_real_price = calculate_breakeven_table(params, current_price=executed_price)

# Активируем цикл с правильной таблицей
cycle.table = table_with_real_price  # ← Используем НОВУЮ таблицу!
```

## Теперь работает правильно:

```
1. Таблица рассчитывается ДО покупки: P0 = 2.028
2. Покупка происходит:                P_real = 2.092
3. ✅ Таблица ПЕРЕСЧИТЫВАЕТСЯ:        P0 = 2.092 (обновлено!)
4. rate в таблице = 2.092
5. start_price = 2.092

target_sell_price = 2.092 * (1 + 0.15/100) = 2.095

Результат: продажа по 2.095+ → ПРИБЫЛЬ! ✅
```

## Изменённые файлы

- ✅ `autotrader_v2.py` (строки 696-698, метод `_try_start_cycle`)
- ✅ `RESTART_WITH_P0_FIX.bat` — скрипт перезапуска с исправлением

## Как проверить

1. Запустить: `RESTART_WITH_P0_FIX.bat`
2. Дождаться покупки
3. Проверить логи:
   ```
   [XRP] [DEBUG] Пересчитываем таблицу с реальной ценой покупки 2.092...
   [XRP] [DEBUG] Таблица пересчитана, target_delta_pct = 0.15%
   ```
4. Дождаться продажи — должна быть **с прибылью**!

## Почему это происходило?

- **Волатильность:** Цена менялась между расчётом таблицы и покупкой
- **XRP особенно волатилен:** Цена скачет быстро
- **ETH, SOL:** Менее волатильны, поэтому проблема не так заметна

## Статус

✅ **ИСПРАВЛЕНО** — таблица теперь пересчитывается с реальной ценой покупки  
🔄 **Требуется перезапуск** — запустите `RESTART_WITH_P0_FIX.bat`  
✅ **Следующая покупка** — будет с правильной таблицей и продажа с прибылью!
