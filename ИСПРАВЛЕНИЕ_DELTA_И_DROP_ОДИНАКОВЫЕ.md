# ✅ Исправление: Одинаковые значения ↓Δ% и ↓% при докупке

## 🎯 Проблема

**Было:**
```
[16:34:09] [ETH] Buy{13.5358; Курс:3147.8600; ↓Δ%:-0.62; ↓%:0.73; Инвест:13.5358}
```

- `↓Δ%` (delta_percent) = -0.62% (изменение от стартовой цены)
- `↓%` (total_drop_percent) = 0.73% (из таблицы безубыточности)

**Проблема:** Эти значения должны быть одинаковыми по модулю!

## 📊 Логика параметров

### `↓Δ%` (delta_percent)
**Формула:** `(executed_price - start_price) / start_price * 100`

**Значение:**
- Положительное → цена выросла от стартовой
- Отрицательное → цена упала от стартовой

**Пример:**
- Стартовая цена: 3167.57
- Текущая цена: 3147.86
- Дельта: (3147.86 - 3167.57) / 3167.57 * 100 = **-0.62%**

### `↓%` (total_drop_percent)
**Формула:** `abs(delta_percent)` (модуль дельты)

**Значение:**
- Всегда положительное
- Показывает **процент падения** от стартовой цены

**Пример:**
- Дельта: -0.62%
- Падение: **0.62%** (не 0.73%!)

## 📝 Изменения в `autotrader_v2.py`

### До изменения:
```python
# Брали из таблицы безубыточности (НЕПРАВИЛЬНО!)
if cycle.table and current_step >= 0 and current_step < len(cycle.table):
    actual_total_drop_pct = abs(float(cycle.table[current_step].get('cumulative_decrease_pct', 0)))
else:
    actual_total_drop_pct = 0.0

if start_price > 0:
    delta_percent = ((executed_price - start_price) / start_price) * 100.0
else:
    delta_percent = 0.0
```

### После изменения:
```python
# Рассчитываем дельту от стартовой цены
if start_price > 0:
    delta_percent = ((executed_price - start_price) / start_price) * 100.0
else:
    delta_percent = 0.0

# ✅ ИСПРАВЛЕНО: total_drop_percent = abs(delta_percent)
# Процент падения = модуль дельты (если цена упала, дельта отрицательная)
actual_total_drop_pct = abs(delta_percent)
```

## 💡 Почему было неправильно?

**Старая логика:**
- Брали `cumulative_decrease_pct` из таблицы безубыточности
- Это значение рассчитывается **заранее** при создании таблицы
- Оно НЕ соответствует реальному падению цены!

**Пример:**
- Таблица говорит: "на шаге 1 падение 0.73%"
- Реальная цена упала на 0.62%
- Получаем разные значения: `↓Δ%:-0.62` и `↓%:0.73`

**Новая логика:**
- Рассчитываем реальное падение от стартовой цены
- `delta_percent` = реальное изменение цены
- `total_drop_percent` = модуль delta_percent
- Получаем одинаковые значения: `↓Δ%:-0.62` и `↓%:0.62`

## ✅ Результат

**Теперь будет:**
```
[16:34:09] [ETH] Buy{13.5358; Курс:3147.8600; ↓Δ%:-0.62; ↓%:0.62; Инвест:13.5358}
```

- ✅ `↓Δ%` = -0.62% (цена упала на 0.62%)
- ✅ `↓%` = 0.62% (падение 0.62%)
- ✅ Значения одинаковые по модулю!

## 🚀 Для проверки

1. Перезапустите автотрейдер
2. Дождитесь докупки
3. Проверьте, что `↓Δ%` и `↓%` одинаковые по модулю

---

**Исправление завершено!** ✅  
**Файл:** `autotrader_v2.py`  
**Дата:** 2025-12-12
