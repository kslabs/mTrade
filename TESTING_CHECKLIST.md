# ✅ Чеклист проверки после исправления

## 🎯 Что проверить после обновления

### 1. Откройте страницу
```
http://localhost:5000
```

### 2. Откройте консоль разработчика
- **Chrome/Edge:** F12 или Ctrl+Shift+I
- **Firefox:** F12 или Ctrl+Shift+K
- Перейдите во вкладку "Console" / "Консоль"

### 3. Проверьте отсутствие ошибок

#### ❌ Этой ошибки больше НЕ должно быть:
```
TypeError: Cannot set properties of null (setting 'textContent')
    at loadMarketData
```

#### ✅ Должны быть только информационные сообщения:
```
Загружено валют: 10, первая: WLD
Загрузка рыночных данных для WLD_USDT
Ответ API /api/pair/data: {success: true, ...}
```

### 4. Проверьте отображение стакана

#### ✅ Стакан должен содержать:
- [ ] **Заголовок:** "📊 Стакан ордеров"
- [ ] **Колонки:** Цена (USDT) | Кол-во | Сумма | Σ Накоп.
- [ ] **ASKS (красные):** Как минимум несколько уровней продажи
- [ ] **Спред:** Желтая/оранжевая строка с разницей цен
- [ ] **BIDS (зеленые):** Как минимум несколько уровней покупки
- [ ] **Числа:** Все значения должны быть числами, не "NaN" или "undefined"

#### ✅ Интерактивность:
- [ ] При наведении мыши строка должна подсвечиваться
- [ ] При клике должно появляться всплывающее окно с деталями

### 5. Проверьте обновление данных

#### ✅ В реальном времени должно обновляться:
- [ ] Цена пары (в шапке)
- [ ] Строки стакана (ASKS и BIDS)
- [ ] Спред между лучшими ценами
- [ ] Балансы (если аккаунт выбран)

### 6. Проверьте смену валютной пары

#### ✅ При выборе другой валюты:
- [ ] Меняется название пары (например, с WLD_USDT на BTC_USDT)
- [ ] Меняется цена
- [ ] Стакан обновляется с новыми данными
- [ ] Символ валюты в заголовке стакана меняется (USDT, BTC и т.д.)

---

## 🚨 Если стакан все еще не виден

### Шаг 1: Проверьте элементы DOM
В консоли выполните:
```javascript
console.log('orderbookAsks:', document.getElementById('orderbookAsks'));
console.log('orderbookBids:', document.getElementById('orderbookBids'));
```

**Ожидаемый результат:**  
Оба должны вернуть HTML элемент, а не `null`.

### Шаг 2: Проверьте данные от API
В консоли выполните:
```javascript
fetch('/api/pair/data?base_currency=WLD&quote_currency=USDT')
  .then(r => r.json())
  .then(d => console.log('API data:', d));
```

**Ожидаемый результат:**
```json
{
  "success": true,
  "pair": "WLD_USDT",
  "data": {
    "orderbook": {
      "asks": [[price, amount], ...],
      "bids": [[price, amount], ...]
    }
  }
}
```

### Шаг 3: Проверьте CSS
В консоли выполните:
```javascript
const asks = document.getElementById('orderbookAsks');
console.log('Display:', window.getComputedStyle(asks).display);
console.log('Visibility:', window.getComputedStyle(asks).visibility);
```

**Ожидаемый результат:**  
- `display: "block"` (не "none")
- `visibility: "visible"` (не "hidden")

### Шаг 4: Очистите кэш браузера
1. Ctrl+Shift+Del
2. Выберите "Изображения и файлы в кэше"
3. Нажмите "Удалить данные"
4. Обновите страницу (F5 или Ctrl+F5)

---

## 📞 Обратная связь

### Если стакан работает:
✅ Отлично! Можно переходить к следующим улучшениям.

### Если стакан НЕ работает:
❌ Сообщите:
1. Скриншот консоли разработчика
2. Результаты проверок из раздела "Если стакан все еще не виден"
3. Браузер и его версию (например, Chrome 119)

---

**Обновлено:** 6 ноября 2025, 19:50
