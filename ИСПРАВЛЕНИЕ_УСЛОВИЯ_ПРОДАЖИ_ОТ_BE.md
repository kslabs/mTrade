# КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Условие продажи от безубытка

## Дата: 12 декабря 2025 г.

## Проблема

Бот не продавал, даже когда текущая цена была значительно выше расчётной цены продажи:

```
Текущая цена:    0.06599
Цена продажи:    0.06015538  ❌ НЕ ПРОДАВАЛ!
Безубыток (BE):  0.0592899
Стартовая (P0):  0.0606
Рост от P0:      8.89%
```

## Причина

**Код сравнивал рост от стартовой цены (P0) вместо безубытка (BE)!**

### Старая логика (НЕПРАВИЛЬНАЯ):
```python
# Рост от P0
current_growth_pct = ((market_price - start_price) / start_price) * 100.0

# Сравнение с target_delta_pct (который тоже от P0)
if current_growth_pct < required_growth_pct:
    return  # Не продаём
```

**Проблема**: После докупок безубыток (BE) отличается от P0, но код всё равно сравнивал с P0!

## Решение

**Продаём по достижению целевой цены от БЕЗУБЫТКА (BE), а не от P0!**

### Новая логика (ПРАВИЛЬНАЯ):
```python
# Получаем данные из таблицы
breakeven_price = float(params_row.get('breakeven_price', start_price))
breakeven_pct = float(params_row.get('breakeven_pct', 0))
target_delta_pct = float(params_row.get('target_delta_pct', 0))

# Профит = target_delta_pct - breakeven_pct
profit_pct = target_delta_pct - breakeven_pct

# Цена продажи = BE × (1 + профит/100)
required_price = breakeven_price * (1 + profit_pct / 100.0)

# Сравниваем напрямую цены
if market_price < required_price:
    return  # Не продаём

# Для логов показываем рост от BE
current_growth_from_be = ((market_price - breakeven_price) / breakeven_price) * 100.0
```

## Исправленный файл

- `autotrader_v2.py` → метод `_try_sell()` (строки ~1237-1290)

## Результат

Теперь бот правильно определяет момент продажи:

1. **Рассчитывает цену продажи от безубытка (BE)**, а не от P0
2. **Сравнивает абсолютные цены**, а не проценты роста от P0
3. **В логах показывает рост от BE** для корректного отображения профита

### Пример с вашими данными:

```
BE = 0.0592899
Профит = 1.46% (от BE)
Требуемая цена = 0.0592899 × 1.0146 = 0.06015538

Текущая цена = 0.06599
0.06599 >= 0.06015538  ✅ ПРОДАЁМ!
```

## Проверка

После перезапуска бота с исправлением:
1. Дождитесь следующей проверки условия продажи
2. В логах увидите новый вывод:
```
💰   Start price (P0): 0.0606
💰   Breakeven price (BE): 0.0592899
💰   Market price: 0.06599
💰   Profit %: 1.46%
💰   Required price: 0.06015538
💰   Current growth from BE: 11.29%
💰   Условие: 0.06599 >= 0.06015538 ?
✅✅✅ Условие продажи ВЫПОЛНЕНО!
```

## Важно

Это исправление критично для корректной работы бота после докупок. Без него бот может:
- Не продавать при достижении цели
- Продавать слишком рано (если рост от P0 достиг target_delta_pct, но от BE ещё не достиг профита)
