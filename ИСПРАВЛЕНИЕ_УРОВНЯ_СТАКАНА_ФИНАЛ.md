# ✅ ИСПРАВЛЕНИЕ: Синхронизация уровня стакана между таблицей и кодом

## 🔴 Проблема

**Несоответствие между отображением в таблице и реальным использованием:**

1. **В UI (таблица безубыточности)** отображался **"Ст. 1"** для шага 0 (рассчитывалось по формуле `(0 × orderbook_level) + 1 = 1`)
2. **В backend (mTrade.py и autotrader_v2.py)** использовалось значение **`orderbook_level = 0`** (по умолчанию из `row.get('orderbook_level', 0)`)
3. **В таблице, генерируемой `breakeven_calculator.py`**, **не было поля `orderbook_level`**, поэтому код всегда возвращал дефолт `0`

### Пример несоответствия:
- **Шаг 0** → В UI показывалось "Ст. 1", но код использовал `bids[0]`
- **Проблема**: Таблица не содержала поле `orderbook_level`, и значения не совпадали с ожиданиями пользователя

---

## ✅ Решение

### 1. Добавлено поле `orderbook_level` в таблицу безубыточности

**Файл:** `breakeven_calculator.py`

**Изменения:**
```python
# Добавлен параметр base_orderbook_level
base_orderbook_level = params.get('orderbook_level', 0)  # Базовый уровень стакана

# В цикле генерации таблицы:
# Формула: step × base_orderbook_level (напрямую соответствует индексу массива)
calculated_orderbook_level = round(step * base_orderbook_level)

# Добавлено в результат:
table_data.append({
    'step': step_num,
    'orderbook_level': calculated_orderbook_level,  # ← НОВОЕ ПОЛЕ
    'decrease_step_pct': decrease_step_pct,
    # ... остальные поля ...
})
```

**Результат (при base_orderbook_level = 0):**
- Для шага 0: `orderbook_level = 0 × 0 = 0` → используется `bids[0]` (лучшая цена)
- Для шага 1: `orderbook_level = 1 × 0 = 0` → используется `bids[0]` (лучшая цена)

**Результат (при base_orderbook_level = 1):**
- Для шага 0: `orderbook_level = 0 × 1 = 0` → используется `bids[0]` (лучшая цена)
- Для шага 1: `orderbook_level = 1 × 1 = 1` → используется `bids[1]` (вторая цена)
- Для шага 2: `orderbook_level = 2 × 1 = 2` → используется `bids[2]` (третья цена)

---

### 2. Прямое соответствие: значение в таблице = индекс массива

**Важно!** Значение `orderbook_level` в таблице **напрямую соответствует индексу массива стакана**:
- `orderbook_level = 0` → `bids[0]` (лучшая цена)
- `orderbook_level = 1` → `bids[1]` (вторая лучшая цена)
- `orderbook_level = 2` → `bids[2]` (третья лучшая цена)

**Никаких преобразований не требуется:**
```python
# Получаем из таблицы (напрямую соответствует индексу массива)
orderbook_level = int(row.get('orderbook_level', 0))

# Используем напрямую для индексации массива
price = bids[orderbook_level][0]
```

---

### 3. Исправлены файлы

#### `autotrader_v2.py`

**Строки 460-475:**
```python
# Получаем orderbook_level из таблицы (напрямую соответствует индексу массива)
orderbook_level = int(cycle.table[active_step].get('orderbook_level', 0))
print(f"[{base}] Уровень стакана из таблицы: {orderbook_level} (используем bids[{orderbook_level}])")

# Получаем цену из стакана на нужном уровне
orderbook_price = self._get_orderbook_price(base, quote, orderbook_level, 'bids')
if orderbook_price:
    print(f"[{base}] Используем цену из стакана bids[{orderbook_level}] = {orderbook_price:.8f}")
```

#### `mTrade.py`

**Строки 1600-1630 (функция индикаторов):**
```python
# Получаем orderbook_level из таблицы (напрямую соответствует индексу массива)
orderbook_level = int(row.get('orderbook_level', 0))

print(f"[INDICATORS] {base_currency}: Уровень стакана из таблицы: {orderbook_level} (используем массив[{orderbook_level}])")

# Используем orderbook_level для индексации массива bids/asks
if bids and orderbook_level < len(bids):
    current_price_from_orderbook = float(bids[orderbook_level][0])
    print(f"[INDICATORS] {base_currency}: Цена из bids[{orderbook_level}] = {current_price_from_orderbook:.8f}")
```

**Строка 1671 (сохранение для UI):**
```python
# Сохраняем значение из таблицы для отображения пользователю (напрямую соответствует индексу массива)
autotrade_levels['orderbook_level'] = int(row.get('orderbook_level', 0))
```

---

### 4. Обновлены дефолтные параметры

**Файл:** `mTrade.py`

```python
DEFAULT_TRADE_PARAMS = {
    'steps': 16,
    'start_volume': 3.0,
    'start_price': 0.0,
    'pprof': 0.6,
    'kprof': 0.02,
    'target_r': 3.65,
    'geom_multiplier': 2.0,
    'rebuy_mode': 'geometric',
    'orderbook_level': 0  # Базовый уровень стакана (0 = лучшая цена, 1 = вторая, и т.д.) ← ДОБАВЛЕНО
}
```

**Файл:** `state_manager.py`:
```python
def _get_default_breakeven_params(self) -> Dict[str, Any]:
    return {
        # ...
        'orderbook_level': 0  # Базовый уровень стакана (0 = лучшая цена, 1 = вторая, и т.д.)
    }
```

---

## 📊 Как это работает теперь

### Пример для XRP (шаг 0, параметр base_orderbook_level = 0):

1. **Таблица генерируется** с `orderbook_level = 0 × 0 = 0`
2. **UI показывает** "Ст. 0" ✅
3. **Backend читает** из таблицы `orderbook_level = 0`
4. **Backend использует напрямую** `bids[0]` (лучшая цена) ✅
5. **Логи показывают:**
   ```
   [XRP] Уровень стакана из таблицы: 0 (используем bids[0])
   [XRP] Используем цену из стакана bids[0] = 2.5432
   ```

### Пример для ETH (шаг 2, если `orderbook_level` в параметрах = 1):

1. **Таблица генерируется** для шага 2: `orderbook_level = 2 × 1 = 2`
2. **UI показывает** "Ст. 2" ✅
3. **Backend читает** из таблицы `orderbook_level = 2`
4. **Backend использует напрямую** `bids[2]` (3-я лучшая цена) ✅

---

## 🎯 Проверка

### Логи для проверки:

**При расчёте таблицы безубыточности:**
- В таблице должно появиться поле `orderbook_level` для каждой строки

**При работе автотрейдера (при base_orderbook_level = 0):**
```
[XRP] Уровень стакана из таблицы: 0 (используем bids[0])
[XRP] Используем цену из стакана bids[0] = 2.5432
```

**При работе автотрейдера (при base_orderbook_level = 1, шаг 1):**
```
[ETH] Уровень стакана из таблицы: 1 (используем bids[1])
[ETH] Используем цену из стакана bids[1] = 3456.78
```

**В блоке "📈 Уровни цен" на фронтенде:**
- Должно отображаться значение напрямую из таблицы (например: `Уровень стакана: 0` для шага 0)

---

## ✅ Итог

**Теперь:**
1. ✅ **Таблица** содержит поле `orderbook_level` для каждого шага
2. ✅ **UI** корректно показывает уровень стакана из таблицы
3. ✅ **Backend** использует значение напрямую (без преобразования) — полное соответствие!
4. ✅ **Логи** показывают используемый индекс массива
5. ✅ **Нет несоответствий** между тем, что показано в UI, и тем, что используется в коде
6. ✅ **Если в таблице "Ст. 1" — код использует `bids[1]`**, если "Ст. 0" — `bids[0]`

**Перезапустите сервер для применения изменений!**

```powershell
python mTrade.py
```

---

## 📝 Документация для пользователя

**Уровень стакана в таблице безубыточности:**
- **"Ст. 0"** означает использование **лучшей цены** из стакана (`bids[0]` для продаж, `asks[0]` для покупок)
- **"Ст. 1"** означает использование **второй лучшей цены** (`bids[1]` для продаж, `asks[1]` для покупок)
- **"Ст. 2"** означает использование **третьей лучшей цены** (`bids[2]` для продаж, `asks[2]` для покупок)
- И т.д.

**Формула расчёта уровня для шага:**
```
orderbook_level = номер_шага × базовый_уровень
```

Где `базовый_уровень` — это параметр `orderbook_level` из настроек (по умолчанию = 0).

**Пример (при базовый_уровень = 0):**
- Шаг 0: `0 × 0 = 0` → используется `bids[0]` (лучшая цена)
- Шаг 1: `1 × 0 = 0` → используется `bids[0]` (лучшая цена)
- Все шаги используют лучшую цену

**Пример (при базовый_уровень = 1):**
- Шаг 0: `0 × 1 = 0` → используется `bids[0]` (лучшая цена)
- Шаг 1: `1 × 1 = 1` → используется `bids[1]` (2-я лучшая цена)
- Шаг 2: `2 × 1 = 2` → используется `bids[2]` (3-я лучшая цена)

---

## 🔧 Следующие шаги

1. ✅ **Перезапустить сервер**
2. ✅ **Проверить логи** — должны появиться сообщения о преобразовании уровня стакана
3. ✅ **Проверить таблицу** — в столбце "Ст." должны быть корректные значения (1, 2, 3...)
4. ✅ **Проверить блок "📈 Уровни цен"** — должен показывать `Уровень стакана: 1`
5. ✅ **Убедиться**, что автотрейдер использует правильную цену из стакана при продаже

---

**Дата исправления:** 2025-01-XX  
**Файлы изменены:** `breakeven_calculator.py`, `autotrader_v2.py`, `mTrade.py`  
**Статус:** ✅ ГОТОВО К ТЕСТИРОВАНИЮ
