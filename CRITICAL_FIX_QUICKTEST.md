# ⚡ Быстрая проверка: P0 сохраняется в state_manager

**КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ:** Метод сохранения P0

---

## 🐛 Что было исправлено

**Проблема:** P0 не сохранялся в config.json  
**Причина:** Вызывался несуществующий метод `update_breakeven_params`  
**Решение:** Исправлено на `set_breakeven_params`

---

## 🔧 Исправление (1 строка!)

### autotrader.py, строка 473:

**ДО:**
```python
self.state_manager.update_breakeven_params(base, current_params)  # ❌
```

**ПОСЛЕ:**
```python
self.state_manager.set_breakeven_params(base, current_params)  # ✅
```

---

## 🧪 Быстрый тест

### 1. Перезапустить сервер:
```powershell
python mTrade.py
```

### 2. Сделать стартовую покупку

### 3. Проверить логи (должны быть оба!):
```
✅ [AutoTrader][XXX] 📊 Обновлён start_price in state_manager: 0.XXXXX
✅ [AutoTrader][XXX] 📊 Таблица пересчитана с новым P0: 0.XXXXX
```

### 4. Проверить таблицу в веб-интерфейсе:
```
Цена (P0): 0.XXXXX  ← должно = цена покупки ✅
```

### 5. Проверить config.json:
```json
"breakeven_params": {
  "XXX": {
    "start_price": 0.XXXXX  ← должно = цена покупки ✅
  }
}
```

---

## ✅ Критерии успеха

- [ ] В логах: `Обновлён start_price in state_manager`
- [ ] В логах: `Таблица пересчитана с новым P0`
- [ ] В таблице: P0 = цена покупки
- [ ] В config.json: start_price = цена покупки
- [ ] После перезагрузки: P0 остаётся новым

---

## 📊 Пример

**Покупка:**
```
[13:18:53] Buy{Курс:0.1585}
[13:18:53] 📊 Обновлён start_price in state_manager: 0.15850000
[13:18:53] 📊 Таблица пересчитана с новым P0: 0.15850000
```

**Таблица:**
```
Цена (P0): 0.1585 ✅
```

**config.json:**
```json
"start_price": 0.1585 ✅
```

---

**Статус:** ✅ ИСПРАВЛЕНО  
**Приоритет:** 🔴 КРИТИЧЕСКИЙ
