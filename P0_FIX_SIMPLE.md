# ✅ ИСПРАВЛЕНИЕ: P0 обновляется при стартовой покупке

**Дата:** 19 ноября 2025 г.  
**Статус:** ✅ ИСПРАВЛЕНО

---

## 🐛 Проблема

P0 (start_price) не сохранялся после стартовой покупки в state_manager.

---

## 🔧 Исправление

### Файл: `autotrader.py`, строка 473

**ДО (неверно):**
```python
self.state_manager.update_breakeven_params(base, current_params)  # ❌ Метод не существует!
```

**ПОСЛЕ (правильно):**
```python
self.state_manager.set_breakeven_params(base, current_params)  # ✅ Правильный метод
```

### Дополнительно (строки 477-481):
- Добавлен пересчёт таблицы с новым P0
- Добавлен вывод traceback при ошибках

---

## 🎯 Как работает

```
СТАРТОВАЯ ПОКУПКА по цене 0.1585
    ↓
autotrader.py обновляет P0:
    cycle['start_price'] = 0.1585
    state_manager.set_breakeven_params(base, {start_price: 0.1585})
    ↓
Таблица пересчитывается с новым P0
    ↓
Сохраняется в app_state.json
    ↓
При следующей загрузке таблицы в веб-интерфейсе:
    P0 берётся из state_manager
    ↓
Таблица отображается с правильным P0
```

---

## 🧪 Проверка

1. Перезапустить сервер: `python mTrade.py`
2. Дождаться стартовой покупки
3. Проверить логи:
   ```
   ✅ [AutoTrader][XXX] 📊 Обновлён start_price in state_manager: 0.XXXXX
   ✅ [AutoTrader][XXX] 📊 Таблица пересчитана с новым P0: 0.XXXXX
   ```
4. Открыть/обновить таблицу безубыточности в браузере
5. Проверить: Цена (P0) = цена покупки ✅

---

## 📋 Что НЕ меняется

- ❌ **НЕ** обновляется автоматически в UI (нужно обновить страницу или переключить валюту)
- ❌ **НЕ** обновляется при докупках (только при стартовой покупке)
- ✅ **Можно** вручную менять между стартовыми покупками
- ✅ **При следующей стартовой покупке** P0 снова обновится

---

**Изменённые файлы:**
- `autotrader.py` (строка 473, 477-483)

**Статус:** ✅ ГОТОВО
