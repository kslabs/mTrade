# Исправление сброса баланса в тестовом режиме

**Дата:** 2025  
**Статус:** ✅ Исправлено

## Проблема

При изменении баланса котируемой валюты в тестовом режиме и последующей перезагрузке страницы баланс сбрасывался в `0.00`.

### Причина

Функция `loadTestBalance` возвращала `null`, если баланс не был найден в localStorage. В этом случае использовался серверный баланс из тестовой сети Gate.io, который равен `0`, так как на тестовых аккаунтах нет средств.

```javascript
// Старая логика
function loadTestBalance(currency){
  if(currentNetworkMode!=='test') return null;
  const key=`test_balance_${currency}`;
  const saved=localStorage.getItem(key);
  return saved ? parseFloat(saved) : null;  // ← Возвращал null!
}
```

## Решение

Добавлены дефолтные балансы для тестового режима, которые автоматически сохраняются в localStorage при первом запуске:

```javascript
function loadTestBalance(currency){
  if(currentNetworkMode!=='test') return null;
  const key=`test_balance_${currency}`;
  const saved=localStorage.getItem(key);
  if(saved) return parseFloat(saved);
  
  // Дефолтные балансы для тестового режима
  const defaults = {
    'USDT': 10000,
    'USDC': 10000,
    'BTC': 1.0,
    'ETH': 10.0
  };
  
  const defaultBalance = defaults[currency] || 1000;
  // Сохраняем дефолтный баланс в localStorage
  localStorage.setItem(key, defaultBalance.toString());
  return defaultBalance;
}
```

### Дефолтные балансы:

| Валюта | Дефолтный баланс |
|--------|------------------|
| USDT   | 10,000          |
| USDC   | 10,000          |
| BTC    | 1.0             |
| ETH    | 10.0            |
| Прочие | 1,000           |

## Как это работает

### 1. Первый запуск в тестовом режиме

При первом запуске в тестовом режиме:
- Функция `loadTestBalance` не находит баланс в localStorage
- Возвращается дефолтный баланс из таблицы
- Баланс автоматически сохраняется в localStorage
- Пользователь видит дефолтный баланс (например, 10,000 USDT)

### 2. Редактирование баланса

Когда пользователь кликает на баланс и редактирует его:
```javascript
function editQuoteBalance(){
  if(currentNetworkMode!=='test') return;
  const currentBalance = parseFloat($('headerQuoteBalance')?.textContent || '0');
  const newBalance = prompt(`Введите новый баланс ${currentQuoteCurrency}:`, currentBalance.toFixed(2));
  if(newBalance !== null && !isNaN(parseFloat(newBalance))){
    const balance = parseFloat(newBalance);
    // Сохраняем в localStorage
    localStorage.setItem(`test_balance_${currentQuoteCurrency}`, balance.toString());
    // Обновляем отображение
    updateHeaderQuoteBalance(balance);
    // Перезагружаем балансы
    loadPairBalances();
  }
}
```

### 3. Перезагрузка страницы

После перезагрузки страницы:
- Функция `loadTestBalance` находит сохранённый баланс в localStorage
- Возвращается сохранённое значение
- Баланс восстанавливается корректно

## Преимущества решения

✅ **Автоматическая инициализация** - при первом запуске в тестовом режиме сразу доступны дефолтные балансы  
✅ **Постоянство данных** - изменённые балансы сохраняются между сессиями  
✅ **Удобство** - не нужно каждый раз вводить балансы вручную  
✅ **Реалистичность** - дефолтные значения близки к реальным для тестирования  
✅ **Гибкость** - можно редактировать балансы в любой момент  

## Тестирование

### Сценарий 1: Первый запуск
1. Выберите тестовый режим
2. Откройте страницу
3. **Проверьте:** Баланс USDT = 10,000.00 ✅
4. Переключите на USDC
5. **Проверьте:** Баланс USDC = 10,000.00 ✅

### Сценарий 2: Редактирование и перезагрузка
1. Выберите тестовый режим
2. Кликните на баланс USDT
3. Введите новое значение (например, 5000)
4. **Проверьте:** Баланс обновился до 5,000.00 ✅
5. Перезагрузите страницу (F5)
6. **Проверьте:** Баланс остался 5,000.00 ✅

### Сценарий 3: Переключение валют
1. Выберите тестовый режим
2. Измените баланс USDT на 5000
3. Измените баланс USDC на 3000
4. Переключайте между USDT и USDC
5. **Проверьте:** Балансы сохраняются для каждой валюты ✅

### Сценарий 4: Переключение режимов
1. Установите баланс USDT = 5000 в тестовом режиме
2. Переключитесь в рабочий режим (work)
3. **Проверьте:** Отображается реальный баланс с сервера ✅
4. Переключитесь обратно в тестовый режим
5. **Проверьте:** Баланс восстановился до 5000 ✅

## О пополнении тестовой сети Gate.io

**Важно:** Gate.io не предоставляет автоматическое пополнение тестовых аккаунтов. Для тестирования используйте:

1. **Тестовый режим приложения** (рекомендуется):
   - Использует локальное хранилище (localStorage)
   - Дефолтные балансы: 10,000 USDT/USDC, 1.0 BTC, 10.0 ETH
   - Можно редактировать балансы в любой момент
   - Не требует реального подключения к тестовой сети

2. **Симуляция без реальных средств**:
   - Приложение работает с виртуальными балансами
   - Все операции сохраняются локально
   - Идеально для тестирования интерфейса и логики

3. **Переход на реальную сеть**:
   - После тестирования переключитесь на рабочий режим (work)
   - Используйте реальные API ключи
   - Начните с минимальных сумм

## Связанные файлы

- `templates/index.html` - исправлена функция `loadTestBalance`
- `QUOTE_BALANCE_READY.md` - документация по работе с балансом
- `QUICK_TEST_v1.7.md` - инструкции по тестированию

## Итог

Проблема со сбросом баланса в тестовом режиме полностью решена. Теперь:
- ✅ При первом запуске доступны дефолтные балансы
- ✅ Изменённые балансы сохраняются между сессиями
- ✅ Каждая валюта имеет свой независимый баланс
- ✅ Переключение между режимами работает корректно

**Тестовый режим полностью готов к работе!** 🎉
