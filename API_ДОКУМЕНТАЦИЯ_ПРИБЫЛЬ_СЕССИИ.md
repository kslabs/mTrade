# 📡 API ДОКУМЕНТАЦИЯ: ПРИБЫЛЬ СЕССИИ

**Версия API:** 1.0  
**Дата:** 2025-01-14

---

## 📋 Список эндпоинтов

### 1. `GET /api/session-profit`
Получить прибыль сессии по валюте или всем валютам

### 2. `POST /api/reset-session`
Сбросить время старта сессии (обнулить прибыль и длительность)

---

## 🔍 Детальное описание

### 1. `GET /api/session-profit`

**Описание:**  
Возвращает прибыль с момента старта сессии. Может вернуть прибыль по конкретной валюте или общую прибыль по всем валютам.

**URL:**  
```
GET /api/session-profit
GET /api/session-profit?currency=BTC
```

**Параметры запроса:**

| Параметр   | Тип    | Обязательный | Описание                                    |
|-----------|--------|--------------|---------------------------------------------|
| `currency` | string | Нет          | Код валюты (например, "BTC", "ETH"). Если не указан — возвращается общая прибыль по всем валютам. |

**Пример запроса:**
```http
GET /api/session-profit?currency=BTC HTTP/1.1
Host: localhost:5000
```

**Пример ответа (для конкретной валюты):**
```json
{
  "success": true,
  "currency": "BTC",
  "currency_profit": 12.3456,
  "session_start_time": "2025-01-14T12:00:00"
}
```

**Пример ответа (для всех валют):**
```json
{
  "success": true,
  "total_profit": 15.7890,
  "profits_by_currency": {
    "BTC": 12.3456,
    "ETH": 3.4434
  },
  "session_start_time": "2025-01-14T12:00:00"
}
```

**Пример ответа (ошибка):**
```json
{
  "success": false,
  "error": "Trade logger not initialized"
}
```

**Поля ответа (success=true, с currency):**

| Поле                  | Тип    | Описание                                              |
|-----------------------|--------|-------------------------------------------------------|
| `success`             | bool   | `true` — успешно, `false` — ошибка                   |
| `currency`            | string | Код валюты (например, "BTC")                         |
| `currency_profit`     | float  | Прибыль по указанной валюте (с 4 знаками)           |
| `session_start_time`  | string | Время старта сессии в ISO формате (UTC)              |

**Поля ответа (success=true, без currency):**

| Поле                  | Тип    | Описание                                              |
|-----------------------|--------|-------------------------------------------------------|
| `success`             | bool   | `true` — успешно, `false` — ошибка                   |
| `total_profit`        | float  | Общая прибыль по всем валютам (с 4 знаками)         |
| `profits_by_currency` | object | Прибыль по каждой валюте: `{"BTC": 12.34, ...}`     |
| `session_start_time`  | string | Время старта сессии в ISO формате (UTC)              |

**Коды ответа:**

| Код | Описание                                                          |
|-----|-------------------------------------------------------------------|
| 200 | Успешно (success=true)                                           |
| 500 | Ошибка сервера (success=false, error содержит описание)          |

**Логика подсчёта прибыли:**

1. Используется глобальная переменная `SESSION_START_TIME` для фильтрации логов по времени
2. Учитываются только **продажи** (`type == 'sell'`)
3. Суммируется поле `total_pnl` из каждой сделки
4. Если указан параметр `currency`, возвращается прибыль только по этой валюте
5. Если `currency` не указан, возвращается общая прибыль по всем валютам

**Примечания:**

- Прибыль считается с момента старта сессии (`SESSION_START_TIME`)
- По умолчанию `SESSION_START_TIME` устанавливается при запуске сервера
- Сбросить `SESSION_START_TIME` можно через `POST /api/reset-session`
- Прибыль показывается с 4 знаками после запятой (`round(profit, 4)`)

---

### 2. `POST /api/reset-session`

**Описание:**  
Сбрасывает время старта сессии на текущий момент. После вызова этого эндпоинта прибыль и длительность сессии обнуляются.

**URL:**  
```
POST /api/reset-session
```

**Параметры запроса:**  
Нет (тело запроса пустое)

**Пример запроса:**
```http
POST /api/reset-session HTTP/1.1
Host: localhost:5000
Content-Type: application/json
```

**Пример ответа (успех):**
```json
{
  "success": true,
  "message": "Время старта сессии сброшено",
  "session_start_time": "2025-01-14T15:30:00"
}
```

**Пример ответа (ошибка):**
```json
{
  "success": false,
  "error": "Internal server error"
}
```

**Поля ответа (success=true):**

| Поле                  | Тип    | Описание                                              |
|-----------------------|--------|-------------------------------------------------------|
| `success`             | bool   | `true` — успешно, `false` — ошибка                   |
| `message`             | string | Сообщение об успешном сбросе                         |
| `session_start_time`  | string | Новое время старта сессии в ISO формате (UTC)        |

**Коды ответа:**

| Код | Описание                                                          |
|-----|-------------------------------------------------------------------|
| 200 | Успешно (success=true)                                           |
| 500 | Ошибка сервера (success=false, error содержит описание)          |

**Логика работы:**

1. Устанавливает `SESSION_START_TIME = datetime.now()`
2. Логирует событие в консоль сервера:  
   `[SESSION] Время старта сессии сброшено: 2025-01-14T15:30:00`
3. Возвращает новое время старта сессии

**Примечания:**

- Сброс сессии **не удаляет** логи сделок из `trades.json`
- Сброс только изменяет точку отсчёта для расчёта прибыли и длительности
- После сброса прибыль и длительность обнуляются
- Следующие запросы `/api/session-profit` будут учитывать только сделки после нового `SESSION_START_TIME`

---

## 🔗 Связь с frontend

### JavaScript функция `updateSessionProfit()`

**Что делает:**
1. Получает текущую валюту из `window.currentBaseCurrency`
2. Формирует URL с параметром `currency` (если валюта выбрана)
3. Запрашивает данные с сервера: `GET /api/session-profit?currency=BTC`
4. Обновляет отображение прибыли и длительности в UI

**Код:**
```javascript
async function updateSessionProfit() {
    const currentCurrency = window.currentBaseCurrency || null;
    const url = currentCurrency 
        ? `/api/session-profit?currency=${currentCurrency}`
        : '/api/session-profit';
    
    const response = await fetch(url);
    const data = await response.json();
    
    if (data.success) {
        // Обновляем прибыль
        const profit = data.currency_profit || data.total_profit;
        updateProfitDisplay(profit);
        
        // Обновляем длительность
        if (data.session_start_time) {
            const duration = calculateDuration(data.session_start_time);
            updateDurationDisplay(duration);
        }
    }
}
```

### JavaScript обработчик кнопки "Сброс сессии"

**Что делает:**
1. Показывает подтверждение пользователю
2. Отправляет запрос: `POST /api/reset-session`
3. Показывает результат (alert)
4. Вызывает `updateSessionProfit()` для обновления UI

**Код:**
```javascript
resetSessionBtn.addEventListener('click', async (e) => {
    e.preventDefault();
    if (!confirm('Вы уверены, что хотите сбросить время старта сессии?')) {
        return;
    }
    
    const response = await fetch('/api/reset-session', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'}
    });
    
    const data = await response.json();
    
    if (data.success) {
        alert('✅ Время старта сессии сброшено!');
        updateSessionProfit();
    } else {
        alert('❌ Ошибка: ' + data.error);
    }
});
```

---

## 📊 Примеры использования

### 1. Получить прибыль по Bitcoin

**Запрос:**
```bash
curl -X GET "http://localhost:5000/api/session-profit?currency=BTC"
```

**Ответ:**
```json
{
  "success": true,
  "currency": "BTC",
  "currency_profit": 12.3456,
  "session_start_time": "2025-01-14T12:00:00"
}
```

### 2. Получить общую прибыль по всем валютам

**Запрос:**
```bash
curl -X GET "http://localhost:5000/api/session-profit"
```

**Ответ:**
```json
{
  "success": true,
  "total_profit": 15.7890,
  "profits_by_currency": {
    "BTC": 12.3456,
    "ETH": 3.4434
  },
  "session_start_time": "2025-01-14T12:00:00"
}
```

### 3. Сбросить время старта сессии

**Запрос:**
```bash
curl -X POST "http://localhost:5000/api/reset-session" \
  -H "Content-Type: application/json"
```

**Ответ:**
```json
{
  "success": true,
  "message": "Время старта сессии сброшено",
  "session_start_time": "2025-01-14T15:30:00"
}
```

---

## 🔒 Безопасность

### Аутентификация
- ❌ Не требуется (локальный сервер)
- ⚠️ **Важно:** В production необходимо добавить аутентификацию!

### CORS
- ❌ Не настроен (только localhost)
- ⚠️ **Важно:** В production настроить CORS headers

### Rate Limiting
- ❌ Не реализован
- ⚠️ **Важно:** В production добавить ограничение запросов

---

## 🐛 Обработка ошибок

### Возможные ошибки

| Ошибка                          | Причина                                  | Решение                              |
|---------------------------------|------------------------------------------|--------------------------------------|
| `Trade logger not initialized`  | Логгер не инициализирован при старте    | Перезапустить сервер                |
| `Invalid currency`              | Неизвестный код валюты                   | Проверить код валюты (BTC, ETH, ...) |
| `Internal server error`         | Ошибка на сервере                        | Проверить логи сервера              |

### Логирование

Все запросы логируются в консоль сервера:

```
[SESSION] Время старта сессии сброшено: 2025-01-14T15:30:00
[ERROR] get_session_profit: Trade logger not initialized
```

---

## 📝 Changelog

### v1.0 (2025-01-14)
- ✅ Добавлен эндпоинт `GET /api/session-profit`
- ✅ Добавлен параметр `currency` для фильтрации по валюте
- ✅ Добавлено поле `session_start_time` в ответе
- ✅ Добавлен эндпоинт `POST /api/reset-session`
- ✅ Добавлено логирование событий сброса сессии

---

**Автор документации:** GitHub Copilot  
**Дата:** 2025-01-14  
**Версия:** 1.0
