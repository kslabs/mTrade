# 🎯 Быстрый старт: Сохранение параметров

## Как это работает

### 1. Изменение параметров
1. Откройте таблицу безубыточности
2. Измените любые параметры (R, Rk, Стакан, Keep и т.д.)
3. Нажмите кнопку **"💾 Сохранить"**
4. Статус: "✓ Сохранено"

### 2. Переключение между валютами
1. Переключитесь с BTC на ETH
2. Параметры **автоматически загрузятся** для ETH
3. Каждая валюта имеет свои собственные параметры

### 3. Перезагрузка страницы
1. Нажмите F5 для перезагрузки
2. Параметры **автоматически восстановятся**
3. Вы увидите последние сохранённые значения

### 4. Перезапуск сервера
1. Остановите и запустите сервер
2. Все параметры **сохранены на диске** (app_state.json)
3. После запуска все настройки восстановлены

## Что сохраняется

**Все 11 параметров таблицы безубыточности:**

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| Шагов | Количество шагов цикла | 16 |
| Старт, $ | Стартовый объём | $3 |
| Цена (P0) | Начальная цена | 0 (текущая) |
| Pprof, % | Процент профита | 0.6% |
| Kprof | Коэффициент уменьшения | 0.02 |
| R | Базовый шаг процента | 3.65% |
| Rk | Коэффициент изменения шага | 0.0% |
| Множитель геометрии | Геометрическая прогрессия | 2.0 |
| Режим сумм докупок | fixed/geometric/martingale | geometric |
| Keep | Остаток на балансе | $0 |
| Стакан | Уровень стакана | 1.0 |

## Примеры использования

### Настройка для BTC
```
1. Выберите вкладку "BTC"
2. Установите:
   - Старт: $5
   - R: 3.5%
   - Rk: 0.1%
   - Стакан: 1.5
3. Нажмите "💾 Сохранить"
```

### Настройка для ETH
```
1. Выберите вкладку "ETH"
2. Установите:
   - Старт: $10
   - R: 4.0%
   - Rk: 0.2%
   - Keep: $5
3. Нажмите "💾 Сохранить"
```

### Переключение
```
1. Переключитесь на BTC → увидите параметры BTC
2. Переключитесь на ETH → увидите параметры ETH
3. Каждая валюта сохраняет свои настройки!
```

## Файл сохранения

Параметры хранятся в файле:
```
bGate.mTrade/app_state.json
```

Структура:
```json
{
  "breakeven_params": {
    "BTC": {
      "steps": 16,
      "start_volume": 5.0,
      "target_r": 3.5,
      "rk": 0.1,
      ...
    },
    "ETH": {
      "steps": 12,
      "start_volume": 10.0,
      "target_r": 4.0,
      "rk": 0.2,
      "keep": 5.0,
      ...
    }
  }
}
```

## Устранение проблем

### Параметры не сохраняются?
1. Проверьте, что сервер запущен
2. Убедитесь, что выбрана правильная валюта
3. Проверьте консоль браузера (F12) на ошибки

### Параметры не восстанавливаются?
1. Проверьте файл `app_state.json` на наличие данных
2. Убедитесь, что права на запись в файл есть
3. Проверьте логи сервера

### Хочу сбросить все параметры?
1. Остановите сервер
2. Откройте `app_state.json`
3. Удалите секцию `"breakeven_params"` или весь файл
4. Запустите сервер (создастся с параметрами по умолчанию)

## Git commit
```
8d52e6d feat: сохранение всех параметров безубыточности (rk и keep)
```

---
✅ Все параметры сохраняются автоматически!
