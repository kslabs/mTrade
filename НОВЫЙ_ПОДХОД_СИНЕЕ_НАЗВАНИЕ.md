# ✅ НОВЫЙ ПОДХОД: СИНЕЕ НАЗВАНИЕ ВАЛЮТЫ ДЛЯ НЕАКТИВНЫХ

## 🎯 Что изменилось

**Старый подход:** Синий бордюр для валют с отключенной торговлей  
**Новый подход:** **Ярко-синий цвет названия валюты** для всех неактивных валют

---

## 🎨 Визуальная индикация

### Для НЕАКТИВНЫХ валют (торговля запрещена или не разрешена):
- **Название валюты (code-label)** окрашивается в **ярко-синий цвет** `#2196F3`
- При наведении: цвет меняется на `#42A5F5` (более светлый синий)
- Для активной вкладки: цвет `#64B5F6` (ещё светлее для контраста с зелёным фоном)
- Бордюр остаётся **стандартным** (зелёный или белый)

### Для АКТИВНЫХ валют (торговля разрешена):
- Название валюты - **стандартный цвет** (белый/серый)
- Бордюр меняется в зависимости от цены:
  - **Жёлтый** - цена выше цены продажи (готов к продаже)
  - **Красный** - цена ниже цены покупки (готов к докупке)
  - **Зелёный** - цена в нормальном диапазоне

---

## 📝 Логика определения неактивной валюты

Валюта считается **НЕАКТИВНОЙ**, если:
```javascript
permissionEnabled !== true
```

Это означает:
- `permissionEnabled === undefined` → **НЕАКТИВНАЯ** (не установлено разрешение)
- `permissionEnabled === null` → **НЕАКТИВНАЯ** 
- `permissionEnabled === false` → **НЕАКТИВНАЯ** (явно запрещено)
- `permissionEnabled === true` → **АКТИВНАЯ** (явно разрешено)

---

## 🔧 Изменения в коде

### 1. CSS (style.css)
Заменили правила для синего бордюра на правила для синего текста:

```css
/* Синий цвет названия валюты - торговля выключена (inactive) */
.base-currency-tabs .tab-item.inactive-currency .code-label {color:#2196F3!important}
.base-currency-tabs .tab-item.inactive-currency:hover .code-label {color:#42A5F5!important}
.base-currency-tabs .tab-item.active.inactive-currency .code-label {color:#64B5F6!important}
```

### 2. JavaScript (app.js)
В функции `updateTabsPermissionsUI()`:

**До:**
```javascript
// Применяли синий бордюр для trading-disabled
tab.classList.add('trading-disabled');
```

**После:**
```javascript
// ШАГ 1: Проверяем, разрешена ли торговля
const isInactive = (permissionEnabled !== true);

if(isInactive){
  // Для неактивных валют - красим название валюты в ярко-синий цвет
  tab.classList.add('inactive-currency');
  console.log(`[CURRENCY_STATUS] ${code}: INACTIVE (permission=${permissionEnabled}) - название синее`);
} else {
  // ШАГ 2: Для активных валют применяем цветную индикацию по ценам
  // ... логика с ready-to-sell, ready-to-buy ...
}
```

---

## ✅ Преимущества нового подхода

1. **Более заметно:** Синее название бросается в глаза сразу
2. **Не конфликтует:** Бордюр остаётся для индикации готовности к покупке/продаже
3. **Интуитивно:** Синий = "холодная", неактивная валюта
4. **Чётко:** Разделение визуальных индикаторов:
   - Цвет текста → статус торговли (активна/неактивна)
   - Цвет бордюра → готовность к действию (покупка/продажа)

---

## 🧪 Как проверить

### Шаг 1: Запустить приложение
```powershell
python _autotrader.py
```

### Шаг 2: Открыть веб-интерфейс
```
http://localhost:8080
```

### Шаг 3: Проверить визуальную индикацию

#### Тест 1: Новая валюта (разрешение не установлено)
1. Добавьте новую валюту через менеджер валют
2. **Ожидаемый результат:**
   - Название валюты - **СИНЕЕ** (#2196F3)
   - Бордюр - зелёный (стандартный)
   - Индикатор разрешения - красный (выключено)

#### Тест 2: Отключение торговли
1. Нажмите на красный индикатор разрешения для активной валюты
2. **Ожидаемый результат:**
   - Название валюты становится **СИНИМ**
   - Бордюр возвращается к стандартному
   - Индикатор разрешения - красный

#### Тест 3: Включение торговли
1. Нажмите на красный индикатор для неактивной валюты
2. **Ожидаемый результат:**
   - Название валюты становится **БЕЛЫМ/СЕРЫМ**
   - Бордюр может менять цвет в зависимости от цены
   - Индикатор разрешения - зелёный

#### Тест 4: Валюта ANIME
1. Найдите вкладку ANIME
2. Проверьте консоль браузера (F12):
   ```
   [PERMISSION_DEBUG] ANIME: {permissionEnabled: undefined, ...}
   [CURRENCY_STATUS] ANIME: INACTIVE (permission=undefined) - название синее
   ```
3. **Ожидаемый результат:**
   - Название "ANIME" - **СИНЕЕ**

---

## 📊 Таблица состояний

| Состояние | permissionEnabled | Цвет названия | Цвет бордюра | CSS класс |
|-----------|------------------|---------------|--------------|-----------|
| Не установлено | `undefined` | 🔵 Синий | 🟢 Зелёный | `inactive-currency` |
| Явно запрещено | `false` | 🔵 Синий | 🟢 Зелёный | `inactive-currency` |
| `null` | `null` | 🔵 Синий | 🟢 Зелёный | `inactive-currency` |
| Явно разрешено | `true` | ⚪ Белый | 🟡/🔴/🟢 По цене | (без класса) |

---

## 🐛 Отладка

### Консоль браузера (F12)
Ищите следующие сообщения:
```
[PERMISSION_DEBUG] ANIME: {permissionEnabled: undefined, ...}
[CURRENCY_STATUS] ANIME: INACTIVE (permission=undefined) - название синее
```

### Инспектор элементов
1. Нажмите F12 → вкладка Elements
2. Найдите элемент `.tab-item[data-code="ANIME"]`
3. Проверьте:
   - Класс `inactive-currency` должен быть в списке
   - `.code-label` должен иметь `color: #2196F3 !important`

---

## 📂 Изменённые файлы

1. **static/style.css** - добавлены правила для `.inactive-currency .code-label`
2. **static/app.js** - изменена логика в `updateTabsPermissionsUI()`

---

## 🎉 ГОТОВО!

Новая визуальная схема реализована. Теперь все неактивные валюты будут иметь **ярко-синее название**, что сразу бросается в глаза и не конфликтует с индикацией готовности к покупке/продаже через цвет бордюра.
