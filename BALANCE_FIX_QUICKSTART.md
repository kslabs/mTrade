# 🎯 Исправление: BALANCE_NOT_ENOUGH при продаже

## Проблема
```
❌ [BALANCE_NOT_ENOUGH] Not enough balance
Баланс DOGE: 85.88543
Количество: 85.89 DOGE  ← больше чем доступно!
```

## Причина
`round(85.88543, 2) = 85.89` — округлил **вверх**, превысил баланс.

## Решение
Заменили `round()` на `math.floor()` — теперь всегда округляет **вниз**:

```python
# ДО:
amount = round(base_balance, amount_precision)  # может быть > balance ❌

# ПОСЛЕ:
import math
amount = math.floor(base_balance * (10 ** amount_precision)) / (10 ** amount_precision)  # всегда <= balance ✅
```

## Результат
```
Баланс: 85.88543 DOGE
Amount: 85.88 DOGE (floor)
✅ Ордер создан успешно
```

## Что изменилось
- **Файл:** `mTrade.py`
- **Функция:** `quick_sell_all()`
- **Строки:** ~1947

## Как проверить
1. Попробуйте продать весь баланс DOGE
2. Убедитесь, что количество в ордере **меньше или равно** балансу
3. Ордер должен создаться без ошибки BALANCE_NOT_ENOUGH

---

**Полная документация:** `BALANCE_ROUNDING_FIX.md`
