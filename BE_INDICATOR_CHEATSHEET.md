# 🚀 Быстрая проверка BE индикатора - Шпаргалка

## Запуск
```powershell
cd "c:\Users\Администратор\Documents\bGate.mTrade"
python mTrade.py
```

## Проверка основного приложения

### 1. Открыть
```
http://localhost:5000
```

### 2. Консоль (F12) → Что искать
```javascript
// Каждые 7 секунд должно быть:
[INDICATORS] Ответ сервера: { autotrade_levels: { breakeven_price: 0.1567 } }
[SCALE] Цены для индикатора: { be: 0.1567 }

// Если BE = null → цикл не активен (это нормально)
// Если ошибки → см. ниже
```

### 3. Визуально проверить
- [ ] Маркер BE (сиреневый) виден на шкале?
- [ ] Значение BE в футере корректное?
- [ ] Тултип показывается при наведении?

---

## Тестовая страница

### 1. Открыть
```
http://localhost:5000/test_be_indicator.html
```

### 2. Выполнить тесты (по порядку)
1. **Проверить /api/trade/indicators** → BE должно быть в ответе
2. **Проверить наличие маркеров** → Все должны быть найдены
3. **Имитировать обновление с BE** → Без ошибок
4. **Смотреть логи** → Должны быть зелёные сообщения

---

## Быстрая диагностика

### Проблема: BE = null
**Причина:** Цикл не активен  
**Решение:** Запустить цикл автотрейдера или подождать

### Проблема: Маркер не найден
**Причина:** Элемент отсутствует в HTML  
**Решение:** Проверить `templates/index.html` → `<div id="markerBE">`

### Проблема: Маркер есть, но не виден
**Причина:** CSS или позиция  
**Решение:** Инспектор (F12 → Elements) → проверить `style.bottom` и `opacity`

---

## API эндпоинт (прямая проверка)
```
http://localhost:5000/api/trade/indicators?base_currency=DOGE&quote_currency=USDT
```

**Что должно быть в ответе:**
```json
{
  "success": true,
  "autotrade_levels": {
    "breakeven_price": 0.1567,  // ← Должно быть число, не null
    ...
  }
}
```

---

## Логи консоли (нормальная работа)
```
[INDICATORS] Ответ сервера: {...}
[INDICATORS] autotrade_levels: { breakeven_price: 0.1567 }
[INDICATORS] Передаём в updateTradeIndicators: {...}
[SCALE] updateVisualIndicatorScale вызван с levels: {...}
[SCALE] Маркеры: { sell: 'найден', be: 'найден', ... }
[SCALE] Цены для индикатора: { be: 0.1567 }
```

---

## Временное решение (если не работает)

Добавить в `static/app.js` → функция `updateVisualIndicatorScale()`:

```javascript
// После строки: const prices = { ... };
// Добавить:

// ВРЕМЕННО ДЛЯ ТЕСТА - принудительное BE
if (!prices.be && prices.current) {
  prices.be = prices.current * 1.01; // BE = цена + 1%
  console.warn('[TEST] BE установлено принудительно:', prices.be);
}
```

Это покажет маркер BE, даже если сервер не возвращает реальное значение.

---

## Файлы для редактирования

### Если проблема в клиенте (JavaScript)
- `static/app.js` → функции `loadPerBaseIndicators()`, `updateVisualIndicatorScale()`

### Если проблема в сервере (Python)
- `mTrade.py` → эндпоинт `/api/trade/indicators`
- `breakeven_calculator.py` → расчёт BE

### Если проблема в HTML
- `templates/index.html` → `<div id="markerBE">`

### Если проблема в CSS
- `static/style.css` → `.marker-be`

---

## Что отправить для помощи

1. **Консоль браузера:**
   - Все логи `[INDICATORS]`
   - Все логи `[SCALE]`
   - Все ошибки (красные)

2. **Ответ API:**
   ```
   http://localhost:5000/api/trade/indicators?base_currency=DOGE&quote_currency=USDT
   ```
   Скопировать весь JSON

3. **Результаты тестов:**
   - Скриншот `test_be_indicator.html`
   - Логи из секции "Консоль логов"

---

## Полная документация

- **FIX_BE_INDICATOR_GUIDE.md** - детальное руководство
- **TEST_BE_INDICATOR.md** - инструкция по тестированию
- **SUMMARY_v1.8.6_FINAL.md** - общий отчёт

---

**Дата:** 2025-01-XX  
**Версия:** v1.8.6 (диагностика)
