# Инструкция по очистке кеша браузера

**Дата:** 2025-11-06, 20:00  
**Проблема:** Браузер показывает старую версию страницы (кеш)

---

## 🔄 Что сделано

1. ✅ Добавлены метатеги для отключения кеширования в HTML
2. ✅ Добавлены консольные логи для отладки спреда
3. ✅ Сервер перезапущен

---

## 🌐 Как обновить страницу в браузере

### Вариант 1: Жесткая перезагрузка (рекомендуется)

#### Chrome / Edge:
- **Windows:** `Ctrl + Shift + R` или `Ctrl + F5`
- **Mac:** `Cmd + Shift + R`

#### Firefox:
- **Windows:** `Ctrl + Shift + R` или `Ctrl + F5`
- **Mac:** `Cmd + Shift + R`

#### Safari:
- **Mac:** `Cmd + Option + R`

### Вариант 2: Очистка кеша через настройки

#### Chrome / Edge:
1. Нажмите `Ctrl + Shift + Del` (Windows) или `Cmd + Shift + Del` (Mac)
2. Выберите временной диапазон: "За всё время" или "За последний час"
3. Отметьте только "Изображения и файлы в кэше"
4. Нажмите "Удалить данные"
5. Обновите страницу (`F5`)

#### Firefox:
1. Нажмите `Ctrl + Shift + Del` (Windows) или `Cmd + Shift + Del` (Mac)
2. Выберите временной диапазон: "Всё"
3. Отметьте только "Кэш"
4. Нажмите "Удалить сейчас"
5. Обновите страницу (`F5`)

### Вариант 3: Режим инкогнито

Откройте страницу в режиме инкогнито (приватном режиме):
- **Chrome/Edge:** `Ctrl + Shift + N`
- **Firefox:** `Ctrl + Shift + P`

Затем откройте: http://localhost:5000

---

## 🔍 Проверка через консоль разработчика

1. Откройте консоль (`F12`)
2. Перейдите во вкладку "Console"
3. Найдите сообщение:
   ```
   Обновление спреда: {spread: ..., spreadFormatted: "0.00010000", ...}
   ```
4. Убедитесь, что `spreadFormatted` отображается в десятичной форме (с 8 знаками)

---

## ✅ Что вы должны увидеть

### В блоке стакана:

```
┌─────────────────────────────────────────────┐
│ 2.5125        Спред: 0.00010000 (0.0040%)  │
│ [Зеленый]            [Оранжевый]           │
└─────────────────────────────────────────────┘
```

**Слева:** Текущий курс (например: 2.5125)  
**Справа:** Спред в десятичной форме с 8 знаками (например: 0.00010000)

### ❌ НЕ должно быть:
- `1e-4` (экспоненциальная форма)
- `0.0001` (меньше 8 знаков)
- `1.0000e-4` (научная нотация)

### ✅ Должно быть:
- `0.00010000` (десятичная форма, 8 знаков)

---

## 🐛 Если проблема сохраняется

### Шаг 1: Проверьте консоль
```javascript
// В консоли браузера выполните:
console.log(document.getElementById('spreadValue').textContent);
```

Должно вывести что-то вроде: `"0.00010000 (0.0040%)"`

### Шаг 2: Проверьте версию файла
```javascript
// В консоли браузера выполните:
console.log(document.querySelector('meta[http-equiv="Cache-Control"]'));
```

Если вывод `null`, значит браузер загрузил старую версию.  
Выполните жесткую перезагрузку (Ctrl+Shift+R).

### Шаг 3: Проверьте логи
В консоли должны быть сообщения:
```
Обновление спреда: {spread: 0.0001, spreadFormatted: "0.00010000", ...}
```

Если их нет, значит данные стакана не обновляются.

---

## 📝 Метатеги для отключения кеша

Добавлены в `<head>`:
```html
<meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate">
<meta http-equiv="Pragma" content="no-cache">
<meta http-equiv="Expires" content="0">
```

Эти теги заставляют браузер всегда загружать свежую версию страницы.

---

## 🎯 После очистки кеша

1. ✅ Текущий курс отображается слева (зеленым)
2. ✅ Спред справа в формате `0.00010000 (0.0040%)`
3. ✅ Никакой экспоненциальной формы
4. ✅ Всегда 8 знаков после запятой

---

**Обновлено:** 2025-11-06, 20:00  
**Сервер:** http://localhost:5000  
**Статус:** ✅ Готово к тестированию
