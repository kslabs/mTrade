# 🔍 Инструкция по тестированию таблицы безубыточности при изменении geom_multiplier

## ✅ Что было сделано

### 1. Добавлено подробное логирование в JavaScript (static/app.js)

**В обработчике изменения параметров (строки ~2340):**
- Логируется каждое изменение поля параметра
- Выводятся все параметры перед обновлением таблицы
- Это позволит видеть, когда пользователь изменяет `geom_multiplier`

**В функции loadBreakEvenTable() (строки ~1560-1595):**
- Логируются параметры, прочитанные из формы
- Логируется значение `geom_multiplier` из формы
- Логируется финальный URL запроса
- Логируется ответ от сервера
- Логируются параметры и данные таблицы из ответа

### 2. Существующее логирование на сервере (trade_params_routes.py)

**В функции get_breakeven_table() (строки ~145-193):**
- Выводятся входящие query string параметры
- Выводятся загруженные параметры
- При переопределении `geom_multiplier` выводится новое значение
- Выводятся финальные параметры перед расчётом таблицы

## 🧪 Порядок тестирования

### Шаг 1: Запустить приложение

```powershell
python autotrader_v2.py
```

### Шаг 2: Открыть веб-интерфейс

1. Открыть браузер (желательно Chrome/Edge для доступа к Developer Tools)
2. Перейти на `http://localhost:8000`
3. Открыть DevTools (F12)
4. Перейти на вкладку "Console"

### Шаг 3: Проверить начальное состояние

В консоли браузера должны быть сообщения о загрузке таблицы:
```
[BREAKEVEN] Цикл неактивен или таблица отсутствует - пересчитываем с текущими параметрами
[BREAKEVEN] 📊 Параметры из формы: {steps: 16, start_volume: 3, ..., geom_multiplier: 2, ...}
[BREAKEVEN] 🔢 geom_multiplier: 2
[BREAKEVEN] 🌐 URL запроса: /api/breakeven/table?base_currency=...&geom_multiplier=2&...
[BREAKEVEN] 📥 Ответ от сервера: {success: true, table: [...], params: {...}}
[BREAKEVEN] 📋 Параметры из ответа: {geom_multiplier: 2, ...}
[BREAKEVEN] 🔢 geom_multiplier из ответа: 2
```

В консоли сервера (терминал PowerShell) должны быть сообщения:
```
[BREAKEVEN_TABLE] 🔍 Входящие query params: {'base_currency': 'WLD', 'geom_multiplier': '2', ...}
[BREAKEVEN_TABLE] 📋 Загруженные params: {'geom_multiplier': 2, ...}
[BREAKEVEN_TABLE] ✅ geom_multiplier переопределён: 2.0
[BREAKEVEN_TABLE] 🎯 Финальные params перед расчётом: {'geom_multiplier': 2.0, ...}
```

### Шаг 4: Изменить значение geom_multiplier

1. В веб-интерфейсе найти поле "Множитель геометрии" (paramGeomMultiplier)
2. Изменить значение с `2` на `3` (или любое другое значение)
3. Подождать 500 мс (автоматическая задержка)

### Шаг 5: Проверить логи в консоли браузера

**Сразу после изменения поля:**
```
[PARAMS_CHANGE] 🔄 Поле "paramGeomMultiplier" изменено на: 3
```

**После задержки 500 мс:**
```
[PARAMS_CHANGE] ⚙️ Все параметры перед обновлением таблицы:
  - paramSteps: 16
  - paramStartVolume: 3
  - paramStartPrice: 0
  - paramPprof: 0.6
  - paramKprof: 0.02
  - paramTargetR: 3.65
  - paramGeomMultiplier: 3  ⬅️ Новое значение!
  - paramRebuyMode: geometric
  - paramKeep: 0
  - paramOrderbookLevel: 1
[BREAKEVEN] Цикл неактивен или таблица отсутствует - пересчитываем с текущими параметрами
[BREAKEVEN] 📊 Параметры из формы: {steps: 16, ..., geom_multiplier: 3, ...}
[BREAKEVEN] 🔢 geom_multiplier: 3  ⬅️ Новое значение!
[BREAKEVEN] 🌐 URL запроса: /api/breakeven/table?...&geom_multiplier=3&...  ⬅️ Новое значение в URL!
[BREAKEVEN] 📥 Ответ от сервера: {success: true, table: [...], params: {geom_multiplier: 3, ...}}
[BREAKEVEN] 🔢 geom_multiplier из ответа: 3  ⬅️ Новое значение!
```

### Шаг 6: Проверить логи сервера

В терминале PowerShell должны появиться новые сообщения:
```
[BREAKEVEN_TABLE] 🔍 Входящие query params: {'base_currency': 'WLD', 'geom_multiplier': '3', ...}  ⬅️ Новое значение!
[BREAKEVEN_TABLE] 📋 Загруженные params: {'geom_multiplier': 2, ...}  ⬅️ Старое сохранённое
[BREAKEVEN_TABLE] ✅ geom_multiplier переопределён: 3.0  ⬅️ Новое значение применено!
[BREAKEVEN_TABLE] 🎯 Финальные params перед расчётом: {'geom_multiplier': 3.0, ...}  ⬅️ Новое значение!
```

### Шаг 7: Проверить таблицу визуально

**Как должна измениться таблица при увеличении geom_multiplier:**

При `geom_multiplier = 2`:
- Шаг 0: Покупка = $3.00
- Шаг 1: Покупка = $6.00 (3 × 2)
- Шаг 2: Покупка = $12.00 (6 × 2)
- Шаг 3: Покупка = $24.00 (12 × 2)

При `geom_multiplier = 3`:
- Шаг 0: Покупка = $3.00
- Шаг 1: Покупка = $9.00 (3 × 3) ✅
- Шаг 2: Покупка = $27.00 (9 × 3) ✅
- Шаг 3: Покупка = $81.00 (27 × 3) ✅

**Колонка "Покупка (USDT)" должна значительно увеличиться!**

## 🔴 Возможные проблемы и решения

### Проблема 1: Таблица не обновляется

**Симптомы:**
- В консоли браузера нет сообщений о запросе
- Статус "⏳ Обновление..." не появляется

**Возможные причины:**
1. Поле `paramGeomMultiplier` не найдено в DOM
2. Обработчик событий не привязан

**Решение:**
- Проверить в консоли браузера: `document.getElementById('paramGeomMultiplier')`
- Должно вернуть элемент input, а не `null`
- Если возвращает `null` - проверить HTML-шаблон

### Проблема 2: Запрос отправляется, но geom_multiplier не передаётся

**Симптомы:**
- В логах браузера: `[BREAKEVEN] 🔢 geom_multiplier: 2` (старое значение)
- В URL запроса: `geom_multiplier=2`

**Возможные причины:**
1. Поле не обновилось в DOM
2. Метод `parseFloat($('paramGeomMultiplier')?.value)` возвращает старое значение

**Решение:**
- Проверить в консоли браузера: `document.getElementById('paramGeomMultiplier').value`
- Если возвращает новое значение - проблема в коде, если старое - проблема в DOM

### Проблема 3: geom_multiplier передаётся, но таблица не меняется

**Симптомы:**
- В логах браузера: `[BREAKEVEN] 🔢 geom_multiplier: 3` (новое значение)
- В URL запроса: `geom_multiplier=3`
- В логах сервера: `[BREAKEVEN_TABLE] ✅ geom_multiplier переопределён: 3.0`
- Но таблица не изменилась

**Возможные причины:**
1. Таблица берётся из кэша активного цикла (если цикл активен)
2. Ошибка в функции расчёта таблицы `calculate_breakeven_table()`
3. Проблема с рендерингом на фронтенде

**Решение:**
1. Проверить, активен ли цикл: в логах браузера должно быть `[BREAKEVEN] Цикл неактивен или таблица отсутствует`
2. Если цикл активен, таблица берётся из сохранённого состояния - это нормальное поведение!
3. Остановить цикл (продать всё) и попробовать снова изменить `geom_multiplier`

### Проблема 4: Кэширование браузером

**Симптомы:**
- Запрос не отправляется повторно
- Или возвращается старый ответ

**Решение:**
- Жёсткая перезагрузка страницы: `Ctrl + Shift + R` (Chrome/Edge) или `Ctrl + F5`
- Очистить кэш браузера
- Отключить кэш в DevTools: вкладка "Network" → галочка "Disable cache"

## 📊 Проверка правильности расчёта

### Формула объёма покупки (geometric mode):

```
V(n) = start_volume × (geom_multiplier ^ n)
```

Где:
- `n` - номер шага (0, 1, 2, 3, ...)
- `start_volume` - стартовый объём (по умолчанию 3)
- `geom_multiplier` - множитель геометрии (по умолчанию 2)

### Примеры расчёта:

**При geom_multiplier = 2:**
- V(0) = 3 × (2^0) = 3 × 1 = 3
- V(1) = 3 × (2^1) = 3 × 2 = 6
- V(2) = 3 × (2^2) = 3 × 4 = 12
- V(3) = 3 × (2^3) = 3 × 8 = 24

**При geom_multiplier = 3:**
- V(0) = 3 × (3^0) = 3 × 1 = 3
- V(1) = 3 × (3^1) = 3 × 3 = 9
- V(2) = 3 × (3^2) = 3 × 9 = 27
- V(3) = 3 × (3^3) = 3 × 27 = 81

### Влияние на другие колонки:

**Инвестиции (Total Inv.):**
- Растут быстрее при большем `geom_multiplier`
- `Total Inv.(n) = Total Inv.(n-1) + V(n) × Rate(n)`

**Цена безубытка (B/E Price):**
- Меняется из-за изменения средневзвешенной цены покупки
- `B/E Price = Total Inv. / Total Volume × (1 + pprof/100)`

**↑Δ% (Target Delta):**
- Зависит от цены безубытка
- При большем `geom_multiplier` требуется больший рост для достижения цели

## ✅ Критерии успешного теста

1. ✅ При изменении `geom_multiplier` в поле на веб-странице:
   - Появляется статус "⏳ Обновление..."
   - В консоли браузера появляются сообщения с новым значением
   - В логах сервера появляются сообщения с новым значением

2. ✅ Таблица визуально изменяется:
   - Колонка "Покупка (USDT)" показывает новые значения
   - Колонка "Инвестиции" показывает новые суммы
   - Колонка "B/E Price" изменилась

3. ✅ Расчёты верны:
   - Объёмы покупок соответствуют формуле `V(n) = start_volume × (geom_multiplier ^ n)`
   - Все проценты и цены пересчитаны корректно

## 📝 Заключение

После проведения теста будет точно известно:
- ✅ Передаётся ли `geom_multiplier` с фронтенда на бэкенд
- ✅ Применяется ли новое значение на сервере
- ✅ Пересчитывается ли таблица с новыми параметрами
- ✅ Отображается ли новая таблица на фронтенде

Если все шаги проходят успешно, но таблица не меняется - проблема в логике расчёта.
Если запрос не отправляется или параметр не передаётся - проблема в JavaScript/HTML.
