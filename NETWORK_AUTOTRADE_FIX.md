# Исправление: Переключение сети и AutoTrade

## Дата: 12 ноября 2025 г.

## Проблемы

1. **Переключение сети (Work/Test) сохраняется, но не работает**
   - При переключении на Test режим не обновлялся баланс котируемой валюты
   - Задержка перед загрузкой данных была недостаточной
   - Отсутствовала принудительная перезагрузка данных после переключения

2. **AutoTrade не сохраняется**
   - Переменная `autoTradeEnabled` объявлялась дважды
   - После перезагрузки страницы всегда включалось состояние ON
   - UI State загружался ПОСЛЕ загрузки режима сети, что перезаписывало сохраненное состояние

## Исправления

### 1. Переменная autoTradeEnabled (app.js)

**Было:**
```javascript
// В начале файла
let autoTradeActive=false;

// Позже в коде (строка ~861)
let autoTradeEnabled = true; // По умолчанию включено (ON)
```

**Стало:**
```javascript
// В начале файла
let autoTradeActive=false;
let autoTradeEnabled = true; // По умолчанию включено (ON), будет загружено из state
let tradingPermissions = {};

// Удалено второе объявление
```

### 2. Порядок загрузки в initApp() (app.js)

**Было:**
```javascript
async function initApp(){
  await loadNetworkMode();      // ❌ Загружает режим с сервера
  await loadTradingMode();      
  await loadUIState();          // ⚠️ Загружается последним, не влияет на режим сети
  updateAutoTradeUI();
```

**Стало:**
```javascript
async function initApp(){
  // ВАЖНО: Сначала загружаем UI State
  await loadUIState();          // ✅ Восстанавливает ВСЕ состояния, включая сеть
  console.log('[INIT] UI State загружен');
  
  await loadNetworkMode();      // ✅ Подтверждает режим с сервера
  await loadTradingMode();
  updateAutoTradeUI();
```

### 3. Восстановление режима сети в loadUIState() (app.js)

**Добавлено:**
```javascript
async function loadUIState() {
  const state = result.state;
  
  // ВАЖНО: Восстанавливаем режим сети ПЕРВЫМ
  if (state.network_mode) {
    currentNetworkMode = state.network_mode;
    updateNetworkUI();
    logDbg('UI State: режим сети восстановлен - ' + currentNetworkMode);
  }
  
  // Восстанавливаем AutoTrade
  if (typeof state.auto_trade_enabled === 'boolean') {
    autoTradeEnabled = state.auto_trade_enabled;
    updateAutoTradeUI();
    logDbg('UI State: автотрейдинг восстановлен - ' + (autoTradeEnabled ? 'ON' : 'OFF'));
  }
```

### 4. Улучшение переключения сети (app.js)

**Было:**
```javascript
// После переключения
setTimeout(()=>{
  loadMarketData();
  loadPairBalances();
  loadPairParams(true);
  loadBreakEvenTable();
},1500);
```

**Стало:**
```javascript
// Даём время на подписку и подключение WebSocket
await new Promise(resolve => setTimeout(resolve, 2000));

// Принудительно обновляем все данные с сервера
console.log('[NETWORK] Обновление данных после переключения режима...');
await loadMarketData(true);  // force=true
await loadPairBalances();    // Загрузит тестовый баланс в test режиме
await loadPairParams(true);
await loadBreakEvenTable();

setNetworkConnectionState('ok');
console.log('[NETWORK] Переключение завершено успешно ✅');
```

## Как это работает теперь

### При загрузке страницы:

1. **loadUIState()** загружает сохраненное состояние из `app_state.json`:
   - `currentNetworkMode` = 'test' или 'work'
   - `autoTradeEnabled` = true или false
   - Активная валютная пара
   - Разрешения торговли
   - Параметры безубыточности

2. **updateNetworkUI()** отображает правильный режим сети

3. **updateAutoTradeUI()** отображает правильное состояние AutoTrade

4. **loadNetworkMode()** подтверждает режим с сервера (уже установлен)

5. Загружаются данные для выбранного режима сети

### При переключении AutoTrade:

1. Переменная `autoTradeEnabled` меняется
2. UI обновляется через `updateAutoTradeUI()`
3. Запрос отправляется на сервер `/api/autotrade/start` или `/stop`
4. Сервер сохраняет состояние в `state_manager`
5. Клиент сохраняет состояние через `UIStateManager.savePartial()`

### При переключении сети (Work/Test):

1. Режим меняется на сервере
2. Сервер сохраняет новый режим в `state_manager`
3. UI обновляется
4. Клиент сохраняет режим через `UIStateManager.savePartial()`
5. Идёт задержка 2000мс для подключения WebSocket
6. **Принудительно** загружаются все данные:
   - Рыночные данные
   - **Балансы** (включая тестовые в test режиме)
   - Параметры пары
   - Таблица безубыточности

## Тестирование

### Тест 1: AutoTrade сохранение
1. ✅ Переключите AutoTrade на **OFF**
2. ✅ Нажмите F5
3. ✅ **Результат:** AutoTrade должен быть **OFF**
4. ✅ Переключите на **ON**
5. ✅ Перезапустите сервер
6. ✅ **Результат:** AutoTrade должен быть **ON**

### Тест 2: Переключение сети и баланс
1. ✅ Переключите на **TEST** режим
2. ✅ Дождитесь сообщения "Переключение завершено успешно ✅" в консоли
3. ✅ **Проверьте баланс** котируемой валюты (должен отличаться от Work)
4. ✅ Нажмите F5
5. ✅ **Результат:** Режим **TEST** сохранен, баланс правильный
6. ✅ Переключите на **WORK**
7. ✅ **Результат:** Баланс изменился на рабочий

### Тест 3: Комплексный тест
1. ✅ AutoTrade → **OFF**
2. ✅ Network → **TEST**
3. ✅ Trading Mode → **COPY**
4. ✅ Выберите валюту → **ETH**
5. ✅ Измените параметры безубыточности
6. ✅ Перезапустите сервер (кнопка 🔄)
7. ✅ **Результат:** ВСЕ настройки сохранены!

## Логи для отладки

В консоли браузера (F12) теперь выводятся подробные логи:

```
[INIT] Начало инициализации приложения
[INIT] UI State загружен
UI State: режим сети восстановлен - test
UI State: автотрейдинг восстановлен - OFF
[NETWORK] Текущий режим: test
[NETWORK] Переключение на режим: work
[NETWORK] ✓ Режим переключен: work
[NETWORK] Обновление данных после переключения режима...
[NETWORK] Переключение завершено успешно ✅
```

## Файлы изменены

1. ✏️ **static/app.js**
   - Перенесена объявление `autoTradeEnabled` в начало
   - Изменён порядок загрузки в `initApp()`
   - Добавлено восстановление режима сети в `loadUIState()`
   - Улучшена функция `switchNetworkMode()`

## Статус

✅ **ПОЛНОСТЬЮ ИСПРАВЛЕНО**

- AutoTrade корректно сохраняется и восстанавливается
- Переключение сети работает с правильным обновлением балансов
- Все состояния синхронизируются между клиентом и сервером

---

**Дата:** 12 ноября 2025 г.
**Автор:** GitHub Copilot
