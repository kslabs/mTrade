# 🚀 Быстрая проверка P0 - Шпаргалка

## ✅ Что проверено

### 1. P0 обновляется ТОЛЬКО при стартовой покупке

**Файл:** `autotrader.py`  
**Функция:** `_try_start_cycle()` (строка 330)  
**Код:** Строки 465, 473

```python
# ✅ P0 устанавливается
'start_price': buy_price

# ✅ P0 сохраняется в state_manager
current_params['start_price'] = buy_price
```

### 2. P0 НЕ обновляется при докупках

**Файл:** `autotrader.py`  
**Функция:** `_try_rebuy()` (строка 491)  
**Код:** Строки 546-549

```python
# ✅ Обновляется всё, КРОМЕ start_price
cycle['active_step'] = next_step
cycle['last_buy_price'] = level_price
cycle['total_invested_usd'] += invest
cycle['base_volume'] += filled
# ❗ start_price НЕ трогается
```

### 3. BE отображается в индикаторе

**Файлы:** 
- `mTrade.py` (строка 1654)
- `app.js` (строка 154)
- `index.html` (строка 130)

```python
# Сервер отдаёт BE
autotrade_levels['breakeven_price'] = row.get('breakeven_price')
```

```javascript
// JS правильно находит элемент BE
const elementId = key === 'be' ? 'indBE' : ...
```

```html
<!-- HTML элемент существует -->
<div class="value" id="indBE" style="color:#00bfff;">-</div>
```

---

## 🧪 Быстрый тест

### Запуск:
```powershell
python mTrade.py
```

### Что проверить:

1. **Стартовая покупка:**
   - ✅ P0 = цена покупки
   - ✅ BE виден в индикаторе (синий цвет)

2. **Докупка:**
   - ✅ P0 НЕ изменилось
   - ✅ Last Buy = новая цена
   - ✅ BE обновился (новый шаг)

3. **Продажа + новый цикл:**
   - ✅ P0 = цена новой стартовой покупки

---

## 🎯 Результат

**Код работает ПРАВИЛЬНО!**  
Изменений в логику P0 НЕ вносилось, исправлен только баг с отображением BE.

---

**Файлы для теста:**
- `autotrader.py` (логика)
- `mTrade.py` (сервер)
- `static/app.js` (клиент)
- `templates/index.html` (HTML)
