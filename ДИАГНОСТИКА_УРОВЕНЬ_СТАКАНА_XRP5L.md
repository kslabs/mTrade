# 🔍 ДИАГНОСТИКА: Проверка уровня стакана XRP5L

## ✅ ЧТО СДЕЛАНО

Добавлен диагностический вывод в `autotrader_v2.py` для проверки, какой уровень стакана используется.

---

## 📝 ЧТО ПРОВЕРЯТЬ В ЛОГАХ

### **1. Ждите ~1 минуту**
Диагностический вывод появляется **раз в минуту**.

### **2. Найдите в терминале строку:**
```
[XRP5L] [DEBUG] Шаг 0: table_orderbook_level=X, orderbook_level=Y (для API)
```

Где:
- **table_orderbook_level** — значение из таблицы безубыточности (столбец "Ст.")
- **orderbook_level** — значение для API (0-based индекс)

---

## 🎯 ОЖИДАЕМЫЕ ЗНАЧЕНИЯ

### **Правильно:**
```
[XRP5L] [DEBUG] Шаг 0: table_orderbook_level=1, orderbook_level=0 (для API)
```
**Означает:** Берётся **1-й уровень стакана** (bids[0] = top bid, лучшая цена)

### **Неправильно (если было бы 0.26):**
```
[XRP5L] [DEBUG] Шаг 0: table_orderbook_level=0, orderbook_level=0 (для API)
```
**Означало бы:** `int(0.26) = 0`, что неверно

---

## 🔍 ДОПОЛНИТЕЛЬНАЯ ПРОВЕРКА

Если логов нет через ~1 минуту, найдите строки с `[XRP5L]` и проверьте:

### **Вариант А: Проверка продажи**
```
[XRP5L] 💰 ПРОВЕРКА ПРОДАЖИ:
[XRP5L] 💰   Market price: 0.04697000
[XRP5L] 💰   Orderbook price: 0.XXXXXXXX  ← СМОТРИМ СЮДА
[XRP5L] 💰   Required price: 0.04691015
[XRP5L] 💰   Условие: 0.XXXXXXXX >= 0.04691015 ?
```

**Если `Orderbook price < 0.04691015`** → продажа блокируется.

### **Вариант Б: Нет логов вообще**
Возможно, цикл не активен или условие продажи не выполняется.

---

## 💡 ЧТО ДЕЛАТЬ ДАЛЬШЕ

### **Если table_orderbook_level = 1:**
✅ Код работает правильно!

**Проверьте:**
1. Цену в стакане (orderbook_price)
2. Возможно, на уровне bids[0] цена действительно ниже required_price

### **Если table_orderbook_level = 0:**
❌ Проблема с генерацией таблицы!

**Проверьте:**
1. Файл `autotrader_cycles_state.json` → XRP5L → table → orderbook_level
2. Возможно, нужно **пересоздать таблицу** (сбросить цикл и начать заново)

---

## 🚀 СЛЕДУЮЩИЕ ШАГИ

1. ⏳ Ждите ~1 минуту после перезапуска
2. ⏳ Найдите строку `[XRP5L] [DEBUG]` в логах
3. ⏳ Сообщите, что показывает `table_orderbook_level` и `orderbook_level`

---

**Сервер перезапущен:** PID 18092  
**Адрес:** http://localhost:5000  
**Статус:** ⏳ ОЖИДАНИЕ ЛОГОВ (~1 минута)
