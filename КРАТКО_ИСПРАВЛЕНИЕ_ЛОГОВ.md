# ⚡ КРАТКО: Исправление пропадающих логов продаж

## 🐛 Проблема
Только первая продажа логировалась, остальные — нет.

## 🔧 Причина
**НЕПРАВИЛЬНОЕ ИМЯ ПАРАМЕТРА** в вызове `log_sell`:
- Было: `growth_percent=...` ❌
- Должно быть: `delta_percent=...` ✅

## ✅ Что исправлено
**Файл**: `autotrader_v2.py`, метод `_try_sell`, строка ~1344

**Было**:
```python
self.logger.log_sell(
    currency=base,
    volume=filled_amount,
    price=executed_price,
    growth_percent=current_growth_pct,  # ❌ НЕПРАВИЛЬНО
    pnl=pnl
)
```

**Стало**:
```python
self.logger.log_sell(
    currency=base,
    volume=filled_amount,
    price=executed_price,
    delta_percent=current_growth_pct,  # ✅ ПРАВИЛЬНО
    pnl=pnl,
    source="AUTO"
)
```

## 🧪 Проверка
```bash
python check_log_sell_fix.py
```

**Результат**: ✅ ВСЕ ВЫЗОВЫ log_sell ПРАВИЛЬНЫЕ!

## 📋 Что теперь изменится
- ✅ **ВСЕ продажи** будут логироваться в файл `trade_logs/{CURRENCY}_logs.jsonl`
- ✅ В консоли будет: `✅ Продажа записана в лог`
- ✅ Больше не будет ошибок `TypeError` при логировании

## 🚀 Что делать дальше
1. Если трейдер запущен — **перезапустите его**
2. Дождитесь следующей продажи
3. Проверьте файл логов — должна быть новая запись
4. Убедитесь, что **все последующие** продажи логируются

---

**Проблема решена!** 🎉
