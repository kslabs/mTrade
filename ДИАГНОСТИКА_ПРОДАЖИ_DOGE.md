# 🔍 ДИАГНОСТИКА: Почему DOGE не продается?

## ❓ ПРОБЛЕМА
- **Текущая цена**: 0.14084 USDT
- **Цена продажи**: 0.13935428 USDT
- **Условие выполнено**: ✅ 0.14084 > 0.13935428
- **Но продажа НЕ происходит!**

## 🔧 ЧТО ИСПРАВЛЕНО

Добавлено **диагностическое логирование** в метод `_try_sell`:

### 1. Проверка активности цикла
```
[DOGE] [SKIP_SELL] Цикл неактивен (state=idle)
```

### 2. Проверка таблицы
```
[DOGE] [SKIP_SELL] Таблица пустая! (table=[])
```
**→ САМАЯ ВЕРОЯТНАЯ ПРИЧИНА!**

### 3. Проверка флага продажи
```
[DOGE] [SKIP_SELL] Продажа уже в процессе (_selling_in_progress=True)
```

### 4. Проверка параметров
```
[DOGE] [SKIP_SELL] Параметры не найдены (get_breakeven_params вернул None)
```

### 5. Проверка стартовой цены
```
[DOGE] [SKIP_SELL] Некорректная стартовая цена (start_price=0.0)
```

### 6. Проверка шага
```
[DOGE] [SKIP_SELL] Некорректный шаг (active_step=0, table_len=0)
```

## 📋 ИНСТРУКЦИЯ ПРОВЕРКИ

1. **Смотрите логи** после перезапуска бота
2. **Найдите** сообщение `[DOGE] [SKIP_SELL] ...`
3. **Это покажет**, что именно блокирует продажу!

## 🎯 ВОЗМОЖНЫЕ РЕШЕНИЯ

### Если таблица пустая (`table=[]` или `table=None`)
**ПРИЧИНА**: Таблица не загрузилась из файла `autotrader_cycles_state.json`

**РЕШЕНИЕ**:
```python
# Пересчитать таблицу вручную через веб-интерфейс
# ИЛИ перезапустить цикл (сброс + новая покупка)
```

### Если флаг `_selling_in_progress=True`
**ПРИЧИНА**: Флаг застрял после неудачной попытки продажи

**РЕШЕНИЕ**:
```python
# В консоли бота:
cycle = autotrader.cycles['DOGE']
cycle._selling_in_progress = False
print("[DOGE] Флаг продажи сброшен!")
```

### Если параметры не найдены
**ПРИЧИНА**: Параметры торговли не сохранены для DOGE

**РЕШЕНИЕ**:
```python
# Проверить файл trading_config.json
# Убедиться, что есть секция для DOGE
```

## ✅ СЛЕДУЮЩИЕ ШАГИ

1. Перезапустите бота
2. Дождитесь сообщения `[DOGE] [SKIP_SELL] ...` в логах
3. Сообщите, что написано — это покажет корень проблемы!

---

**Дата исправления**: 12.12.2025  
**Версия**: autotrader_v2.py (с диагностикой)
