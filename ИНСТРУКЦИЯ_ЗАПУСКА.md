# 🚀 ИНСТРУКЦИЯ ПО ЗАПУСКУ ПОСЛЕ ИСПРАВЛЕНИЯ

## ✅ ЧТО БЫЛО ИСПРАВЛЕНО
Логика автопродаж теперь работает **строго по таблице безубыточности**:
- ❌ Убрана аварийная защита (+0.5% от цены покупки)
- ✅ Продажа ТОЛЬКО при достижении `target_sell_price` из таблицы
- ✅ Полная блокировка продаж по цене покупки или ниже
- ✅ Подробное логирование всех параметров

---

## 📋 ШАГИ ПЕРЕД ЗАПУСКОМ

### 1. Проверьте параметры в `config.json`
```json
{
  "pprof": 0.6,      // Целевая прибыль (≥ 0.6% рекомендуется)
  "kprof": 0.02,     // Снижение профита на шаг (≤ 0.05%)
  "target_r": 3.65,  // Шаг процента закупки
  "rk": 0.0          // Коэффициент изменения шага
}
```

**Важно:**
- Если `pprof` слишком мал → `target_sell_price` может быть ≤ `start_price`
- Если `kprof` слишком велик → на больших шагах прибыль будет отрицательной

### 2. Убедитесь, что нет запущенных экземпляров
```powershell
# Проверка процессов
Get-Process python | Where-Object {$_.Path -like "*autotrader*"}

# Остановка (если найдены)
Stop-Process -Name python -Force
```

### 3. Очистите старые состояния (опционально)
```powershell
# Резервная копия
Copy-Item autotrader_cycles_state.json autotrader_cycles_state.json.backup

# Если хотите начать с нуля (ВНИМАНИЕ: потеряете активные циклы!)
Remove-Item autotrader_cycles_state.json
```

---

## 🚀 ЗАПУСК

### Вариант 1: Обычный запуск (в текущем окне)
```powershell
python autotrader_v2.py
```

### Вариант 2: Фоновый запуск (рекомендуется)
```powershell
# PowerShell
Start-Process python -ArgumentList "autotrader_v2.py" -WindowStyle Minimized

# Или через nohup (если установлен Git Bash)
nohup python autotrader_v2.py > autotrader.log 2>&1 &
```

### Вариант 3: С перенаправлением в файл лога
```powershell
python autotrader_v2.py 2>&1 | Tee-Object -FilePath autotrader.log
```

---

## 🔍 МОНИТОРИНГ ЛОГОВ

### Просмотр в реальном времени
```powershell
Get-Content autotrader.log -Wait -Tail 50
```

### Поиск продаж
```powershell
# Все продажи
Get-Content autotrader.log | Select-String "ПРОДАЖА ИСПОЛНЕНА|MARKET SELL"

# Только последние 10
Get-Content autotrader.log | Select-String "ПРОДАЖА ИСПОЛНЕНА" | Select-Object -Last 10
```

### Поиск ошибок
```powershell
# Критические ошибки
Get-Content autotrader.log | Select-String "КРИТИЧЕСКАЯ ОШИБКА|🚨"

# Пропущенные продажи
Get-Content autotrader.log | Select-String "SKIP.*не достигла цели"
```

### Анализ параметров продаж
```powershell
Get-Content autotrader.log | Select-String "ДИАГНОСТИКА ПЕРЕД ПРОДАЖЕЙ" -Context 0,10
```

---

## ⚠️ ЧТО ПРОВЕРЯТЬ В ЛОГАХ

### 1. При каждой продаже должны быть эти строки:
```
[BTC] 🔴 ========== ДИАГНОСТИКА ПЕРЕД ПРОДАЖЕЙ ==========
[BTC]   rate (из таблицы): 96.35
[BTC]   target_delta_pct (из таблицы): 4.65
[BTC]   breakeven_price (из таблицы): 100.25
[BTC]   start_price (цена покупки): 100.00
[BTC]   price (текущая цена): 101.00
[BTC]   target_sell_price (вычислено): 100.83
[BTC] 🔴 ===============================================
```

### 2. Проверка перед созданием ордера:
```
[BTC] 🔴 ПОВТОРНАЯ ПРОВЕРКА ЦЕНЫ:
[BTC]   current_price_before_sell: 101.00
[BTC]   target_sell_price: 100.83
[BTC]   start_price (цена покупки): 100.00
[BTC]   Минимальная дельта для продажи: 0.83%
```

### 3. Успешная продажа:
```
[BTC] 🔵 Создание MARKET SELL: 0.05 BTC
[BTC]   Цена перед продажей: 101.00 (целевая: 100.83) ✅
[BTC] ✅ ПРОДАЖА ИСПОЛНЕНА!
```

### 4. ВНИМАНИЕ! Если увидите это:
```
[BTC] 🚨 КРИТИЧЕСКАЯ ОШИБКА В ТАБЛИЦЕ!
[BTC] 🚨 ПРОДАЖА ОТМЕНЕНА - целевая цена не выше цены покупки!
```
→ **Проверьте параметры `pprof` и `kprof` в config.json**

---

## 🧪 ТЕСТОВЫЙ ПЕРИОД (24 часа)

### День 1: Наблюдение
1. Запустите бот
2. Следите за логами в течение дня
3. Проверяйте каждую продажу:
   - Цена продажи > цена покупки ✅
   - Дельта ≥ целевой из таблицы ✅
   - Нет критических ошибок ✅

### Проверка в конце дня
```powershell
# Сколько всего продаж
(Get-Content autotrader.log | Select-String "ПРОДАЖА ИСПОЛНЕНА").Count

# Были ли ошибки
(Get-Content autotrader.log | Select-String "КРИТИЧЕСКАЯ ОШИБКА").Count

# Средняя дельта продаж (извлечь вручную из логов)
Get-Content autotrader.log | Select-String "Минимальная дельта для продажи"
```

---

## 📊 КОНТРОЛЬ КАЧЕСТВА

### Каждая продажа должна соответствовать:
1. **sell_price > buy_price** (ОБЯЗАТЕЛЬНО!)
2. **actual_delta ≥ target_delta_pct** (из таблицы)
3. **sell_price ≥ target_sell_price** (из таблицы)

### Если хоть одна продажа НЕ соответствует:
1. Остановите бот немедленно
2. Скопируйте логи проблемной продажи
3. Проверьте параметры в config.json
4. Создайте issue с подробным описанием

---

## 🛑 ОСТАНОВКА

### Безопасная остановка:
```powershell
# Ctrl+C в окне терминала (если запущен в текущем окне)

# Или найти процесс и остановить
Get-Process python | Where-Object {$_.Path -like "*autotrader*"} | Stop-Process

# Проверка, что остановлен
Get-Process python | Where-Object {$_.Path -like "*autotrader*"}
```

---

## 📝 ОТЧЁТЫ

### После 24 часов работы создайте отчёт:
```
1. Количество покупок: _____
2. Количество продаж: _____
3. Минимальная дельта продажи: _____% (из логов)
4. Максимальная дельта продажи: _____% (из логов)
5. Средняя дельта продажи: _____% (вычислить)
6. Количество критических ошибок: _____ (должно быть 0!)
7. Количество блокировок продаж: _____ (SKIP)
```

### Проверка формулы:
Для каждой продажи убедитесь:
```
target_sell_price = rate × (1 + target_delta_pct / 100)

Пример:
rate = 96.35
target_delta_pct = 4.65%
target_sell_price = 96.35 × 1.0465 = 100.83 ✅
```

---

## ✅ КРИТЕРИИ УСПЕХА

Система работает правильно, если:

- ✅ **Все продажи с положительной дельтой** (прибыль > 0)
- ✅ **Нет продаж по цене покупки или ниже**
- ✅ **Нет критических ошибок в логах**
- ✅ **Все продажи соответствуют таблице безубыточности**
- ✅ **Диагностические сообщения присутствуют в логах**

---

## 🆘 ЭКСТРЕННАЯ ПОМОЩЬ

### Если продажа произошла по цене покупки или ниже:
1. **НЕМЕДЛЕННО остановите бот!**
   ```powershell
   Stop-Process -Name python -Force
   ```

2. **Сохраните логи:**
   ```powershell
   Copy-Item autotrader.log autotrader_emergency_$(Get-Date -Format 'yyyyMMdd_HHmmss').log
   ```

3. **Найдите проблемную транзакцию:**
   ```powershell
   Get-Content autotrader.log | Select-String "ПРОДАЖА ИСПОЛНЕНА" -Context 20
   ```

4. **Проверьте код в autotrader_v2.py:**
   - Строка ~855: `if target_sell_price <= start_price: return`
   - Строка ~930: `if current_price_before_sell < target_sell_price: return`

5. **Создайте issue с данными:**
   - Версия кода (git commit)
   - Параметры из config.json
   - Логи проблемной транзакции
   - Состояние цикла из autotrader_cycles_state.json

---

## 📌 ФАЙЛЫ ДЛЯ КОНТРОЛЯ

1. **autotrader_v2.py** - основной код
2. **config.json** - параметры торговли
3. **autotrader.log** - логи работы (создаётся автоматически)
4. **autotrader_cycles_state.json** - состояние циклов

---

**Дата:** 2024-12-08  
**Версия:** 2.0 Final  
**Статус:** ✅ ГОТОВО К ЗАПУСКУ
