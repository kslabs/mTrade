# 🔧 ИСПРАВЛЕНИЕ ПРОБЛЕМ АВТОТРЕЙДЕРА

**Дата:** 19 ноября 2025 г.  
**Версия:** 1.9.0

## 🐛 Обнаруженные проблемы

### 1. Стартовая покупка при наличии баланса
**Проблема:** Автотрейдер начинал новый цикл (стартовая покупка строки 0), даже если у пользователя уже есть монеты BASE валюты на балансе.

**Пример из логов:**
```
[07:27:09] Buy{Объем:64.0900; Курс:0.1560; ↓Δ%:0.00; ↓%:0.00; Инвест:10.0000}  ← Стартовая
[06:34:28] Buy{Объем:164.3500; Курс:0.1570; ↓Δ%:0.99; ↓%:-2.58; Инвест:68.3518}
[06:08:39] Buy{Объем:118.7900; Курс:0.1584; ↓Δ%:0.86; ↓%:-1.59; Инвест:42.5522}
[04:48:59] Buy{Объем:86.0400; Курс:0.1596; ↓Δ%:0.73; ↓%:-0.73; Инвест:23.7323}
[17:19:20] Buy{Объем:62.2900; Курс:0.1605; ↓Δ%:0.00; ↓%:0.00; Инвест:9.9994}  ← Новая стартовая!
```

**Причина:** Автотрейдер не проверял существующий баланс BASE валюты перед началом нового цикла.

**Последствия:**
- Двойная покупка одной и той же валюты
- Нарушение стратегии breakeven (P0 не соответствует реальной средней цене)
- Риск превышения лимита баланса

### 2. Логи не содержат название валюты
**Проблема:** В логах торговли не указана валюта, непонятно какая валюта торгуется.

**Было:**
```
[07:27:09] Buy{Объем:64.0900; Курс:0.1560; ↓Δ%:0.00; ↓%:0.00; Инвест:10.0000}
```

**Стало:**
```
[07:27:09] Buy DOGE {Объем:64.0900; USDT:10.0000; Курс:0.1560; ↓Δ%:0.00; ↓%:0.00; Инвест:10.0000}
```

### 3. Отсутствует объём покупки в USDT
**Проблема:** В логах не указан объём покупки в котируемой валюте (USDT), только количество монет BASE.

**Последствия:**
- Сложно отследить реальные затраты
- Невозможно быстро оценить размер сделки

## ✅ Внесённые исправления

### Исправление 1: Проверка баланса BASE перед стартом цикла

**Файл:** `autotrader.py`  
**Функция:** `_try_start_cycle()`

**Добавлена проверка:**
```python
# Проверка 2: Проверка существующего баланса BASE валюты
# Если уже есть монеты - не начинаем новый цикл
try:
    api_client = self.api_client_provider()
    if api_client:
        balance = api_client.get_account_balance()
        if isinstance(balance, list):
            for item in balance:
                if item.get('currency', '').upper() == base.upper():
                    base_available = float(item.get('available', 0))
                    if base_available > 0:
                        # Логируем только раз в 10 минут (чтобы не спамить)
                        if not hasattr(self, '_base_balance_warned'):
                            self._base_balance_warned = {}
                        last_warn = self._base_balance_warned.get(base, 0)
                        current_time = time.time()
                        if current_time - last_warn > 600:  # 10 минут
                            print(f"[AutoTrader][{base}] ⚠️ Обнаружен существующий баланс: {base_available:.8f} {base}")
                            print(f"  💡 Автотрейдер не начнёт новый цикл, пока есть монеты.")
                            print(f"  💡 Продайте монеты вручную или нажмите 'Сброс цикла' для принудительного старта.")
                            self._base_balance_warned[base] = current_time
                        return
except Exception as e:
    # В случае ошибки проверки баланса - продолжаем (может быть симуляция)
    pass
```

**Как работает:**
1. Перед стартом нового цикла проверяется баланс BASE валюты
2. Если баланс > 0 - новый цикл НЕ начинается
3. Предупреждение выводится раз в 10 минут (чтобы не спамить логи)
4. Пользователь может:
   - Продать монеты вручную
   - Использовать кнопку "Сброс цикла" для принудительного старта

### Исправление 2: Название валюты в логах

**Файл:** `trade_logger.py`  
**Функция:** `log_buy()` и `log_sell()`

**Изменён формат вывода:**

**Было:**
```python
print(f"[TRADE_LOG] {currency} Buy: V={volume:.4f} P={price:.4f} ↓Δ%={delta_percent:.2f} ↓%={total_drop_percent:.2f} Inv={investment:.4f}")
```

**Стало:**
```python
print(f"[{entry['time']}] Buy {currency} {{Объем:{volume:.4f}; USDT:{volume_usdt:.4f}; Курс:{price:.4f}; ↓Δ%:{delta_percent:.2f}; ↓%:{total_drop_percent:.2f}; Инвест:{investment:.4f}}}")
```

**Изменения:**
- ✅ Добавлено название валюты после операции (Buy/Sell)
- ✅ Использован формат времени `[HH:MM:SS]` вместо `[TRADE_LOG]`
- ✅ Русскоязычные метки для понятности

### Исправление 3: Объём в USDT в логах

**Файл:** `trade_logger.py`  
**Функции:** `log_buy()` и `log_sell()`

**Добавлен расчёт объёма в USDT:**

```python
def log_buy(self, currency: str, volume: float, price: float, 
            delta_percent: float, total_drop_percent: float, investment: float):
    """Логировать операцию покупки (в файл конкретной валюты)"""
    currency = currency.upper()
    
    # Рассчитываем объём покупки в USDT
    volume_usdt = volume * price
    
    entry = {
        'timestamp': datetime.now().isoformat(),
        'time': datetime.now().strftime('%H:%M:%S'),
        'type': 'buy',
        'currency': currency,
        'volume': volume,
        'volume_usdt': volume_usdt,  # ← НОВОЕ ПОЛЕ
        'price': price,
        'delta_percent': delta_percent,
        'total_drop_percent': total_drop_percent,
        'investment': investment
    }
    # ...
    print(f"[{entry['time']}] Buy {currency} {{Объем:{volume:.4f}; USDT:{volume_usdt:.4f}; Курс:{price:.4f}; ↓Δ%:{delta_percent:.2f}; ↓%:{total_drop_percent:.2f}; Инвест:{investment:.4f}}}")
```

**Аналогично для продажи:**
```python
def log_sell(self, currency: str, volume: float, price: float, 
             delta_percent: float, pnl: float):
    """Логировать операцию продажи (в файл конкретной валюты)"""
    currency = currency.upper()
    
    # Рассчитываем объём продажи в USDT
    volume_usdt = volume * price
    
    entry = {
        'timestamp': datetime.now().isoformat(),
        'time': datetime.now().strftime('%H:%M:%S'),
        'type': 'sell',
        'currency': currency,
        'volume': volume,
        'volume_usdt': volume_usdt,  # ← НОВОЕ ПОЛЕ
        'price': price,
        'delta_percent': delta_percent,
        'pnl': pnl
    }
    # ...
    print(f"[{entry['time']}] Sell {currency} {{Объем:{volume:.4f}; USDT:{volume_usdt:.4f}; Курс:{price:.4f}; ↑Δ%:{delta_percent:.2f}; PnL:{pnl:.4f}}}")
```

## 📊 Новый формат логов

### Покупка (Buy)
```
[07:27:09] Buy DOGE {Объем:64.0900; USDT:10.0000; Курс:0.1560; ↓Δ%:0.00; ↓%:0.00; Инвест:10.0000}
```

**Расшифровка:**
- `[07:27:09]` - время операции
- `Buy DOGE` - операция покупки валюты DOGE
- `Объем:64.0900` - количество купленных монет DOGE
- `USDT:10.0000` - стоимость покупки в USDT
- `Курс:0.1560` - цена покупки (1 DOGE = 0.1560 USDT)
- `↓Δ%:0.00` - процент падения от предыдущей покупки (0 для стартовой)
- `↓%:0.00` - общий процент падения от P0 (0 для стартовой)
- `Инвест:10.0000` - общая сумма инвестиций в цикле

### Продажа (Sell)
```
[16:45:54] Sell DOGE {Объем:62.5800; USDT:9.9690; Курс:0.1593; ↑Δ%:0.50; PnL:0.1234}
```

**Расшифровка:**
- `[16:45:54]` - время операции
- `Sell DOGE` - операция продажи валюты DOGE
- `Объем:62.5800` - количество проданных монет DOGE
- `USDT:9.9690` - сумма продажи в USDT
- `Курс:0.1593` - цена продажи (1 DOGE = 0.1593 USDT)
- `↑Δ%:0.50` - процент роста от P0
- `PnL:0.1234` - прибыль/убыток в USDT

## 🔄 Сценарий использования

### Нормальный цикл
```
[10:00:00] Buy DOGE {Объем:64.0000; USDT:10.0000; Курс:0.1562; ↓Δ%:0.00; ↓%:0.00; Инвест:10.0000}
[10:15:30] Buy DOGE {Объем:62.0000; USDT:9.6200; Курс:0.1551; ↓Δ%:0.70; ↓%:-0.70; Инvest:19.6200}
[10:30:45] Sell DOGE {Объем:126.0000; USDT:20.0000; Курс:0.1587; ↑Δ%:1.60; PnL:0.3800}
```

### Блокировка повторной стартовой покупки
```
[10:00:00] Buy DOGE {Объем:64.0000; USDT:10.0000; Курс:0.1562; ↓Δ%:0.00; ↓%:0.00; Инвест:10.0000}
[AutoTrader][DOGE] ⚠️ Обнаружен существующий баланс: 64.00000000 DOGE
  💡 Автотрейдер не начнёт новый цикл, пока есть монеты.
  💡 Продайте монеты вручную или нажмите 'Сброс цикла' для принудительного старта.
```

## 🧪 Тестирование

### Тест 1: Проверка блокировки стартовой покупки
1. Убедитесь, что на балансе есть DOGE (или другая валюта)
2. Запустите автотрейдер
3. Проверьте логи - должно быть предупреждение о существующем балансе
4. Новая стартовая покупка НЕ должна произойти

### Тест 2: Проверка нового формата логов
1. Выполните любую торговую операцию
2. Проверьте логи:
   - Есть название валюты?
   - Есть объём в USDT?
   - Формат времени `[HH:MM:SS]`?

### Тест 3: Проверка файлов логов
1. Откройте файл `trade_logs/DOGE_logs.jsonl`
2. Проверьте наличие поля `volume_usdt` в записях:
```json
{
  "timestamp": "2025-11-19T07:27:09",
  "time": "07:27:09",
  "type": "buy",
  "currency": "DOGE",
  "volume": 64.09,
  "volume_usdt": 10.0,
  "price": 0.156,
  "delta_percent": 0.0,
  "total_drop_percent": 0.0,
  "investment": 10.0
}
```

## ⚙️ Настройки

### Отключение предупреждений о балансе
Если вы хотите уменьшить частоту предупреждений, измените интервал в `autotrader.py`:

```python
if current_time - last_warn > 600:  # 10 минут (600 секунд)
```

Измените `600` на нужное количество секунд:
- `300` = 5 минут
- `1800` = 30 минут
- `3600` = 1 час

### Принудительный старт цикла
Если вам нужно начать новый цикл при наличии баланса:

1. **Вариант А:** Продайте монеты вручную через интерфейс
2. **Вариант Б:** Используйте кнопку "Сброс цикла" (🔄) в веб-интерфейсе
   - Это сбросит внутреннее состояние автотрейдера
   - Но не продаст монеты!
   - Используйте с осторожностью

## 📝 Дополнительные заметки

### Обратная совместимость
Старые файлы логов (`trade_logs/*.jsonl`) будут читаться без проблем, но в них не будет поля `volume_usdt`. При новых записях оно будет добавлено автоматически.

### Производительность
Добавленная проверка баланса выполняется только при попытке старта цикла (не на каждой итерации), поэтому не влияет на производительность.

### Логирование
Предупреждения о существующем балансе логируются раз в 10 минут, чтобы не засорять логи.

## 🆘 Устранение неполадок

### Проблема: Автотрейдер не начинает новый цикл после продажи
**Решение:**
1. Проверьте баланс BASE валюты - возможно остались "пылинки" (очень малое количество)
2. Продайте остатки вручную или используйте кнопку "Продать всё"
3. Подождите 1-2 секунды для обновления баланса
4. Автотрейдер начнёт новый цикл автоматически

### Проблема: Логи не отображаются в новом формате
**Решение:**
1. Перезапустите сервер mTrade.py
2. Очистите кеш браузера (Ctrl + Shift + R)
3. Проверьте, что изменения применены в файле `trade_logger.py`

### Проблема: Предупреждение о балансе появляется постоянно
**Решение:**
Это означает, что у вас есть монеты на балансе. Варианты:
1. Продайте монеты вручную
2. Используйте кнопку "Сброс цикла" для принудительного старта
3. Увеличьте интервал предупреждений в коде (см. раздел "Настройки")

---

**Автор исправлений:** GitHub Copilot  
**Статус:** ✅ Исправлено и готово к использованию  
**Версия файлов:**
- `autotrader.py` - обновлён
- `trade_logger.py` - обновлён
