# 🌐 Режимы работы с сетями Gate.io

## 📋 Обзор

Платформа mTrade поддерживает два режима работы с API Gate.io:
- **WORK** (рабочий) - основная сеть Gate.io с реальными деньгами
- **TEST** (тестовый) - тестовая сеть Gate.io с виртуальными деньгами

## 🔄 Как это работает

### Режим WORK (Рабочий)
```
┌─────────────────────────────────────┐
│         WORK MODE (Production)      │
├─────────────────────────────────────┤
│ API Host: api.gateio.ws             │
│ Credentials: config/secrets.json    │
│ Funds: REAL MONEY 💰                │
├─────────────────────────────────────┤
│ Market Data:  ✅ From mainnet       │
│ Balances:     ✅ From mainnet       │
│ Trading:      ✅ On mainnet         │
└─────────────────────────────────────┘
```

### Режим TEST (Тестовый)
```
┌─────────────────────────────────────┐
│          TEST MODE (Testnet)        │
├─────────────────────────────────────┤
│ API: fx-api-testnet.gateio.ws       │
│ Credentials: config/secrets_test.json│
│ Funds: VIRTUAL MONEY 🎮             │
├─────────────────────────────────────┤
│ Market Data:  ✅ From MAINNET*      │
│ Balances:     ✅ From TESTNET       │
│ Trading:      ✅ On TESTNET         │
└─────────────────────────────────────┘
```

**\*Важно:** Рыночные данные (стаканы, тикеры) **всегда** берутся из основной сети, так как:
- Тестовая сеть Gate.io не предоставляет актуальные рыночные данные
- Стаканы и цены одинаковы для всех пользователей (публичные данные)
- Алгоритму нужны реальные рыночные данные для принятия правильных решений
- В TEST режиме мы торгуем виртуальными деньгами **по реальным ценам**

## 🎯 Что различается между режимами

| Компонент | WORK | TEST |
|-----------|------|------|
| **API хост** | `api.gateio.ws` | `fx-api-testnet.gateio.ws` |
| **Конфиг** | `config/secrets.json` | `config/secrets_test.json` |
| **Рыночные данные** | ✅ Mainnet | ✅ Mainnet (реальные цены) |
| **Балансы** | ✅ Mainnet (реальные деньги) | ✅ Testnet (виртуальные деньги) |
| **Ордера** | ✅ Mainnet | ✅ Testnet |
| **Торговля** | ✅ Mainnet | ✅ Testnet |
| **Риск потери денег** | ❌ ДА! | ✅ НЕТ! |

## 🚀 Алгоритм торговли

**Важно:** Алгоритм торговли работает **одинаково** в обоих режимах:
- Использует одни и те же логики и стратегии
- Принимает решения на основе одних и тех же рыночных данных
- Единственная разница - в каком аккаунте (work/test) выполняются ордера

```python
# Псевдокод алгоритма
def trading_algorithm():
    # 1. Получить рыночные данные (всегда из mainnet)
    orderbook = get_orderbook_from_mainnet()
    ticker = get_ticker_from_mainnet()
    
    # 2. Получить балансы (из текущей сети)
    if current_mode == 'work':
        balances = get_balances_from_mainnet()
    else:  # test
        balances = get_balances_from_testnet()
    
    # 3. Принять решение (логика одинакова!)
    decision = make_trading_decision(orderbook, ticker, balances)
    
    # 4. Выполнить торговлю (в текущей сети)
    if current_mode == 'work':
        execute_trade_on_mainnet(decision)
    else:  # test
        execute_trade_on_testnet(decision)
```

## 📝 Конфигурация

### Файл `config/secrets.json` (WORK режим)
```json
{
  "api_key": "your_mainnet_api_key_here",
  "api_secret": "your_mainnet_api_secret_here"
}
```

### Файл `config/secrets_test.json` (TEST режим)
```json
{
  "api_key": "your_testnet_api_key_here",
  "api_secret": "your_testnet_api_secret_here"
}
```

**Важно:** Никогда не коммитьте эти файлы в git! Они в `.gitignore`.

## 🔐 Получение тестовых credentials

1. Зарегистрируйтесь на тестнете Gate.io: https://www.gate.io/testnet
2. Получите тестовые API ключи в настройках аккаунта
3. Положите их в `config/secrets_test.json`
4. Получите виртуальные деньги из testnet faucet

## 🎮 Переключение режимов

### Через UI
Используйте переключатель в заголовке приложения:
- **WORK** (зелёный) - рабочий режим
- **TEST** (синий) - тестовый режим

### Через API
```bash
# Переключить на TEST
curl -X POST http://localhost:5000/api/network \
  -H "Content-Type: application/json" \
  -d '{"mode":"test"}'

# Переключить на WORK
curl -X POST http://localhost:5000/api/network \
  -H "Content-Type: application/json" \
  -d '{"mode":"work"}'
```

## ⚠️ Важные замечания

1. **Нет симуляции!** Все данные реальные - из Gate.io API (mainnet или testnet)
2. **TEST режим безопасен** - вы не потеряете реальные деньги
3. **WORK режим опасен** - будьте осторожны, торгуете реальными деньгами!
4. **Рыночные данные всегда из mainnet** - это правильно и ожидаемо
5. **Алгоритм работает одинаково** - тестируйте логику в TEST, потом включайте WORK

## 🧪 Рекомендованный workflow

```
1. Разработка → TEST режим
   ├─ Тестирование стратегий
   ├─ Отладка алгоритма
   └─ Проверка на виртуальных деньгах

2. Проверено → WORK режим
   ├─ Включить AutoTrade
   ├─ Начать с малых сумм
   └─ Мониторинг результатов

3. Работает → Масштабирование
   ├─ Увеличить капитал
   ├─ Добавить больше валют
   └─ Оптимизация параметров
```

## 📊 Технические детали

### Backend (mTrade.py)
- `CURRENT_NETWORK_MODE` - глобальная переменная режима
- `GateAPIClient(api_key, api_secret, network_mode)` - переключается автоматически
- `Config.load_secrets_by_mode(mode)` - загружает нужные credentials

### WebSocket (gateio_websocket.py)
- Всегда подключается к `wss://api.gateio.ws/ws/v4/` для рыночных данных
- Не зависит от network_mode для публичных каналов
- Торговые операции выполняются через REST API текущей сети

### Frontend (index.html)
- `currentNetworkMode` - JS переменная режима
- `switchNetworkMode(targetMode)` - функция переключения
- UI обновляется автоматически при смене режима

## 🐛 Отладка

Логи показывают текущий режим:
```
[NETWORK] Текущий режим сети: work
[NETWORK] Переключение режима: work -> test
[WEBSOCKET] Инициализация WebSocket менеджера (network_mode=test, ws_url=wss://api.gateio.ws/ws/v4/)
[PAIR_DATA] Loaded real market data for BTC_USDT (mode=test, asks=50, bids=50)
```

## ✅ Проверка работы

1. Откройте UI
2. Переключите режим WORK → TEST
3. Проверьте балансы - должны отличаться
4. Проверьте стаканы - одинаковые (это правильно!)
5. Проверьте цены - одинаковые (это правильно!)

---

**Дата:** 9 ноября 2025  
**Версия:** 1.0  
**Автор:** mTrade Development Team
