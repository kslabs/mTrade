# 🔧 Исправление проблемы WS для 6 валют

**Проблемные валюты:** XRP, ADA, LINK, TAO, ANIME, ICP  
**Симптом:** Светло-синий бордюр (ws-disconnected) уже несколько часов  
**Причина:** WebSocket для этих валют не передаёт данные

---

## 🚨 Важно!

**Эта проблема НЕ связана с рефакторингом!**

Мы только вынесли простые утилиты (`logDbg`, `formatPrice`, `$`), которые не влияют на WebSocket подключения. Проблема существовала и раньше.

---

## 🔍 Диагностика (Шаг 1)

Откройте консоль браузера (F12) и выполните:

### 1. Скопируйте содержимое файла `diagnose_ws.js`

Весь код из файла, вставьте в консоль и нажмите Enter.

**Что проверяем:**
- Есть ли данные о ценах для проблемных валют?
- Правильно ли установлены классы на вкладках?
- Есть ли эти валюты в списке `currenciesList`?

---

## 🔄 Переподключение (Шаг 2)

### Вариант 1: Автоматическое переподключение

Скопируйте содержимое файла `reconnect_ws.js`, вставьте в консоль и нажмите Enter.

Скрипт автоматически переподключит все 6 проблемных валют.

### Вариант 2: Ручное переподключение

В консоли браузера выполните для каждой валюты:

```javascript
await subscribeToPairData('XRP', 'USDT');
await subscribeToPairData('ADA', 'USDT');
await subscribeToPairData('LINK', 'USDT');
await subscribeToPairData('TAO', 'USDT');
await subscribeToPairData('ANIME', 'USDT');
await subscribeToPairData('ICP', 'USDT');
```

### Вариант 3: Переключение на проблемную валюту

Просто кликните на вкладку проблемной валюты. Функция `switchBaseCurrency` автоматически вызовет переподключение.

---

## 🔄 Перезапуск сервера (если ничего не помогло)

Если переподключение не помогает, возможно проблема на стороне сервера:

```bash
# Остановите сервер (Ctrl+C в терминале)
# Затем запустите снова:
python mTrade.py
```

После перезапуска сервера обновите страницу в браузере.

---

## 🔍 Что проверить на сервере

Посмотрите логи сервера на наличие ошибок WebSocket для этих валют:

```
[WS] Ошибка подписки на XRP_USDT
[WS] Timeout для ADA_USDT
```

Возможные причины:
1. **Биржа Gate.io не передаёт данные** для этих пар
2. **Проблема с WebSocket соединением** на сервере
3. **Эти пары временно недоступны** на бирже

---

## 🎯 Быстрое решение

1. Откройте консоль (F12)
2. Вставьте и выполните код из `reconnect_ws.js`
3. Подождите 2-3 секунды
4. Проверьте, изменился ли цвет бордюра

Если проблема повторяется:
- Проверьте логи сервера
- Проверьте доступность пар на бирже Gate.io
- Перезапустите сервер

---

## 📊 Ожидаемый результат

После переподключения:
- ✅ Светло-синий бордюр должен смениться на обычный/зелёный/красный
- ✅ В консоли должны появиться логи `[BORDER_WS] XRP: ready-to-sell/normal/ready-to-buy`
- ✅ Цены должны обновляться в реальном времени

Если этого не произошло → проблема на сервере или на бирже.

---

**Дата:** 14 декабря 2025  
**Статус:** Готово к диагностике
