# 🎯 НАЙДЕНА ВЕРОЯТНАЯ ПРИЧИНА!

## Проблема

Бот **молча выходит** из `_try_start_cycle` если:
1. `get_breakeven_params()` возвращает `None`
2. `calculate_breakeven_table()` возвращает пустую таблицу
3. Недостаточно USDT для покупки

**БЕЗ КАКИХ-ЛИБО СООБЩЕНИЙ В ЛОГЕ!**

## Что было добавлено

### Детальное логирование ВСЕХ точек выхода:

```python
[ETH] [DEBUG] Получаем параметры торговли...
[ETH] [ERROR] Не удалось получить параметры торговли  <- ЕСЛИ НЕТ ПАРАМЕТРОВ

[ETH] [DEBUG] Параметры получены: start_volume=10.0
[ETH] [DEBUG] Рассчитываем таблицу breakeven...
[ETH] [ERROR] Не удалось рассчитать таблицу breakeven  <- ЕСЛИ ТАБЛИЦА ПУСТА

[ETH] [DEBUG] Таблица рассчитана, шагов: 17
[ETH] [DEBUG] Проверяем баланс USDT...
[ETH] [DEBUG] Баланс USDT: 100.0, Требуется: 10.0
[ETH] [ERROR] Недостаточно USDT для покупки  <- ЕСЛИ НЕТ ДЕНЕГ
```

## Что делать

### 1. Перезапустите сервер
Нажмите "Перезагрузка"

### 2. Дождитесь попытки покупки

### 3. Смотрите в консоль

Теперь вы ТОЧНО увидите, на каком этапе происходит выход:

#### Если видите:
```
[ETH] [ERROR] Не удалось получить параметры торговли
```
→ **Проблема в `get_breakeven_params`** - нет конфигурации для ETH

#### Если видите:
```
[ETH] [ERROR] Не удалось рассчитать таблицу breakeven
```
→ **Проблема в `calculate_breakeven_table`** - неправильные параметры

#### Если видите:
```
[ETH] [ERROR] Недостаточно USDT для покупки
```
→ **Проблема в балансе** - недостаточно средств

#### Если НЕ видите НИКАКИХ из этих сообщений
→ **Покупка идёт дальше**, проблема в другом месте

## Скопируйте логи

После попытки покупки скопируйте все логи от:
```
[ETH] Начало обработки...
```
до:
```
[ETH] [OK] Покупка записана в лог
```

Или до места, где логи обрываются.

---

**С этими логами я ТОЧНО найду, где именно бот останавливается!**
