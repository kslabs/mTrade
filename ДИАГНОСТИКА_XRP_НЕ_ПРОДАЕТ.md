# ПРОВЕРКА: Почему не продает XRP?

## Данные из интерфейса:
- **Текущая цена**: 2.034
- **Цена продажи**: 2.023063
- **Безубыток (BE)**: 2.014
- **Стартовая (P0)**: 2.014
- **Шаг**: 0 / 16

## Условие продажи должно быть:
```
2.034 >= 2.023063  ✅ ДА → ДОЛЖНО ПРОДАВАТЬ!
```

## Что нужно проверить:

### 1. Перезапустите бота
```powershell
# Ctrl+C для остановки
python autotrader_v2.py
```

### 2. Найдите в логах строку "ПРОВЕРКА ПРОДАЖИ"

Должно быть:
```
[XRP] 💰 ПРОВЕРКА ПРОДАЖИ:
[XRP] 💰   Start price (P0): 2.014
[XRP] 💰   Breakeven price (BE): 2.014
[XRP] 💰   Market price: 2.034
[XRP] 💰   Profit %: X.XX%
[XRP] 💰   Required price: 2.023063
[XRP] 💰   Current growth from BE: X.XX%
[XRP] 💰   Условие: 2.034 >= 2.023063 ?
```

### 3. Проверьте результат

**Если видите:**
```
✅✅✅ УСЛОВИЕ ВЫПОЛНЕНО! Начинаем продажу...
```
→ Бот должен создать FOK ордер и продать

**Если видите:**
```
❌ Цена недостаточна для продажи
```
→ Это значит, что `required_price` рассчитывается неправильно

### 4. Если не продает

Пришлите мне **полный вывод** раздела "ПРОВЕРКА ПРОДАЖИ" из логов для XRP.

## Возможные причины:

1. **Таблица не пересчиталась** после стартовой покупки
2. **breakeven_price != 2.014** в таблице (может быть старое значение)
3. **profit_pct рассчитывается неправильно**

## Быстрая диагностика

Если есть доступ к файлу `autotrader_cycles_state.json`, проверьте:
```json
{
  "XRP": {
    "active": true,
    "start_price": 2.014,
    "table": [
      {
        "step": 0,
        "breakeven_price": 2.014,  ← Должно быть = start_price
        "breakeven_pct": 0.0,
        "target_delta_pct": 0.45   ← Пример, зависит от Pprof
      }
    ]
  }
}
```

Если `breakeven_price` в таблице **не равен** `start_price` — проблема в пересчёте таблицы!
