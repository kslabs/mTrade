# ✅ Баланс котируемой валюты в заголовке (редактируемый в тесте)

## Дата: 9 ноября 2025

## 🎯 Задача

Добавить отображение баланса котируемой валюты рядом с селектором в заголовке секции "Рынок и стакан", с возможностью редактирования в тестовом режиме.

---

## ✨ Что реализовано

### 1. Отображение баланса в заголовке

**HTML структура**:
```html
<div style="display:flex;align-items:center;gap:4px;">
    <span style="font-size:11px;color:#888;">Баланс:</span>
    <span id="headerQuoteBalance" 
          style="font-size:13px;color:#4a9eff;font-weight:600;cursor:pointer;" 
          onclick="editQuoteBalance()" 
          title="Клик для редактирования (тест режим)">0.00</span>
    <span id="headerQuoteSymbol" style="font-size:11px;color:#888;">USDT</span>
</div>
```

**Пример отображения**:
```
📈 Рынок и стакан     Баланс: 1000.00 USDT [USDT ▼]
```

### 2. Редактирование баланса (только в тестовом режиме)

**Функция `editQuoteBalance()`**:
- Открывается по клику на баланс
- Работает только в тестовом режиме
- Запрашивает новое значение через `prompt()`
- Сохраняет в `localStorage` под ключом `test_balance_{CURRENCY}`
- Обновляет отображение во всех местах
- Перезагружает балансы

**Пример использования**:
1. Переключить сервер в тестовый режим
2. Кликнуть на баланс в заголовке
3. Ввести новое значение (например, `5000`)
4. Баланс обновится везде

### 3. Автоматическое использование тестовых балансов

**Функция `loadTestBalance(currency)`**:
- Загружает сохранённый баланс из `localStorage`
- Работает только в тестовом режиме
- Возвращает `null`, если баланс не сохранён

**Интеграция в `loadPairBalances()`**:
```javascript
// В тестовом режиме используем сохранённые балансы, если есть
if(currentNetworkMode==='test'){
  const testBase=loadTestBalance(currentBaseCurrency);
  const testQuote=loadTestBalance(currentQuoteCurrency);
  if(testBase!==null) baseAvail=testBase;
  if(testQuote!==null) quoteAvail=testQuote;
}
```

### 4. Обновление баланса в заголовке

**Функция `updateHeaderQuoteBalance(balance)`**:
- Обновляет отображение баланса в заголовке
- Форматирует с точностью 2 знака
- Обновляет символ валюты
- Настраивает курсор и подсказку в зависимости от режима

```javascript
function updateHeaderQuoteBalance(balance){
  const balanceEl=$('headerQuoteBalance');
  const symbolEl=$('headerQuoteSymbol');
  if(balanceEl){
    const bal=parseFloat(balance)||0;
    balanceEl.textContent=bal.toFixed(2);
    // В тестовом режиме добавляем индикатор редактирования
    if(currentNetworkMode==='test'){
      balanceEl.style.cursor='pointer';
      balanceEl.title='Клик для редактирования (тест режим)';
    }else{
      balanceEl.style.cursor='default';
      balanceEl.title='Баланс котируемой валюты';
    }
  }
  if(symbolEl){
    symbolEl.textContent=currentQuoteCurrency;
  }
}
```

---

## 🔧 Изменённые файлы

### templates/index.html

**1. HTML структура (строка ~389)**:
- Добавлены элементы для отображения баланса
- `#headerQuoteBalance` - значение баланса
- `#headerQuoteSymbol` - символ валюты

**2. JavaScript функции (строка ~730)**:
- `editQuoteBalance()` - редактирование баланса (тест режим)
- `loadTestBalance(currency)` - загрузка сохранённого баланса
- `updateHeaderQuoteBalance(balance)` - обновление отображения

**3. Обновлена функция `loadPairBalances()` (строка ~854)**:
- Добавлена поддержка тестовых балансов
- Вызов `updateHeaderQuoteBalance()` для обновления заголовка

---

## 🎮 Как использовать

### В работающем режиме (work)

1. Баланс отображается из реального API
2. Клик по балансу не работает
3. Подсказка: "Баланс котируемой валюты"

### В тестовом режиме (test)

1. **Установить тестовый баланс**:
   - Переключить сервер в тестовый режим
   - Кликнуть на баланс в заголовке
   - Ввести новое значение (например, `10000`)
   - Нажать OK

2. **Баланс сохранится**:
   - В `localStorage` браузера
   - Для каждой валюты отдельно
   - Сохраняется между перезагрузками страницы

3. **Баланс используется**:
   - При отображении в заголовке
   - При отображении в основном блоке балансов
   - При всех операциях с балансами

---

## 📝 Примеры

### Установка баланса USDT в тестовом режиме

```javascript
// 1. Переключить в тестовый режим
// 2. Кликнуть на "0.00" рядом с USDT
// 3. Ввести: 5000
// Результат: Баланс: 5000.00 USDT

// Сохранено в localStorage:
// test_balance_USDT = "5000"
```

### Установка баланса BTC

```javascript
// 1. Переключить котируемую валюту на BTC
// 2. Кликнуть на баланс
// 3. Ввести: 0.5
// Результат: Баланс: 0.50 BTC

// Сохранено в localStorage:
// test_balance_BTC = "0.5"
```

### Очистка тестового баланса

```javascript
// В консоли браузера (F12):
localStorage.removeItem('test_balance_USDT');
localStorage.removeItem('test_balance_BTC');
// ... и т.д.
```

---

## 🔍 Технические детали

### Формат хранения

```javascript
// Ключ в localStorage
const key = `test_balance_${currency}`;

// Значение (строка)
localStorage.setItem('test_balance_USDT', '5000');

// Чтение
const balance = localStorage.getItem('test_balance_USDT');
// => "5000"
```

### Приоритет балансов

```javascript
// 1. Если тестовый режим И есть сохранённый баланс
if(currentNetworkMode==='test' && testBalance!==null){
  useBalance = testBalance;
}
// 2. Иначе используем баланс из API
else{
  useBalance = apiBalance;
}
```

### Синхронизация отображения

Баланс обновляется в:
1. ✅ Заголовке (`#headerQuoteBalance`)
2. ✅ Основном блоке балансов (`#quoteBalance`)
3. ✅ При переключении валюты
4. ✅ После редактирования

---

## ⚠️ Важно

### Ограничения

1. **Тестовые балансы только в браузере**
   - Сохраняются в `localStorage`
   - Не синхронизируются между вкладками автоматически
   - Привязаны к домену

2. **Не влияет на реальную торговлю**
   - Тестовые балансы используются только для отображения
   - Реальные API запросы не изменяются
   - Безопасно для работы

3. **Очистка данных**
   - При очистке кеша браузера тестовые балансы удаляются
   - Используйте консоль для ручной очистки

### Рекомендации

1. **Установите реалистичные балансы**
   - Например, 1000 USDT для начала
   - 0.5 BTC, 10 ETH и т.д.

2. **Проверяйте режим работы**
   - Перед редактированием убедитесь, что включен тестовый режим
   - Индикатор режима в нижней части интерфейса

3. **Используйте для тестирования**
   - Проверка отображения балансов
   - Тестирование торговых операций
   - Отладка UI компонентов

---

## ✅ Чек-лист

- [x] Убран показ объёма 24h из заголовка
- [x] Добавлено отображение баланса котируемой валюты
- [x] Реализовано редактирование в тестовом режиме
- [x] Сохранение в localStorage
- [x] Автоматическое использование тестовых балансов
- [x] Обновление при переключении валюты
- [x] Синхронизация между всеми элементами UI
- [x] Документация создана
- [ ] Тестирование в work режиме
- [ ] Тестирование в test режиме
- [ ] Проверка сохранения балансов
- [ ] Готово к релизу

---

## 🚀 Быстрый тест

```bash
# 1. Запустить сервер
.\START.bat

# 2. Открыть http://localhost:5000

# 3. Переключить в тестовый режим (внизу интерфейса)

# 4. Кликнуть на "0.00" рядом с USDT в заголовке

# 5. Ввести: 10000

# 6. Проверить:
#    - Баланс: 10000.00 USDT (в заголовке)
#    - Баланс USDT: 10000.00000000 (в блоке балансов)

# 7. Перезагрузить страницу (F5)

# 8. Проверить, что баланс сохранился
```

---

**Дата**: 9 ноября 2025  
**Версия**: v1.7.1  
**Статус**: ✅ РЕАЛИЗОВАНО
