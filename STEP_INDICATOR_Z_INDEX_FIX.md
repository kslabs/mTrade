# ИСПРАВЛЕНИЕ ПОЗИЦИОНИРОВАНИЯ ИНДИКАТОРА ШАГА

## Дата: 7 декабря 2025 г.
## Статус: ✅ ИСПРАВЛЕНО

---

## Проблема

После добавления кнопки включения/выключения валюты, номер торгового шага не отображался или был перекрыт кнопкой разрешения торговли.

**Причина:**
- Индикатор шага (`.step-indicator`) имел `z-index: 1`
- Кнопка разрешения (`.perm-indicator`) имела `z-index: 2`
- Индикатор шага был ниже по слоям и мог быть перекрыт

---

## Решение

Обновлены CSS стили для `.step-indicator` в файле `static/style.css`:

### До:
```css
.base-currency-tabs .tab-item .step-indicator{
    position:absolute;
    bottom:2px;
    right:2px;           /* ← совпадает с позицией кнопки */
    background:#2196F3;
    color:#fff;
    font-size:10px;
    font-weight:700;
    padding:2px 4px;
    border-radius:4px;
    line-height:1;
    z-index:1;           /* ← ниже кнопки разрешения */
    box-shadow:0 0 4px rgba(0,0,0,.5);
}
```

### После:
```css
.base-currency-tabs .tab-item .step-indicator{
    position:absolute;
    bottom:2px;
    right:24px;          /* ← сдвинут влево, чтобы не перекрываться */
    background:#2196F3;
    color:#fff;
    font-size:10px;
    font-weight:700;
    padding:2px 4px;
    border-radius:4px;
    line-height:1;
    z-index:3;           /* ← выше всех элементов */
    box-shadow:0 0 4px rgba(0,0,0,.5);
    pointer-events:none; /* ← не перехватывает клики */
}
```

---

## Изменения

1. **`z-index: 3`** (было `z-index: 1`)
   - Теперь индикатор шага отображается поверх всех элементов
   - Выше кнопки разрешения (`z-index: 2`)

2. **`right: 24px`** (было `right: 2px`)
   - Индикатор сдвинут влево от правого края
   - Не перекрывается с кнопкой разрешения
   - Визуально более читаемо

3. **`pointer-events: none`** (новое свойство)
   - Индикатор шага не перехватывает клики мыши
   - Клики проходят "сквозь" индикатор к элементам под ним
   - Не мешает взаимодействию с вкладкой

---

## Визуальная схема

```
┌────────────────────────────────────────────┐
│  ВКЛАДКА ВАЛЮТЫ (tab-item)                │
│                                            │
│  🟢 ← .perm-indicator (right:2px, top:2px) │
│                                    z-index:2│
│                                            │
│  BTC ← .code-label                         │
│                                            │
│                               3 ← .step-   │
│                                  indicator │
│                        (right:24px, z:3)   │
└────────────────────────────────────────────┘

ПОЗИЦИОНИРОВАНИЕ:
- Кнопка разрешения: правый верхний угол (top:2px, right:2px)
- Индикатор шага: правый нижний угол (bottom:2px, right:24px)
- Не перекрываются благодаря разным позициям и z-index
```

---

## Тестирование

### 1. Перезагрузите страницу в браузере
```
Ctrl + F5 (или Cmd + Shift + R на Mac)
```

### 2. Проверьте отображение

**Кнопка разрешения торговли:**
- Должна быть в правом верхнем углу вкладки
- Зелёная 🟢 или красная 🔴
- Кликабельна

**Индикатор активного шага:**
- Должен быть в правом нижнем углу вкладки (чуть левее кнопки)
- Синий с белой цифрой
- НЕ кликабельный (клики проходят сквозь него)
- Отображается ПОВЕРХ кнопки разрешения

### 3. Проверьте в консоли браузера (F12)

Выполните в консоли:
```javascript
// Проверить z-index индикаторов
document.querySelectorAll('.step-indicator').forEach(el => {
  console.log('step-indicator z-index:', window.getComputedStyle(el).zIndex);
});

document.querySelectorAll('.perm-indicator').forEach(el => {
  console.log('perm-indicator z-index:', window.getComputedStyle(el).zIndex);
});
```

**Ожидаемый результат:**
```
step-indicator z-index: 3
perm-indicator z-index: 2
```

---

## Если проблема остаётся

### 1. Очистите кеш браузера
```
Ctrl + Shift + Delete → Изображения и файлы → Удалить
```

### 2. Проверьте, что CSS загружен
Откройте DevTools (F12) → Network → Перезагрузите страницу → найдите `style.css`
- Проверьте, что статус 200 (не 304 Not Modified)

### 3. Проверьте стили в инспекторе
F12 → Elements → выберите `.step-indicator` → вкладка Styles
- Проверьте, что `z-index: 3` применен
- Проверьте, что `right: 24px` применен
- Проверьте, что нет перечёркнутых стилей

### 4. Принудительная перезагрузка CSS
Добавьте версию к ссылке на CSS в `templates/index.html`:
```html
<link rel="stylesheet" href="/static/style.css?v=2">
```

---

## Файлы изменены

1. **`static/style.css`**
   - Обновлены стили для `.step-indicator`
   - Изменения: `z-index`, `right`, добавлен `pointer-events`

---

## Заключение

✅ Индикатор шага теперь отображается поверх всех элементов  
✅ Позиция сдвинута для лучшей читаемости  
✅ Не перехватывает клики мыши  
✅ Не перекрывается кнопкой разрешения  

Проблема решена полностью!
