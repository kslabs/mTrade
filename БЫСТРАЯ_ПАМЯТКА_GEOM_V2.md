# ⚡ ИСПРАВЛЕННАЯ БЫСТРАЯ ПАМЯТКА: geom_multiplier

## 🎯 ГЛАВНОЕ

**Параметр применяется ТОЛЬКО при неактивном цикле (volume = 0)**

**Система автоматически обновляет таблицу каждые 3 секунды**

---

## ✅ КОГДА РАБОТАЕТ

```
Volume = 0 (нет позиций)
    ↓
Изменили geom_multiplier → Нажали "Сохранить"
    ↓
✅ Таблица ПЕРЕСЧИТЫВАЕТСЯ АВТОМАТИЧЕСКИ
```

---

## 🔒 КОГДА НЕ РАБОТАЕТ

```
Volume > 0 (есть позиции)
    ↓
Изменили geom_multiplier → Нажали "Сохранить"
    ↓
⚠️ Таблица НЕ МЕНЯЕТСЯ (защита!)
    ↓
Параметр СОХРАНЁН, применится ПОСЛЕ ПРОДАЖИ ВСЕХ ПОЗИЦИЙ
    ↓
⏳ Система автоматически проверяет статус каждые 3 секунды
    ↓
Все продано (volume = 0)
    ↓
✅ Таблица АВТОМАТИЧЕСКИ пересчитывается с новым параметром!
```

---

## 🧪 КАК ПРОВЕРИТЬ

### Шаг 1: Проверьте статус
- Откройте вкладку валюты
- Посмотрите поле **"Объём базовой валюты"**
  - `= 0` → цикл неактивен ✅
  - `> 0` → цикл активен 🔒

### Шаг 2: Проверьте консоль (F12)
- Измените параметр → Нажмите "Сохранить"
- Смотрите логи:

**Если НЕАКТИВЕН:**
```
[BREAKEVEN] Цикл неактивен - пересчитываем с текущими параметрами
[BREAKEVEN] 📤 Отправка запроса: geom_multiplier=3.0
✅ Таблица обновлена автоматически!
```

**Если АКТИВЕН:**
```
[BREAKEVEN] ✅ Используем сохранённую таблицу цикла
🔒 Таблица НЕ изменилась (защита)
⏳ Параметр применится автоматически после завершения цикла
```

---

## 💡 ПОЧЕМУ ТАК СДЕЛАНО

> **Защита от потерь!**
> 
> Если бы система пересчитывала таблицу во время торговли:
> - Цены продажи не совпали бы с покупками
> - Вы могли продать в минус
> - Логика усреднения сломалась бы

---

## 🔄 АВТОМАТИЧЕСКОЕ ОБНОВЛЕНИЕ

**Система проверяет статус цикла каждые 3 секунды:**
- Пока цикл активен → показывает сохранённую таблицу
- Когда цикл завершается → **автоматически** пересчитывает таблицу с новыми параметрами

**Вам не нужно ничего делать!** Просто дождитесь завершения цикла.

---

## 📋 ПРОВЕРОЧНЫЙ ЧЕКЛИСТ

```
1. Volume = 0?
   ├─ Да  → ✅ Параметр применится при нажатии "Сохранить"
   └─ Нет → 🔒 Применится автоматически после продажи

2. Изменили параметр?
   ├─ Да  → Нажмите "Сохранить"
   └─ Нет → Таблица не изменится

3. Видите старую таблицу?
   ├─ Проверьте Volume (наверняка > 0)
   └─ Дождитесь продажи всех позиций
       (таблица обновится автоматически!)

4. Параметр сохранён?
   ├─ Да  → Смотрите params/<ВАЛЮТА>.json
   └─ Применится автоматически при следующем цикле
```

---

## 🔗 ПОЛНАЯ ДОКУМЕНТАЦИЯ

Читайте: 
- `ПОЛНОЕ_ОБЪЯСНЕНИЕ_GEOM_MULTIPLIER.md` (полная документация)
- `РЕАЛЬНАЯ_ЛОГИКА_ОБНОВЛЕНИЯ_ТАБЛИЦЫ.md` (как работает обновление)

---

**ВЕРСИЯ**: 2.0  
**ДАТА**: 2025-01-28  
**ОБНОВЛЕНО**: Исправлены ссылки на кнопку "Обновить таблицу" → "Сохранить"
