# 🚀 Быстрый старт: Сохранение настроек UI

## Что сделано?

✅ Все настройки интерфейса теперь **автоматически сохраняются** и восстанавливаются при перезагрузке!

## Что сохраняется?

- ✅ Включенные/выключенные валюты для торговли
- ✅ Состояние автотрейдинга (вкл/выкл)
- ✅ Сетевой режим (work/test)
- ✅ Выбранная валютная пара
- ✅ Настройки отображения

## Где хранится?

📁 Файл: **`ui_state.json`** (создается автоматически)

## Как использовать?

### 1️⃣ Просто работайте как обычно!

Все изменения настроек **автоматически сохраняются** на сервер.

### 2️⃣ Перезагрузите страницу

Все ваши настройки **автоматически восстановятся**.

### 3️⃣ Даже после перезапуска сервера

Все настройки **сохраняются между запусками**.

## API для разработчиков

```javascript
// Получить текущие настройки
GET /api/ui/state

// Сохранить изменение
POST /api/ui/state/partial
Body: { "updates": { "theme": "dark" } }

// Сброс к дефолту
POST /api/ui/state/reset
```

## Пример в JavaScript

```javascript
// Загрузить настройки при открытии страницы
await uiStateManager.init();

// Сохранить настройку
uiStateManager.save('theme', 'dark');

// Получить значение
const theme = uiStateManager.get('theme', 'dark');
```

## Проверка работы

Откройте консоль сервера - должно быть:
```
[BOOT] Загружено сохраненное состояние UI
[BOOT] Восстановлены разрешения торговли: {...}
[BOOT] Автотрейдинг: True/False
```

## Документация

📖 **UI_STATE_README.md** - подробная инструкция  
📖 **UI_STATE_GUIDE.md** - руководство разработчика  
📖 **UI_STATE_TESTING.md** - чеклист тестирования  
📖 **UI_STATE_SUMMARY.md** - полное резюме

## 🎉 Готово!

Больше не нужно настраивать интерфейс после каждой перезагрузки!

---

**Статус:** ✅ Работает  
**Версия:** 1.0  
**Дата:** 9 ноября 2025
