# ✅ ИСПРАВЛЕНИЕ: Реальное поведение системы geom_multiplier

## 🎯 ИЗВИНИТЕ ЗА ПУТАНИЦУ!

В предыдущих документах была **неточность**: не существует кнопки "Обновить таблицу".

**РЕАЛЬНОЕ ПОВЕДЕНИЕ:**

1. Вы изменяете параметры в полях
2. Нажимаете кнопку **"Сохранить"**
3. Параметры сохраняются в файл
4. Таблица **автоматически** обновляется (если цикл неактивен)
5. ПЛЮС: Таблица обновляется автоматически **каждые 3 секунды**

---

## 🔄 КАК ЭТО РАБОТАЕТ НА САМОМ ДЕЛЕ

### Сценарий 1: Цикл НЕАКТИВЕН (Volume = 0)

```
1. Изменяете geom_multiplier: 2.0 → 3.0
2. Нажимаете кнопку "Сохранить"
   ↓
   • Параметры сохраняются в params/<ВАЛЮТА>.json
   • Таблица АВТОМАТИЧЕСКИ пересчитывается с новым параметром
   • Вы СРАЗУ видите новые объёмы
   ↓
✅ Готово! Никаких дополнительных действий не нужно!
```

### Сценарий 2: Цикл АКТИВЕН (Volume > 0)

```
1. Изменяете geom_multiplier: 2.0 → 5.0
2. Нажимаете кнопку "Сохранить"
   ↓
   • Параметры сохраняются в params/<ВАЛЮТА>.json ✅
   • Таблица НЕ пересчитывается (защита!) 🔒
   • Вы видите СТАРУЮ таблицу (из активного цикла)
   ↓
3. Цена растёт, все позиции продаются...
4. Volume становится = 0 (цикл завершён)
   ↓
5. АВТОМАТИЧЕСКИ (в течение 3 секунд):
   • Система проверяет статус цикла
   • Видит, что цикл неактивен
   • Таблица АВТОМАТИЧЕСКИ пересчитывается с geom=5.0 ✅
   ↓
✅ Параметр применился БЕЗ ваших действий!
```

---

## 🤖 АВТОМАТИЧЕСКОЕ ОБНОВЛЕНИЕ

**В коде (`static/app.js`, строка ~2439):**

```javascript
// Автоматическое обновление таблицы каждые 3 секунды
setInterval(() => { loadBreakEvenTable(); }, 3000);
```

**Что это значит:**

- ✅ Таблица обновляется САМА каждые 3 секунды
- ✅ Проверяется статус цикла (активен/неактивен)
- ✅ Если цикл завершился → таблица пересчитывается с новыми параметрами
- ✅ Вам НЕ НУЖНО нажимать никакие кнопки!

---

## 📋 ПРАВИЛЬНАЯ ИНСТРУКЦИЯ

### Как изменить параметр:

```
1. Откройте вкладку валюты (например, BTC)
2. Измените "Множитель геометрии": 2.0 → 3.0
3. Нажмите кнопку "Сохранить"
4. Дождитесь сообщения "✓ Сохранено"
5. ✅ ВСЁ! Больше ничего делать не нужно!
```

**Что произойдёт дальше:**

- **Если цикл неактивен** → таблица обновится СРАЗУ (в течение секунды)
- **Если цикл активен** → таблица обновится АВТОМАТИЧЕСКИ после завершения цикла (в течение 3 секунд)

---

## 🔍 КАК ПРОВЕРИТЬ

### Тест с неактивным циклом:

```
1. Убедитесь: "Объём базовой валюты" = 0
2. Откройте консоль браузера (F12)
3. Измените geom_multiplier: 2.0 → 3.0
4. Нажмите "Сохранить"
5. Смотрите консоль:

[PARAMS] Параметры сохранены
[BREAKEVEN] Цикл неактивен - пересчитываем...
[BREAKEVEN] ✅ Таблица обновлена

6. Смотрите таблицу → объёмы изменились!
```

### Тест с активным циклом:

```
1. Убедитесь: "Объём базовой валюты" > 0 (есть позиции)
2. Откройте консоль браузера (F12)
3. Измените geom_multiplier: 2.0 → 5.0
4. Нажмите "Сохранить"
5. Смотрите консоль:

[PARAMS] Параметры сохранены
[BREAKEVEN] ✅ Используем сохранённую таблицу цикла

6. Таблица НЕ изменилась (защита работает)
7. Дождитесь завершения цикла (все продано)
8. В течение 3 секунд автоматически:

[BREAKEVEN] Цикл неактивен - пересчитываем...
[BREAKEVEN] ✅ Таблица обновлена с geom=5.0

9. Таблица изменилась БЕЗ ваших действий!
```

---

## 🎯 КЛЮЧЕВЫЕ МОМЕНТЫ

### 1. Кнопка "Сохранить"

- ✅ Сохраняет параметры в файл
- ✅ Автоматически вызывает пересчёт таблицы (если можно)
- ✅ Не требует дополнительных действий

### 2. Автоматическое обновление (каждые 3 секунды)

- ✅ Проверяет статус цикла
- ✅ Обновляет таблицу при неактивном цикле
- ✅ Применяет сохранённые параметры

### 3. Защита при активном цикле

- 🔒 Таблица не пересчитывается
- ✅ Параметры сохраняются
- ✅ Применяются автоматически после завершения цикла

---

## 💡 ВАЖНО ПОНЯТЬ

### Вы НЕ ДОЛЖНЫ:

❌ Искать кнопку "Обновить таблицу" (её нет!)  
❌ Делать что-то после нажатия "Сохранить"  
❌ Ждать и обновлять таблицу вручную  
❌ Перезагружать страницу  

### Система ДЕЛАЕТ САМА:

✅ Сохраняет параметры при нажатии "Сохранить"  
✅ Обновляет таблицу автоматически (сразу или через 3 сек)  
✅ Применяет параметры после завершения цикла  
✅ Показывает актуальную таблицу  

---

## 🔄 ВРЕМЕННАЯ ШКАЛА

### Цикл неактивен (Volume = 0):

```
00:00:00 - Изменили параметр, нажали "Сохранить"
00:00:01 - ✅ Таблица автоматически пересчитана
00:00:03 - Автоматическое обновление (подтверждение)
00:00:06 - Автоматическое обновление (подтверждение)
...каждые 3 секунды...
```

### Цикл активен (Volume > 0):

```
00:00:00 - Изменили параметр, нажали "Сохранить"
00:00:01 - 🔒 Таблица НЕ пересчитана (защита)
00:00:03 - Автоматическое обновление (всё ещё защита)
00:00:06 - Автоматическое обновление (всё ещё защита)
...

[прошло время, цена растёт...]

01:23:45 - ✅ Все позиции проданы, Volume = 0
01:23:46 - Автоматическое обновление...
01:23:47 - ✅ Таблица автоматически пересчитана с новым параметром!
01:23:48 - Новый цикл может начаться с новыми параметрами
```

---

## 📚 ИСПРАВЛЕННАЯ ДОКУМЕНТАЦИЯ

Все документы будут исправлены с учётом реального поведения:

- ✅ Убраны упоминания несуществующей кнопки "Обновить таблицу"
- ✅ Добавлено объяснение автоматического обновления каждые 3 секунды
- ✅ Уточнено, что после "Сохранить" не требуется дополнительных действий
- ✅ Объяснено, что параметр применится автоматически после завершения цикла

---

## ✅ ИТОГ

**Система работает АВТОМАТИЧЕСКИ!**

1. Нажали "Сохранить" → параметры сохранены
2. Система САМА проверяет статус цикла (каждые 3 сек)
3. Система САМА пересчитывает таблицу (когда можно)
4. Вы просто ждёте (ничего не делая!)

**Никаких кнопок "Обновить таблицу" нет и не нужно!**

---

**ВЕРСИЯ**: 1.1 (исправленная)  
**ДАТА**: 2025-01-28  
**СТАТУС**: ✅ Соответствует реальному поведению
