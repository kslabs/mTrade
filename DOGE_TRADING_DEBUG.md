# Диагностика торговли DOGE - Пошаговая инструкция

## Дата: 17 ноября 2025

## Проблема
Включена валюта DOGE и автоторговля, но стартовая закупка не происходит.

## ✅ Что уже сделано

### 1. Проверка разрешений
- ✅ Алгоритм автотрейдера проверяет разрешения торговли
- ✅ Торгуются только валюты с `enabled=true`

### 2. Улучшенная диагностика
Добавлено подробное логирование на каждом этапе:

#### При запуске автотрейдера:
```
[AutoTrader] Запущен breakeven алгоритм
[AutoTrader] Валюты с разрешением торговли: ['DOGE', ...]
[AutoTrader] ✅ API клиент инициализирован (реальная торговля)
```
или
```
[AutoTrader] ⚠️ API клиент не доступен (режим симуляции)
```

#### При обработке валюты:
```
[AutoTrader][DOGE] Обработка: получение цены DOGE_USDT...
[AutoTrader][DOGE] Цена получена: 0.12345678 USDT
```

#### При попытке стартовой закупки:
```
[AutoTrader][DOGE] 📊 Попытка стартовой закупки:
  • Текущая цена: 0.12345678 USDT
  • Объём покупки: 3.0000 USDT
  • Keep резерв: 0.0000 USDT
  • Min quote: 1.0000, Min base: 0.00000001
  • Итоговая покупка: 24.32187654 DOGE за 3.0000 USDT
  • Баланс USDT: 100.0000 (требуется: 3.0000)
[AutoTrader][DOGE] 🔄 Отправка ордера на покупку...
[AutoTrader][DOGE] 📤 Отправка BUY ордера: 24.32187654 DOGE по цене 0.12345678
[AutoTrader][DOGE] ✅ FOK ордер исполнен: 24.32187654 DOGE
[AutoTrader][DOGE] ✅ Старт цикла row=0 price=0.12345678, filled=24.32187654
```

## 🔍 Как проверить статус

### Шаг 1: Перезапустите сервер
```bash
python mTrade.py
```

### Шаг 2: Проверьте логи при запуске
Ищите строки:
```
[INIT] Разрешения торговли: X/Y валют включено
[AutoTrader] Запущен breakeven алгоритм
[AutoTrader] Валюты с разрешением торговли: [...]
```

### Шаг 3: Проверьте, что DOGE в списке включенных
Должна быть строка:
```
[AutoTrader] Валюты с разрешением торговли: ['DOGE']
```

Если DOGE в списке "Валюты БЕЗ разрешения торговли", то:
1. Откройте UI приложения
2. Найдите DOGE в списке валют
3. Включите переключатель разрешения торговли

### Шаг 4: Смотрите на детали обработки
Каждые 10 циклов (примерно каждые 25 секунд) должны появляться строки:
```
[AutoTrader][DOGE] Обработка: получение цены DOGE_USDT...
[AutoTrader][DOGE] Цена получена: X.XXXXXXXX USDT
```

### Шаг 5: Проверьте попытку закупки
Если цена получена, должна появиться попытка закупки:
```
[AutoTrader][DOGE] 📊 Попытка стартовой закупки:
  • Текущая цена: ...
  • Объём покупки: ...
  • Баланс USDT: ...
```

## ❌ Возможные проблемы и решения

### 1. ❌ "Нет разрешений на торговлю валютами"
**Причина**: В `app_state.json` пустой объект `trading_permissions`

**Решение**:
1. Откройте `app_state.json`
2. Добавьте:
```json
{
  "trading_permissions": {
    "DOGE": true
  }
}
```
3. Перезапустите сервер

### 2. ❌ DOGE в списке выключенных валют
**Причина**: `"DOGE": false` в разрешениях

**Решение**: Через UI включите переключатель для DOGE

### 3. ❌ "Цена не получена, пропуск"
**Причина**: Проблемы с WebSocket или REST API

**Решение**:
1. Проверьте интернет-соединение
2. Проверьте, что пара DOGE_USDT торгуется:
```bash
curl "https://api.gateio.ws/api/v4/spot/tickers?currency_pair=DOGE_USDT"
```

### 4. ❌ "Недостаточно USDT: нужно X, доступно Y"
**Причина**: На балансе недостаточно USDT

**Решение**:
- **Вариант А**: Пополните баланс USDT на Gate.io
- **Вариант Б**: Уменьшите параметр `start_volume` для DOGE:
  1. Откройте UI
  2. Настройки валюты DOGE
  3. Измените `start_volume` с 3.0 на меньшее значение (например, 1.0)

### 5. ❌ "API клиент не доступен (режим симуляции)"
**Причина**: Не настроен активный аккаунт с API ключами

**Решение**:
1. Проверьте `accounts.json` - должен быть аккаунт с API ключами
2. Проверьте, что есть активный аккаунт
3. Для тестовой сети: убедитесь, что используете testnet API ключи

### 6. ❌ "FOK ошибка: ..." или "IOC ошибка: ..."
**Причина**: Проблема с размещением ордера

**Возможные причины**:
- Недостаточный баланс
- Неправильные API ключи
- Нет прав на торговлю у API ключей
- Проблемы с биржей

**Решение**:
1. Проверьте баланс на Gate.io
2. Проверьте права API ключей (должны включать "Spot Trading")
3. Проверьте режим сети (work/test)

### 7. ❌ "Таблица не рассчитана"
**Причина**: Отсутствуют параметры breakeven для DOGE

**Решение**:
1. Откройте UI
2. Перейдите к настройкам DOGE
3. Установите параметры (steps, start_volume, pprof, kprof, target_r)
4. Сохраните

## 📊 Проверка конфигурации

### 1. Проверьте `app_state.json`
```json
{
  "auto_trade_enabled": true,
  "trading_permissions": {
    "DOGE": true
  },
  "breakeven_params": {
    "DOGE": {
      "steps": 16,
      "start_volume": 3.0,
      "start_price": 0.0,
      "pprof": 0.6,
      "kprof": 0.02,
      "target_r": 3.65,
      "geom_multiplier": 2.0,
      "rebuy_mode": "geometric",
      "keep": 0.0,
      "orderbook_level": 1.0
    }
  }
}
```

### 2. Проверьте `currencies.json`
```json
[
  {
    "code": "DOGE",
    "symbol": "Ð"
  }
]
```

### 3. Проверьте наличие активного аккаунта
```json
// accounts.json
{
  "accounts": [
    {
      "name": "Main",
      "api_key": "ваш_api_key",
      "api_secret": "ваш_api_secret"
    }
  ],
  "active": "Main"
}
```

## 🧪 Режим тестирования

Если вы используете тестовую сеть (testnet):

1. **Проверьте режим сети**:
```
[INFO] Режим сети: test
```

2. **Убедитесь, что используете testnet API ключи**:
   - Testnet API: https://fx-api-testnet.gateio.ws
   - Получить ключи: https://www.gate.io/testnet

3. **Пополните testnet баланс**:
   - Перейдите на https://www.gate.io/testnet
   - Используйте faucet для получения тестовых USDT

## 📝 Типичный рабочий лог

При правильной настройке вы должны увидеть:

```
[INIT] Разрешения торговли: 1/12 валют включено
[AutoTrader] Запущен breakeven алгоритм
[AutoTrader] Валюты с разрешением торговли: ['DOGE']
[AutoTrader] ✅ API клиент инициализирован (реальная торговля)
[AutoTrader] 🔄 Цикл запущен с разрешениями:
  • Включено: ['DOGE']
[AutoTrader][DOGE] Обработка: получение цены DOGE_USDT...
[AutoTrader][DOGE] Цена получена: 0.07854321 USDT
[AutoTrader][DOGE] 📊 Попытка стартовой закупки:
  • Текущая цена: 0.07854321 USDT
  • Объём покупки: 3.0000 USDT
  • Keep резерв: 0.0000 USDT
  • Min quote: 1.0000, Min base: 1.00000000
  • Итоговая покупка: 38.19660112 DOGE за 3.0000 USDT
  • Баланс USDT: 100.0000 (требуется: 3.0000)
[AutoTrader][DOGE] 🔄 Отправка ордера на покупку...
[AutoTrader][DOGE] 📤 Отправка BUY ордера: 38.19660112 DOGE по цене 0.07854321
[AutoTrader][DOGE] ✅ FOK ордер исполнен: 38.19660112 DOGE
[AutoTrader][DOGE] ✅ Старт цикла row=0 price=0.07854321, filled=38.19660112
```

## 🆘 Если ничего не помогает

1. **Соберите полные логи**:
   - Скопируйте весь вывод консоли после запуска `python mTrade.py`
   - Особенно важны строки с `[AutoTrader]` и `[INIT]`

2. **Проверьте файлы конфигурации**:
   - `app_state.json`
   - `currencies.json`
   - `accounts.json`

3. **Проверьте API через curl**:
```bash
# Проверка тикера
curl "https://api.gateio.ws/api/v4/spot/tickers?currency_pair=DOGE_USDT"

# Проверка информации о паре
curl "https://api.gateio.ws/api/v4/spot/currency_pairs/DOGE_USDT"
```

4. **Проверьте баланс через API**:
```bash
# С вашими API ключами
curl -X GET "https://api.gateio.ws/api/v4/spot/accounts" \
  -H "KEY: your_api_key" \
  -H "SIGN: your_signature"
```

## 🎯 Быстрая проверка

После перезапуска сервера выполните:

```bash
# 1. Проверьте статус автотрейдера
curl http://localhost:5000/api/autotrade/status

# 2. Проверьте разрешения
curl http://localhost:5000/api/trade/permissions

# 3. Проверьте параметры DOGE
curl "http://localhost:5000/api/trade/params?base_currency=DOGE"

# 4. Проверьте цену DOGE
curl "http://localhost:5000/api/pair/data?base_currency=DOGE&quote_currency=USDT"

# 5. Проверьте баланс
curl "http://localhost:5000/api/pair/balances?base_currency=DOGE&quote_currency=USDT"
```

---

**Важно**: После внесенных изменений в код, автотрейдер выводит **ОЧЕНЬ подробные** диагностические сообщения. Просто перезапустите сервер и следите за логами - они покажут точную причину проблемы!

🚀 **Удачной торговли!**
