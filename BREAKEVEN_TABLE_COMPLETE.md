# Таблица безубыточности - Реализация завершена ✅

**Дата:** 10 ноября 2025 г.  
**Версия:** 1.0

## 📋 Описание

Реализована полная интеграция таблицы безубыточности для мульти-трейдингового бота с использованием реальных балансов Gate.io testnet и динамическим расчетом всех параметров.

---

## ✅ Что реализовано

### 1. Backend (mTrade.py)

#### Глобальные параметры торговли (`TRADE_PARAMS`)
```python
TRADE_PARAMS = {
    'start_rate': 1.0,              # Стартовый курс
    'start_volume': 10.0,           # Стартовый объем, $
    'step_decrease': 0.5,           # Шаг снижения, %
    'step_count': 10,               # Количество шагов
    'rebuy_coefficient': 1.5,       # Коэффициент докупок
    'profit_percent': 0.8,          # Процент профита, %
    'profit_coefficient': 0.1,      # Коэффициент профита
    'commission_buy': 0.2,          # Комиссия покупки, %
    'commission_sell': 0.2,         # Комиссия продажи, %
    'rebuy_mode': 'geometric'       # Режим сумм докупок
}
```

#### API Endpoints

**1. GET `/api/trade/params`** - Получение параметров
- Возвращает текущие значения всех торговых параметров

**2. POST `/api/trade/params`** - Сохранение параметров
- Обновляет глобальные параметры торговли
- Принимает JSON с полями параметров
- Автоматически применяется к таблице

**3. GET `/api/breakeven/table`** - Расчет таблицы безубыточности
- Параметры: `base_currency` (например, BTC)
- Возвращает массив строк таблицы с расчетами
- Динамически пересчитывается при каждом запросе

---

### 2. Frontend (templates/index.html)

#### Блок редактируемых параметров

Компактный блок с 10 редактируемыми полями:
1. **Стартовый курс** - начальная цена для расчета
2. **Стартовый объём, $** - размер первой покупки
3. **Шаг снижения, %** - процент падения курса на каждом шаге
4. **Количество шагов** - число докупок (1-50)
5. **Коэффициент докупок** - множитель для геометрической прогрессии
6. **Процент профита, %** - целевая прибыль
7. **Коэффициент профита** - корректировка профита по шагам
8. **Комиссия покупки, %** - комиссия Gate.io на покупку
9. **Комиссия продажи, %** - комиссия Gate.io на продажу
10. **Режим сумм докупок** - fixed/geometric/martingale

#### Таблица безубыточности

Таблица с 8 столбцами:

| # | Описание | Формула |
|---|----------|---------|
| **#** | Номер шага | 0, 1, 2, ..., N |
| **↓, %** | Процент снижения курса | `-step * step_decrease` |
| **Курс** | Расчетный курс | `start_rate * (1 + decrease_pct/100)` |
| **Покупка, $** | Сумма покупки | По режиму: fixed/geometric/martingale |
| **Инв.Сумм,$** | Общая сумма инвестиций | Сумма всех предыдущих покупок |
| **Ц.БезУб,$** | Цена безубыточности | `total_invested * (1 + comm_buy + comm_sell) / total_coins` |
| **↑ БезУб,%** | Процент роста для безубытка | `((breakeven - rate) / rate) * 100` |
| **tΔPsell, %** | Процент для профита | `↑ БезУб,% + Pprof,% - (# * Kprof)` |

---

## 🎨 Дизайн и UX

### Параметры
- Компактный layout с сеткой 3 столбца
- Кнопка "💾 Сохранить" в заголовке блока справа
- Индикатор статуса сохранения
- Автоматическая валидация полей (min/max/step)

### Таблица
- Цветовая кодировка:
  - 🟢 **Зеленый** - положительные значения, рост
  - 🔴 **Красный** - отрицательные значения, падение
  - 🟡 **Оранжевый** - цена безубыточности
  - 🔵 **Синий** - общие инвестиции
- Чередующиеся строки для удобства чтения
- Нулевая строка выделена зеленым фоном
- Monospace шрифт для числовых значений

---

## 📊 Режимы докупок

### 1. **Fixed** (Фиксированный)
```
Каждая покупка = start_volume
Пример: 10, 10, 10, 10...
```

### 2. **Geometric** (Геометрический)
```
Каждая покупка = start_volume * (rebuy_coef ^ step)
Пример при coef=1.5: 10, 15, 22.5, 33.75...
```

### 3. **Martingale** (Мартингейл)
```
Каждая покупка = start_volume * (2 ^ step)
Пример: 10, 20, 40, 80...
```

---

## 🔄 Workflow

1. **Пользователь заходит на страницу** → автоматически загружаются текущие параметры
2. **Изменяет параметры** → вводит новые значения
3. **Нажимает "💾 Сохранить"** → параметры отправляются на сервер
4. **Таблица пересчитывается** → автоматически обновляется с новыми значениями
5. **Результат отображается** → пользователь видит актуальную таблицу безубыточности

---

## 🧮 Формулы расчета

### Общее количество купленных монет
```
total_coins = Σ(purchase_amount[i] / rate[i]) для i от 0 до step
```

### Цена безубыточности с комиссиями
```
breakeven_price = total_invested * (1 + commission_buy/100 + commission_sell/100) / total_coins
```

### Процент роста для безубытка
```
breakeven_pct = ((breakeven_price - current_rate) / current_rate) * 100
```

### Целевой процент для профита
```
target_delta_pct = breakeven_pct + profit_percent - (step * profit_coefficient)
```

---

## 🧪 Тестирование

### API Endpoints
```bash
# Получение параметров
curl http://localhost:5000/api/trade/params

# Сохранение параметров
curl -X POST http://localhost:5000/api/trade/params \
  -H "Content-Type: application/json" \
  -d '{"start_rate": 1.5, "start_volume": 20}'

# Получение таблицы
curl http://localhost:5000/api/breakeven/table?base_currency=BTC
```

### Проверка UI
1. Откройте http://localhost:5000
2. Найдите блок "Параметры торговли"
3. Измените любой параметр
4. Нажмите "💾 Сохранить"
5. Проверьте обновление таблицы ниже

---

## 📝 Пример ответа API

### GET `/api/trade/params`
```json
{
  "success": true,
  "params": {
    "start_rate": 1.0,
    "start_volume": 10.0,
    "step_decrease": 0.5,
    "step_count": 10,
    "rebuy_coefficient": 1.5,
    "profit_percent": 0.8,
    "profit_coefficient": 0.1,
    "commission_buy": 0.2,
    "commission_sell": 0.2,
    "rebuy_mode": "geometric"
  }
}
```

### GET `/api/breakeven/table`
```json
{
  "success": true,
  "table": [
    {
      "step": 0,
      "decrease_pct": 0.0,
      "rate": 1.0,
      "purchase_usd": 10.0,
      "total_invested": 10.0,
      "breakeven_price": 1.004,
      "breakeven_pct": 0.4,
      "target_delta_pct": 1.2
    },
    {
      "step": 1,
      "decrease_pct": -0.5,
      "rate": 0.995,
      "purchase_usd": 15.0,
      "total_invested": 25.0,
      "breakeven_price": 1.0025,
      "breakeven_pct": 0.75,
      "target_delta_pct": 1.45
    }
    // ... еще 9 строк
  ],
  "params": { /* текущие параметры */ }
}
```

---

## 🐛 Исправленные проблемы

1. ✅ **Сервер не запускался** - добавлен блок `if __name__ == '__main__'` в mTrade.py
2. ✅ **Отсутствовали endpoints** - добавлены `/api/trade/params` и `/api/breakeven/table`
3. ✅ **Параметры не сохранялись** - реализована POST обработка
4. ✅ **Таблица не рассчитывалась** - добавлена полная логика расчета
5. ✅ **Неправильные поля в UI** - обновлены на соответствие backend

---

## 📦 Файлы изменены

1. **mTrade.py**
   - Добавлены глобальные `TRADE_PARAMS`
   - Добавлены 3 новых API endpoint
   - Добавлен блок запуска Flask

2. **templates/index.html**
   - Обновлены поля параметров (10 полей)
   - Обновлены функции `loadTradeParams()` и `saveTradeParams()`
   - Таблица уже была реализована ранее

---

## 🚀 Запуск и использование

```bash
# Запуск сервера
python start.py

# Проверка статуса
python status.py

# Перезапуск
python restart.py

# Остановка
python stop.py
```

Веб-интерфейс: **http://localhost:5000**

---

## ✨ Итог

Полностью функциональная таблица безубыточности с:
- ✅ Редактируемыми параметрами через UI
- ✅ Динамическим расчетом на backend
- ✅ Поддержкой 3 режимов докупок
- ✅ Учетом комиссий покупки/продажи
- ✅ Цветовой индикацией значений
- ✅ API для интеграции с другими модулями
- ✅ Реальными балансами Gate.io testnet

**Статус: Готово к использованию! 🎉**
