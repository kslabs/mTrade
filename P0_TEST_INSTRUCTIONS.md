# 🧪 Инструкция по тестированию исправления P0

## ⚡ Быстрый тест

### 1. Перезапустить сервер
```powershell
# Остановить текущий процесс (Ctrl+C)
# Запустить заново:
python mTrade.py
```

### 2. Запустить автоматический тест
```powershell
# В новом терминале:
python test_p0_fix_final.py
```

**Ожидаемый результат:**
```
✅ ВСЕ ПРОВЕРКИ ПРОЙДЕНЫ!
```

---

## 🔍 Ручной тест

### Шаг 1: Проверка текущего состояния
1. Открыть http://127.0.0.1:5000
2. Посмотреть текущий P0 в таблице безубыточности (row 0, rate)
3. Запомнить значение

### Шаг 2: Сброс цикла (если активен)
1. Если есть активный цикл, дождаться продажи
2. Или вручную продать монеты через биржу
3. Убедиться, что цикл сброшен

### Шаг 3: Новая стартовая покупка
1. Включить автотрейдер
2. Дождаться стартовой покупки
3. Проверить в логах:
   ```
   [AutoTrader][BTC] ✅ Старт цикла row=0 price=97458.83, filled=0.00102050
   [AutoTrader][BTC] 📊 Обновлён start_price в state_manager: 97458.83000000
   [AutoTrader][BTC] 📊 Таблица пересчитана с новым P0: 97458.83000000
   [AutoTrader][BTC] 🔍 DEBUG: P0 в таблице (row 0): 97458.83000000
   ```

### Шаг 4: Проверка в UI
1. Обновить страницу (F5)
2. Проверить, что P0 (row 0, rate) = цене покупки из логов
3. **Критически важно:** P0 должен совпадать с "Цена покупки" из логов

### Шаг 5: Проверка пересчёта таблицы
1. Дождаться изменения рыночной цены (например, +1%)
2. Таблица должна пересчитаться автоматически
3. P0 (row 0, rate) должен **остаться прежним** (не меняться)
4. Проверить в логах:
   ```
   [AutoTrader][BTC] 📊 Таблица рассчитана с P0=97458.83 (saved_start_price=97458.83, current=98500.00)
   ```

---

## 🎯 Критерии успеха

✅ P0 в таблице = цене первой покупки  
✅ P0 не меняется при пересчёте таблицы  
✅ P0 обнуляется после продажи  
✅ При новом цикле P0 снова фиксируется на цене новой покупки  

---

## 🐛 Если что-то пошло не так

### P0 не обновляется после покупки
1. Проверить логи на ошибки при сохранении `start_price`
2. Проверить `app_state.json` - должен быть `"start_price": 97458.83`
3. Перезапустить сервер

### P0 меняется при пересчёте таблицы
1. Проверить, что исправление применено (git log)
2. Проверить логи: должно быть `saved_start_price=...` в сообщении о пересчёте
3. Если `saved_start_price=0`, значит start_price не был сохранён

### Тест test_p0_fix_final.py не проходит
1. Убедиться, что сервер запущен: `python mTrade.py`
2. Проверить, что есть хотя бы одна валюта с параметрами в `app_state.json`
3. Запустить тест снова

---

## 📞 Поддержка

Если проблема не решена:
1. Сохранить логи сервера
2. Сохранить содержимое `app_state.json`
3. Сохранить содержимое `autotrader_cycles_state.json`
4. Описать проблему с указанием шагов воспроизведения
