# 🔧 Исправление: Поддержка дробных значений для параметра "Стакан"

**Дата:** 2025-11-16  
**Версия:** v1.8.5 (обновлено)

## 🐛 Проблема

Параметр "Стакан" не работал с дробными значениями (например, 0.1, 0.3, 0.5), так как в коде использовался `parseInt()`, который округлял дробные числа до целых.

### Пример проблемы:
- Вводили: **0.3**
- Получали: **0** (parseInt отбрасывает дробную часть, 0 заменяется на дефолтное значение 1)
- Результат: параметр всегда сбрасывался на **1**

---

## ✅ Решение

Заменили `parseInt()` на `parseFloat()` во всех местах, где обрабатывается параметр "Стакан".

### Изменённые файлы:

#### 1. `templates/index.html` (строка 191)
**Было:**
```html
<input type="number" id="paramOrderbookLevel" step="1" value="1" min="1" max="10" />
```

**Стало:**
```html
<input type="number" id="paramOrderbookLevel" step="0.1" value="1" min="0" />
```

**Изменения:**
- ✅ `step="0.1"` — позволяет вводить дробные значения
- ✅ `min="0"` — убрано ограничение min="1"
- ✅ Удалён `max="10"` — убрано ограничение максимального значения

---

#### 2. `static/app.js` (строка 543) - функция `renderBreakEvenTable()`
**Было:**
```javascript
const orderbookLevel = parseInt($('paramOrderbookLevel')?.value) || 1;
```

**Стало:**
```javascript
const orderbookLevel = parseFloat($('paramOrderbookLevel')?.value) || 1;
```

---

#### 3. `static/app.js` (строка 613) - функция `loadBreakEvenTable()`
**Было:**
```javascript
orderbook_level: parseInt($('paramOrderbookLevel')?.value) || 1
```

**Стало:**
```javascript
orderbook_level: parseFloat($('paramOrderbookLevel')?.value) || 1
```

---

#### 4. `static/app.js` (строка 724) - функция сохранения параметров
**Было:**
```javascript
orderbook_level: parseInt($('paramOrderbookLevel').value) || 1
```

**Стало:**
```javascript
orderbook_level: parseFloat($('paramOrderbookLevel').value) || 1
```

---

## 📊 Примеры работы после исправления

### Теперь корректно работают дробные значения:

#### Стакан = 0.1
```
Формула: (# × 0.1) + 1
Строка 0: (0 × 0.1) + 1 = 1.0
Строка 1: (1 × 0.1) + 1 = 1.1
Строка 2: (2 × 0.1) + 1 = 1.2
Строка 5: (5 × 0.1) + 1 = 1.5
```

#### Стакан = 0.3
```
Формула: (# × 0.3) + 1
Строка 0: (0 × 0.3) + 1 = 1.0
Строка 1: (1 × 0.3) + 1 = 1.3
Строка 2: (2 × 0.3) + 1 = 1.6
Строка 5: (5 × 0.3) + 1 = 2.5
```

#### Стакан = 0.5
```
Формула: (# × 0.5) + 1
Строка 0: (0 × 0.5) + 1 = 1.0
Строка 1: (1 × 0.5) + 1 = 1.5
Строка 2: (2 × 0.5) + 1 = 2.0
Строка 5: (5 × 0.5) + 1 = 3.5
```

#### Стакан = 2.5
```
Формула: (# × 2.5) + 1
Строка 0: (0 × 2.5) + 1 = 1.0
Строка 1: (1 × 2.5) + 1 = 3.5
Строка 2: (2 × 2.5) + 1 = 6.0
Строка 5: (5 × 2.5) + 1 = 13.5
```

---

## 🎯 Технические детали

### parseInt() vs parseFloat()

**parseInt():**
- Преобразует строку в **целое число**
- `parseInt("0.3")` → `0`
- `parseInt("1.5")` → `1`
- `parseInt("2.9")` → `2`

**parseFloat():**
- Преобразует строку в **число с плавающей точкой**
- `parseFloat("0.3")` → `0.3`
- `parseFloat("1.5")` → `1.5`
- `parseFloat("2.9")` → `2.9`

---

## ✅ Проверка работоспособности

### Тест 1: Дробное значение 0.1
1. Откройте веб-интерфейс
2. Установите параметр **Стакан = 0.1**
3. Проверьте колонку "Ст." в таблице
4. Ожидаемые значения: 1.0, 1.1, 1.2, 1.3...

### Тест 2: Дробное значение 0.3
1. Установите параметр **Стакан = 0.3**
2. Проверьте колонку "Ст." в таблице
3. Ожидаемые значения: 1.0, 1.3, 1.6, 1.9...

### Тест 3: Дробное значение 0.5
1. Установите параметр **Стакан = 0.5**
2. Проверьте колонку "Ст." в таблице
3. Ожидаемые значения: 1.0, 1.5, 2.0, 2.5...

### Тест 4: Целое значение (проверка обратной совместимости)
1. Установите параметр **Стакан = 5**
2. Проверьте колонку "Ст." в таблице
3. Ожидаемые значения: 1, 6, 11, 16...

---

## 📝 Заметки

1. ✅ **Обратная совместимость:** целые значения работают как раньше
2. ✅ **Дробные значения:** теперь полностью поддерживаются
3. ✅ **Без ограничений:** можно вводить любые положительные числа
4. ✅ **Точность:** используется нативная точность JavaScript для чисел с плавающей точкой

---

## 🎉 Итоги

### Что исправлено:
- ✅ HTML input теперь принимает дробные значения (step="0.1", min="0")
- ✅ JavaScript корректно обрабатывает дробные значения (parseFloat вместо parseInt)
- ✅ Удалены все ограничения (max="10", min="1")
- ✅ Параметр работает с любыми положительными числами

### Результат:
- ✅ **Стакан = 0.1** работает ✓
- ✅ **Стакан = 0.3** работает ✓
- ✅ **Стакан = 0.5** работает ✓
- ✅ **Стакан = 1** работает ✓
- ✅ **Стакан = 100** работает ✓

**Статус:** Готово к использованию! 🚀
