# ИСПРАВЛЕНИЕ: ЛОГИРОВАНИЕ TOTAL_DROP_PERCENT В ДОКУПКАХ

## Проблема обнаружена ✅

**Симптом**: В логах докупок (rebuy) параметр `↓%` (total_drop_percent) всегда показывает **0.00** вместо реального суммарного падения от начальной цены цикла.

### Пример из логов:
```
[17:09:37] [ADA] Buy{18.7952; Курс:0.4129; ↓Δ%:-4.49; ↓%:0.00; Инвест:18.7952}
[16:36:38] [ADA] Buy{13.7086; Курс:0.4168; ↓Δ%:-3.59; ↓%:0.00; Инвест:13.7086}
[07:40:38] [ADA] Buy{9.9991; Курс:0.4323; ↓Δ%:0.00; ↓%:0.00; Инвест:9.9991}
[07:39:31] [ADA] Sell{10.0357; Курс:0.4322; ↑Δ%:0.46; PnL:0.0378; Профит:0.0378}
```

Видно, что `↓%:0.00` во всех докупках, хотя должны быть реальные значения падения.

## Корень проблемы 🔍

В функции `_try_rebuy` в `autotrader_v2.py`:

### Старая логика (НЕПРАВИЛЬНО):

```python
# ШАГ 5: Получаем данные из СТАРОЙ таблицы (до докупки)
next_step = cycle.table[next_step_index]
total_drop_pct = float(next_step.get('total_drop_pct', 0))  # ← СТАРОЕ значение

# ... выполнение докупки ...

# ШАГ 6: Обновляем состояние и ПЕРЕСЧИТЫВАЕМ таблицу
cycle.active_step = next_step_index
cycle.total_invested_usd += executed_cost
cycle.base_volume += executed_amount

weighted_avg_price = cycle.total_invested_usd / cycle.base_volume
new_table = calculate_breakeven_table(params, current_price=weighted_avg_price)
cycle.table = new_table  # ← НОВАЯ таблица с другими значениями!

# ШАГ 7: Логируем с УСТАРЕВШИМ total_drop_pct
self.logger.log_buy(..., total_drop_percent=total_drop_pct)  # ← ОШИБКА!
```

### Что происходило:

1. **До докупки**: Получали `total_drop_pct` из таблицы, рассчитанной для следующего шага
2. **Выполняли докупку**: Цена, объём, invested изменялись
3. **Пересчитывали таблицу**: С новым средневзвешенным курсом — значения изменялись!
4. **Логировали с устаревшим значением**: Использовали `total_drop_pct` из старой таблицы

Результат: **0.00** или неправильные значения в логах.

## Решение ✅

### Новая логика (ПРАВИЛЬНО):

```python
# ШАГ 6: Обновляем состояние и ПЕРЕСЧИТЫВАЕМ таблицу
cycle.active_step = next_step_index
cycle.total_invested_usd += executed_cost
cycle.base_volume += executed_amount

weighted_avg_price = cycle.total_invested_usd / cycle.base_volume
new_table = calculate_breakeven_table(params, current_price=weighted_avg_price)
cycle.table = new_table  # ← НОВАЯ таблица

# ШАГ 7: Берём total_drop_pct из НОВОЙ таблицы
with lock:
    cycle = self.cycles[base]
    start_price = cycle.start_price
    current_step = cycle.active_step
    
    # 🔴 КРИТИЧЕСКОЕ ИСПРАВЛЕНИЕ: Берём из НОВОЙ таблицы!
    if cycle.table and current_step >= 0 and current_step < len(cycle.table):
        actual_total_drop_pct = abs(float(cycle.table[current_step].get('cumulative_decrease_pct', 0)))
    else:
        actual_total_drop_pct = 0.0

# Логируем с АКТУАЛЬНЫМ значением
self.logger.log_buy(..., total_drop_percent=actual_total_drop_pct)
```

### Ключевые изменения:

1. **Не сохраняем** `total_drop_pct` до пересчёта таблицы
2. **Получаем** `actual_total_drop_pct` из **НОВОЙ** (пересчитанной) таблицы
3. **Используем** `cumulative_decrease_pct` — это и есть суммарное падение от начальной цены
4. **Берём модуль** через `abs()` — значения отрицательные (падение)

## Что изменится после исправления ✅

### Было (НЕПРАВИЛЬНО):
```
[17:09:37] [ADA] Buy{18.7952; Курс:0.4129; ↓Δ%:-4.49; ↓%:0.00; Инвест:18.7952}
                                                         ^^^^ НОЛЬ!
```

### Стало (ПРАВИЛЬНО):
```
[XX:XX:XX] [ADA] Buy{18.7952; Курс:0.4129; ↓Δ%:-4.49; ↓%:4.49; Инвест:18.7952}
                                                         ^^^^ РЕАЛЬНОЕ ЗНАЧЕНИЕ!
```

Теперь в логах будет видно реальное суммарное падение цены от начала цикла.

## Проверка исправления 🧪

### 1. Перезапустите сервер:

```powershell
taskkill /F /IM python.exe
python mTrade.py
```

### 2. Дождитесь докупки:

Когда произойдёт следующая докупка, проверьте лог:

```
[XX:XX:XX] [ВАЛЮТА] ✅ Докупка записана в лог (Δ=X.XX%, падение=Y.YY%)
                                                              ^^^^^ НЕ 0.00!
```

### 3. Проверьте файл логов:

```
trade_logs/trade_logs_YYYY-MM-DD.jsonl
```

В строках с `"action": "buy"` параметр `"total_drop_percent"` должен быть **не равен 0** при докупках.

## Технические детали 📋

### Поле в таблице breakeven:

- **`cumulative_decrease_pct`**: Суммарное падение от начальной цены (отрицательное значение)
  - Шаг 0: `0.0%` (стартовая покупка)
  - Шаг 1: `-0.73%` (первая докупка)
  - Шаг 2: `-1.59%` (вторая докупка)
  - Шаг 3: `-2.58%` (третья докупка)
  - ...

### Формат логирования:

```python
self.logger.log_buy(
    currency=base,
    volume=executed_amount,
    price=executed_price,
    delta_percent=delta_percent,           # Отклонение от start_price (может быть +/-)
    total_drop_percent=actual_total_drop_pct,  # СУММАРНОЕ падение (всегда положительное в логе)
    investment=executed_cost
)
```

### Пример расчёта для ADA:

```
Start price (шаг 0): 0.4323 USDT
Шаг 1: 0.4168 USDT → cumulative_decrease_pct = -3.59% → лог: ↓%:3.59
Шаг 2: 0.4129 USDT → cumulative_decrease_pct = -4.49% → лог: ↓%:4.49
```

## Файлы изменены ✅

- **`autotrader_v2.py`**: Функция `_try_rebuy`, строки ~1107-1125
  - Изменена логика получения `total_drop_percent` для логирования
  - Теперь берётся из новой (пересчитанной) таблицы после докупки

## Следующие шаги 🎯

1. ✅ Исправление внесено
2. 🔄 **Перезапустите сервер** для применения
3. 👀 Дождитесь следующей докупки
4. ✅ Проверьте, что `↓%` показывает реальные значения

После перезапуска логи докупок будут показывать корректные значения суммарного падения! 📊
