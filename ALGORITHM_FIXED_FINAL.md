# АЛГОРИТМ АВТОТРЕЙДЕРА mTrade

**Дата:** 19 ноября 2025 г.  
**Статус:** ✅ АКТУАЛЬНО

---

## 🎯 АЛГОРИТМ

### 1. НАЧАЛО ЦИКЛА

**Условия старта:**
1. AutoTrade = ON
2. Валюта разрешена
3. Нет активного цикла
4. `(баланс_BASE * цена) - keep < purchase_usd` строки 0
5. `баланс_QUOTE >= purchase_usd` строки 0

**Примечание:** `keep` — резерв в котируемой валюте, который вычитается из эквивалента базовой валюты. Это минимальный остаток BASE валюты (выраженный в QUOTE), который должен сохраняться на балансе.

**Действие:**
- Покупка объёма из строки 0, столбец "Покупка, $"
- Фиксация стартовой цены P0
- Цикл активируется (row = 0)

**Пример:**
```
Баланс: 0.00396 DOGE (~0.0006 QUOTE), keep = 0 QUOTE
Строка 0: purchase_usd = 10.00 QUOTE
Проверка: (0.0006 - 0) < 10.00 ✅ и баланс_QUOTE >= 10.00 ✅
→ Покупка 10.00 котируемой валюты
```

---

### 2. УСРЕДНЕНИЕ (покупки во время цикла)

**Условия:**
- Цикл активен
- Цена упала на процент следующей строки таблицы (столбец "Падение, %")
- Баланс котируемой валюты достаточен для покупки

**Действие:**
- Покупка объёма следующей строки (столбец "Покупка, $")
- Пересчёт средней цены
- Переход на следующую строку (row++)
- Обновление целевой цены продажи

**Пример:**
```
Row 0: Куплено за 10.00 котируемой валюты по цене 0.1560
Цена упала до 0.1482 (-5%, условие строки 1)
→ Покупка ещё 10.00 котируемой валюты по цене 0.1482
Средняя цена = 0.1519
Новая цель продажи = 0.1534 (тейк-профит 1%)
```

---

### 3. ПРОДАЖА (завершение цикла)

**Условия:**
- Цикл активен
- Текущая цена >= цели продажи (столбец "Продажа")

**Действие:**
- Продажа ВСЕГО баланса BASE минус keep
- `объём_продажи = баланс_BASE - (keep / текущая_цена)`
- Цикл завершается (active = false, row = 0)
- Готовность к новому циклу

**Пример:**
```
Баланс: 131.5789 DOGE, keep = 0 QUOTE
Цена достигла 0.1534
→ Продажа 131.5789 DOGE за 20.18 котируемой валюты
Прибыль: +0.18 котируемой валюты (+0.9%)
Цикл завершён
```

---

## 💻 КОД

```python
# 1. НАЧАЛО ЦИКЛА
if not cycle['active']:
    base_balance_quote = баланс_BASE * цена - keep
    if base_balance_quote < purchase_usd and баланс_QUOTE >= purchase_usd:
        buy(purchase_usd)  # Покупка строки 0
        cycle['active'] = True
        cycle['row'] = 0

# 2. УСРЕДНЕНИЕ
if cycle['active'] and есть_следующая_строка:
    if цена_упала_на_процент_следующей_строки and баланс_QUOTE >= purchase_usd_next:
        buy(purchase_usd_next)  # Покупка следующей строки
        cycle['row'] += 1
        пересчёт_средней_цены()
        обновление_цели_продажи()

# 3. ПРОДАЖА
if cycle['active'] and цена >= цель_продажи:
    объём = баланс_BASE - (keep / цена)
    sell(объём)  # Продажа всех монет минус keep
    cycle['active'] = False
    cycle['row'] = 0
```

---

## 📋 ВАЖНО

**Keep-резерв:**
- Это объём BASE валюты (в котируемой валюте), который НЕ продаётся
- При продаже: `sell_amount = base_balance - (keep / price)`
- При проверке старта: `(base_balance * price) - keep < purchase_usd`

**Таблица безубыточности:**
- Строка 0: стартовая покупка
- Строки 1+: усреднения при падении цены
- Столбец "Покупка, $": объём покупки в котируемой валюте
- Столбец "Падение, %": условие для покупки
- Столбец "Продажа": целевая цена выхода

