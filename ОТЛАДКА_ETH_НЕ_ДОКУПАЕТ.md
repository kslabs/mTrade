# 🔍 ОТЛАДКА: Почему ETH не докупает?

## 📊 ДАННЫЕ ИЗ UI

```
Текущая цена: 3,165.32
Стартовая (P0): 3,208.58
Последняя покупка: 3,208.58
След. покупка: 3,185.16
Текущий шаг: 0 / 16
Рост от P0: -1.35%
```

## ❓ ПРОБЛЕМА

**Условие докупки ВЫПОЛНЕНО**:
- Текущая цена: **3,165.32 USDT**
- Порог докупки: **3,185.16 USDT**
- **3,165.32 < 3,185.16** ✅ Цена упала ниже порога!

**НО докупка НЕ происходит!** Статус: "⏸️ Ожидание"

## 🔎 ВОЗМОЖНЫЕ ПРИЧИНЫ

### 1. ❌ Недостаточно USDT на балансе
Для докупки на шаге 1 требуется определённая сумма USDT (зависит от параметров).

**Проверка**: Смотрим в логах строку:
```
[ETH] 💰 Баланс USDT: X.XX, Требуется: Y.YY
[ETH] [ERROR] Недостаточно USDT для докупки
```

### 2. ⏱️ Задержка 5 секунд после последней попытки
Если была попытка докупки менее 5 секунд назад, следующая попытка заблокирована.

**Проверка**: Смотрим в логах строку:
```
[ETH] [SKIP] Задержка после последней докупки (X.Xs < 5s)
```

### 3. 🔒 Есть открытые BUY ордера
Если есть незакрытые BUY ордера, докупка заблокирована (защита от дублирования).

**Проверка**: Смотрим в логах строку:
```
[ETH] [SKIP] Есть открытые BUY ордера (N)
```

### 4. 📋 Нет следующего шага в таблице
Если таблица неполная или `active_step + 1 >= len(table)`.

**Проверка**: Смотрим в логах строку:
```
[ETH] [SKIP] Нет следующего шага (active_step=X, всего шагов=Y)
```

### 5. 🔄 Докупка уже в процессе
Флаг `_rebuy_in_progress = True` (другой поток выполняет докупку).

**Проверка**: Смотрим в логах строку:
```
[ETH] [SKIP] Докупка уже в процессе (_rebuy_in_progress=True)
```

### 6. ⏸️ Цикл не активен
Состояние цикла изменилось на `IDLE` (маловероятно, так как UI показывает "Активен").

**Проверка**: Смотрим в логах строку:
```
[ETH] [SKIP] Цикл не активен (state=idle)
```

## 🛠️ ЧТО СДЕЛАНО

Добавлено **детальное логирование** в функцию `_try_rebuy`:

```python
[ETH] 🔄 _try_rebuy вызван | Цена: 3165.32000000
[ETH] 🔄 [DEBUG] Начало проверки условий докупки...
[ETH] 🔄 [DEBUG] Проверка 1: Цикл активен?
[ETH] 🔄 [DEBUG] ✅ Цикл активен
[ETH] 🔄 [DEBUG] Проверка 2: Есть ли таблица?
[ETH] 🔄 [DEBUG] ✅ Таблица есть (17 шагов)
[ETH] 🔄 [DEBUG] Проверка 3: Есть ли следующий шаг?
[ETH] 🔄 [DEBUG] active_step=0, next_step_index=1, всего шагов=17
[ETH] 🔄 [DEBUG] ✅ Следующий шаг есть (индекс 1)
[ETH] 🔄 [DEBUG] Проверка 4: Докупка уже в процессе?
[ETH] 🔄 [DEBUG] ✅ Докупка не в процессе
[ETH] 🔄 [DEBUG] Проверка 5: Задержка после последней докупки
[ETH] 🔄 [DEBUG] ✅ Задержка OK (или не было попыток)
[ETH] 🔄 [DEBUG] last_buy_price=3208.58000000
[ETH] 📊 Проверка условия докупки:
[ETH]   Текущая цена: 3165.32000000
[ETH]   Последняя цена покупки: 3208.58000000
[ETH]   Порог докупки (-0.73%): 3185.16000000
[ETH]   Условие: 3165.32000000 < 3185.16000000 ?
[ETH] ✅ Условие докупки выполнено! Падение: 1.35%
[ETH] [LOCK] Флаг _rebuy_in_progress установлен, начинаем докупку...
...
```

Теперь в логах будет **видно точно**, на каком шаге блокируется докупка!

## 📋 ИНСТРУКЦИЯ ПРОВЕРКИ

1. **Перезапустите автотрейдер** (если он был запущен без детального логирования)
2. **Дождитесь**, когда цена ETH снова упадёт ниже 3,185.16 USDT
3. **Смотрите в логи консоли** (или в терминал, где запущен `python mTrade.py`)
4. **Найдите строки с `[ETH]`** и **определите**, где именно останавливается докупка
5. **Сообщите результат** — какая проверка не прошла

## 🎯 ВЕРОЯТНАЯ ПРИЧИНА

**Недостаточно USDT на балансе** для докупки на шаге 1.

### Расчёт требуемой суммы:

Для шага 1 (следующий после стартовой покупки) требуется сумма из таблицы `table[1]['purchase_usd']`.

Если стартовый объём был **9.95 USDT**, то объём на шаге 1 зависит от параметра `rebuy_multiplier` (например, 1.5x = **14.93 USDT**).

**Решение**: Пополните баланс USDT или уменьшите `rebuy_multiplier` в параметрах ETH.

---

Создано: 2025-12-11
Файл: `autotrader_v2.py` (функция `_try_rebuy`)
Статус: ✅ ДЕТАЛЬНОЕ ЛОГИРОВАНИЕ ДОБАВЛЕНО
