# Исправление структуры заголовка таблицы безубыточности

## Дата исправления
16 ноября 2025

## Проблема
Предыдущее изменение было неправильным:
- Удалён блок `.params-header`, который нужен для стилей
- Кнопка "💾 Сохранить" пропала или отображалась неправильно
- Размер заголовка не соответствовал ожидаемому

## Причина
Попытка скопировать стиль "Рынок и стакан" была выполнена неправильно:
- У "Рынок и стакан" нет отдельного класса-обёртки
- У "Таблица безубыточности" есть специальный класс `.params-header` с собственными стилями

## Решение

### Правильная структура:
```html
<div class="params-header">
    <h3 style="display:flex;justify-content:space-between;align-items:center;gap:10px;margin:0;">
        <span>📊 Таблица безубыточности</span>
    </h3>
    <div class="params-header-actions">
        <div id="paramsSaveStatus" class="params-save-status"></div>
        <button id="saveParamsBtn" class="save-params-btn-compact">💾 Сохранить</button>
    </div>
</div>
```

### CSS стили для .params-header:
```css
.params-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 12px;
    padding-bottom: 8px;
    border-bottom: 1px solid #3a3a3a;
}

.params-header h3 {
    margin: 0;
    color: #4CAF50;
    font-size: 14px;
    font-weight: 600;
}

.params-header-actions {
    display: flex;
    align-items: center;
    gap: 10px;
}
```

## Что было исправлено

1. ✅ Восстановлена обёртка `.params-header`
2. ✅ H3 размещён внутри `.params-header`
3. ✅ Кнопка "💾 Сохранить" отображается корректно
4. ✅ Статус сохранения работает
5. ✅ Все существующие CSS-стили применяются правильно

## Различия со стилем "Рынок и стакан"

### "Рынок и стакан":
- Прямой H3 с inline-стилями
- Нет дополнительной обёртки
- Нет нижней границы

### "Таблица безубыточности":
- Обёртка `.params-header` с нижней границей
- H3 внутри обёртки
- Специальные стили для параметров

## Почему нельзя было использовать тот же подход?

Карточки имеют разное назначение:
- **"Рынок и стакан"** - простая карточка с заголовком и кнопками
- **"Таблица безубыточности"** - сложная карточка с редактируемыми параметрами и специальным визуальным разделением

## Git commits

```bash
# Отменён неправильный коммит
git reset --soft HEAD~1

# Создан правильный коммит
aa1a2f4 fix: исправлена структура заголовка таблицы безубыточности
```

## Проверка
- ✅ Заголовок отображается корректно
- ✅ Кнопка "💾 Сохранить" видна и работает
- ✅ Статус сохранения отображается
- ✅ Нижняя граница присутствует
- ✅ Размер и цвет заголовка соответствуют дизайну

## Урок
Не всегда можно копировать стили между разными компонентами. Каждый компонент может иметь свою специфическую структуру и CSS-классы, которые нужно учитывать.

---
Дата: 16 ноября 2025
