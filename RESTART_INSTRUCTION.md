# 🔄 ПЕРЕЗАПУСК СЕРВЕРА — ИНСТРУКЦИЯ

**Дата:** 8 декабря 2025  
**Проблема:** Сервер использовал СТАРУЮ версию кода

## ЧТО СЛУЧИЛОСЬ

Было запущено **5 процессов Python** одновременно:
- 2 процесса с 7 декабря 20:31 (СТАРЫЙ КОД)
- 2 процесса с 8 декабря 07:04 (СТАРЫЙ КОД)
- 1 процесс с `main.py`

**Старые процессы продолжали торговать с убытком!**

## ЧТО СДЕЛАНО

✅ Все процессы Python **остановлены**:
```powershell
Stop-Process -Name python -Force
```

## КАК ЗАПУСТИТЬ СЕРВЕР ПРАВИЛЬНО

### 1. Убедитесь, что все процессы остановлены
```powershell
Get-Process python -ErrorAction SilentlyContinue
```
Должно вернуть пусто или ошибку.

### 2. Запустите сервер ОДИН РАЗ
```
START.bat
```

ИЛИ

```powershell
python mTrade.py
```

### 3. Проверьте, что запущен ОДИН процесс
```powershell
Get-Process python
```
Должен быть **только ОДИН** процесс!

## КАК ПРОВЕРИТЬ, ЧТО ИСПРАВЛЕНИЕ РАБОТАЕТ

1. Дождитесь стартовой покупки XRP
2. Посмотрите в логах строку **"Проверка продажи"**:
   ```
   [XRP] Проверка продажи:
     Текущая цена: 2.0770
     Расчетный курс шага (rate): 2.0780
     Цена безубыточности: 2.0780
     Целевая дельта (от rate): +0.15%
     Целевая цена продажи: 2.08111700
     Объём для продажи: 4.807 XRP
   ```

3. **Убедитесь**:
   - Целевая цена продажи = `rate * (1 + 0.15/100)` = **2.0811**
   - Продажа НЕ происходит при ценах **2.0770, 2.0790** (они ниже порога!)
   - Продажа происходит **только при ≥ 2.0812**

## ВАЖНО!

⚠️ **НЕ ЗАПУСКАЙТЕ СЕРВЕР НЕСКОЛЬКО РАЗ!**  
⚠️ **ПЕРЕД ПЕРЕЗАПУСКОМ ВСЕГДА ОСТАНАВЛИВАЙТЕ ВСЕ ПРОЦЕССЫ!**

Используйте:
```powershell
# Остановить всё
Stop-Process -Name python -Force

# Проверить
Get-Process python -ErrorAction SilentlyContinue

# Запустить ОДИН РАЗ
python mTrade.py
```
