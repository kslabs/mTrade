# 🔍 Отладка загрузки состояния автотрейдера

## Проблема
Автотрейдер показывает `step=0, invested=9.99` для ICP, хотя в файле `autotrader_cycles_state.json` записано `step=3, invested=68.24`.

## Гипотеза
Либо:
1. Метод `_load_and_sync_state()` не выполняется при старте
2. Состояние загружается, но затем перезаписывается
3. Есть проблема с синхронизацией/кодировкой

## Добавленная диагностика

### 1. Отладочный файл
Добавлено принудительное логирование в файл `autotrader_load_debug.txt` (обходит проблемы с кодировкой консоли):

```python
def debug_log(msg):
    try:
        with open("autotrader_load_debug.txt", "a", encoding="utf-8") as df:
            df.write(f"{datetime.now()}: {msg}\n")
    except:
        pass
```

### 2. Логирование для ICP
Для валюты ICP добавлено детальное логирование:
- Значения из файла (base_volume, total_invested_usd)
- Состояние после загрузки в память
- Подтверждение сохранения в `self.cycles['ICP']`

## Как проверить

### Шаг 1: Перезапустите сервер
Через веб-интерфейс: **Настройки → Перезапустить сервер**

Или через терминал:
```powershell
# Остановить сервер (если запущен вручную)
# Ctrl+C в терминале, где запущен сервер

# Запустить заново
python server.py
```

### Шаг 2: Проверьте отладочный файл
```powershell
Get-Content autotrader_load_debug.txt
```

**Ожидаемый результат** для ICP:
```
2025-12-10 ...: ========== НАЧАЛО ЗАГРУЗКИ СОСТОЯНИЯ ==========
2025-12-10 ...: [AutoTraderV2] Загружено 16 циклов из файла
2025-12-10 ...: [LOAD_STATE][ICP] Загрузка из файла:
2025-12-10 ...:   - active (из файла): True
2025-12-10 ...:   - cycle_id: 142
2025-12-10 ...:   - base_volume: 22.45
2025-12-10 ...:   - total_invested_usd: 68.24134
2025-12-10 ...: [LOAD_STATE][ICP] [+] Цикл АКТИВИРОВАН в памяти:
2025-12-10 ...:   - cycle.state = CycleState.ACTIVE
2025-12-10 ...:   - cycle.is_active() = True
2025-12-10 ...:   - cycle.base_volume = 22.45
2025-12-10 ...:   - cycle.total_invested_usd = 68.24134
2025-12-10 ...: [LOAD_STATE][ICP] СОХРАНЕНО В ПАМЯТЬ: self.cycles['ICP'] создан
2025-12-10 ...: [AutoTraderV2] Итого загружено циклов: 16
2025-12-10 ...: ========== ЗАГРУЗКА ЗАВЕРШЕНА ==========
```

### Шаг 3: Проверьте актуальное состояние в веб-интерфейсе
1. Откройте вкладку **ICP**
2. Проверьте значения:
   - **Invested**: должно быть **~68.24 USDT**
   - **Step**: должно быть **3**
   - **Breakeven**: должен соответствовать таблице

### Шаг 4: Проверьте логи работы
```powershell
Get-Content server_debug.log -Tail 50 | Select-String -Pattern "ICP|autotrade_levels"
```

Должно быть:
```
[DEBUG] autotrade_levels для ICP_USDT: step=3, REAL_BE=3.03764... (invested=68.24, volume=22.45), active=True
```

## Возможные сценарии

### ✅ Сценарий 1: Всё работает
- Отладочный файл содержит правильные значения (base_volume=22.45, invested=68.24)
- Логи показывают `step=3, invested=68.24`
- Веб-интерфейс отображает правильные данные

**Действие:** Проблема решена! Переходим к тестированию докупки.

### ⚠️ Сценарий 2: Загрузка не происходит
- Файл `autotrader_load_debug.txt` **пустой** или отсутствует

**Причина:** Метод `_load_and_sync_state()` не вызывается

**Действие:** Проверить, инициализируется ли `AutoTraderV2` при старте сервера

### ⚠️ Сценарий 3: Загрузка происходит, но затем сбрасывается
- Отладочный файл показывает правильную загрузку (base_volume=22.45)
- НО логи показывают `step=0, invested=9.99`

**Причина:** Состояние перезаписывается после загрузки (вероятно, метод `_ensure_cycle()`)

**Действие:** Исправить логику `_ensure_cycle()` - не создавать новый цикл, если он уже загружен

## Следующие шаги

После подтверждения правильной загрузки:
1. ✅ Убедиться, что автотрейдер видит актуальное состояние ICP
2. 🧪 Протестировать докупку (цена должна упасть на 0.99% от `last_buy_price=3.01`)
3. 📊 Проверить новое логирование докупки (метки [BLOCK_X])
4. 🐛 При необходимости - исправить блокирующие условия

---

**Дата создания:** 2025-12-10  
**Статус:** В процессе диагностики
