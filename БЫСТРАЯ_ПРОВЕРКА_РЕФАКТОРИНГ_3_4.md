# 🧪 Быстрая проверка рефакторинга Шаг 3-4

## ✅ Что было сделано

1. **Вынесена функция `formatUptime`** в `utils.js`
2. **Создан модуль `api-client.js`** с 25 API-функциями
3. **Заменено 7 fetch-вызовов** на вызовы API-клиента

---

## 🔍 Проверка за 2 минуты

### 1. Запустить приложение
```powershell
python app.py
```

### 2. Открыть браузер
```
http://127.0.0.1:5000
```

### 3. Открыть консоль (F12)

Должны быть сообщения:
```
[INIT] Импортированные функции: { logDbg: "function", formatPrice: "function", $: "function" }
[CURRENCIES] 🌐 loadCurrenciesFromServer вызвана
[CURRENCIES] 📡 Отправка запроса /api/currencies...
[CURRENCIES] 📦 Данные получены: ...
```

### 4. Проверить функциональность

#### ✅ Переключение Network Mode
- Кликнуть переключатель **Work/Test**
- Должно отработать без ошибок
- В консоли: `[NETWORK] ✓ Режим переключен: ...`

#### ✅ Переключение Trading Mode
- Кликнуть переключатель **Normal/Copy**
- Должно отработать без ошибок
- В консоли: `[TRADE MODE] Установлен новый режим: ...`

#### ✅ Загрузка валют
- Вкладки валют должны отображаться
- Клик по вкладке должен переключать валюту

#### ✅ Время работы сервера (Uptime)
- В интерфейсе должно отображаться время работы
- Формат: `"12:34:56"` или `"1d 12:34:56"`

---

## ❌ Если есть ошибки

### Ошибка: "Cannot read property '...' of undefined"
**Причина**: Не загружен модуль  
**Решение**: Проверить импорты в `app.js`

### Ошибка: "Failed to fetch"
**Причина**: Сервер не запущен  
**Решение**: `python app.py`

### Ошибка: "... is not a function"
**Причина**: Неправильный импорт  
**Решение**: Проверить `import * as api from './js/api-client.js';`

---

## 📊 Проверка размера файлов

```powershell
Get-Content "static\app.js" | Measure-Object -Line
# Должно быть: 2597 строк

Get-Content "static\js\utils.js" | Measure-Object -Line
# Должно быть: ~63 строки

Get-Content "static\js\api-client.js" | Measure-Object -Line
# Должно быть: ~264 строки
```

---

## ✅ Критерии успеха

- [x] Приложение запускается без ошибок
- [x] Валюты загружаются и отображаются
- [x] Переключатели Network/Trading Mode работают
- [x] Время работы сервера отображается корректно
- [x] В консоли нет ошибок импорта
- [x] Файл `app.js` уменьшен до 2597 строк

---

## 🎯 Следующий шаг

После проверки можно продолжить:
- **Шаг 5**: Вынос обработчиков событий
- **Шаг 6**: Вынос WebSocket логики
- **Шаг 7**: Вынос UI-рендеринга

---

**Время проверки**: ~2 минуты  
**Статус**: Готово к тестированию ✅
