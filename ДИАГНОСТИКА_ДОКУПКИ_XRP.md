# 🔍 ДИАГНОСТИКА: Почему XRP не докупается?

**Дата:** 2025-01-XX  
**Проблема:** Цена XRP упала до 12.917 (ниже уровня докупки 13.1801625), но автотрейдер не совершает покупку

## 📊 Текущая ситуация

### Уровни цен XRP/USDT:
- **Текущая цена:** 12.917 USDT ❗
- **Start (P0):** 13.705 USDT
- **Уровень продажи:** 13.692535 USDT
- **Уровень докупки:** 13.1801625 USDT ❗
- **Безубыток (BE):** 13.44268359 USDT

### Статус:
- **Отклонение от P0:** -5.75% (падение)
- **Шаг:** 4/16
- **Инвестировано:** 103.24 USDT
- **Статус:** ⏸️ Ожидание

### ❌ ПРОБЛЕМА:
Цена **12.917** находится **НИЖЕ** уровня докупки **13.1801625**, но покупка НЕ происходит!

---

## 🔎 Анализ кода autotrader_v2.py

### 1. Функция `_try_rebuy` (строки 866-1100)

Логика докупки:
```python
def _try_rebuy(self, base: str, quote: str, price: float):
    # ... проверки ...
    
    # Получаем порог докупки из таблицы
    tabular_rate = float(next_step.get('rate', 0))
    decrease_step_pct = float(next_step.get('decrease_step_pct', 0))
    
    # Условие 1: Цена достигла табличной цены
    rebuy_threshold = tabular_rate
    if price >= rebuy_threshold:
        return  # ❌ БЛОКИРОВКА!
    
    # Условие 2: Проверяем реальное падение
    if last_buy_price > 0 and decrease_step_pct != 0:
        min_price_for_real_drop = last_buy_price * (1 + decrease_step_pct / 100.0)
        if price >= min_price_for_real_drop:
            print(f"[BLOCK] Реальное падение недостаточно!")
            return  # ❌ БЛОКИРОВКА!
```

**Возможные причины блокировки:**

#### A) `tabular_rate` больше текущей цены
- Если `next_step.get('rate')` возвращает неправильное значение
- Если таблица не пересчитывается корректно

#### B) Реальное падение недостаточно
- `last_buy_price` может быть выше текущей `start_price`
- Проверка `min_price_for_real_drop` может быть слишком строгой

#### C) Цена из стакана выше табличной
```python
# СТРОГАЯ ПРОВЕРКА 1: Цена из стакана НЕ ДОЛЖНА быть выше табличной!
if orderbook_price > rebuy_threshold:
    print(f"[BLOCK] Цена из стакана выше табличной, докупка ОТМЕНЕНА")
    return  # ❌ БЛОКИРОВКА!
```

---

## 🎯 Гипотезы

### Гипотеза #1: Проблема с `last_buy_price`
**Описание:** После последней покупки `last_buy_price` был сохранён с более высокой ценой, чем текущая `start_price`

**Проверка:**
- Вывести в лог: `cycle.last_buy_price`, `cycle.start_price`, `price`
- Сравнить с табличными значениями

**Решение:**
- При активации цикла правильно устанавливать `last_buy_price = start_price`

---

### Гипотеза #2: Таблица не пересчитывается
**Описание:** Таблица breakeven рассчитана с одной ценой `start_price`, а реальная цена покупки была другой

**Проверка:**
- Вывести в лог: `cycle.table[next_step_index]['rate']`
- Сравнить с ожидаемым уровнем докупки

**Решение:**
- Пересчитывать таблицу после каждой покупки с реальной ценой

---

### Гипотеза #3: Цена из стакана выше табличной
**Описание:** Gate.io возвращает цену из стакана, которая выше табличной цены докупки

**Проверка:**
- Вывести в лог: `orderbook_price`, `tabular_rate`
- Проверить, срабатывает ли блокировка `[BLOCK] Цена из стакана выше табличной`

**Решение:**
- Использовать `price` (ticker.last) вместо `orderbook_price` для MARKET ордеров
- Или расширить допустимый диапазон проверки

---

## 🛠️ План исправления

### ШАГ 1: Добавить детальное логирование
Добавить в функцию `_try_rebuy` (после строки 925):

```python
print(f"[{base}] 🔍 ДИАГНОСТИКА ДОКУПКИ:")
print(f"[{base}]   Текущая цена (ticker): {price:.8f}")
print(f"[{base}]   Табличная цена докупки: {tabular_rate:.8f}")
print(f"[{base}]   Предыдущая цена покупки: {last_buy_price:.8f}")
print(f"[{base}]   Start price (P0): {cycle.start_price:.8f}")
print(f"[{base}]   Требуемое падение: {decrease_step_pct:.2f}%")
print(f"[{base}]   Минимум для реального падения: {min_price_for_real_drop:.8f}")
print(f"[{base}]   Цена >= tabular_rate? {price >= rebuy_threshold}")
print(f"[{base}]   Цена >= min_price_for_real_drop? {price >= min_price_for_real_drop}")
```

### ШАГ 2: Проверить, срабатывает ли блокировка
Запустить сервер и дождаться момента, когда цена XRP снова упадёт ниже уровня докупки

### ШАГ 3: Исправить логику
В зависимости от того, какая гипотеза подтвердится:

**Если Гипотеза #1:**
```python
# В функции activate():
self.last_buy_price = start_price  # ✅ Правильно
```

**Если Гипотеза #2:**
```python
# После каждой покупки пересчитывать таблицу:
table = calculate_breakeven_table(params, current_price=executed_price)
cycle.table = table
```

**Если Гипотеза #3:**
```python
# Использовать ticker.last для проверки условия:
if price < tabular_rate:  # Используем ticker, а не orderbook
    # Покупаем по MARKET
```

---

## 📝 Следующие действия

1. ✅ Добавить детальное логирование в `_try_rebuy`
2. ⏳ Запустить сервер и дождаться падения цены XRP
3. ⏳ Проанализировать логи, определить причину блокировки
4. ⏳ Применить соответствующее исправление
5. ⏳ Протестировать исправление

---

## 🔗 Связанные файлы

- `autotrader_v2.py` (строки 866-1100) - функция `_try_rebuy`
- `autotrader_v2.py` (строки 400-550) - главный цикл `_main_loop`
- `breakeven_calculator.py` - расчёт таблицы безубытка

---

**Статус:** 🔍 В процессе диагностики  
**Приоритет:** 🔴 Критический
