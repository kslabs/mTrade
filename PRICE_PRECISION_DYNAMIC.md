# 🎯 Динамическая Точность Отображения Курсов

## ✅ Что Реализовано

Добавлена **динамическая точность отображения курсов** в таблице безубыточности. Теперь для каждой валюты используется своя точность, основанная на параметрах торговой пары.

---

## 📊 Формула Точности

```
Точность отображения = Price Precision + 1
```

**Примеры:**
- Если `Price Precision = 5`, то курсы показываются с `6` знаками после запятой
- Если `Price Precision = 2`, то курсы показываются с `3` знаками после запятой
- Если `Price Precision = 8`, то курсы показываются с `9` знаками после запятой

---

## 🔧 Что Изменилось

### Колонки с Динамической Точностью:
1. **"Курс"** - цена покупки на каждом шаге
2. **"Ц.БезУб,$"** - цена безубыточности

### Колонки с Фиксированной Точностью:
- **"↓, %"** - 2 знака (проценты)
- **"Покупка, $"** - 2 знака (доллары)
- **"Инв.Сумм,$"** - 2 знака (доллары)
- **"↑ БезУб,%"** - 2 знака (проценты)
- **"tΔPsell,%"** - 2 знака (проценты)

---

## 💻 Технические Детали

### Глобальная Переменная:
```javascript
let currentPairPricePrecision = 8; // Точность цены для текущей пары (по умолчанию 8)
```

### Загрузка Точности:
При переключении валюты система автоматически:
1. Запрашивает параметры пары через `/api/pair/info`
2. Извлекает `price_precision` из ответа
3. Сохраняет в `currentPairPricePrecision`
4. Логирует: `[PAIR_PARAMS] Price Precision для BTC_USDT: 5`

### Применение в Таблице:
```javascript
// Динамическая точность для курсов: Price Precision + 1
const pricePrecisionPlus1 = currentPairPricePrecision + 1;

// Форматирование с динамической точностью
const rate = row.rate !== undefined ? row.rate.toFixed(pricePrecisionPlus1) : '—';
const breakEvenPrice = row.breakeven_price !== undefined ? row.breakeven_price.toFixed(pricePrecisionPlus1) : '—';
```

---

## 📝 Примеры Отображения

### BTC/USDT (Price Precision = 2):
```
Курс: 43521.456      (3 знака)
Ц.БезУб,$: 44123.789 (3 знака)
```

### WLD/USDT (Price Precision = 5):
```
Курс: 2.123456       (6 знаков)
Ц.БезУб,$: 2.234567  (6 знаков)
```

### SHIB/USDT (Price Precision = 8):
```
Курс: 0.000012345    (9 знаков)
Ц.БезУб,$: 0.000012678 (9 знаков)
```

---

## 🔍 Отладка

### Консольные Логи:
```
[PAIR_PARAMS] Price Precision для WLD_USDT: 5
[BREAKEVEN] Точность цены (Price Precision): 5, будет использовано: 6
```

### Проверка в Браузере:
1. Откройте **DevTools** (F12)
2. Перейдите на вкладку **Console**
3. Переключайте валюты и наблюдайте логи
4. Проверьте таблицу безубыточности - точность курсов должна меняться

---

## 🎯 Преимущества

### 1. **Точность Для Каждой Валюты**
   - Не слишком много знаков для дорогих валют (BTC)
   - Не слишком мало знаков для дешевых валют (SHIB)

### 2. **Лучшая Читаемость**
   - Оптимальное количество знаков для каждой пары
   - Нет избыточных нулей

### 3. **Автоматическая Адаптация**
   - При добавлении новой валюты точность настраивается автоматически
   - Нет необходимости вручную настраивать отображение

### 4. **Соответствие Биржевым Стандартам**
   - Использует официальные параметры пар от Gate.io
   - Точность всегда актуальна

---

## 📦 Изменённые Файлы

**static/app.js:**
- Функция `renderBreakEvenTable()` - добавлена динамическая точность
- Функция `loadPairParams()` - уже сохраняет `currentPairPricePrecision`
- Добавлены логи для отладки

---

## ✅ Статус

**Готово к использованию!** ✨

При переключении между валютами точность отображения курсов будет автоматически адаптироваться под параметры каждой торговой пары.

---

**Дата:** 16 ноября 2025 г.  
**Версия:** v1.8.4  
**Коммит:** Динамическая точность курсов в таблице безубыточности
