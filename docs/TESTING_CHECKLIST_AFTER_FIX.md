# Чек-лист тестирования после исправления дублирующегося метода _run()

## ✅ Проверка кода

- [x] Компиляция: **Ошибок нет**
- [x] Структура: Только один метод `_run()` (строка ~1975)
- [x] Метод `_determine_active_step_by_cumulative_drop()` корректен (возвращает `int`)
- [x] Файл заканчивается корректно (строка 2080)

## 📋 План тестирования

### 1. Перезапуск автотрейдера

```powershell
# Остановить текущий процесс
# Очистить кеш браузера (Ctrl+Shift+Delete)
# Перезапустить приложение
python app.py
```

### 2. Мониторинг стартовых покупок

**Что смотреть в логах:**

```
[START][BTC] 🔍 Проверка возможности начать новый цикл...
[START][BTC] BASE баланс в quote: X.XX < минимум первого шага: Y.YY → ОК
[START][BTC] ✅ Стартовая покупка выполнена
```

**Что НЕ должно происходить:**

❌ **Два сообщения подряд** для одной и той же валюты:
```
[START][BTC] ✅ Стартовая покупка выполнена
[START][BTC] ✅ Стартовая покупка выполнена  # <-- ПЛОХО!
```

### 3. Проверка работы ребая

**Что должно происходить:**

✅ Ребай **только** когда достигнуто кумулятивное падение:
```
[REBUY][BTC] Текущее падение: 5.2% >= требуется: 5.0% → Докупка шага 1
[REBUY][BTC] ✅ Докупка выполнена: step=1, filled=X.XX
```

### 4. Проверка балансов

**После стартовой покупки:**
- BASE баланс должен быть **больше** минимума первого шага
- Новая стартовая покупка **НЕ** должна начаться до полной продажи

**После продажи:**
- BASE баланс должен быть **меньше** минимума первого шага
- Новая стартовая покупка **может** начаться

### 5. Проверка состояния цикла

**Используйте Debug Panel:**

1. Откройте панель отладки в интерфейсе
2. Проверьте для каждой валюты:
   - `active`: `true` или `false`
   - `active_step`: `-1` (нет цикла) или `0, 1, 2...` (активный шаг)
   - `base_volume`: текущий объём BASE в цикле

## 🔍 Диагностика проблем

### Если множественные стартовые покупки всё ещё происходят:

1. **Проверьте логи на дублирующиеся сообщения:**
   ```
   [START][XXX] 🔍 Проверка возможности начать новый цикл...
   ```
   Если таких сообщений **два подряд** для одной валюты → проблема не решена

2. **Проверьте файл состояния:**
   ```powershell
   cat autotrader_cycles_state.json
   ```
   Убедитесь, что `active: true` только для валют с активными циклами

3. **Проверьте баланс BASE:**
   ```powershell
   # В интерфейсе: Balances → выберите валюту
   # BASE баланс должен быть либо:
   # - Почти ноль (< минимум) → может начаться цикл
   # - Больше минимума → цикл активен, старт запрещён
   ```

### Дополнительные действия при необходимости:

Если проблема сохраняется, потребуется:

1. **Добавить блокировки (locks):**
   ```python
   import threading
   
   class AutoTrader:
       def __init__(self):
           self._start_lock = threading.Lock()
   
       def _try_start_cycle(self, base, quote):
           with self._start_lock:
               # Атомарная проверка и покупка
   ```

2. **Добавить глобальный флаг "обработка в процессе":**
   ```python
   self._processing = {}  # {base: True/False}
   
   def _try_start_cycle(self, base, quote):
       if self._processing.get(base):
           return
       self._processing[base] = True
       try:
           # ... логика ...
       finally:
           self._processing[base] = False
   ```

## 📊 Ожидаемые результаты

✅ **Один поток** обрабатывает все валюты  
✅ **Одна стартовая покупка** за раз для каждой валюты  
✅ **Ребай работает** только при достижении кумулятивного падения  
✅ **Продажа работает** корректно  
✅ **Нет дублирующихся ордеров**  

## 📝 Примечания

- Файл `autotrader.py` теперь содержит **2080 строк** (было 2164)
- Удалено ~84 строки неправильного кода
- Структура кода исправлена
