# 🔧 Решение проблем оптимизации mTrade

## Быстрая диагностика

### ❓ Как проверить, работает ли кэш?

```bash
python monitor_cache_performance.py --once
```

**Ожидаемо:**
```
Попадания (hits):      > 0
Hit Rate:              > 75%
```

**Если всё нули** → Кэш не работает, см. [Кэш не работает](#кэш-не-работает)

---

## Проблемы и решения

### 1. Кэш не работает (Hit Rate = 0%)

#### Симптомы:
```
Попадания (hits):      0
Промахи (misses):      0
Hit Rate:              0%
```

#### Причины и решения:

**A. Импорт не работает**
```bash
# Проверка
python -c "from balance_cache import get_balance_cache; print('OK')"

# Если ошибка → убедитесь, что balance_cache.py существует
ls balance_cache.py
```

**B. autotrader.py не обновлён**
```bash
# Проверка
grep "self.balance_cache" autotrader.py

# Должно быть 2+ совпадения
# Если нет → autotrader.py не обновлён, см. документацию
```

**C. mTrade не перезапущен**
```bash
# Остановить mTrade (Ctrl+C)
# Запустить снова
python mTrade.py
```

---

### 2. Hit Rate слишком низкий (<75%)

#### Симптомы:
```
Hit Rate:              45.3% ⚠️ СРЕДНЕ
Инвалидации:            50
```

#### Причины и решения:

**A. Слишком частые инвалидации (частые сделки)**

Это нормально при активной торговле. Hit Rate 50-75% приемлем.

**Опционально:**
```python
# balance_cache.py
BALANCE_CACHE_TTL = 10  # Увеличить TTL (было: 5)
```

**B. TTL слишком короткий**

```python
# balance_cache.py
BALANCE_CACHE_TTL = 10  # Было: 5
```

Перезапустить mTrade.

**C. Цикл слишком медленный**

```python
# dual_thread_autotrader.py
cycle_sleep = 0.3  # Было: 0.5
```

Перезапустить mTrade.

---

### 3. Балансы не обновляются после сделок

#### Симптомы:
- Баланс на бирже изменился
- В mTrade баланс старый

#### Причины и решения:

**A. Инвалидация не работает**

```bash
# Проверить логи
grep "Invalidated" mTrade.log

# Должны быть записи вида:
# [BalanceCache] Invalidated (reason: buy_XRP_USDT)
```

**Если нет** → autotrader.py не вызывает `invalidate()`, см. документацию.

**B. TTL слишком длинный**

```python
# balance_cache.py
BALANCE_CACHE_TTL = 3  # Уменьшить (было: 5)
```

**C. Принудительное обновление**

```python
# В коде (если нужно срочно)
balance = self._get_account_balance('XRP', force_refresh=True)
```

---

### 4. Ошибки при импорте

#### Симптомы:
```
ImportError: cannot import name 'get_balance_cache'
```

#### Решение:

```bash
# 1. Проверить существование файла
ls balance_cache.py

# 2. Проверить синтаксис
python -m py_compile balance_cache.py

# 3. Проверить путь
python -c "import sys; print(sys.path)"

# 4. Если balance_cache.py в другой директории:
# Скопировать в директорию mTrade.py
```

---

### 5. Медленно работает (всё равно)

#### Причины и решения:

**A. Кэш не используется**

См. [Кэш не работает](#кэш-не-работает)

**B. Hit Rate низкий**

См. [Hit Rate слишком низкий](#hit-rate-слишком-низкий-75)

**C. API Gate.io медленный**

```bash
# Проверить задержку API
python -c "
import time
from gate_api import ApiClient
start = time.time()
api = ApiClient()
# ... какой-то запрос ...
print(f'Задержка: {time.time() - start:.2f}s')
"

# Если > 2-3 секунды → проблемы с сетью/API
```

**D. Слишком много валют**

- При 20+ валютах → увеличить TTL до 10 секунд
- При 50+ валютах → рассмотреть разделение по аккаунтам

---

### 6. Кэш постоянно инвалидируется

#### Симптомы:
```
Инвалидации:            100
Hit Rate:              30% ⚠️
```

#### Причины:

- Очень активная торговля (много сделок)
- Неправильная логика инвалидации

#### Решение:

**A. Это нормально при активной торговле**

Hit Rate 30-50% приемлем при 5+ сделках/минуту.

**B. Проверить логику инвалидации**

```python
# autotrader.py — должно быть ТОЛЬКО после успешных операций:
if order_placed:
    self.balance_cache.invalidate(...)

# НЕ должно быть перед операцией или при ошибках!
```

**C. Увеличить TTL**

```python
# balance_cache.py
BALANCE_CACHE_TTL = 10  # Было: 5
```

---

### 7. Статистика не обновляется

#### Симптомы:
```bash
python monitor_cache_performance.py --once
# Показывает одинаковые значения
```

#### Причины и решения:

**A. mTrade не запущен**

```bash
# Проверить процесс
ps aux | grep mTrade

# Если нет → запустить
python mTrade.py
```

**B. Кэш не используется**

См. [Кэш не работает](#кэш-не-работает)

**C. Автотрейдер не активен**

Проверить, что валюты добавлены в автотрейдер.

---

### 8. Ошибки в логах

#### `[BalanceCache] ERROR: ...`

**Причины:**
- Проблемы с API Gate.io
- Неверные данные в кэше

**Решение:**

Кэш автоматически переключится на прямые запросы. Проверить:
```bash
# Логи Gate.io API
grep "gate_api" mTrade.log
```

#### `KeyError: 'available'` или `KeyError: 'total'`

**Причины:**
- Неверный формат ответа от API
- Баланс валюты = 0

**Решение:**

```python
# balance_cache.py уже обрабатывает это
# Если ошибка всё равно → обновить balance_cache.py
```

---

## Диагностические команды

### Проверить импорт модулей:
```bash
python -c "from balance_cache import get_balance_cache; print('✅ balance_cache OK')"
python -c "from autotrader import AutoTrader; print('✅ autotrader OK')"
```

### Проверить кэш:
```bash
python monitor_cache_performance.py --once
```

### Проверить логи:
```bash
# Последние 50 строк
tail -50 mTrade.log

# Поиск ошибок
grep "ERROR" mTrade.log
grep "Exception" mTrade.log

# Поиск инвалидаций
grep "Invalidated" mTrade.log
```

### Проверить процессы:
```bash
# Linux/Mac
ps aux | grep mTrade

# Windows PowerShell
Get-Process | Where-Object {$_.ProcessName -like "*python*"}
```

---

## Быстрые исправления

### Сбросить кэш вручную:
```python
from balance_cache import get_balance_cache
cache = get_balance_cache()
cache.invalidate(reason="manual_reset")
print("Кэш сброшен ✅")
```

### Проверить состояние кэша:
```python
from balance_cache import get_balance_cache
cache = get_balance_cache()
print(cache.get_stats())
```

### Принудительно обновить баланс:
```python
# В autotrader.py
balance = self._get_account_balance('XRP', force_refresh=True)
```

---

## Контрольный чеклист

- [ ] `balance_cache.py` существует
- [ ] `autotrader.py` обновлён (содержит `self.balance_cache`)
- [ ] mTrade перезапущен после изменений
- [ ] Валюты добавлены в автотрейдер
- [ ] Hit Rate > 75% через 10 минут работы
- [ ] Логи не содержат `ERROR` связанных с кэшем
- [ ] Инвалидация происходит после сделок

---

## Дополнительная помощь

**Не помогло?**

1. Соберите диагностику:
   ```bash
   python monitor_cache_performance.py --once > cache_stats.txt
   grep "BalanceCache" mTrade.log > cache_logs.txt
   ```

2. Проверьте документацию:
   - [OPTIMIZATION_FINAL_REPORT.md](OPTIMIZATION_FINAL_REPORT.md)
   - [OPTIMIZATION_CHECKLIST.md](OPTIMIZATION_CHECKLIST.md)

3. Откатитесь к предыдущей версии (если критично):
   ```bash
   git checkout <previous_commit> autotrader.py
   # Удалить balance_cache.py (не используется)
   ```

---

**Версия:** 1.0  
**Обновлено:** 2024-01-XX
