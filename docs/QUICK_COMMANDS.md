# ⚡ Быстрые команды для автотрейдера

## 🚀 ЗАПУСК

```powershell
# Самый простой способ - через тестовый скрипт:
python test_dual_process_autotrader.py

# Или вручную:
Invoke-WebRequest -Uri "http://localhost:5000/api/autotrade/start" -Method POST
```

## 📊 СТАТУС

```powershell
# Проверить статус:
Invoke-WebRequest -Uri "http://localhost:5000/api/autotrade/status" -Method GET

# Или через тестовый скрипт:
python test_dual_process_autotrader.py
```

## ⛔ ОСТАНОВКА

```powershell
# Остановить автотрейдер:
Invoke-WebRequest -Uri "http://localhost:5000/api/autotrade/stop" -Method POST
```

## 🌐 ЧЕРЕЗ БРАУЗЕР

```
http://localhost:5000
→ Раздел "Автотрейдер"
→ Кнопка "Включить/Выключить"
```

## 📁 ВАЖНЫЕ ФАЙЛЫ

- `test_dual_process_autotrader.py` - тестовый скрипт
- `AUTOTRADER_WORKS.md` - подробная инструкция
- `DUAL_PROCESS_CHEATSHEET.md` - полная шпаргалка
- `app_state.json` - настройки разрешений

## ✅ ПРОБЛЕМА РЕШЕНА

**Было:** `Can't pickle local object`  
**Стало:** ✅ Работает!  
**Исправлено:** Глобальная функция `_create_api_client()` в mTrade.py

---

**Версия:** 2.0.0  
**Статус:** ✅ РАБОТАЕТ
