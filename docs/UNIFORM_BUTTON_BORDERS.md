# ✅ ЕДИНЫЙ BORDER ДЛЯ КНОПОК ВАЛЮТЫ

**Дата:** 26 ноября 2025  
**Статус:** ✅ ВЫПОЛНЕНО

---

## 📋 ЗАДАЧА

Убрать белый/зелёный border у выбранной кнопки валюты. Все кнопки должны иметь одинаковый border независимо от того, выбрана валюта или нет.

---

## 🔧 ВНЕСЁННЫЕ ИЗМЕНЕНИЯ

### 1. **static/style.css** (строка 625)

**БЫЛО:**
```css
.symbol-item.selected {
    background: #1e4620;
    border-color: #4CAF50;  /* Зелёный border для выбранной */
    box-shadow: 0 0 12px rgba(76, 175, 80, 0.3);
}
```

**СТАЛО:**
```css
.symbol-item.selected {
    background: #1e4620;
    border-color: #3a3a3a;  /* Такой же как у остальных */
    box-shadow: 0 0 12px rgba(76, 175, 80, 0.3);
}
```

---

### 2. **templates/index.html** (строка 12)

Обновлён cache-buster для загрузки новых стилей:

**БЫЛО:**
```html
<link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}?v=step_numbers_v2">
```

**СТАЛО:**
```html
<link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}?v=uniform_borders">
```

---

## 🎨 ВИЗУАЛЬНЫЙ ЭФФЕКТ

### Теперь все кнопки валюты имеют:

| Состояние | Border | Background | Box-shadow |
|-----------|--------|------------|------------|
| **Обычная** | `2px solid #3a3a3a` | `#1a1a1a` | Нет |
| **Hover** | `2px solid #4a4a4a` | `#252525` | `0 4px 12px rgba(0,0,0,0.4)` |
| **Выбрана** | `2px solid #3a3a3a` | `#1e4620` (зелёный) | `0 0 12px rgba(76,175,80,0.3)` |

---

## ✅ РЕЗУЛЬТАТ

✅ **Единый border** для всех кнопок валюты (`2px solid #3a3a3a`)  
✅ **Выбранная валюта** отличается только:
   - Зелёным фоном (`#1e4620`)
   - Зелёным свечением (box-shadow)
✅ **Border остаётся одинаковым** независимо от состояния

---

## 🔍 КАК ПРОВЕРИТЬ

1. **Очистите кеш:** `Ctrl + F5`
2. **Откройте веб-интерфейс**
3. **Проверьте кнопки валюты:**
   - Все кнопки должны иметь одинаковый серый border
   - Выбранная валюта - зелёный фон + свечение
   - НЕТ зелёного или белого border у выбранной кнопки

---

## 📂 ИЗМЕНЁННЫЕ ФАЙЛЫ

| Файл | Строка | Изменение |
|------|--------|-----------|
| `static/style.css` | 625 | `border-color: #4CAF50` → `#3a3a3a` |
| `templates/index.html` | 12 | Cache-buster: `step_numbers_v2` → `uniform_borders` |

---

## 🎯 СВЯЗАННЫЕ ИЗМЕНЕНИЯ

Это изменение дополняет предыдущие визуальные улучшения:
- ✅ Усиление border кнопок до 2px (выполнено ранее)
- ✅ Усиление номера шага в таблице (выполнено ранее)
- ✅ **Унификация border для всех состояний кнопок** (это изменение)

---

**Готово!** 🎉
