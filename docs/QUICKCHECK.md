# 🚀 Быстрая проверка mTrade - Шпаргалка

## ✅ Все работает, если:

### 1. Сервер запускается
```bash
python mTrade.py
```
Должно быть:
```
✅ [DUAL-AT] Активных циклов: 16
✅ [DUAL-AT] 🚀 Двухпоточный автотрейдер активен
✅ [FLASK] Запуск веб-сервера на http://localhost:5000
```

### 2. Циклы активны в API
```bash
python check_web_api_cycles.py
```
Должно быть:
```
✅ Автотрейдер enabled: True
✅ Автотрейдер running: True
✅ Активных циклов: 16
✅ Все валюты с active_cycle: True
```

### 3. Обработка быстрая
В логах сервера:
```
[09:10:15] [ETH] Buy-DIAG{...}
[09:10:17] [ETH] Sell-DIAG{...}  ✅ ~2s между проверками
```

---

## ❌ Если что-то не работает:

### Проблема: Сервер не запускается
```bash
# Проверить синтаксис
python -m py_compile autotrader.py
python -m py_compile dual_thread_autotrader.py
python -m py_compile mTrade.py
```

### Проблема: Циклы не активны
```bash
# Проверить файл состояния
python diagnose_cycle_issue.py

# Проверить API
python check_web_api_cycles.py
```

### Проблема: Медленная обработка
Открыть `dual_thread_autotrader.py`, найти:
```python
cycle_sleep = 0.05  # должно быть 0.05, не 0.5!
```

Открыть `mTrade.py`, найти:
```python
debounce_seconds=0.01  # должно быть 0.01, не 0.1!
```

---

## 📊 Нормальные значения

- **Обработка 1 валюты:** ~0.1s
- **Полный цикл 16 валют:** ~1.6-2s
- **Частота проверки ETH:** каждые 2-3s
- **Активных циклов:** 16 (или количество включенных валют)

---

## 🔧 Файлы для проверки

1. `autotrader.py` - должен заканчиваться на:
   ```python
   except Exception as e:
       print(f"[AutoTrader] Ошибка цикла: {e}")
       time.sleep(self._sleep_interval)
   
   # Конец файла
   ```

2. `dual_thread_autotrader.py` - должен содержать:
   ```python
   def _load_cycles_state(self):
       self.cycles.clear()  # ✅ clear + update
       self.cycles.update(...)
   
   def _save_cycles_state(self):
       self.cycles.clear()  # ✅ clear + update
       self.cycles.update(...)
   ```

3. `mTrade.py` - должен содержать:
   ```python
   debounce_seconds=0.01  # ✅ не 0.1!
   ```

---

## 🎯 Быстрая диагностика

```bash
# 1. Проверка синтаксиса
python -m py_compile autotrader.py && echo "OK: autotrader.py"

# 2. Проверка циклов
python check_web_api_cycles.py | Select-String "Активных циклов"

# 3. Запуск сервера
python mTrade.py
```

**Если все 3 команды выполнены успешно - система работает! ✅**
