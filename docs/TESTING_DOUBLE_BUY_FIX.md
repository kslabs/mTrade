# 🧪 Тестирование исправления двойных покупок

## Быстрая проверка

### 1️⃣ Перезапустите приложение

```powershell
# Остановите текущий процесс (Ctrl+C)
# Запустите заново:
python app.py
```

### 2️⃣ Тест: Ручная продажа + автоматический старт

**Шаги**:

1. **Отключите** автотрейдер (если включён)
2. **Купите вручную** любую валюту (~$10)
3. **Включите** автотрейдер
4. **Продайте всё** через "Sell All"
5. **Наблюдайте** за логами в течение 10 секунд

**Ожидаемый результат**:

✅ **Правильно**:
```
[11:30:00] [SOL] Sell{...}
[MANUAL_SELL][SOL] Цикл сброшен: active=False, last_sell_time=...
[PROTECTION][SOL] ОТКАЗ: прошло 1.2с после продажи (минимум 5с)
[PROTECTION][SOL] ОТКАЗ: прошло 2.5с после продажи (минимум 5с)
[PROTECTION][SOL] ОТКАЗ: прошло 3.8с после продажи (минимум 5с)
[START_CYCLE][SOL] Прошло 5.1с после продажи - разрешаем новый цикл
[11:30:05] [SOL] Buy{...}  # Одна покупка через ~5 секунд
```

❌ **Неправильно** (если проблема не решена):
```
[11:30:00] [SOL] Sell{...}
[11:30:03] [SOL] Buy{...}  # Слишком быстро!
[11:30:04] [SOL] Buy{...}  # Ещё одна покупка!
```

### 3️⃣ Проверка файла состояния

```powershell
cat autotrader_cycles_state.json
```

**После продажи** файл должен быть:
- Либо **пустым**: `{}`
- Либо **без этой валюты** (если цикл сброшен)

❌ Если валюта **есть** в файле с `"active": true` сразу после продажи - проблема!

## Детальная диагностика

### Проверка 1: Срабатывает ли сброс цикла?

**Что смотреть**: Строка `[MANUAL_SELL]` в логах после продажи

**Где**: Консоль, где запущен `python app.py`

**Если НЕТ** этой строки:
- Проблема в файле `handlers/quick_trades.py`
- Проверьте, что изменения применились

### Проверка 2: Работает ли защита по времени?

**Что смотреть**: Строки `[PROTECTION]` с упоминанием времени

**Пример**:
```
[PROTECTION][SOL] ОТКАЗ: прошло 2.3с после продажи (минимум 5с)
```

**Если НЕТ** таких строк:
- `last_sell_time` не устанавливается
- Или проверка не выполняется

### Проверка 3: Единственная ли покупка?

**Что смотреть**: Количество строк `Buy{...}` после продажи

**Ожидается**: **Одна** покупка через ~5 секунд

**Если две или больше**:
- Lock не работает
- Или проблема в другом месте (например, параллельные вызовы)

## Мониторинг в реальном времени

### PowerShell команда для отслеживания:

```powershell
# Показывать только важные строки
Get-Content -Path "path\to\log.txt" -Wait | Select-String "SELL|BUY|PROTECTION|MANUAL_SELL"
```

Или просто смотрите на консоль где запущен `app.py`.

## Если проблема осталась

### Шаг 1: Увеличьте задержку

В файле `autotrader.py`, строка с проверкой:
```python
if elapsed < 5:  # ← Измените на 10
```

### Шаг 2: Проверьте сохранение состояния

Добавьте диагностику:
```python
print(f"[DEBUG] Saved cycle state: {self.cycles.get(base)}")
```

После строки `self._save_cycles_state()`

### Шаг 3: Отключите кэш балансов

В `_try_start_cycle` используйте:
```python
force_refresh=True  # Всегда обновляем баланс
```

## Успех!

Если видите:
- ✅ `[MANUAL_SELL]` сразу после продажи
- ✅ `[PROTECTION]` несколько раз в течение 5 секунд
- ✅ **Одна** покупка через ~5 секунд

**Проблема решена!** 🎉

## Дополнительные тесты

### Тест на параллельность

1. Включите автотрейдер
2. Включите несколько валют (SOL, BTC, ETH)
3. Продайте все через "Sell All"
4. Проверьте: каждая валюта должна иметь задержку 5 секунд

### Тест на быструю продажу/покупку

1. Купите вручную
2. Сразу продайте
3. Попытайтесь купить снова вручную
4. Проверьте: автотрейдер не должен мешать

## Контакты

Если проблема не решена после всех шагов - сообщите в issue с логами:
- Строки с `[MANUAL_SELL]`
- Строки с `[PROTECTION]`
- Строки с `Buy{...}` и `Sell{...}`
- Содержимое `autotrader_cycles_state.json`
