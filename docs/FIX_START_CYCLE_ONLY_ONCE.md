# Исправление: Стартовая покупка только один раз

**Дата:** 4 декабря 2025 г.  
**Проблема:** После ручной продажи и сброса цикла происходит несколько стартовых покупок подряд, хотя должна быть только одна.  
**Цель:** Гарантировать, что стартовая покупка происходит ТОЛЬКО ОДИН РАЗ в начале торгового цикла.

---

## 🎯 СУТЬ ПРОБЛЕМЫ

**До исправления:**
- Метод `_try_start_cycle()` вызывался КАЖДЫЙ РАЗ в основном цикле автотрейдера
- Даже когда цикл был уже активен, метод всё равно вызывался
- Внутренние проверки могли не срабатывать из-за race conditions
- В результате: **несколько стартовых покупок подряд**

**Критическая ошибка в коде:**
```python
# СТАРЫЙ КОД (НЕПРАВИЛЬНО)
self._try_start_cycle(base, quote)  # Вызывается ВСЕГДА!
self._try_rebuy(base, quote)        # Вызывается ВСЕГДА!
```

---

## ✅ РЕШЕНИЕ

### 1. Главное изменение: Условный вызов методов

**В методе `run()` (строка ~2075-2104):**

```python
# НОВЫЙ КОД (ПРАВИЛЬНО)
self._ensure_cycle_struct(base)
cycle = self.cycles.get(base, {})

if cycle.get('active'):
    # Цикл АКТИВЕН - выполняем только усреднение
    self._try_rebuy(base, quote)
else:
    # Цикл НЕ активен - пытаемся начать новый цикл
    self._try_start_cycle(base, quote)
```

**Результат:**
- ✅ `_try_start_cycle()` вызывается **ТОЛЬКО** когда `active=False`
- ✅ `_try_rebuy()` вызывается **ТОЛЬКО** когда `active=True`
- ✅ Невозможна ситуация, когда оба метода вызываются одновременно

---

### 2. Усиление защиты в `_try_start_cycle()`

Добавлены **тройные проверки** на самом входе в метод:

```python
def _try_start_cycle(self, base: str, quote: str):
    # ПРОВЕРКА #1: active=True?
    if cycle.get('active'):
        return  # Цикл активен - выход
    
    # ПРОВЕРКА #2: base_volume > 0?
    if cycle.get('base_volume', 0.0) > 0:
        return  # Объём уже есть - выход
    
    # ПРОВЕРКА #3: start_price > 0?
    if cycle.get('start_price', 0.0) > 0:
        return  # Цена старта есть - выход
    
    # ПРОВЕРКА #4: pending_start=True?
    if cycle.get('pending_start'):
        return  # Покупка уже идёт - выход
```

**Логика:**
- Если `base_volume > 0` → цикл уже начался, даже если `active=False`
- Если `start_price > 0` → стартовая покупка уже была сделана
- Если `pending_start=True` → покупка уже выполняется другим потоком

---

### 3. Усиление защиты в `_try_start_cycle_impl()`

Добавлены аналогичные **тройные проверки** перед выполнением покупки:

```python
def _try_start_cycle_impl(self, base: str, quote: str):
    # ФИНАЛЬНАЯ ПРОВЕРКА #1: active=True?
    if cycle.get('active'):
        return  # Цикл активен - ОТКАЗ
    
    # ФИНАЛЬНАЯ ПРОВЕРКА #2: base_volume > 0?
    if cycle.get('base_volume', 0.0) > 0:
        return  # Объём уже есть - ОТКАЗ
    
    # ФИНАЛЬНАЯ ПРОВЕРКА #3: start_price > 0?
    if cycle.get('start_price', 0.0) > 0:
        return  # Цена старта есть - ОТКАЗ
    
    # ФИНАЛЬНАЯ ПРОВЕРКА #4: pending_start=True?
    if cycle.get('pending_start'):
        return  # Покупка уже идёт - ОТКАЗ
```

---

## 🔒 УРОВНИ ЗАЩИТЫ

Теперь система имеет **4 уровня защиты** от множественных стартовых покупок:

### Уровень 1: В основном цикле `run()`
```python
if cycle.get('active'):
    self._try_rebuy(base, quote)  # ТОЛЬКО усреднение
else:
    self._try_start_cycle(base, quote)  # ТОЛЬКО старт цикла
```

### Уровень 2: На входе в `_try_start_cycle()`
```python
if active or base_volume > 0 or start_price > 0 or pending_start:
    return  # БЛОКИРОВКА
```

### Уровень 3: На входе в `_try_start_cycle_impl()`
```python
if active or base_volume > 0 or start_price > 0 or pending_start:
    return  # ОТКАЗ
```

### Уровень 4: Перед размещением ордера
```python
# Проверка баланса BASE перед покупкой
if current_base_usd >= purchase_usd * 0.9:
    return  # КРИТИЧЕСКАЯ БЛОКИРОВКА
```

---

## 📊 ЛОГИКА РАБОТЫ

### Сценарий 1: Нормальный старт цикла
```
1. run() → active=False → вызов _try_start_cycle()
2. _try_start_cycle() → проверки OK → захват Lock
3. _try_start_cycle_impl() → проверки OK → покупка
4. active=True, base_volume > 0, start_price > 0
5. run() → active=True → вызов ТОЛЬКО _try_rebuy()
```

### Сценарий 2: Попытка повторного старта (БЛОКИРОВКА)
```
1. run() → active=False → вызов _try_start_cycle()
2. _try_start_cycle() → base_volume > 0 → БЛОКИРОВКА
3. Покупка НЕ происходит
```

### Сценарий 3: Параллельные потоки (БЛОКИРОВКА)
```
Поток 1: _try_start_cycle() → захватил Lock → покупка идёт
Поток 2: _try_start_cycle() → Lock занят → БЛОКИРОВКА
```

---

## 🧪 ТЕСТИРОВАНИЕ

### Проверка 1: Один цикл - одна покупка
```bash
# Запустить автотрейдер
python mTrade.py

# Наблюдать логи:
# [START_CYCLE][ADA] Попытка стартовой покупки...
# [START_CYCLE][ADA] Покупка выполнена: 0.12345678 ADA
# 
# Дальнейшие вызовы:
# (нет логов _try_start_cycle, только _try_rebuy)
```

### Проверка 2: После ручной продажи
```bash
# 1. Ручная продажа → Сброс цикла
# 2. Автотрейдер делает ОДНУ стартовую покупку
# 3. Дальнейшие итерации → только усреднение
```

### Проверка 3: Race condition
```bash
# Запустить monitor_live.py для наблюдения за состоянием
python monitor_live.py

# Проверить количество покупок в логах:
# Должна быть ТОЛЬКО ОДНА стартовая покупка
```

---

## 🚨 КРИТИЧЕСКИЕ МОМЕНТЫ

### ⚠️ ВАЖНО: Условие в run()
```python
# ЭТО ГЛАВНОЕ ИЗМЕНЕНИЕ!
if cycle.get('active'):
    self._try_rebuy(base, quote)  # ТОЛЬКО усреднение
else:
    self._try_start_cycle(base, quote)  # ТОЛЬКО старт
```

Без этого изменения все остальные защиты могут не сработать!

### ⚠️ ВАЖНО: Проверка base_volume
```python
# Проверяем не только active, но и base_volume!
if cycle.get('base_volume', 0.0) > 0:
    return  # Цикл уже начат
```

Это защищает от случая, когда `active=False`, но покупка уже была сделана.

### ⚠️ ВАЖНО: Проверка start_price
```python
# Проверяем наличие start_price
if cycle.get('start_price', 0.0) > 0:
    return  # Стартовая цена уже зафиксирована
```

Это защищает от повторного старта после частичного завершения цикла.

---

## 📝 ИЗМЕНЁННЫЕ ФАЙЛЫ

1. **autotrader.py**
   - Метод `run()` (строка ~2075-2104)
   - Метод `_try_start_cycle()` (строка ~920-955)
   - Метод `_try_start_cycle_impl()` (строка ~1035-1080)

---

## ✅ РЕЗУЛЬТАТ

### До исправления:
- ❌ Несколько стартовых покупок подряд
- ❌ Стартовая покупка при активном цикле
- ❌ Race conditions между потоками

### После исправления:
- ✅ **ТОЛЬКО ОДНА** стартовая покупка в начале цикла
- ✅ Стартовая покупка **НЕВОЗМОЖНА** при активном цикле
- ✅ **4 уровня защиты** от множественных покупок
- ✅ Полная защита от race conditions

---

## 🔄 СЛЕДУЮЩИЕ ШАГИ

1. **Перезапустить автотрейдер** с новым кодом
2. **Проверить логи** на наличие множественных покупок
3. **Провести ручной тест**: продажа → проверка количества стартовых покупок
4. **Мониторить работу** в течение нескольких циклов

---

## 📞 ДИАГНОСТИКА

### Если проблема сохраняется:

1. **Проверить логи:**
   ```bash
   # Искать строки:
   [START_CYCLE][валюта] Попытка стартовой покупки...
   [PROTECTION][валюта] БЛОКИРОВКА: ...
   ```

2. **Проверить состояние цикла:**
   ```bash
   python quick_check_autotrader.py
   ```

3. **Проверить количество покупок:**
   ```bash
   # В логах должна быть ТОЛЬКО ОДНА строка:
   [START_CYCLE][валюта] Покупка выполнена
   ```

---

**Автор:** GitHub Copilot  
**Статус:** ✅ ИСПРАВЛЕНО  
**Версия:** 1.0  
**Дата последнего обновления:** 4 декабря 2025 г.
