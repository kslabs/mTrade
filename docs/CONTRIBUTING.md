# 🤝 Руководство по участию в проекте

Спасибо за интерес к улучшению Gate.io Multi-Trading Platform! Этот документ содержит рекомендации по внесению изменений.

## 📋 Содержание

- [Начало работы](#начало-работы)
- [Процесс разработки](#процесс-разработки)
- [Стандарты кода](#стандарты-кода)
- [Тестирование](#тестирование)
- [Отправка изменений](#отправка-изменений)

## 🚀 Начало работы

### Требования

- Python 3.8+
- Git
- Аккаунт Gate.io (для тестирования)

### Установка окружения

```bash
# Клонировать репозиторий
git clone https://github.com/yourusername/bGate.mTrade.git
cd bGate.mTrade

# Создать виртуальное окружение
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# Установить зависимости
pip install -r requirements.txt

# Скопировать примеры конфигурации
copy config.json.example config.json
copy accounts.json.example accounts.json
copy config\secrets.example.json config\secrets.json
```

## 🔧 Процесс разработки

### Создание ветки

```bash
# Создать новую ветку от main
git checkout -b feature/your-feature-name
```

### Именование веток

- `feature/` - новая функциональность
- `fix/` - исправление ошибок
- `docs/` - изменения в документации
- `refactor/` - рефакторинг кода
- `test/` - добавление тестов

**Примеры:**
- `feature/add-stop-loss`
- `fix/orderbook-precision`
- `docs/update-readme`

### Коммиты

Следуйте [Conventional Commits](https://www.conventionalcommits.org/):

```
<тип>(<область>): краткое описание

Подробное описание (опционально)

Футер (опционально)
```

**Типы:**
- `feat:` - новая функция
- `fix:` - исправление бага
- `docs:` - документация
- `style:` - форматирование
- `refactor:` - рефакторинг
- `test:` - тесты
- `chore:` - вспомогательные задачи

**Примеры:**
```bash
git commit -m "feat(autotrade): добавить stop-loss для автотрейдинга"
git commit -m "fix(websocket): исправить reconnect при потере соединения"
git commit -m "docs(readme): обновить инструкции по установке"
```

## 📝 Стандарты кода

### Python (PEP 8)

```python
"""
Модуль для работы с Gate.io API
Автор: Ваше имя
Дата: 2025-11-06
"""

import os
import sys
from typing import Dict, List, Optional


class TradeManager:
    """Менеджер для управления торговыми операциями"""
    
    def __init__(self, api_key: str, api_secret: str):
        """
        Инициализация менеджера
        
        Args:
            api_key: API ключ Gate.io
            api_secret: Секретный ключ Gate.io
        """
        self.api_key = api_key
        self.api_secret = api_secret
    
    def place_order(self, symbol: str, side: str, amount: float) -> Dict:
        """
        Разместить ордер
        
        Args:
            symbol: Символ валютной пары (например, "BTC_USDT")
            side: Сторона сделки ("buy" или "sell")
            amount: Количество базовой валюты
        
        Returns:
            Dict с результатом размещения ордера
        
        Raises:
            ValueError: Если параметры некорректны
            APIError: Если API вернул ошибку
        """
        # Реализация...
        pass
```

**Правила:**
- Отступы: 4 пробела
- Максимальная длина строки: 120 символов
- Комментарии на русском языке
- Docstrings в формате Google Style
- Type hints для всех функций

### JavaScript

```javascript
/**
 * Менеджер состояния UI
 * Управляет сохранением и восстановлением состояния интерфейса
 */
class UIStateManager {
    /**
     * Конструктор
     * @param {string} storageKey - Ключ для localStorage
     */
    constructor(storageKey) {
        this.storageKey = storageKey;
        this.state = {};
    }
    
    /**
     * Загрузить состояние с сервера
     * @returns {Promise<Object>} Объект состояния
     */
    async loadState() {
        try {
            const response = await fetch('/api/ui/state');
            if (!response.ok) {
                throw new Error(`HTTP error! status: ${response.status}`);
            }
            this.state = await response.json();
            return this.state;
        } catch (error) {
            console.error('Ошибка загрузки состояния:', error);
            return {};
        }
    }
}
```

**Правила:**
- Отступы: 4 пробела
- Точка с запятой обязательна
- JSDoc комментарии для всех функций
- Использовать `const` и `let`, избегать `var`
- Комментарии на русском языке

### HTML/CSS

```html
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gate.io Multi-Trading</title>
    <!-- Стили и скрипты -->
</head>
<body>
    <!-- Основной контент -->
    <div class="container">
        <!-- Хедер -->
        <header class="header">
            <h1 class="header__title">mTrade</h1>
        </header>
    </div>
</body>
</html>
```

**Правила:**
- Отступы: 4 пробела
- BEM методология для CSS классов
- Семантическая разметка HTML5
- Комментарии на русском языке

## 🧪 Тестирование

### Запуск тестов

```bash
# Все тесты
python -m pytest

# Конкретный файл
python test_websocket.py

# С покрытием кода
python -m pytest --cov=. --cov-report=html
```

### Написание тестов

```python
"""Тесты для модуля WebSocket"""
import pytest
from gateio_websocket import GateIOWebSocket


class TestWebSocket:
    """Тесты для класс GateIOWebSocket"""
    
    def setup_method(self):
        """Подготовка перед каждым тестом"""
        self.ws = GateIOWebSocket()
    
    def test_connection(self):
        """Тест подключения к WebSocket"""
        result = self.ws.connect()
        assert result is True
        assert self.ws.is_connected()
    
    def test_subscribe_orderbook(self):
        """Тест подписки на orderbook"""
        self.ws.connect()
        result = self.ws.subscribe_orderbook("BTC_USDT")
        assert result is True
```

### Обязательные проверки

Перед отправкой PR убедитесь:

- ✅ Все тесты проходят
- ✅ Код соответствует PEP 8 (проверить: `flake8 mTrade.py`)
- ✅ Нет предупреждений pylint
- ✅ Добавлены тесты для новой функциональности
- ✅ Обновлена документация
- ✅ CHANGELOG.md обновлен

## 📤 Отправка изменений

### Pull Request

1. **Убедитесь, что ваша ветка актуальна:**
   ```bash
   git checkout main
   git pull origin main
   git checkout your-branch
   git rebase main
   ```

2. **Отправьте изменения:**
   ```bash
   git push origin your-branch
   ```

3. **Создайте Pull Request на GitHub:**
   - Заголовок: краткое описание изменений
   - Описание: подробная информация о том, что изменено и почему
   - Ссылки на связанные issues

### Шаблон Pull Request

```markdown
## Описание
Краткое описание изменений и мотивация.

## Тип изменений
- [ ] Исправление бага (fix)
- [ ] Новая функция (feature)
- [ ] Критическое изменение (breaking change)
- [ ] Документация (docs)

## Тестирование
Описание проведенного тестирования:
- [ ] Локальные тесты пройдены
- [ ] Проверена работа в тестовом режиме
- [ ] Проверена работа с реальным API

## Чеклист
- [ ] Код соответствует стандартам проекта
- [ ] Добавлены комментарии для сложной логики
- [ ] Обновлена документация
- [ ] Добавлены/обновлены тесты
- [ ] Все тесты проходят
- [ ] Обновлен CHANGELOG.md

## Скриншоты (если применимо)
Добавьте скриншоты для изменений UI.
```

## 🐛 Сообщения об ошибках

### Шаблон Issue

```markdown
## Описание ошибки
Четкое описание проблемы.

## Шаги для воспроизведения
1. Перейти в '...'
2. Нажать на '...'
3. Прокрутить до '...'
4. Появляется ошибка

## Ожидаемое поведение
Что должно было произойти.

## Скриншоты
Если применимо, добавьте скриншоты.

## Окружение
- ОС: [например, Windows 10]
- Python версия: [например, 3.11]
- Версия проекта: [например, v1.4]

## Дополнительный контекст
Любая другая полезная информация.
```

## 📚 Ресурсы

- [Документация Gate.io API](https://www.gate.io/docs/developers/apiv4)
- [Flask документация](https://flask.palletsprojects.com/)
- [Python PEP 8](https://pep8.org/)
- [Conventional Commits](https://www.conventionalcommits.org/)

## ❓ Вопросы?

Если у вас есть вопросы, создайте Issue с меткой `question`.

---

**Спасибо за ваш вклад в проект! 🙏**
