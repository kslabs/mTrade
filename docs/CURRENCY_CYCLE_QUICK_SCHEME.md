# 🔄 Схема циклической обработки валют в mTrade

## 📌 Быстрый обзор

```
┌─────────────────────────────────────────────────────────────┐
│                   mTrade Автотрейдер                        │
│                                                             │
│  CYCLER Thread ←──────┐    ┌──────→ REACTOR Thread         │
│  (10мс/валюту)        │    │       (50мс задержка)         │
│                       │    │                               │
│  BTC → ETH → SOL →... │    │  WebSocket → Urgent Queue    │
│  ↓      ↓      ↓      │    │       ↓           ↓          │
│  Process each         │    │     Price      Priority       │
│  currency in cycle    │    │     updates    processing     │
│                       │    │                               │
│  └────────┬───────────┘    └────────┬─────────────────────┘
│           │                         │                      │
│           └─────────────┬───────────┘                      │
│                         ▼                                  │
│              _try_process_currency()                       │
│                         │                                  │
│           ┌─────────────┴─────────────┐                   │
│           │  Lock + Debounce + Perms  │                   │
│           └─────────────┬─────────────┘                   │
│                         ▼                                  │
│              _execute_trading_logic()                      │
│                         │                                  │
│         ┌───────────────┼───────────────┐                 │
│         ▼               ▼               ▼                 │
│    _try_start      _try_rebuy      _try_sell              │
│    (Старт)         (Докупка)       (Продажа) ❌ ОТСУТСТВУЕТ│
└─────────────────────────────────────────────────────────────┘
```

---

## 🔁 Цикл обработки ОДНОЙ валюты (например, BTC)

### Временная шкала

```
Time  Event                    Action
──────┼─────────────────────────┼──────────────────────────────────
0.00s │ CYCLER: BTC (cycle)   │ 
      │                        │ ├─ Lock check
      │                        │ ├─ Debounce check (0.1s)
      │                        │ ├─ Permission check
      │                        │ ├─ Set processing_flags[BTC]=True
      │                        │ │
      │                        │ ├─ _try_start_cycle("BTC","USDT")
      │                        │ │  └─ active? → Yes → return
      │                        │ │
0.01s │                        │ ├─ _try_rebuy("BTC","USDT")
      │                        │ │  └─ Падение < порога → return
      │                        │ │
0.02s │                        │ ├─ _try_sell("BTC","USDT")  ❌ ERROR!
      │                        │ │  └─ AttributeError
      │                        │ │
      │                        │ └─ Set processing_flags[BTC]=False
──────┼─────────────────────────┼──────────────────────────────────
0.03s │ CYCLER: ETH (cycle)   │ ... аналогично ...
──────┼─────────────────────────┼──────────────────────────────────
0.05s │ CYCLER: SOL (cycle)   │ ... аналогично ...
──────┼─────────────────────────┼──────────────────────────────────
0.15s │ REACTOR: BTC update   │
      │                        │ ├─ WebSocket: price changed
      │                        │ ├─ Debounce OK (0.15s - 0.00s)
      │                        │ └─ urgent_queue.put(BTC)
──────┼─────────────────────────┼──────────────────────────────────
0.16s │ CYCLER: Urgent BTC    │
      │                        │ ├─ urgent_queue.get(BTC)
      │                        │ ├─ Process BTC (reason: price_update)
      │                        │ └─ Same logic: start/rebuy/sell
```

---

## 📊 Этапы торгового цикла

### 1. 🚀 Старт цикла

```
Условия:
  ✅ active = False
  ✅ pending_start = False
  ✅ Торговля разрешена

Действия:
  1. Lock.acquire()
  2. Set pending_start = True
  3. Получить цену из orderbook (asks[level])
  4. Купить шаг 0
  5. Set active = True, active_step = 0
  6. Set start_price, last_buy_price
  7. Set pending_start = False
  8. Lock.release()

Результат:
  Цикл АКТИВЕН, шаг 0
```

### 2. 📉 Докупка (Усреднение)

```
Условия:
  ✅ active = True
  ✅ next_step < len(table)
  ✅ cumulative_drop >= required_drop

Действия:
  1. Получить порог падения: required_drop_pct
  2. Рассчитать падение: (start_price - current_price) / start_price
  3. Если падение >= порога:
     ├─ Получить цену из orderbook (asks[level])
     ├─ Купить следующий шаг
     ├─ active_step++
     └─ Обновить last_buy_price, base_volume

Результат:
  Шаг увеличен, объём увеличен
```

### 3. 💰 Продажа

```
❌ ПРОБЛЕМА: Функция _try_sell() ОТСУТСТВУЕТ в autotrader.py!

Должна быть:

Условия:
  ✅ active = True
  ✅ current_growth >= breakeven_pct

Действия:
  1. Рассчитать рост: (current_price - start_price) / start_price
  2. Если рост >= breakeven:
     ├─ Получить цену из orderbook (bids[level])
     ├─ Продать весь объём base_volume
     ├─ Set active = False
     ├─ Обнулить start_price, base_volume
     └─ Set last_sell_time

Результат:
  Цикл ЗАКРЫТ, прибыль зафиксирована
```

---

## 🎯 Где происходит переключение между валютами

### Файл: `dual_thread_autotrader.py`

### Функция: `_run_cycler()`

```python
current_index = 0  # Индекс текущей валюты
currencies = ["BTC", "ETH", "SOL", "XRP", ...]  # Список валют

while running:
    # 1. Обработать срочные задачи (до 5 за итерацию)
    for i in range(max_urgent_per_cycle):
        task = urgent_queue.get_nowait()
        _try_process_currency(task['currency'], "urgent")
    
    # 2. Обработать текущую валюту
    currency = currencies[current_index]  # ← Берём валюту по индексу
    _try_process_currency(currency, "cycle")
    
    # 3. Переключиться на следующую валюту
    current_index = (current_index + 1) % len(currencies)  # ← Циклический переход
    #                                   ↑
    #                    При достижении конца → возврат к началу
    
    # 4. Пауза (10мс)
    time.sleep(0.01)
```

**Пример:**
```
Итерация  current_index  Обрабатывается  Следующий
────────────────────────────────────────────────────
   1          0             BTC              1
   2          1             ETH              2
   3          2             SOL              3
   4          3             XRP              0  ← Возврат к началу
   5          0             BTC              1
   ...
```

---

## 🔐 Защита от дублирования

### 3 уровня защиты:

```
1️⃣ Lock (в _try_start_cycle)
   └─ Атомарная блокировка threading.Lock()
   └─ Гарантия: только один поток может начать старт цикла

2️⃣ Debounce (в _try_process_currency)
   └─ Минимум 0.1с между обработками одной валюты
   └─ last_processed[currency] + 0.1s < now

3️⃣ processing_flags (в _try_process_currency)
   └─ Флаг "валюта обрабатывается"
   └─ Устанавливается ДО обработки, сбрасывается ПОСЛЕ
```

---

## 📈 Статистика обработки

### Выводится через `/api/autotrade/stats`

```json
{
  "cycler_iterations": 10000,      // Сколько итераций сделал Cycler
  "cycler_processed": 5000,        // Сколько валют обработано (cycle)
  "cycler_skipped": 5000,          // Сколько пропущено (debounce/flags)
  "reactor_updates": 2000,         // WebSocket обновлений
  "reactor_queued": 500,           // Поставлено в urgent_queue
  "reactor_debounced": 1500,       // Пропущено по debounce
  "urgent_processed": 450,         // Обработано срочных задач
  "urgent_skipped": 50,            // Пропущено срочных (flags)
  "errors": 10                     // Ошибки
}
```

---

## 🚨 Критическая проблема

### ❌ Отсутствует `_try_sell()` в `autotrader.py`

**Симптомы:**
- Ошибка: `AttributeError: 'AutoTrader' object has no attribute '_try_sell'`
- Продажи НЕ ПРОИСХОДЯТ
- Циклы не закрываются
- Прибыль не фиксируется

**Решение:**
- См. [MISSING_TRY_SELL_PROBLEM.md](MISSING_TRY_SELL_PROBLEM.md)
- Восстановить функцию `_try_sell()` в `autotrader.py`

---

## 📚 Документация

1. [AUTOTRADER_COMPLETE_ARCHITECTURE.md](AUTOTRADER_COMPLETE_ARCHITECTURE.md) - Полная архитектура
2. [MISSING_TRY_SELL_PROBLEM.md](MISSING_TRY_SELL_PROBLEM.md) - Проблема с _try_sell
3. [AUTOTRADER_FIX_SUMMARY.md](AUTOTRADER_FIX_SUMMARY.md) - Резюме исправлений
4. [DOCS_INDEX_AUTOTRADER_FIX.md](DOCS_INDEX_AUTOTRADER_FIX.md) - Индекс документации

---

**Создано:** 2025-01-XX  
**Версия:** 1.0
