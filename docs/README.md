# 🚀 Gate.io Multi-Trading Platform

Профессиональная платформа для обычной торговли и копитрейдинга на Gate.io

## 🎨 Версия интерфейса: v1.2

### ✨ Новое в v1.2:
- **Компактная шапка** с информацией о сервере
- **Аптайм с точностью до секунд** в формате `00д 00:00:00`
- **PID процесса** с индикацией статуса
- **Минималистичный дизайн** с экономией места
- **Автообновление** каждую секунду для точности

### 📊 Интерфейс:
```
┌─────────────────────────────────────────────────────────┐
│ 🚀 mTrade              PID: 12345  00д 05:23:47  🔄⏹  │
│ Gate.io Trading Platform                                │
└─────────────────────────────────────────────────────────┘
```

---

## 📋 Возможности

- ✅ **Веб-интерфейс** - управление с любого устройства через браузер
- ✅ **Управление сервером** - старт/стоп/рестарт только этого сервера
- ✅ **Мультиаккаунт** - работа с несколькими API ключами
- ✅ **Два режима** - обычный трейдинг и копитрейдинг
- ✅ **PID-контроль** - изолированное управление процессом
- ✅ **Graceful shutdown** - корректное завершение работы

---

## 🛠️ Установка

### 1. Установите зависимости:

```powershell
pip install -r requirements.txt
```

---

## 🎯 Управление сервером

### Способ 1: BAT файлы (рекомендуется для Windows)

Просто дважды кликните на нужный файл:

- **START.bat** - Запустить сервер
- **STOP.bat** - Остановить сервер
- **RESTART.bat** - Перезапустить сервер
- **STATUS.bat** - Проверить статус

### Способ 2: Python скрипты

```powershell
python start.py      # Запустить
python stop.py       # Остановить
python restart.py    # Перезапустить
python status.py     # Статус
```

### Способ 3: Через веб-интерфейс

Откройте http://localhost:5000 и используйте кнопки:
- 🔄 **Перезапустить сервер**
- ⏹️ **Остановить сервер**

---

## 🌐 Доступ к интерфейсу

### Локальный доступ:
```
http://localhost:5000
```

### С других устройств (телефон, планшет):
```
http://<IP-адрес-вашего-компьютера>:5000
```

**Как узнать IP компьютера:**
```powershell
ipconfig
```
Ищите строку "IPv4 Address" (обычно вида 192.168.x.x)

---

## 🔐 Первый запуск

1. **Запустите сервер:**
   ```
   START.bat
   ```

2. **Откройте браузер:**
   ```
   http://localhost:5000
   ```

3. **Добавьте аккаунт:**
   - Введите имя аккаунта
   - API Key от Gate.io
   - API Secret от Gate.io
   - Нажмите "Добавить аккаунт"

4. **Начните торговать!**

---

## 💡 Особенности управления

### ✅ Изолированное управление процессом

Система использует **PID-файл** (`mtrade_server.pid`) для отслеживания только этого сервера.

**Это значит:**
- ✅ Команды влияют **ТОЛЬКО** на mTrade сервер
- ✅ Другие серверы/процессы **НЕ ЗАТРАГИВАЮТСЯ**
- ✅ Безопасный restart без риска убить другие процессы
- ✅ Можно запускать несколько проектов одновременно

### 🔒 Защита от двойного запуска

Сервер автоматически проверяет, не запущен ли он уже:

```
ОШИБКА: Сервер уже запущен!
PID: 12345
Используйте stop.py для остановки
```

### 🔄 Graceful Shutdown

При остановке сервер:
1. Корректно закрывает все соединения
2. Удаляет PID-файл
3. Освобождает ресурсы

---

## 📊 Проверка статуса

### Через STATUS.bat или `python status.py`:

```
📊 Статус mTrade сервера
============================================================
PID файл: mtrade_server.pid
PID: 12345
Процесс: ✅ Запущен

Веб-сервер: ✅ Доступен
Адрес: http://localhost:5000
Uptime: 2ч 15мин
============================================================

✅ Сервер работает нормально
```

---

## 🔧 Устранение проблем

### Проблема: Сервер не останавливается

```powershell
# Проверьте статус
python status.py

# Принудительно остановите
python stop.py
```

### Проблема: PID файл остался после сбоя

```powershell
# Просто удалите файл вручную
del mtrade_server.pid

# И запустите заново
python start.py
```

### Проблема: Порт 5000 занят

Отредактируйте в `mTrade.py`:
```python
app.run(
    host='0.0.0.0',
    port=5001,  # Измените порт
    ...
)
```

---

## 🔐 Безопасность

### Для использования в локальной сети:

1. **Настройте файрвол Windows:**
   - Откройте порт 5000 для локальной сети
   - Блокируйте доступ из интернета

2. **Используйте сильные API ключи:**
   - Ограничьте права API ключей в Gate.io
   - Разрешите только нужные операции

3. **Рекомендация:** Добавьте авторизацию (логин/пароль)

---

## 📁 Структура проекта

```
bGate.mTrade/
├── mTrade.py           # Основной сервер
├── start.py            # Скрипт запуска
├── stop.py             # Скрипт остановки
├── restart.py          # Скрипт перезапуска
├── status.py           # Проверка статуса
├── START.bat           # BAT для запуска
├── STOP.bat            # BAT для остановки
├── RESTART.bat         # BAT для перезапуска
├── STATUS.bat          # BAT для статуса
├── requirements.txt    # Зависимости
├── templates/
│   └── index.html      # Веб-интерфейс
├── mtrade_server.pid   # PID файл (создается автоматически)
├── config.json         # Конфигурация (создается автоматически)
└── accounts.json       # Аккаунты (создается автоматически)
```

---

## 🎨 Режимы работы

### 📊 Normal Trading (Обычный трейдинг)
- Ручное размещение ордеров
- Полный контроль над сделками
- Spot и Futures торговля

### 🔄 Copy Trading (Копитрейдинг)
- Автоматическое копирование сделок
- Следование за мастер-трейдерами
- *В разработке*

Переключение между режимами - одна кнопка в интерфейсе!

---

## 🚀 Следующие шаги

После успешного запуска вы можете:

1. **Настроить аккаунты** - добавьте API ключи
2. **Проверить баланс** - убедитесь что API работает
3. **Протестировать торговлю** - сделайте тестовую сделку
4. **Настроить автозапуск** - добавьте в Task Scheduler Windows

---

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте статус: `python status.py`
2. Перезапустите сервер: `RESTART.bat`
3. Проверьте логи в консоли

---

## ⚠️ Важно

- 🔒 **Никогда не делитесь API ключами!**
- 💾 **Регулярно делайте backup `accounts.json`**
- 🧪 **Тестируйте на малых суммах**
- 📊 **Следите за балансом и ордерами**

---

## 📝 Лицензия

Для личного использования

---

**Создано:** 4 ноября 2025  
**Версия:** 1.0.0  
**Статус:** Production Ready ✅
