# Визуализация работы AutoTrader

## 📊 Блок-схема главного цикла

```
┌─────────────────────────────────────────────────────────────────┐
│                    AutoTrader.start()                           │
│  Запускает поток с функцией _run() в daemon режиме             │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ↓
┌─────────────────────────────────────────────────────────────────┐
│                       _run() - Главный цикл                     │
│                                                                 │
│  while self.running:                                            │
│      1. Проверка auto_trade_enabled                             │
│      2. Получение списка разрешённых валют                      │
│      3. Обход всех валют по очереди ───────────┐                │
│      4. sleep(0.5 сек)                        │                │
│      5. Повторение цикла                      │                │
└───────────────────────────────────────────────┼────────────────┘
                                                │
                                                ↓
┌────────────────────────────────────────────────────────────────┐
│            Обработка одной валюты (например, BTC)              │
├────────────────────────────────────────────────────────────────┤
│  ① _ensure_ws_subscription(BTC, USDT)                          │
│     └─> Подписка на WebSocket данные                           │
│                                                                │
│  ② price = _get_market_price(BTC, USDT)                        │
│     └─> Получение текущей цены                                 │
│         ├─ WebSocket ticker (приоритет 1)                      │
│         ├─ WebSocket orderbook (приоритет 2)                   │
│         └─ REST API (fallback)                                 │
│                                                                │
│  ③ _ensure_cycle_struct(BTC)                                   │
│     └─> Создание структуры цикла, если её нет                  │
│                                                                │
│  ④ Проверка состояния: cycle.get('active')?                    │
├────────────────┬───────────────────────────────────────────────┤
│     ДА (активен)      │        НЕТ (неактивен)                 │
│        │               │               │                        │
│        ↓               │               ↓                        │
│  _try_rebuy()         │         _try_start_cycle()             │
│  (Усреднение)         │         (Старт цикла)                  │
└────────────────────────┴─────────────────────────────────────────┘
```

## 🔄 Детализация _try_start_cycle (Старт цикла)

```
┌────────────────────────────────────────────────────────────────┐
│                     _try_start_cycle(BTC, USDT)                │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  ① КРИТИЧЕСКАЯ ЗАЩИТА: Захват Lock                             │
│     ┌─────────────────────────────────────────────┐            │
│     │  with self._locks_creation_lock:            │            │
│     │      if BTC not in self._start_cycle_locks: │            │
│     │          create new Lock() for BTC          │            │
│     └─────────────────────────────────────────────┘            │
│                                                                │
│  ② Попытка захватить Lock (без блокировки)                     │
│     ┌───────────────────────────────────────┐                  │
│     │ lock.acquire(blocking=False)          │                  │
│     │   ├─ Успех → продолжаем               │                  │
│     │   └─ Неудача → выход (дублирование!)  │                  │
│     └───────────────────────────────────────┘                  │
│                                                                │
│  ③ Проверка состояния цикла                                    │
│     ┌────────────────────────────────────────────┐             │
│     │ if cycle.get('active'):                    │             │
│     │     return  # Уже активен                  │             │
│     │ if cycle.get('pending_start'):             │             │
│     │     return  # Уже запускается              │             │
│     └────────────────────────────────────────────┘             │
│                                                                │
│  ④ Установка флага pending_start = True                        │
│                                                                │
│  ⑤ Вычисление параметров и выполнение покупки                  │
│     └─> Определение объёма, цены, создание ордера              │
│                                                                │
│  ⑥ ЕСЛИ покупка успешна (amount > 0):                          │
│     ├─ Установка active = True                                 │
│     ├─ Сохранение данных цикла (start_price, base_volume...)   │
│     └─ Создание таблицы усреднения                             │
│                                                                │
│  ⑦ Снятие флага pending_start = False                          │
│                                                                │
│  ⑧ Освобождение Lock                                           │
│     lock.release()                                             │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

## 🔄 Детализация _try_rebuy (Усреднение)

```
┌────────────────────────────────────────────────────────────────┐
│                      _try_rebuy(BTC, USDT)                     │
├────────────────────────────────────────────────────────────────┤
│                                                                │
│  ① Проверка активности цикла                                   │
│     if not cycle.get('active'):                                │
│         return  # Цикл неактивен                               │
│                                                                │
│  ② Получение текущего active_step                              │
│     └─> Определяется на основе кумулятивного падения цены      │
│                                                                │
│  ③ Проверка условий усреднения                                 │
│     ├─ Есть ли новый шаг для выполнения?                       │
│     ├─ Прошло ли достаточно времени с последней покупки?       │
│     └─ Достаточно ли баланса для покупки?                      │
│                                                                │
│  ④ Выполнение покупки (rebuy)                                  │
│     └─> Создание ордера по текущему шагу                       │
│                                                                │
│  ⑤ Обновление состояния цикла                                  │
│     ├─ Увеличение base_volume                                  │
│     ├─ Увеличение quote_spent                                  │
│     ├─ Обновление last_rebuy_time                              │
│     └─ Обновление last_rebuy_step                              │
│                                                                │
│  ⑥ Проверка условий продажи                                    │
│     ├─ Достигнут ли профит?                                    │
│     ├─ Активна ли опция Keep?                                  │
│     └─ Если условия выполнены → _try_sell()                    │
│                                                                │
└────────────────────────────────────────────────────────────────┘
```

## 🔐 Схема многоуровневой защиты

```
                    ┌──────────────────────┐
                    │ Запрос: Старт цикла  │
                    └──────────┬───────────┘
                               │
              ┌────────────────▼─────────────────┐
              │   УРОВЕНЬ 1: Lock (атомарность)  │
              │                                  │
              │   if not lock.acquire(False):    │
              │       return  # Блокировано!     │
              └────────────────┬─────────────────┘
                               │ ✓ Lock захвачен
                               │
              ┌────────────────▼─────────────────┐
              │   УРОВЕНЬ 2: Флаги состояния     │
              │                                  │
              │   if cycle['active']:            │
              │       return  # Уже активен!     │
              │   if cycle['pending_start']:     │
              │       return  # Запускается!     │
              └────────────────┬─────────────────┘
                               │ ✓ Неактивен
                               │
              ┌────────────────▼─────────────────┐
              │   УРОВЕНЬ 3: Разделение логики   │
              │   (в главном цикле _run)         │
              │                                  │
              │   if cycle['active']:            │
              │       _try_rebuy()  # Только!    │
              │   else:                          │
              │       _try_start_cycle()  # Только!
              └────────────────┬─────────────────┘
                               │ ✓ Правильный путь
                               │
              ┌────────────────▼─────────────────┐
              │     Установка pending_start      │
              │                                  │
              │   cycle['pending_start'] = True  │
              └────────────────┬─────────────────┘
                               │
              ┌────────────────▼─────────────────┐
              │      Выполнение покупки          │
              │                                  │
              │   amount = execute_buy_order()   │
              └────────────────┬─────────────────┘
                               │
              ┌────────────────▼─────────────────┐
              │     Активация цикла              │
              │                                  │
              │   if amount > 0:                 │
              │       cycle['active'] = True     │
              │       cycle['pending_start'] = False
              └────────────────┬─────────────────┘
                               │
              ┌────────────────▼─────────────────┐
              │     Освобождение Lock            │
              │                                  │
              │   lock.release()                 │
              └──────────────────────────────────┘
```

## 🎯 Временная диаграмма обработки 3 валют

```
Время (сек)    BTC         ETH         DOGE        Действие
───────────────────────────────────────────────────────────
0.00           [START] ─────────────────────────> Получаем цену BTC
                                                  Проверяем цикл BTC
                                                  
0.05                    [START] ──────────────> Получаем цену ETH
                                                Проверяем цикл ETH
                                                
0.10                                [START] ─> Получаем цену DOGE
                                              Проверяем цикл DOGE
                                              
0.15           ◀──────────────────────────────── SLEEP(0.5)
0.50           [START] ─────────────────────────> Снова BTC
0.55                    [START] ──────────────> Снова ETH
0.60                                [START] ─> Снова DOGE
0.65           ◀──────────────────────────────── SLEEP(0.5)
1.00           [START] ─────────────────────────> Снова BTC
...            ...     ...     ...
```

**Частота обработки одной валюты:**
- При 3 валютах: ~каждые 0.5 секунды
- При 10 валютах: ~каждые 0.5 секунды (обработка всех последовательно)
- Интервал между проверками одной и той же валюты: `0.5 сек`

## 📦 Структура данных цикла в памяти

```
self.cycles = {
    'BTC': {
        'active': True,                    # ✓ Цикл активен
        'pending_start': False,            # Не запускается
        'active_step': 2,                  # Текущий шаг усреднения
        'start_price': 50000.0,            # Стартовая цена
        'base_volume': 0.05,               # Купленный объём BTC
        'quote_spent': 2500.0,             # Потрачено USDT
        'table': [                         # Таблица шагов
            {
                'step': 0,
                'decrease_pct': 0.5,
                'cumulative_decrease_pct': 0.5,
                'price': 49750.0,
                'amount': 0.01,
                'executed': True
            },
            {
                'step': 1,
                'decrease_pct': 0.5,
                'cumulative_decrease_pct': 1.0,
                'price': 49500.0,
                'amount': 0.02,
                'executed': True
            },
            {
                'step': 2,
                'decrease_pct': 0.5,
                'cumulative_decrease_pct': 1.5,
                'price': 49250.0,
                'amount': 0.02,
                'executed': False  # ← Следующий шаг
            }
        ],
        'last_rebuy_time': 1733174523.456,
        'last_rebuy_step': 1,
        'start_buy_time': 1733170000.123
    },
    'ETH': {
        'active': False,                   # ✗ Цикл неактивен
        'pending_start': False,
        ...
    }
}
```

---

**Дата создания:** 2024-12-02  
**Версия:** 1.0  
**Статус:** Актуально после исправлений
