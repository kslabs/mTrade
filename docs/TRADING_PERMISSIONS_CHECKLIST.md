# ✅ Чек-лист: Как проверить исправление trading_permissions

**Дата:** 7 декабря 2024

---

## 🎯 Что было исправлено

Исправлена **критическая ошибка**, из-за которой разрешения на торговлю для валют сбрасывались при каждом перезапуске сервера.

---

## 📋 Быстрая проверка (5 минут)

### Шаг 1: Запустить автоматический тест
```powershell
python test_permissions_no_overwrite.py
```

**Ожидаемый результат:**
```
================================================================================
ВСЕ ТЕСТЫ ПРОЙДЕНЫ УСПЕШНО! ✓
================================================================================
```

Если тест прошёл - всё работает! ✅

---

## 🧪 Полная проверка (10 минут)

### Шаг 1: Запустить сервер
```powershell
python mTrade.py
```

### Шаг 2: Открыть UI
Откройте в браузере: http://localhost:5000

### Шаг 3: Отключить торговлю для валюты
1. В списке валют найдите, например, **ETH**
2. Кликните на переключатель рядом с ETH (должен стать красным/выключенным)
3. Проверьте, что в консоли сервера появилось сообщение о сохранении

### Шаг 4: Проверить файл app_state.json
```powershell
notepad app_state.json
```

Найдите секцию `trading_permissions`. Должно быть примерно так:
```json
{
  "trading_permissions": {
    "BTC": true,
    "ETH": false,    ← Отключено!
    "LTC": true,
    ...
  }
}
```

### Шаг 5: Перезапустить сервер
1. Нажмите `Ctrl+C` в консоли (или закройте окно)
2. Снова запустите:
```powershell
python mTrade.py
```

### Шаг 6: Проверить, что настройки сохранились
1. Снова откройте app_state.json
2. Проверьте, что ETH всё ещё `false`
3. Откройте UI: http://localhost:5000
4. Проверьте, что ETH всё ещё отключен в списке валют

### Шаг 7: Проверить логи сервера
В консоли при старте должно быть примерно так:
```
[INIT] Инициализация разрешений торговли...
[INIT] Разрешения торговли: 15/16 валют включено
```

Обратите внимание: если вы отключили 1 валюту, должно быть на 1 меньше включенных.

---

## ✅ Критерии успеха

Проверка прошла успешно, если:
- ✅ Тест `test_permissions_no_overwrite.py` прошёл успешно
- ✅ Отключенная валюта осталась отключенной после перезапуска
- ✅ Файл app_state.json содержит корректные значения
- ✅ UI показывает корректное состояние переключателей
- ✅ В логах сервера корректное количество включенных валют

---

## ❌ Что делать, если не работает

### Проблема 1: Тест не проходит
```powershell
# Проверьте, что файл state_manager.py обновлён
python -c "import state_manager; print(state_manager.__file__)"

# Если Python использует закешированную версию, удалите кеш
del __pycache__\state_manager*.pyc
python test_permissions_no_overwrite.py
```

### Проблема 2: Настройки всё равно сбрасываются
1. Проверьте, что вы используете обновлённый `state_manager.py`
2. Убедитесь, что сервер полностью перезапущен (не в режиме reload)
3. Проверьте логи сервера на наличие ошибок

### Проблема 3: Переключатель не работает в UI
Это другая проблема, не связанная с исправлением. Проверьте:
1. Консоль браузера (F12) на наличие ошибок JavaScript
2. Сетевые запросы (вкладка Network) - должен идти PUT запрос на `/api/trade/permissions`

---

## 📚 Документация

Подробности смотрите в:
- **docs/TRADING_PERMISSIONS_FIX.md** - полное описание исправления
- **docs/FINAL_WORK_SUMMARY.md** - общая сводка выполненных работ
- **docs/INDEX.md** - индекс всей документации

---

## 🚀 Готово!

Если все проверки прошли успешно - система работает корректно! 

Теперь можно безопасно:
- Отключать/включать валюты в UI
- Перезапускать сервер
- Добавлять новые валюты в currencies.json

Все настройки будут сохраняться и восстанавливаться автоматически! ✅
