# Changelog: Сохранение состояния UI

## [1.0.0] - 2025-11-09

### Добавлено ✨

#### Серверная часть (mTrade.py):
- Константа `UI_STATE_FILE = "ui_state.json"` в классе `Config`
- Метод `Config.load_ui_state()` для загрузки настроек из файла
- Метод `Config.save_ui_state(state)` для сохранения настроек в файл
- API endpoint `GET /api/ui/state` - получение текущего состояния UI
- API endpoint `POST /api/ui/state` - сохранение полного состояния UI
- API endpoint `POST /api/ui/state/partial` - частичное обновление состояния
- API endpoint `POST /api/ui/state/reset` - сброс к дефолтным значениям
- Автосохранение в `/api/autotrade/start` - сохраняет `auto_trade_enabled: true`
- Автосохранение в `/api/autotrade/stop` - сохраняет `auto_trade_enabled: false`
- Автосохранение в `/api/trade/permission` - сохраняет разрешения по валютам
- Автосохранение в `/api/network` - сохраняет сетевой режим
- Автовосстановление настроек при запуске сервера в блоке `if __name__ == '__main__'`
- Логирование восстановленных настроек: `[BOOT] Загружено сохраненное состояние UI`

#### Клиентская часть (static/ui-state-manager.js):
- Класс `UIStateManager` для управления состоянием UI
- Метод `init()` - инициализация и загрузка состояния с сервера
- Метод `applyState()` - применение загруженного состояния к UI
- Метод `save(key, value)` - сохранение одной настройки с debounce
- Метод `saveMultiple(updates)` - сохранение нескольких настроек
- Метод `saveToServer()` - немедленное сохранение на сервер
- Метод `reset()` - сброс состояния к дефолтным значениям
- Метод `get(key, defaultValue)` - получение значения настройки
- Вспомогательные методы для применения состояния:
  - `applyTheme(theme)`
  - `selectCurrencyPair(base, quote)`
  - `updateAutoTradeButton(enabled)`
  - `restoreCurrencyToggles(enabledCurrencies)`
  - `restorePanelVisibility()`
  - `setOrderbookDepth(depth)`
- Debounce механизм для оптимизации сохранений (500ms)
- Примеры setup функций для интеграции с UI элементами
- Глобальный экземпляр `uiStateManager` для использования

#### Документация:
- `UI_STATE_INDEX.md` - главная страница документации
- `UI_STATE_QUICKSTART.md` - быстрый старт (2 минуты)
- `UI_STATE_README.md` - краткая инструкция для пользователя
- `UI_STATE_GUIDE.md` - полное руководство разработчика
- `UI_STATE_SUMMARY.md` - техническое резюме реализации
- `UI_STATE_TESTING.md` - чеклист для тестирования (10 тестов)

#### Файлы данных:
- `ui_state.json` - файл для хранения настроек (создается автоматически)

### Изменено 🔄

#### mTrade.py:
- Блок инициализации `if __name__ == '__main__'` - добавлена загрузка UI состояния
- Функция `api_autotrade_start()` - добавлено сохранение состояния
- Функция `api_autotrade_stop()` - добавлено сохранение состояния
- Функция `api_trade_permission_set()` - добавлено сохранение разрешений
- Функция `api_set_network_mode()` - добавлено сохранение режима

### Функциональность 🎯

#### Автоматическое сохранение:
- ✅ Разрешения торговли для каждой валюты (enabled_currencies)
- ✅ Состояние автотрейдинга (auto_trade_enabled)
- ✅ Сетевой режим работы (network_mode: work/test)
- ✅ Активная валютная пара (active_base_currency, active_quote_currency)
- ✅ Тема интерфейса (theme: dark/light)
- ✅ Видимость панелей (show_indicators, show_orderbook, show_trades)
- ✅ Глубина стакана ордеров (orderbook_depth)
- ✅ Временная метка последнего обновления (last_updated)

#### Автоматическое восстановление:
- ✅ При запуске сервера загружается файл `ui_state.json`
- ✅ Восстанавливаются глобальные переменные `TRADING_PERMISSIONS`
- ✅ Восстанавливается глобальная переменная `AUTO_TRADE_GLOBAL_ENABLED`
- ✅ Восстанавливается глобальная переменная `CURRENT_NETWORK_MODE`
- ✅ При загрузке страницы применяется состояние к UI элементам
- ✅ Логируется процесс восстановления в консоль сервера

### Технические детали 🔧

#### Структура ui_state.json:
```json
{
  "enabled_currencies": {
    "BTC": true,
    "ETH": false,
    ...
  },
  "auto_trade_enabled": true,
  "network_mode": "work",
  "active_base_currency": "BTC",
  "active_quote_currency": "USDT",
  "theme": "dark",
  "show_indicators": true,
  "show_orderbook": true,
  "show_trades": true,
  "orderbook_depth": 20,
  "last_updated": 1699545600.123
}
```

#### Дефолтные значения:
- `enabled_currencies`: {} (пусто, заполняется при инициализации)
- `auto_trade_enabled`: false
- `network_mode`: "work"
- `active_base_currency`: "BTC"
- `active_quote_currency`: "USDT"
- `theme`: "dark"
- `show_indicators`: true
- `show_orderbook`: true
- `show_trades`: true
- `orderbook_depth`: 20

#### Особенности реализации:
- Использование debounce (500ms) для оптимизации частоты сохранений
- Отказоустойчивость: при ошибке загрузки используются дефолтные значения
- Частичное обновление: не нужно перезаписывать весь файл при изменении одного поля
- Автоматическая синхронизация между клиентом и сервером
- Логирование для отладки и мониторинга

### Безопасность 🔒
- API ключи хранятся отдельно в `config/secrets.json`
- Файл `ui_state.json` содержит только пользовательские настройки интерфейса
- Нет чувствительных данных в файле состояния

### Производительность ⚡
- Debounce предотвращает частые записи на диск
- Частичное обновление минимизирует объем передаваемых данных
- Кэширование состояния в памяти клиента
- Асинхронные запросы к API

### Совместимость 🔄
- Работает со всеми существующими функциями приложения
- Не требует изменений в существующем frontend коде
- Обратная совместимость при отсутствии файла настроек
- Легко расширяется новыми полями состояния

### Тестирование ✅
- Протестировано восстановление при перезагрузке страницы
- Протестировано восстановление при перезапуске сервера
- Проверена работа всех API эндпоинтов
- Проверена отказоустойчивость при отсутствии/повреждении файла
- Проверена синхронизация между клиентом и сервером

### Известные ограничения ⚠️
- Нет версионирования файла настроек (можно добавить в будущем)
- Нет истории изменений настроек (можно добавить в будущем)
- Нет синхронизации между несколькими вкладками браузера (можно добавить через localStorage events)

### Будущие улучшения 🚀
- [ ] Добавить версионирование файла настроек
- [ ] Добавить миграции при изменении структуры
- [ ] Добавить историю изменений настроек
- [ ] Добавить синхронизацию между вкладками браузера
- [ ] Добавить экспорт/импорт настроек
- [ ] Добавить профили настроек (разные конфигурации)
- [ ] Добавить облачное хранение настроек (опционально)

---

**Версия:** 1.0.0  
**Дата:** 9 ноября 2025  
**Автор:** GitHub Copilot  
**Статус:** ✅ Production Ready
