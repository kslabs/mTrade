# Сохранение настроек интерфейса - Краткая инструкция

## ✅ Что было реализовано

Добавлена система автоматического сохранения всех настроек интерфейса на сервер.

### Что сохраняется автоматически:

1. **Включенные/выключенные валюты** - состояние переключателей для каждой валюты
2. **Автотрейдинг** - включен или выключен
3. **Сетевой режим** - work или test
4. **Активная валютная пара** - выбранные BASE и QUOTE валюты
5. **Настройки отображения**:
   - Тема (светлая/темная)
   - Видимость панелей (индикаторы, стакан, сделки)
   - Глубина стакана ордеров

## 📁 Файл настроек

Все настройки хранятся в: **`ui_state.json`**

## 🔄 Как это работает

### При запуске сервера:
1. Автоматически загружается файл `ui_state.json`
2. Восстанавливаются все сохраненные настройки
3. В консоли появятся сообщения:
   ```
   [BOOT] Загружено сохраненное состояние UI
   [BOOT] Восстановлены разрешения торговли: {'BTC': True, 'ETH': False, ...}
   [BOOT] Автотрейдинг: True
   ```

### При изменении настроек:
- Настройки **автоматически сохраняются** на сервер
- Не нужно нажимать "Сохранить" - все происходит автоматически
- При перезагрузке страницы настройки восстановятся

## 🎯 Новые API эндпоинты

### Получить текущие настройки:
```
GET /api/ui/state
```

### Сохранить настройки:
```
POST /api/ui/state
```

### Частичное обновление:
```
POST /api/ui/state/partial
```

### Сброс к значениям по умолчанию:
```
POST /api/ui/state/reset
```

## 🚀 Интеграция в frontend

1. Добавьте скрипт в ваш HTML:
```html
<script src="/static/ui-state-manager.js"></script>
```

2. Скрипт автоматически:
   - Загрузит настройки при открытии страницы
   - Применит их к интерфейсу
   - Будет сохранять изменения автоматически

3. Пример использования в вашем коде:
```javascript
// Сохранить одну настройку
uiStateManager.save('theme', 'dark');

// Сохранить несколько настроек
uiStateManager.saveMultiple({
  theme: 'dark',
  show_indicators: true,
  orderbook_depth: 50
});

// Получить значение настройки
const theme = uiStateManager.get('theme', 'dark');
```

## 📋 Что изменилось в mTrade.py

1. **Добавлен файл конфигурации** `UI_STATE_FILE = "ui_state.json"`

2. **Новые методы в классе Config:**
   - `Config.load_ui_state()` - загрузка настроек
   - `Config.save_ui_state(state)` - сохранение настроек

3. **Новые API routes:**
   - `/api/ui/state` (GET/POST)
   - `/api/ui/state/partial` (POST)
   - `/api/ui/state/reset` (POST)

4. **Автосохранение в существующих эндпоинтах:**
   - `/api/autotrade/start` - сохраняет состояние автотрейдинга
   - `/api/autotrade/stop` - сохраняет состояние автотрейдинга
   - `/api/trade/permission` - сохраняет разрешения по валютам
   - `/api/network` - сохраняет сетевой режим

5. **Восстановление при запуске:**
   - При старте сервера загружаются и применяются сохраненные настройки

## ⚙️ Пример ui_state.json

```json
{
  "enabled_currencies": {
    "BTC": true,
    "ETH": true,
    "SOL": false,
    "WLD": true
  },
  "auto_trade_enabled": true,
  "network_mode": "work",
  "active_base_currency": "BTC",
  "active_quote_currency": "USDT",
  "theme": "dark",
  "show_indicators": true,
  "show_orderbook": true,
  "show_trades": true,
  "orderbook_depth": 20,
  "last_updated": 1699545600.123
}
```

## 🔧 Устранение проблем

### Настройки не сохраняются?
1. Проверьте права на запись в папку приложения
2. Убедитесь, что файл `ui_state.json` не защищен от записи
3. Проверьте логи сервера

### Нужно сбросить все настройки?
- Удалите файл `ui_state.json` и перезапустите сервер
- Или используйте: `POST /api/ui/state/reset`

### Настройки не восстанавливаются после перезагрузки?
1. Проверьте наличие файла `ui_state.json`
2. Убедитесь, что JSON корректный
3. Посмотрите логи при запуске `[BOOT]`

## 📚 Дополнительная документация

Подробное руководство: **UI_STATE_GUIDE.md**

## ✨ Преимущества

- ✅ Автоматическое сохранение - не нужно нажимать "Сохранить"
- ✅ Персистентность - настройки сохраняются между сеансами
- ✅ Безопасность - API ключи хранятся отдельно
- ✅ Простота - понятная структура JSON
- ✅ Отказоустойчивость - при ошибках используются дефолтные значения

## 🎉 Готово!

Теперь все ваши настройки будут автоматически сохраняться и восстанавливаться при перезагрузке страницы или сервера!
