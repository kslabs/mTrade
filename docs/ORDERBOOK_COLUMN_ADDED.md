# Добавлена колонка "Стакан" в таблицу безубыточности

## 📋 Описание изменений

В таблицу безубыточности добавлена новая колонка **"Ст."** (Стакан), которая показывает уровень стакана для покупки базовой валюты.

## 🎯 Реализованная функциональность

### 1. Новая колонка "Ст." (Стакан)

Расположение: между колонками **"#"** (номер строки) и **"↓, %"** (процент снижения).

**Формула расчёта:**
```
Уровень стакана = (# × Стакан) + 1
```

Где:
- **#** - номер строки в таблице (0, 1, 2, ...)
- **Стакан** - параметр из настроек (по умолчанию: 1)

### 2. Примеры расчёта

При значении параметра **Стакан = 1**:
- Строка 0: `(0 × 1) + 1 = 1`
- Строка 1: `(1 × 1) + 1 = 2`
- Строка 2: `(2 × 1) + 1 = 3`

При значении параметра **Стакан = 3**:
- Строка 0: `(0 × 3) + 1 = 1`
- Строка 1: `(1 × 3) + 1 = 4`
- Строка 2: `(2 × 3) + 1 = 7`

## 💻 Технические детали

### Изменённые файлы

#### 1. `templates/index.html`
```html
<th style="padding:6px 8px;text-align:center;" 
    title="Уровень стакана для покупки: (# + 1) * Стакан">Ст.</th>
```

- Добавлен новый заголовок колонки
- Добавлена всплывающая подсказка (title)
- Обновлён `colspan="9"` в tbody для корректного отображения

#### 2. `static/app.js` - функция `renderBreakEvenTable()`

```javascript
// Получаем текущее значение параметра "Стакан"
const orderbookLevel = parseInt($('paramOrderbookLevel')?.value) || 1;

// Расчёт уровня стакана для покупки: (# * Стакан) + 1
const orderbookLevelForStep = (stepNum * orderbookLevel) + 1;

// Добавление колонки в таблицу
<td style='padding:6px 8px;text-align:center;color:#9C27B0;font-weight:600;' 
    title='Уровень стакана: (${stepNum} × ${orderbookLevel}) + 1 = ${orderbookLevelForStep}'>
  ${orderbookLevelForStep}
</td>
```

## 🎨 Визуальное оформление

- **Цвет текста**: `#9C27B0` (фиолетовый)
- **Выравнивание**: по центру
- **Шрифт**: жирный (font-weight: 600)
- **Подсказка**: отображает формулу расчёта с подставленными значениями

## 🔍 Как это работает

1. **При загрузке таблицы** функция `loadBreakEvenTable()` читает значение параметра "Стакан" из формы
2. **При отрисовке каждой строки** функция `renderBreakEvenTable()`:
   - Вычисляет уровень стакана по формуле `(# × Стакан) + 1`
   - Добавляет колонку с вычисленным значением
   - Добавляет всплывающую подсказку с развёрнутой формулой

3. **При изменении параметра "Стакан"** таблица автоматически пересчитывается и перерисовывается

## 📊 Пример использования

### Сценарий: Определение уровня стакана для покупки

**Настройки:**
- Стакан = 5
- Просматриваем строку #7 в таблице

**Результат:**
- Колонка "Ст." показывает: `(7 × 5) + 1 = 36`
- Это означает: покупать базовую валюту по **36-му уровню стакана**

## ✅ Проверка работоспособности

1. Откройте веб-интерфейс приложения
2. Перейдите на вкладку "Безубыточность"
3. Убедитесь, что колонка "Ст." отображается между "#" и "↓, %"
4. Измените параметр "Стакан" в настройках
5. Проверьте, что значения в колонке "Ст." обновились согласно формуле

## 🔗 Связанные изменения

- **Динамическая точность курсов**: используется `Price Precision + 1` для отображения курсов и цены безубыточности
- См. также: `PRICE_PRECISION_DYNAMIC.md`, `PRICE_PRECISION_QUICKSTART.md`

## 📝 Примечания

- Значение параметра "Стакан" берётся из поля формы `paramOrderbookLevel`
- Значение по умолчанию: **1**
- Минимальное значение: **1**
- Формула учитывает, что нумерация строк начинается с 0
- При Стакан = 1: все строки получают последовательные номера (1, 2, 3, ...)
- При Стакан > 1: номера увеличиваются с шагом, равным значению Стакан
