# ✅ СЕРВЕРНОЕ ХРАНЕНИЕ ТЕСТОВЫХ БАЛАНСОВ



## 🎯 Проблема которую решили



**Было (localStorage):**

- ❌ Каждый браузер имел свой баланс

- ❌ Chrome: 10000 USDT

- ❌ Firefox: 5000 USDT  

- ❌ Нет синхронизации между браузерами



**Стало (серверное хранилище):**

- ✅ Один баланс для всех браузеров

- ✅ Chrome: 10000 USDT

- ✅ Firefox: 10000 USDT

- ✅ Simple Browser: 10000 USDT

- ✅ Синхронизация через сервер!



---



## 🏗️ Архитектура решения



```

┌──────────────┐

│   Chrome     │─┐

└──────────────┘ │

                 │

┌──────────────┐ │    ┌─────────────────┐    ┌───────────────────┐

│   Firefox    │─┼───→│  Flask Server   │───→│ test_balances.json│

└──────────────┘ │    └─────────────────┘    └───────────────────┘

                 │          ↓ API

┌──────────────┐ │    /api/test/balance

│Simple Browser│─┘    GET / POST

└──────────────┘

```



**Все браузеры** → **Серверное API** → **Файл test_balances.json**



---



## 📁 Файл test_balances.json



**Формат:**

```json

{

  "USDT": 10000,

  "BTC": 1.5,

  "ETH": 10,

  "USDC": 5000

}

```



**Расположение:**  

`c:\Users\Администратор\Documents\bGate.mTrade\test_balances.json`



**Важно:**

- ✅ Файл добавлен в `.gitignore` (не публикуется на GitHub)

- ✅ Хранится локально на сервере

- ✅ Доступен для всех подключений к серверу



---



## 🔌 API Endpoints



### 1. Получить все балансы

```http

GET /api/test/balances

```



**Response:**

```json

{

  "success": true,

  "balances": {

    "USDT": 10000,

    "BTC": 1.5,

    "ETH": 10

  }

}

```



### 2. Получить баланс одной валюты

```http

GET /api/test/balance?currency=USDT

```



**Response:**

```json

{

  "success": true,

  "currency": "USDT",

  "balance": 10000

}

```



### 3. Установить баланс валюты

```http

POST /api/test/balance

Content-Type: application/json



{

  "currency": "USDT",

  "balance": 10000

}

```



**Response:**

```json

{

  "success": true,

  "currency": "USDT",

  "balance": 10000

}

```



---



## 💻 Реализация на сервере (mTrade.py)



### Константы:

```python

TEST_BALANCES_FILE = "test_balances.json"

```



### Функции:

```python

def load_test_balances():

    """Загрузить тестовые балансы из файла"""

    if os.path.exists(TEST_BALANCES_FILE):

        with open(TEST_BALANCES_FILE, 'r', encoding='utf-8') as f:

            return json.load(f)

    return {}



def save_test_balances(balances):

    """Сохранить тестовые балансы в файл"""

    with open(TEST_BALANCES_FILE, 'w', encoding='utf-8') as f:

        json.dump(balances, f, ensure_ascii=False, indent=2)

    return True

```



### Endpoints:

```python

@app.route('/api/test/balances', methods=['GET'])

def get_test_balances():

    """Получить все тестовые балансы"""

    balances = load_test_balances()

    return jsonify({"success": True, "balances": balances})



@app.route('/api/test/balance', methods=['GET'])

def get_test_balance():

    """Получить баланс конкретной валюты"""

    currency = request.args.get('currency', 'USDT').upper()

    balances = load_test_balances()

    balance = balances.get(currency, 0)

    return jsonify({"success": True, "currency": currency, "balance": balance})



@app.route('/api/test/balance', methods=['POST'])

def set_test_balance():

    """Установить баланс для валюты"""

    data = request.json

    currency = data.get('currency', '').upper()

    balance = float(data.get('balance', 0))

    

    balances = load_test_balances()

    balances[currency] = balance

    save_test_balances(balances)

    

    return jsonify({"success": True, "currency": currency, "balance": balance})

```



---



## 🌐 Реализация на клиенте (index.html)



### Функция editQuoteBalance():

```javascript

async function editQuoteBalance(){

  if(currentNetworkMode!=='test'){

    alert('⚠️ Редактирование баланса доступно только в тестовом режиме');

    return;

  }

  

  const input=prompt(`💰 Редактирование тестового баланса ${currentQuoteCurrency}:\n\n(Будет сохранён на сервере и синхронизирован между всеми браузерами)\n\nВведите новую сумму:`, currentValue);

  

  const newBalance=parseFloat(input);

  

  // Сохраняем на сервере через API

  const response = await fetch('/api/test/balance', {

    method: 'POST',

    headers: {'Content-Type': 'application/json'},

    body: JSON.stringify({

      currency: currentQuoteCurrency,

      balance: newBalance

    })

  });

  

  const data = await response.json();

  

  if(data.success){

    loadPairBalances(); // Перезагружаем балансы

  }

}

```



### Функция loadTestBalance():

```javascript

async function loadTestBalance(currency){

  if(currentNetworkMode!=='test') return null;

  

  const response = await fetch(`/api/test/balance?currency=${currency}`);

  const data = await response.json();

  

  if(data.success && data.balance > 0){

    return data.balance;

  }

  

  return null; // Если баланса нет, используем баланс из Gate.io API

}

```



### Использование в loadPairBalances():

```javascript

async function loadPairBalances(){

  const r=await fetch(`/api/pair/balances?base_currency=${currentBaseCurrency}&quote_currency=${currentQuoteCurrency}`);

  const d=await r.json();

  

  let quoteAvail=parseFloat(d.balances?.quote?.available||'0');

  

  // В тестовом режиме проверяем серверные тестовые балансы

  if(currentNetworkMode==='test'){

    const testQuote=await loadTestBalance(currentQuoteCurrency);

    if(testQuote!==null) quoteAvail=testQuote;

  }

  

  updateHeaderQuoteBalance(quoteAvail);

}

```



---



## 🔄 Сценарии использования



### Сценарий 1: Установка баланса в Chrome

```

1. Chrome: Открыть http://127.0.0.1:5000

2. Chrome: Переключиться в тестовый режим

3. Chrome: Кликнуть на баланс USDT

4. Chrome: Ввести 10000

5. Сервер: Сохранить в test_balances.json → {"USDT": 10000}

```



### Сценарий 2: Открытие в Firefox

```

1. Firefox: Открыть http://127.0.0.1:5000

2. Firefox: Переключиться в тестовый режим

3. Firefox: Баланс USDT = 10000 ✅ (загружен с сервера!)

```



### Сценарий 3: Открытие в Simple Browser

```

1. Simple Browser: Открыть http://127.0.0.1:5000

2. Simple Browser: Переключиться в тестовый режим

3. Simple Browser: Баланс USDT = 10000 ✅ (загружен с сервера!)

```



### Сценарий 4: Изменение в любом браузере

```

1. Firefox: Изменить баланс USDT на 5000

2. Сервер: Обновить test_balances.json → {"USDT": 5000}

3. Chrome: Перезагрузить страницу → Баланс USDT = 5000 ✅

4. Simple Browser: Перезагрузить → Баланс USDT = 5000 ✅

```



---



## ✅ Преимущества серверного подхода



1. **Синхронизация** - все браузеры видят один баланс

2. **Централизация** - одно место хранения данных

3. **Простота** - не нужно синхронизировать localStorage

4. **Надёжность** - данные не теряются при очистке кэша браузера

5. **Масштабируемость** - можно добавить аутентификацию, историю и т.д.



---



## 🔐 Безопасность



### Защита:

```python

# Валидация валюты

if not currency:

    return jsonify({"success": False, "error": "Не указана валюта"}), 400



# Валидация баланса

if balance < 0:

    return jsonify({"success": False, "error": "Баланс не может быть отрицательным"}), 400

```



### Ограничения:

- ✅ Редактирование только в тестовом режиме

- ✅ Баланс не может быть отрицательным

- ✅ Валидация типов данных (float, string)



---



## 📋 Измененные файлы



### mTrade.py

- ✅ Добавлена константа `TEST_BALANCES_FILE`

- ✅ Добавлены функции `load_test_balances()`, `save_test_balances()`

- ✅ Добавлены endpoints:

  - `GET /api/test/balances`

  - `GET /api/test/balance`

  - `POST /api/test/balance`



### templates/index.html

- ✅ Изменена функция `editQuoteBalance()` - теперь async, сохраняет на сервер

- ✅ Изменена функция `loadTestBalance()` - теперь async, загружает с сервера

- ✅ Изменена функция `loadPairBalances()` - использует await для loadTestBalance



### .gitignore

- ✅ Уже содержит `test_balances.json`



---



## 🧪 Тестирование



### Тест 1: Установка баланса

```bash

curl -X POST http://127.0.0.1:5000/api/test/balance \

  -H "Content-Type: application/json" \

  -d '{"currency":"USDT","balance":10000}'

```



**Ожидаемый результат:**

```json

{"success":true,"currency":"USDT","balance":10000}

```



### Тест 2: Получение баланса

```bash

curl http://127.0.0.1:5000/api/test/balance?currency=USDT

```



**Ожидаемый результат:**

```json

{"success":true,"currency":"USDT","balance":10000}

```



### Тест 3: Проверка файла

```bash

cat test_balances.json

```



**Ожидаемый результат:**

```json

{

  "USDT": 10000

}

```



### Тест 4: Синхронизация между браузерами

```

1. Chrome: Установить USDT = 10000

2. Firefox: Открыть страницу → Должен показать 10000

3. Simple Browser: Открыть → Должен показать 10000

```



---



## 🎉 Итог



**Теперь тестовые балансы:**

- ✅ Хранятся на сервере в файле `test_balances.json`

- ✅ Синхронизируются между всеми браузерами

- ✅ Сохраняются при перезапуске сервера

- ✅ Доступны через удобное API

- ✅ Защищены от случайного изменения (только в тестовом режиме)



**Вы можете:**

- 💰 Установить баланс в любом браузере

- 🔄 Открыть в другом браузере → увидеть тот же баланс

- 📊 Безопасно тестировать торговые стратегии

- 🎓 Обучать пользователей без риска



---



**Дата:** 2025-11-09  

**Версия:** v1.8.3  

**Тип изменения:** Feature (серверное хранилище)  

**Приоритет:** Высокий



---



**Как использовать:**

1. Откройте http://127.0.0.1:5000 в **любом браузере**

2. Переключитесь в **тестовый режим**

3. **Кликните на баланс** котируемой валюты

4. **Введите сумму** (например, 10000)

5. Откройте в **другом браузере** → Баланс будет **тот же**! 🎉

