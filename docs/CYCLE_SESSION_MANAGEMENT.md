# 🔄 Управление циклами и сессиями торговли

**Дата**: 14 ноября 2025, 11:00  
**Версия**: 1.8.4

---

## ✅ Реализовано

### 1. **Новые кнопки управления**

Добавлены 4 кнопки рядом с балансом:

```
┌────┬────┬────┬────┐
│ 🛒 │ 💰 │ 🔄 │ 🔁 │
└────┴────┴────┴────┘
 Buy  Sell Reset Reset
          Cycle Session
```

- **🛒 Купить** — покупка минимального ордера
- **💰 Продать** — продажа всех средств (минус Keep)
- **🔄 Сброс цикла** — сброс текущего торгового цикла
- **🔁 Сброс сессии** — сброс всей статистики (скоро)

---

### 2. **Компактный заголовок логов**

Новая структура заголовка:

```
┌────────────────────────────────────────────────────────────────┐
│ [🔄] [🗑️] | 14 записей    PnL: 0.123 | Equity: 45.67 |      │
│                            Start: 45.00 | ΔEq: +0.67 (+1.49%) │
│                            Dur: 0d 1h 23m 45s                   │
└────────────────────────────────────────────────────────────────┘
```

**Элементы:**
- **🔄** — обновить логи
- **🗑️** — очистить логи
- **Записей** — количество записей в логах
- **PnL** — суммарный профит/убыток
- **Equity** — текущий капитал
- **Start** — начальный капитал
- **ΔEq** — изменение капитала (абсолютное и процентное)
- **Dur** — длительность сессии

---

## 🎯 Функционал

### Сброс цикла (🔄)

**Что делает:**
- Сбрасывает стартовую цену (P0) в параметрах
- Обнуляет накопленные данные текущего цикла
- Готовит систему к началу нового цикла

**Что НЕ делает:**
- НЕ закрывает открытые позиции
- НЕ продаёт валюту
- НЕ удаляет логи

**Когда использовать:**
- После завершения цикла продажей
- Когда нужно начать новый цикл с текущей цены
- При изменении стратегии торговли

**Подтверждение:**
```
Сбросить текущий торговый цикл для BTC/USDT?

Это сбросит:
- Текущую статистику цикла
- Стартовую цену (P0)
- Накопленные данные

Торговые позиции останутся.
```

**Результат:**
```
✅ Цикл сброшен!

Стартовая цена обнулена.
При следующей покупке будет установлена новая стартовая цена.
```

---

### Сброс сессии (🔁) — В разработке

**Что будет делать:**
- Сбрасывать всю статистику торговли
- Обнулять PnL, Equity, Duration
- Сохранять текущую сессию в архив
- Начинать новую торговую сессию

**Когда будет доступно:**
- После реализации автотрейдера
- После добавления системы статистики

**Текущий статус:**
```
🔜 Сброс сессии

Эта функция будет доступна после реализации автотрейдера.

Сброс сессии позволит:
- Обнулить всю статистику
- Сбросить все циклы
- Начать торговлю заново
- Сохранить историю в архив
```

---

## 📊 Статистика в заголовке логов

### Параметры:

#### **PnL** (Profit and Loss)
- Суммарный профит/убыток за сессию
- Цвет: зелёный если >= 0, красный если < 0
- Формат: `0.123` (3 знака после запятой)
- Источник: сумма всех PnL из логов

#### **Equity** (Капитал)
- Текущий капитал в USDT (или другой котируемой валюте)
- Формат: `45.67` (2 знака после запятой)
- Расчёт: `Start + PnL`

#### **Start** (Начальный капитал)
- Капитал на начало сессии
- Устанавливается при сбросе сессии
- Формат: `45.00` (2 знака после запятой)

#### **ΔEq** (Изменение капитала)
- Изменение капитала от начала сессии
- Абсолютное: `+0.67`
- Процентное: `(+1.49%)`
- Формат: `+/-X.XXX (+/-X.XX%)`
- Расчёт: `Equity - Start` и `((Equity - Start) / Start) * 100`

#### **Dur** (Длительность)
- Время работы текущей сессии
- Формат: `0d 0h 0m 0s`
- Обновляется в реальном времени
- Сбрасывается при сбросе сессии

---

## 🎨 Визуальное оформление

### Кнопки управления:

```css
Купить:  🛒  зелёный  (#28a745)  32x32px
Продать: 💰  красный  (#dc3545)  32x32px
Цикл:    🔄  оранжевый (#ff9800) 32x32px
Сессия:  🔁  фиолетовый (#9c27b0) 32x32px
```

### Заголовок логов:

```
Высота: 40px (компактно)
Фон: #1a1a1a (тёмный)
Шрифт статистики: 11px
Цвет текста: #aaa
Цвет значений: #4a9eff (синий)
```

### Кнопки действий в логах:

```css
Размер: 24x24px (квадратные)
Фон: прозрачный
Ховер: rgba(74, 158, 255, 0.2)
Иконки: 16px
```

---

## 🔧 Технические детали

### Изменённые файлы:

1. **`templates/index.html`**
   - Добавлены кнопки `resetCycleBtn` и `resetSessionBtn`
   - Изменены иконки кнопок покупки/продажи

2. **`static/trade-logs-manager.js`**
   - Обновлена структура заголовка логов
   - Добавлены элементы статистики
   - Обновлены стили для компактного отображения

3. **`static/app.js`**
   - Добавлены функции `handleResetCycle()` и `handleResetSession()`
   - Добавлены обработчики событий для новых кнопок

---

## 📝 Структура данных

### Статистика сессии (будет добавлена в API):

```json
{
  "session_start": "2025-11-14T10:00:00",
  "start_equity": 45.00,
  "current_equity": 45.67,
  "pnl": 0.67,
  "delta_equity": 0.67,
  "delta_equity_percent": 1.49,
  "duration_seconds": 5025,
  "total_trades": 14,
  "total_buys": 10,
  "total_sells": 4
}
```

### Формат длительности:

```javascript
function formatDuration(seconds) {
    const days = Math.floor(seconds / 86400);
    const hours = Math.floor((seconds % 86400) / 3600);
    const minutes = Math.floor((seconds % 3600) / 60);
    const secs = seconds % 60;
    return `${days}d ${hours}h ${minutes}m ${secs}s`;
}
```

---

## 🚀 Использование

### 1. Начать новый цикл:

```
1. Завершите текущий цикл (продайте всё)
2. Нажмите кнопку "🔄 Сброс цикла"
3. Подтвердите действие
4. Стартовая цена обнулена
5. При следующей покупке установится новая P0
```

### 2. Просмотреть статистику:

```
1. Переключитесь на вкладку "Логи"
2. В заголовке увидите актуальную статистику
3. Статистика обновляется автоматически каждые 5 секунд
```

### 3. Сбросить сессию (скоро):

```
1. Нажмите кнопку "🔁 Сброс сессии"
2. Подтвердите действие
3. Вся статистика обнулится
4. Начнётся новая сессия
5. Старая сессия сохранится в архив
```

---

## ⚠️ Важные замечания

### Сброс цикла:
- ⚠️ Не закрывает позиции автоматически
- ⚠️ Рекомендуется использовать после полной продажи
- ✅ Безопасно использовать в любой момент
- ✅ Можно отменить перед подтверждением

### Сброс сессии:
- 🔜 Функция в разработке
- 🔜 Будет доступна с автотрейдером
- 🔜 Позволит архивировать историю

---

## 🧪 Тестирование

### Проверка сброса цикла:

```
1. Откройте веб-интерфейс
2. Выберите валютную пару
3. Посмотрите текущую стартовую цену (P0)
4. Нажмите "🔄 Сброс цикла"
5. Подтвердите действие
6. Проверьте, что P0 = 0
7. Таблица безубыточности обновлена
```

### Проверка статистики в логах:

```
1. Переключитесь на вкладку "Логи"
2. Проверьте отображение статистики
3. Убедитесь, что PnL показывается корректно
4. Остальные параметры пока показывают 0
5. Ждём реализации статистики сессий
```

---

## 📚 Следующие шаги

### Ближайшие задачи:

1. ✅ Добавить кнопки управления ← **Готово**
2. ✅ Сделать компактный заголовок логов ← **Готово**
3. ✅ Добавить заглушки статистики ← **Готово**
4. 🔜 Реализовать сброс цикла на backend
5. 🔜 Добавить систему статистики сессий
6. 🔜 Реализовать сброс сессии
7. 🔜 Добавить расчёт Equity
8. 🔜 Добавить расчёт Duration
9. 🔜 Добавить архивирование сессий

---

## 🎯 Итог

**Реализовано:**
- ✅ Кнопки "Сброс цикла" и "Сброс сессии"
- ✅ Компактный заголовок логов
- ✅ Отображение статистики (моки)
- ✅ Квадратные кнопки с иконками
- ✅ Обработчики событий

**В разработке:**
- 🔜 Backend для сброса цикла
- 🔜 Система статистики сессий
- 🔜 Реальные данные для статистики
- 🔜 Архивирование истории

---

**Статус**: ✅ UI готов, логика частично реализована  
**Требуется**: Перезагрузка страницы (Ctrl+F5) для применения изменений

---

## 💡 Совет

Используйте "Сброс цикла" после каждой полной продажи, чтобы начать новый торговый цикл с актуальной ценой.

Кнопка "Сброс сессии" пока неактивна — это нормально, она будет работать после реализации автотрейдера.
