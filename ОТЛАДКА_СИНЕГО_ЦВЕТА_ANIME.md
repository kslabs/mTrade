# 🔍 Инструкция по отладке синего цвета для ANIME

## Проблема
Название валюты ANIME не становится синим, когда торговля запрещена (индикатор красный).

## Что было сделано

### 1. Улучшен мониторинг цвета
В `static/app.js` добавлено детальное логирование:
- **Каждые 500ms** проверяются все вкладки
- **Каждые 5 секунд** (каждый 10-й цикл) выводится детальная диагностика

### 2. Логи в консоли браузера

#### Постоянные логи (при изменениях):
```
[AUTO_PAINT] ANIME: индикатор OFF → покрашен в СИНИЙ #2196F3 (было: rgb(...)
[AUTO_PAINT] ANIME: индикатор ON → убран синий цвет (было: rgb(...)
```

#### Диагностические логи (каждые 5 секунд):
```
[AUTO_PAINT_DEBUG] ANIME: indicator=найден, label=найден
[AUTO_PAINT_DEBUG] ANIME: классы индикатора = [perm-indicator, off]
[AUTO_PAINT_DEBUG] ANIME: уже синий (rgb(33, 150, 243))
```

## Как проверить работу

### Шаг 1: Откройте консоль браузера
- **Chrome/Edge**: F12 → вкладка Console
- **Firefox**: F12 → вкладка Console

### Шаг 2: Обновите страницу
- Нажмите Ctrl+F5 (жёсткое обновление с очисткой кэша)

### Шаг 3: Наблюдайте за логами

#### Что должно быть в логах:
```
[INIT] Применяем синий цвет для неактивных валют после загрузки страницы
[AUTO_PAINT] ANIME: индикатор OFF → покрашен в СИНИЙ #2196F3 (было: rgb(255, 255, 255))
...
(каждые 5 секунд)
[AUTO_PAINT_DEBUG] ANIME: indicator=найден, label=найден
[AUTO_PAINT_DEBUG] ANIME: классы индикатора = [perm-indicator, off]
[AUTO_PAINT_DEBUG] ANIME: уже синий (rgb(33, 150, 243))
```

### Шаг 4: Проверьте индикатор ANIME
- Найдите вкладку ANIME
- **Красный кружок** (⚫ красный) = индикатор OFF → **название должно быть синим**
- **Зелёный кружок** (⚫ зелёный) = индикатор ON → **название обычного цвета**

### Шаг 5: Кликните на индикатор ANIME
- Кликните на красный/зелёный кружок рядом с ANIME
- Цвет индикатора должен переключиться
- Название должно **сразу** изменить цвет (в течение 0.5 сек)

## Возможные проблемы и решения

### 🔴 Проблема 1: В логах нет [AUTO_PAINT_DEBUG]
**Причина**: Страница не перезагружена или кэш браузера.  
**Решение**: 
```
1. Ctrl+Shift+Delete → Очистить кэш
2. Ctrl+F5 (жёсткое обновление)
3. Перезапустить браузер
```

### 🔴 Проблема 2: [AUTO_PAINT_DEBUG] показывает "indicator=НЕТ"
**Причина**: HTML структура вкладки ANIME не содержит `.perm-indicator`.  
**Решение**: Проверьте, создаётся ли индикатор для ANIME в серверном коде.

### 🔴 Проблема 3: Классы индикатора = [perm-indicator] (нет "off" или "on")
**Причина**: Классы `off`/`on` не добавляются к индикатору ANIME.  
**Решение**: Проверьте код создания вкладки ANIME в `app.js` (функция `createBaseCurrencyTabs`).

### 🔴 Проблема 4: Цвет НЕ меняется, но логи показывают "покрашен в СИНИЙ"
**Причина**: CSS правила с более высоким приоритетом перекрывают `!important`.  
**Решение**: Откройте DevTools → Elements → найдите `.code-label` у ANIME → вкладка Computed → смотрите, какой цвет применён.

### 🔴 Проблема 5: Расширения браузера мешают
**Причина**: AdBlock, Dark Reader и другие расширения могут изменять стили.  
**Решение**: 
```
1. Откройте страницу в режиме инкогнито (Ctrl+Shift+N)
2. Или отключите все расширения
```

## Логика работы (упрощённо)

```javascript
// Каждые 500ms:
для каждой вкладки {
  если индикатор.classList.contains('off') {
    → покрасить название в СИНИЙ #2196F3
  } 
  иначе если индикатор.classList.contains('on') {
    → вернуть обычный цвет
  }
}
```

## Проверка текущего состояния в DevTools

### В консоли выполните:
```javascript
// Найти вкладку ANIME
const animeTab = Array.from(document.querySelectorAll('.tab-item')).find(t => t.dataset.code === 'ANIME');

// Проверить индикатор
const indicator = animeTab?.querySelector('.perm-indicator');
console.log('ANIME индикатор найден:', !!indicator);
console.log('ANIME классы индикатора:', indicator?.classList);
console.log('ANIME индикатор OFF:', indicator?.classList.contains('off'));
console.log('ANIME индикатор ON:', indicator?.classList.contains('on'));

// Проверить название
const label = animeTab?.querySelector('.code-label');
console.log('ANIME название найдено:', !!label);
console.log('ANIME цвет названия:', window.getComputedStyle(label).color);
console.log('ANIME inline стиль:', label?.style.cssText);
```

## Ожидаемый результат

✅ **ANIME с КРАСНЫМ индикатором**: название **ярко-синее** (#2196F3)  
✅ **ANIME с ЗЕЛЁНЫМ индикатором**: название **обычного цвета** (белое/серое)  
✅ **Переключение индикатора**: цвет меняется **мгновенно** (0.5 сек)  
✅ **В логах**: чёткие сообщения о покраске и классах

---

**Дата**: ${new Date().toLocaleString('ru-RU')}  
**Файлы**: `static/app.js` (строки 2525-2570)
