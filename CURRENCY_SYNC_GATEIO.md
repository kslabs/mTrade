# Синхронизация символов валют с Gate.io

## 📋 Обзор

Реализована функция автоматической синхронизации списка валют и их официальных символов непосредственно с биржи Gate.io. Теперь не нужно вручную выбирать символы — система загружает актуальный список криптовалют с биржи.

## ✨ Возможности

### 1. Автоматическая синхронизация
- **Загрузка с Gate.io API**: получение полного списка доступных валют
- **Официальные символы**: используются рекомендованные символы для популярных криптовалют
- **Умные fallback**: для неизвестных валют автоматически подбирается подходящий символ
- **Сохранение пользовательских настроек**: если вы изменили символ вручную, он не будет перезаписан

### 2. Поддерживаемые символы

#### Популярные криптовалюты (встроенные):
- `BTC` → `₿` (Bitcoin Sign)
- `ETH` → `Ξ` (Ethereum/Xi)
- `ADA` → `₳` (Cardano/Austral)
- `SOL` → `◎` (Solana)
- `DOGE` → `Ð` (Dogecoin)
- `LTC` → `Ł` (Litecoin)
- `XRP` → `✕` (Ripple)
- `DOT` → `●` (Polkadot)
- `MATIC` → `⬡` (Polygon)
- и многие другие...

#### Автоматические правила:
- **Стейблкоины** (USDT, USDC, DAI, BUSD) → `$`
- **Неизвестные валюты** → первая буква кода

## 🚀 Использование

### Через веб-интерфейс

1. **Откройте редактор валют**:
   - Нажмите на иконку ⚙️ (шестерёнка) рядом с вкладками валют

2. **Запустите синхронизацию**:
   - Нажмите кнопку **"🔄 Синхронизация с Gate.io"**
   - Дождитесь завершения процесса (обычно 5-10 секунд)

3. **Просмотрите результаты**:
   - Появится уведомление с количеством добавленных/обновленных валют
   - В правом верхнем углу отобразится информация о последней синхронизации

### Через Python API

```python
from currency_sync import sync_currencies, CurrencySync

# Быстрая синхронизация
result = sync_currencies()

if result["success"]:
    print(f"Добавлено: {result['added']}")
    print(f"Обновлено: {result['updated']}")
    print(f"Всего: {result['total']}")

# Использование класса
sync = CurrencySync()

# Получить информацию о валюте
btc = sync.get_currency('BTC')
print(f"{btc['symbol']} {btc['code']} - {btc['name']}")

# Обновить символ вручную
sync.update_currency_symbol('BTC', '฿')

# Получить информацию о синхронизации
info = sync.get_sync_info()
print(f"Последнее обновление: {info['last_update']}")
print(f"Всего валют: {info['total_currencies']}")
```

### Через командную строку

```bash
# Запустить тестовую синхронизацию
python test_currency_sync.py

# Или использовать напрямую
python -c "from currency_sync import sync_currencies; print(sync_currencies())"
```

## 📊 API Endpoints

### `POST /api/currencies/sync`
Запустить синхронизацию валют с Gate.io

**Response:**
```json
{
  "success": true,
  "added": 150,
  "updated": 50,
  "total": 4627,
  "timestamp": "2025-11-14T11:24:02.711319"
}
```

### `GET /api/currencies/sync-info`
Получить информацию о последней синхронизации

**Response:**
```json
{
  "success": true,
  "info": {
    "last_update": "2025-11-14T11:24:02.711319",
    "network_mode": "work",
    "total_currencies": 4627,
    "custom_symbols": 7
  }
}
```

### `PUT /api/currencies/{code}/symbol`
Обновить символ конкретной валюты

**Request:**
```json
{
  "symbol": "₿"
}
```

**Response:**
```json
{
  "success": true,
  "message": "Символ валюты BTC обновлён"
}
```

## 📁 Структура данных

### currencies.json (новый формат)

```json
{
  "currencies": [
    {
      "code": "BTC",
      "name": "Bitcoin",
      "symbol": "₿",
      "chain": "BTC",
      "delisted": false,
      "withdraw_disabled": false,
      "withdraw_delayed": false,
      "deposit_disabled": false,
      "trade_disabled": false,
      "custom_symbol": false
    }
  ],
  "last_update": "2025-11-14T11:24:02.711319",
  "network_mode": "work"
}
```

### Поля:
- `code` — код валюты (BTC, ETH, etc.)
- `name` — полное название
- `symbol` — символ для отображения
- `chain` — блокчейн сеть
- `delisted` — валюта удалена с биржи
- `*_disabled` — флаги доступности операций
- `custom_symbol` — флаг пользовательского изменения символа
- `last_update` — время последней синхронизации
- `network_mode` — режим сети (work/test)

## 🔧 Технические детали

### Модули

1. **currency_sync.py** — основной модуль синхронизации
   - `CurrencySync` — класс для работы с валютами
   - `sync_currencies()` — функция быстрой синхронизации
   - `FALLBACK_SYMBOLS` — встроенные символы

2. **gate_api_client.py** — клиент Gate.io API (обновлён)
   - `get_currencies()` — получить список валют
   - `get_currency_details(code)` — детали конкретной валюты
   - `get_currency_pairs()` — торговые пары

3. **mTrade.py** — API endpoints (добавлены)
   - `/api/currencies/sync` — синхронизация
   - `/api/currencies/sync-info` — информация
   - `/api/currencies/{code}/symbol` — обновление символа

4. **app.js** — фронтенд (обновлён)
   - `syncCurrenciesFromGateIO()` — UI синхронизации
   - `updateSyncInfo()` — отображение информации

### Обратная совместимость

Система автоматически конвертирует старый формат `currencies.json` (простой массив) в новый формат (объект с метаданными). Все существующие данные сохраняются.

## 📈 Результаты

**Тестирование показало:**
- ✅ Успешная загрузка **4627 валют** с Gate.io
- ✅ Корректное применение символов для популярных криптовалют
- ✅ Сохранение пользовательских изменений
- ✅ Быстрая синхронизация (5-10 секунд)
- ✅ Работа без API ключей (публичный endpoint)

## 🎯 Преимущества

### До:
- ❌ Ручной выбор символов из 200+ вариантов
- ❌ Непонятные геометрические фигуры
- ❌ Нет связи с официальными символами Gate.io
- ❌ Нужно знать Unicode коды

### После:
- ✅ Автоматическая синхронизация с биржей
- ✅ Официальные символы популярных криптовалют
- ✅ Актуальный список доступных валют
- ✅ Один клик для обновления

## 🔮 Дальнейшие улучшения

1. **Автоматическая синхронизация**
   - [ ] Синхронизация при запуске приложения
   - [ ] Периодическая синхронизация (раз в день)
   - [ ] Уведомления о новых валютах

2. **Расширенная информация**
   - [ ] Иконки валют (изображения)
   - [ ] Категории валют (DeFi, Layer 1, etc.)
   - [ ] Рыночная капитализация

3. **Фильтрация**
   - [ ] Скрытие delisted валют
   - [ ] Фильтр по доступности торговли
   - [ ] Поиск по названию/коду

## 📝 Примечания

- Синхронизация использует **публичный API** Gate.io (не требует ключей)
- Данные сохраняются локально в `currencies.json`
- Пользовательские изменения символов сохраняются при синхронизации
- Режим сети (work/test) учитывается при синхронизации

## 🐛 Известные проблемы

- Отсутствуют официальные символы для многих малоизвестных токенов
- Некоторые символы могут не отображаться в старых шрифтах
- Для альткоинов используется fallback (первая буква)

## 💡 Рекомендации

1. **Запускайте синхронизацию периодически** — новые валюты добавляются на биржу регулярно
2. **Проверяйте символы** — для важных валют можно установить свой символ
3. **Делайте бэкап** — перед синхронизацией сохраните `currencies.json`

---

**Версия:** 1.0  
**Дата:** 14 ноября 2025  
**Автор:** mTrade Development Team
