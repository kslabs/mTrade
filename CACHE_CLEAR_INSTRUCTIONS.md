# КРИТИЧЕСКАЯ ИНСТРУКЦИЯ: Очистка кэша после изменений

**Дата:** 2024-01-XX  
**Проблема:** Изменения в HTML/CSS не отображаются в браузере

---

## 🔥 СРОЧНЫЕ ДЕЙСТВИЯ

### 1. Остановить сервер (если запущен)

```powershell
# Нажмите Ctrl+C в терминале где запущен сервер
```

### 2. Очистить кэш браузера

#### Chrome/Edge:
1. Откройте DevTools (F12)
2. **Щелкните правой кнопкой** по кнопке обновления (⟳)
3. Выберите **"Очистить кэш и жесткая перезагрузка"** (Hard Reload)

**ИЛИ:**
- Нажмите `Ctrl + Shift + Delete`
- Выберите "Кэш" или "Изображения и файлы"
- Очистите за "Всё время"

#### Firefox:
- `Ctrl + Shift + Delete` → Очистить кэш

### 3. Проверить текущий код

```powershell
cd "c:\Users\Администратор\Documents\bGate.mTrade"

# Показать последние коммиты
git log --oneline -3

# Показать содержимое файла (строки с кнопкой)
Get-Content templates\index.html | Select-String -Pattern "saveParamsBtn" -Context 3
```

### 4. Запустить сервер заново

```powershell
python mTrade.py
```

### 5. Открыть браузер с отключенным кэшем

#### Chrome DevTools:
1. F12 → открыть DevTools
2. ⚙️ Settings (или F1)
3. Network → ✅ **"Disable cache (while DevTools is open)"**
4. Держите DevTools открытым при работе

---

## 🔍 Проверка что изменения применены

### Что должно быть в HTML (строки 133-138):

```html
<h3 style="display:flex;justify-content:space-between;align-items:center;gap:10px;">
    <span>📊 Таблица безубыточности</span>
    <div style="display:flex;align-items:center;gap:8px;">
        <div id="paramsSaveStatus" class="params-save-status"></div>
        <button id="saveParamsBtn" class="save-params-btn-compact">💾 Сохранить</button>
    </div>
</h3>
```

### Проверка через DevTools:

1. F12 → Elements
2. Найти элемент с `id="saveParamsBtn"`
3. Проверить что:
   - Кнопка существует в DOM
   - Нет `display: none` в стилях
   - Родительский `h3` имеет `display: flex`

---

## 🐛 Возможные проблемы

### Проблема 1: Кнопка не видна
**Причина:** CSS скрывает элемент  
**Решение:** Проверить computed styles в DevTools

### Проблема 2: Старый HTML загружается
**Причина:** Кэш браузера или Flask  
**Решение:** 
```python
# В mTrade.py должен быть cache_buster
@app.context_processor
def inject_cache_buster():
    return {'cache_buster': str(int(time.time()))}
```

### Проблема 3: Файл не обновился
**Причина:** Git не применил изменения  
**Решение:**
```powershell
git status  # Проверить изменения
git diff templates/index.html  # Показать отличия
```

---

## ✅ Финальная проверка

После всех действий в браузере должно быть:

1. ✅ Заголовок "📊 Таблица безубыточности" использует flexbox
2. ✅ Кнопка "💾 Сохранить" справа от заголовка
3. ✅ Статус сохранения слева от кнопки
4. ✅ Стиль идентичен "📈 Рынок и стакан"

---

## 🚨 Если ничего не помогает

### Полная перезагрузка:

```powershell
# 1. Остановить сервер
Ctrl+C

# 2. Проверить что файл правильный
Get-Content templates\index.html -Encoding UTF8 | Select-String "saveParamsBtn"

# 3. Закрыть ВСЕ вкладки браузера

# 4. Очистить весь кэш браузера полностью

# 5. Запустить сервер
python mTrade.py

# 6. Открыть в ИНКОГНИТО режиме
# Chrome: Ctrl+Shift+N
# Firefox: Ctrl+Shift+P
```

---

## 📝 Для разработчика

Если изменения НЕ видны после всех действий:
1. Сделать скриншот DevTools → Elements
2. Проверить `View Page Source` (Ctrl+U)
3. Сравнить с файлом на диске
4. Проверить что сервер читает правильный файл

**Возможно используется другой файл шаблона!**

```powershell
# Найти все index.html
Get-ChildItem -Recurse -Filter "index.html"
```
